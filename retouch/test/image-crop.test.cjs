'use strict';
const {test}=require('node:test'),assert=require('node:assert/strict'),{box,markup}=require('../shell/image-crop.js');
test('crop zoom preserves intrinsic aspect and positions the crop inside the original image',()=>{assert.deepEqual(box(400,200,1,50,50),[0,0,400,200]);assert.deepEqual(box(400,200,2,100,50),[200,50,200,100]);assert.deepEqual(box(400,200,4,0,100),[0,150,100,50]);for(const values of [[400,200,0,50,50],[400,200,2,-1,50],[400,200,2,50,101],[Infinity,200,2,50,50]])assert.throws(()=>box(...values));});
test('crop SVG retains original image data and dimensions without resampling',()=>{const svg=markup({width:400,height:200,zoom:2,x:100,y:50,data:'data:image/png;base64,YWJj'});assert.match(svg,/width="400" height="200" viewBox="200 50 200 100"/);assert.match(svg,/href="data:image\/png;base64,YWJj"/);assert.throws(()=>markup({width:400,height:200,zoom:2,x:50,y:50,data:'https://example.com/image.png'}));});
test('rotated crop corners remain inside the original at every pan extreme',()=>{
 const {layout}=require('../shell/image-crop.js');for(const [width,height]of [[400,200],[200,400],[300,300]])for(const angle of [-180,-135,-90,-33,0,33,90,135,180])for(const zoom of [1,1.1,2,8])for(const x of [0,50,100])for(const y of [0,50,100]){const {view:[left,top,w,h]}=layout(width,height,zoom,x,y,angle),c=Math.cos(angle*Math.PI/180),s=Math.sin(angle*Math.PI/180);for(const [px,py]of [[left,top],[left+w,top],[left+w,top+h],[left,top+h]]){const dx=px-width/2,dy=py-height/2,sx=c*dx+s*dy+width/2,sy=-s*dx+c*dy+height/2;assert.ok(sx>=-1e-6&&sx<=width+1e-6&&sy>=-1e-6&&sy<=height+1e-6);}}
});
test('crop panning follows the rotated preview axes',()=>{const {pan}=require('../shell/image-crop.js'),next=pan({width:400,height:200,zoom:2,x:50,y:50,rotation:90},40,0,400);assert.equal(next.x,50);assert.equal(next.y,60);assert.throws(()=>markup({width:400,height:200,zoom:2,x:50,y:50,rotation:181,data:'data:image/png;base64,YWJj'}));});

test('crop flips reflect image pixels before rotation while preserving the crop view',()=>{const model={width:400,height:200,zoom:2,x:50,y:50,rotation:90,data:'data:image/png;base64,YWJj'};assert.match(markup({...model,flipX:true}),/transform="rotate\(90 200 100\) translate\(400 0\) scale\(-1 1\)"/);assert.match(markup({...model,flipY:true}),/translate\(0 200\) scale\(1 -1\)/);assert.match(markup({...model,flipX:true,flipY:true}),/translate\(400 200\) scale\(-1 -1\)/);assert.throws(()=>markup({...model,flipX:'true'}));});

test('crop panning accounts for nonuniform image fill before undoing rotation',()=>{const {pan}=require('../shell/image-crop.js'),next=pan({width:400,height:200,zoom:2,x:50,y:50,rotation:90},0,40,200,200);assert.ok(Math.abs(next.x-(50-1000/350))<1e-8);assert.equal(next.y,50);});

test('image adjustment values are bounded and neutral settings emit no filter',()=>{const {adjustments}=require('../shell/image-crop.js');assert.equal(adjustments({}),'');for(const values of [{exposure:4},{contrast:101},{saturation:-101},{exposure:NaN}])assert.throws(()=>adjustments(values));assert.match(adjustments({exposure:-1}),/slope="0.5"/);assert.match(adjustments({contrast:-100}),/slope="0" intercept="0.5"/);});

test('temperature and tint values are bounded and neutral balance preserves existing filter markup',()=>{const {adjustments}=require('../shell/image-crop.js');for(const values of [{temperature:101},{tint:-101},{temperature:Infinity}])assert.throws(()=>adjustments(values));assert.equal(adjustments({exposure:1}),adjustments({exposure:1,temperature:0,tint:0}));assert.match(adjustments({temperature:100}),/slope="1.4142135623730951"/);});
