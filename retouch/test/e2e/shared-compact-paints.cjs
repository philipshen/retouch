'use strict';
const assert=require('node:assert/strict');
exports.run=async({page,app,read,wait,settled,kind})=>{
 const sourceReads=await page.evaluate(()=>{const B=RetouchBackgroundPaintUI;return [B.sourceColor({className:'[fill:#12345680] md:[fill:#abcdef]'},'','fill'),B.sourceColor({className:'[fill:var(--brand)]'},'','fill'),B.sourceColor({cssAuthoring:true,cssRules:{0:{'border-color':'red blue'},768:{'border-color':'green'}}},'','border-color'),B.sourceState({className:'[background-color:transparent]'},''),B.sourceState({className:'[background-color:red]'},'')?.color];});assert.equal(sourceReads[0],'#12345680');assert.equal(sourceReads[1],null);assert.equal(sourceReads[2],'red blue');assert.equal(sourceReads[3],null);assert.ok(sourceReads[4]);
 const fill=()=>page.locator('#panelBody input[data-paint-property="background-color"]'),stroke=()=>page.locator('#panelBody input[data-paint-property="border-color"]'),first=app.locator('h1'),second=app.locator('p').first(),state=target=>target.evaluate(el=>parent.RetouchBackgroundPaintUI.read({},el)),parse=raw=>page.evaluate(raw=>RetouchPaintPicker.parsePaint(raw),raw);
 const edit=async(input,value)=>{const before=read();await input.fill(value);await input.press('Enter');await wait(()=>read()!==before);await settled();};
 await edit(fill(),'#33669980');await edit(stroke(),'#ff000080');if(process.env.RT_E2E_SHARED_TEXT_PAINTS)await edit(page.locator('#panelBody input[data-paint-property=color]'),'#12345680');let before=read();await page.getByRole('button',{name:'Hide background color',exact:true}).click();await wait(()=>read()!==before);await settled();
 await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();await edit(fill(),'color(display-p3 0.2 0.4 0.6 / 0.25)');await edit(stroke(),'color(display-p3 0.1 0.8 0.2 / 0.25)');if(process.env.RT_E2E_SHARED_TEXT_PAINTS)await edit(page.locator('#panelBody input[data-paint-property=color]'),'color(display-p3 0.2 0.4 0.6 / 0.25)');
 await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();
 const states=[read()],alpha=page.getByLabel('Shared Fill opacity (%)',{exact:true}),strokeAlpha=page.getByLabel('Shared Stroke opacity (%)',{exact:true}),record=async()=>{await wait(()=>read()!==states.at(-1));await settled();states.push(read());};
 assert.equal(await alpha.getAttribute('placeholder'),'Mixed');assert.equal(await alpha.isEnabled(),true);assert.equal(await strokeAlpha.getAttribute('placeholder'),'Mixed');
 await fill().fill('ABCDEF');await fill().press('Enter');await record();assert.equal((await state(first)).hidden,true);const a=await parse((await state(first)).color),b=await parse((await state(second)).color);assert.deepEqual(a.channels.map(n=>Math.round(n*255)),[171,205,239]);assert.deepEqual(b.channels.map(n=>Math.round(n*255)),[171,205,239]);assert.ok(Math.abs(a.alpha-128/255)<1e-8);assert.equal(b.alpha,.25);
 await alpha.fill('40');await alpha.press('Enter');await record();assert.equal((await parse((await state(first)).color)).alpha,.4);assert.equal((await parse((await state(second)).color)).alpha,.4);assert.equal((await state(first)).hidden,true);
 const unchanged=read();await fill().focus();assert.equal(await fill().inputValue(),'ABCDEF');await fill().press('Tab');await settled();assert.equal(read(),unchanged);
 await alpha.fill('101');await alpha.press('Enter');assert.equal(await alpha.evaluate(el=>el.checkValidity()),false);assert.equal(read(),unchanged);await alpha.press('Escape');
 await strokeAlpha.fill('35');await strokeAlpha.press('Enter');await record();const borders=await app.locator('h1,p').evaluateAll(els=>els.slice(0,2).map(el=>parent.RetouchPaintPicker.parsePaint(getComputedStyle(el).borderTopColor)));assert.deepEqual(borders[0].channels,[1,0,0]);assert.equal(borders[1].space,'display-p3');assert.deepEqual(borders[1].channels,[.1,.8,.2]);assert.ok(borders.every(p=>Math.abs(p.alpha-.35)<1e-6));
 if(process.env.RT_E2E_SHARED_TEXT_PAINTS){const textAlpha=page.getByLabel('Shared Text opacity (%)',{exact:true}),text=page.locator('#panelBody input[data-paint-property=color]'),values=()=>app.locator('h1,p').evaluateAll(els=>els.slice(0,2).map(el=>parent.RetouchPaintPicker.parsePaint(getComputedStyle(el).color)));assert.equal(await textAlpha.getAttribute('placeholder'),'Mixed');await textAlpha.fill('45');await textAlpha.press('Enter');await record();const colors=await values();assert.ok(colors.every(value=>value.alpha===.45));assert.deepEqual(colors[0].channels.map(n=>Math.round(n*255)),[18,52,86]);assert.equal(colors[1].space,'display-p3');assert.deepEqual(colors[1].channels,[.2,.4,.6]);await text.fill('ABCDEF');await text.press('Enter');await record();for(const value of await values()){assert.equal(value.alpha,.45);assert.deepEqual(value.channels.map(n=>Math.round(n*255)),[171,205,239]);}}
 if(process.env.RT_E2E_SHARED_COLOR_BASE){
  const screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true});await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await fill().fill('#11223380');await fill().press('Enter');await record();let tablet=[await state(first),await state(second)];
  if(process.env.RT_E2E_SHARED_COLOR_INACTIVE){await screen.focus();await screen.selectOption('390x844');await settled();const phone=[await state(first),await state(second)];await wait(async()=>await alpha.isEnabled());assert.ok(Math.abs(Number(await alpha.inputValue())-128/255*100)<1e-6);await alpha.fill('70');await app.locator('body').evaluate(el=>el.ownerDocument.defaultView.dispatchEvent(new Event('resize')));assert.equal(await alpha.inputValue(),'70');await alpha.press('Enter');await record();assert.deepEqual([await state(first),await state(second)],phone);await screen.focus();await screen.selectOption('768x1024');await settled();tablet=[await state(first),await state(second)];for(const entry of tablet)assert.equal((await parse(entry.color)).alpha,.7);
   if(process.env.RT_E2E_SHARED_VISIBILITY_INACTIVE){await screen.focus();await screen.selectOption('390x844');await settled();const phone=[await state(first),await state(second)],hide=page.getByRole('button',{name:'Hide selected background colors',exact:true});await wait(async()=>await hide.isEnabled());assert.equal(await hide.getAttribute('aria-pressed'),'mixed');await hide.click();await record();assert.deepEqual([await state(first),await state(second)],phone);const show=page.getByRole('button',{name:'Show selected background colors',exact:true});await show.click();await record();assert.deepEqual([await state(first),await state(second)],phone);await screen.focus();await screen.selectOption('768x1024');await settled();tablet=[await state(first),await state(second)];assert.ok(tablet.every(value=>!value.hidden));for(const entry of tablet)assert.equal((await parse(entry.color)).alpha,.7);}
}

  await scope.selectOption('');await settled();assert.equal(await alpha.inputValue(),'40');await alpha.fill('60');await alpha.press('Enter');await record();assert.deepEqual([await state(first),await state(second)],tablet);await screen.focus();await screen.selectOption('390x844');await settled();const base=[await state(first),await state(second)];assert.equal(base[0].hidden,true);assert.equal(base[1].hidden,false);for(const entry of base){const paint=await parse(entry.color);assert.equal(paint.alpha,.6);assert.deepEqual(paint.channels.map(n=>Math.round(n*255)),[171,205,239]);}
 }
 if(process.env.RT_E2E_SHARED_PAINT_SCREENSHOT){await fill().focus();await page.screenshot({path:process.env.RT_E2E_SHARED_PAINT_SCREENSHOT});await fill().press('Tab');}
 for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
 for(const expected of states.slice(1)){await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===expected);await settled();}
 console.log(kind+': PASS shared hex with per-layer alpha, mixed opacity, hidden paint, P3 stroke colors, invalid drafts and exact undo/redo');
};
