'use strict';
const assert=require('node:assert/strict');
module.exports=async({page,app,kind,read,wait,settled})=>{
 const unit=process.env.RT_E2E_UNDERLINE_PERCENT?'%':'px';
 const show=control=>wait(()=>control.evaluate(el=>{window.RetouchInspectorUI.reveal(el);return new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(()=>r(el.isConnected&&!el.matches(':disabled')&&el.getClientRects().length>0))));}));
 await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();
 for(const [name,thickness,offset]of [['h1 · Headline','3'+unit,'2'+unit],['p · Other text','7'+unit,'8'+unit]]){
  await page.getByRole('treeitem',{name,exact:true}).click();await settled();for(const [label,value]of [['Underline thickness',thickness],['Underline offset',offset]]){const before=read(),input=page.getByLabel(label,{exact:true});await show(input);await input.fill(value);await input.press('Enter');await wait(()=>read()!==before);await settled();}
 }
 await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();const original=read(),nodes=app.locator('h1,p.other-font'),styles=await nodes.evaluateAll(nodes=>nodes.map(el=>el.getAttribute('style')));
 for(const [label,property,initial,expected]of [['Underline thickness','text-decoration-thickness',['3'+unit,'7'+unit],['9'+unit,'13'+unit]],['Underline offset','text-underline-offset',['2'+unit,'8'+unit],['8'+unit,'14'+unit]]]){
  const input=page.getByLabel('Shared '+label,{exact:true}),measure=()=>nodes.evaluateAll((nodes,p)=>nodes.map(el=>getComputedStyle(el).getPropertyValue(p)),property);
  const drag=async(delta=6)=>{await show(input);const label=input.locator('..').locator(':scope > span');let box;await wait(async()=>!!(box=await label.boundingBox()));await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+delta,box.y+box.height/2,{steps:3});};
  assert.equal(await input.inputValue(),'');await drag();await wait(async()=>JSON.stringify(await measure())===JSON.stringify(expected));assert.equal(read(),original);await page.keyboard.press('Escape');await page.mouse.up();await settled();assert.deepEqual(await measure(),initial);assert.deepEqual(await nodes.evaluateAll(nodes=>nodes.map(el=>el.getAttribute('style'))),styles);
  await drag();await page.mouse.up();await wait(()=>read()!==original);await settled();await wait(async()=>JSON.stringify(await measure())===JSON.stringify(expected));const changed=read();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();await wait(async()=>JSON.stringify(await measure())===JSON.stringify(initial));await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===changed);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();
  await drag(0);await page.mouse.up();assert.equal(read(),original);assert.equal(await input.inputValue(),'');
  await drag(-10);await wait(async()=>JSON.stringify(await measure())===JSON.stringify((property==='text-decoration-thickness'?[0,4]:[-8,-2]).map(value=>value+unit)));await page.keyboard.press('Escape');await page.mouse.up();await settled();assert.deepEqual(await measure(),initial);
  await drag();await nodes.first().evaluate((el,p)=>el.style.setProperty(p,'77px','important'),property);await wait(async()=>await page.locator('[data-numeric-scrub-speed]').count()===0);await page.mouse.up();assert.equal(read(),original);assert.deepEqual(await measure(),['77px',initial[1]]);await nodes.first().evaluate((el,style)=>style===null?el.removeAttribute('style'):el.setAttribute('style',style),styles[0]);
  await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();const single=page.getByLabel(label,{exact:true}),otherUnit='5'+(unit==='%'?'px':'%');await show(single);await single.fill(otherUnit);await single.press('Enter');await wait(()=>read()!==original);await settled();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();const incompatible=read();assert.deepEqual(await measure(),[initial[0],otherUnit]);await drag();assert.equal(await page.locator('[data-numeric-scrub-speed]').count(),0);await page.mouse.up();assert.equal(read(),incompatible);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();
  await show(input);await input.fill('auto');await input.press('Enter');await wait(()=>read()!==original);await settled();const automatic=read();await drag();assert.equal(await page.locator('[data-numeric-scrub-speed]').count(),0);await page.mouse.up();assert.equal(read(),automatic);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();
 }
 console.log(kind+' '+unit+': PASS shared mixed underline dragging, bounds, no-op, foreign-change cancellation, mixed-unit refusal, preview/cancel, exact styles, grouped undo/redo and automatic-value refusal');
};
