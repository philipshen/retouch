'use strict';
const assert=require('node:assert/strict');
module.exports=async({page,app,read,wait,settled,kind})=>{
 const source=read(),targets=app.locator('h1,p.other-font'),styles=await targets.evaluateAll(els=>els.map(el=>el.getAttribute('style')));
 await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();
 const state=()=>targets.evaluateAll(els=>els.map(el=>{const css=getComputedStyle(el);return [css.gridTemplateColumns,css.gridTemplateRows,css.gridAutoFlow];})),initial=await state();
 for(const [label,value,index,expected]of [['Grid columns','3',0,null],['Grid rows','3',1,null],['Grid flow','col-dense',2,'column dense'],['Column sizes','40px 80px',0,'40px 80px'],['Row sizes','30px 60px',1,'30px 60px']]){
  const input=page.getByLabel('Shared '+label,{exact:true});await wait(()=>input.evaluate(el=>{for(let n=el.parentElement;n;n=n.parentElement)if(n.tagName==='DETAILS')n.open=true;return new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(()=>resolve(el.isConnected&&el.getClientRects().length>0))));}));assert.equal(await input.isEnabled(),true);
  if(label==='Grid flow')await input.selectOption(value);else{await input.fill(value);await input.press('Enter');}await wait(()=>read()!==source);await settled();await wait(async()=>(await state()).every(values=>expected?values[index]===expected:values[index].split(/\s+/).length===3));assert.deepEqual(await targets.evaluateAll(els=>els.map(el=>el.getAttribute('style'))),styles);
  const changed=read();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.deepEqual(await state(),initial);await wait(async()=>await input.isDisabled());await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
  await page.getByRole('button',{name:'Reset shared '+label.toLowerCase(),exact:true}).click();await settled();await wait(()=>read()!==changed);assert.deepEqual(await state(),initial);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===changed);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);
 }
 await targets.nth(1).evaluate(el=>el.style.setProperty('grid-template-columns','30px 50px','important'));await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();assert.equal(await page.getByLabel('Shared Grid columns',{exact:true}).isDisabled(),true);assert.equal(await page.getByLabel('Shared Column sizes',{exact:true}).isDisabled(),true);assert.equal(read(),source);
 console.log(kind+': PASS inline grid counts, flow, custom tracks, scope isolation, reset, exact undo and inline source preservation');
};
