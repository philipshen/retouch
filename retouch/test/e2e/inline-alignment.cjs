'use strict';
const assert=require('node:assert/strict');
module.exports=async({page,app,read,wait,settled})=>{
 const source=read(),targets=app.locator('h1,p.other-font'),styles=()=>targets.evaluateAll(nodes=>nodes.map(el=>el.getAttribute('style'))),originalStyles=await styles(),state=()=>targets.evaluateAll(nodes=>nodes.map(el=>{const c=getComputedStyle(el);return [c.alignItems,c.justifyItems,c.alignContent,c.justifyContent];})),before=await state();
 await page.getByRole('button',{name:'Shared Align children bottom right',exact:true}).click();await settled();await wait(()=>read()!==source);const aligned=read(),after=await state();assert.deepEqual([after[0][0],after[0][3]],['flex-end','flex-end']);assert.equal(after[0][2],before[0][2]);assert.deepEqual(after[1].slice(0,2),['start','end']);assert.deepEqual(after[1].slice(2),before[1].slice(2));assert.deepEqual(await styles(),originalStyles);
 await page.getByRole('button',{name:'Reset shared child alignment',exact:true}).click();await settled();await wait(()=>read()!==aligned);assert.deepEqual(await state(),before);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===aligned);assert.deepEqual(await state(),after);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);
 await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await page.getByRole('button',{name:'Align children bottom right',exact:true}).click();await settled();await wait(()=>read()!==source);const single=read();assert.deepEqual((await state())[0],after[0]);assert.deepEqual((await state())[1],before[1]);assert.deepEqual(await styles(),originalStyles);
 await page.getByRole('button',{name:'Reset child alignment',exact:true}).click();await settled();await wait(()=>read()!==single);assert.deepEqual(await state(),before);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===single);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);assert.deepEqual(await styles(),originalStyles);
 await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();
 console.log('PASS inline flex/grid alignment, single/shared controls, reset and exact undo');
};
