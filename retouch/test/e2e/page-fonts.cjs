'use strict';
const fs=require('node:fs'),os=require('node:os'),path=require('node:path'),assert=require('node:assert/strict'),{spawn}=require('node:child_process'),{once}=require('node:events');
const variableFont=process.env.RT_E2E_VARIABLE_FONT||process.env.RT_E2E_OPTICAL_FONT;
const canonicalProperties={'font-size':'32px','font-weight':'700','letter-spacing':'2px','text-align':'center','font-style':'oblique','text-decoration-line':'underline line-through','text-transform':'uppercase'};
const canonicalClasses=process.env.RT_E2E_CANONICAL_TEXT?' '+Object.values(require('../../src/text-style-classes.cjs').encode(canonicalProperties)).join(' '):'';
const fixture=process.env.RT_INSPECTOR_FIXTURE;if(!fixture)throw Error('Set RT_INSPECTOR_FIXTURE');const kind=process.env.RT_E2E_RENDERER||'html',engine=process.env.RT_E2E_BROWSER||'chromium',browserType=require(path.join(fixture,'node_modules/playwright'))[engine];
(async()=>{const root=fs.realpathSync(fs.mkdtempSync(path.join(os.tmpdir(),'retouch-page-fonts-'))),file=path.join(root,kind==='react'?((process.env.RT_E2E_COMPONENT_CHOICES||process.env.RT_E2E_TYPED_EXTRACTION)?'app/page.tsx':'app/page.jsx'):kind==='liquid'?'sections/main.liquid':'index.html'),css=(variableFont?'@font-face{font-family:"Variable Test";font-weight:100 900;src:url(data:font/'+(process.env.RT_E2E_OPTICAL_FONT?'ttf':'woff2')+';base64,'+fs.readFileSync(process.env.RT_E2E_OPTICAL_FONT?path.resolve(__dirname,'../fixtures/fonts/RobotoFlex.ttf'):path.join(fixture,'node_modules/next/dist/next-devtools/server/font/geist-latin.woff2')).toString('base64')+')}':'')+(process.env.RT_E2E_SITE_VARIABLES?':root{--brand:#123456;--accent:var(--brand)}.other-font{--accent:#008844}@media(min-width:700px){:root{--brand:#cc3300}}':'')+'@font-face{font-family:"Page Face";src:local("Arial")} .type-editorial{font-family:'+ (variableFont?'"Variable Test"':'Georgia') +',serif;font-size:32px;font-weight:700}.other-font{font-family:"Studio_Test",serif}.named-font{font-family:"Page Face",serif}';fs.mkdirSync(path.dirname(file),{recursive:true});const body='<main><h1 '+(kind==='react'?'className':'class')+'="type-editorial font-bold'+canonicalClasses+' md:opacity-90 [&:hover]:opacity-50">Headline</h1><p '+(kind==='react'?'className':'class')+'="other-font">Other text</p><p '+(kind==='react'?'className':'class')+'="named-font">Named text</p>'+(process.env.RT_E2E_SVG_COLORS?'<svg width="120" height="100" viewBox="0 0 120 100"><rect x="10" y="10" width="80" height="60" fill="red" stroke="blue" /></svg>':'')+'</main>',original=kind==='react'?'export default function Page(){return '+(process.env.RT_E2E_COUPLED_TYPE?body.replace('className="other-font"','className="other-font text-lg/7"').replace('className="named-font"','className="named-font text-xl/9"'):body)+'}':'<html><head><style>'+css+'</style></head><body>'+(kind==='liquid'&&process.env.RT_E2E_LIQUID_DYNAMIC?body.replace('type-editorial font-bold','type-editorial font-bold {% if alternate %}font-mono{% else %}font-serif{% endif %}').replace('class="other-font"','class="other-font {% if alternate %}tracking-wide{% else %}tracking-normal{% endif %}"'):body)+'</body></html>';fs.writeFileSync(file,process.env.RT_E2E_SINGLE_COUPLED?original.replace(kind==='liquid'?'class="other-font"':'className="other-font text-lg/7"',kind==='liquid'?'class="other-font text-lg/9"':'className="other-font text-lg/9"'):original);if(process.env.RT_E2E_FLUID_TYPE)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('text-lg/9','text-[length:clamp(16px,2vw,20px)]/9'));if(process.env.RT_E2E_HTML_SHARED_TYPE)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','.other-font{font-size:20px}.named-font{font-size:32px}</style>'));
 if(process.env.RT_E2E_PAINT_SCOPE)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','h1{color:#123456;background-color:#abcdef;border:2px solid #654321;box-shadow:2px 4px 8px #12345680}</style>'));
 if(process.env.RT_E2E_NUMERIC_RESPONSIVE)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','h1{width:100px}</style>'));
 if(process.env.RT_E2E_HTML_EFFECT_NUMERIC)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','h1{border-style:solid;opacity:.875;rotate:12.5deg;transform:translateX(3px) scale(.9);filter:brightness(.8) blur(2.125px);backdrop-filter:contrast(1.2) blur(3.125px);box-shadow:2.125px 4px 8px 1px #0004,inset 1px 2px 3px 0px #f004}</style>'));
 if(process.env.RT_E2E_HTML_GRADIENT_NUMERIC||process.env.RT_E2E_GRADIENT_INTERRUPTION||process.env.RT_E2E_GRADIENT_SCOPE||process.env.RT_E2E_PAINT_SCOPE)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','h1{background-image:linear-gradient(90.125deg,red 0%,green 40.125%,blue 100%),radial-gradient(ellipse at 50.125% 50%,white 0%,black 100%)}</style>'));
 if(kind==='liquid'&&process.env.RT_E2E_LIQUID_PATH)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</svg>','<path aria-label="Vector path" d="M10 10 L90 10 L50 70 Z M100 10 C110 0 120 20 110 30" fill="#8844cc"/></svg>'));
 if(process.env.RT_E2E_SVG_CONVERT){const tag=process.env.RT_E2E_SVG_CONVERT_KIND||'rect',attrs={rect:'x="10" y="10" width="80" height="60" rx="8"',circle:'cx="50" cy="45" r="30"',ellipse:'cx="50" cy="45" rx="35" ry="20"',line:'x1="10" y1="10" x2="90" y2="70"'}[tag];fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('<rect x="10" y="10" width="80" height="60" fill="red" stroke="blue" />','<'+tag+' '+attrs+' fill="red" stroke="blue" />'));}
 if(process.env.RT_E2E_SVG_METADATA){const tag=process.env.RT_E2E_SVG_CONVERT_KIND||'rect';fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('fill="red" stroke="blue" />','fill="red" stroke="blue" role="img" aria-labelledby="shape-title" aria-describedby="shape-description"><title id="shape-title">Accessible shape</title><desc id="shape-description">A shape with a preserved description</desc></'+tag+'>'));}
 if(kind==='liquid'&&process.env.RT_E2E_LIQUID_GEOMETRY_DYNAMIC)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('<rect x="10"','<rect x="{{ shape_x }}"'));
 if(process.env.RT_E2E_EFFECT_STYLES)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','.type-editorial{box-shadow:0px 4px 8px 1px rgba(0,0,0,0.3);filter:blur(1px);backdrop-filter:blur(2px)}</style>'));
 if(process.env.RT_E2E_IMPORTED_GRADIENT)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('font-bold','font-bold ![background-image:linear-gradient(90deg,red_0%_25%,blue_25%,green,black)]'));
 if(process.env.RT_E2E_GRADIENTS)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('font-bold','font-bold !bg-cover !bg-center !bg-no-repeat !bg-[#12345678]'));
 if(process.env.RT_E2E_COLLECTION_BINDINGS){
  assert.ok(['react','liquid'].includes(kind));process.env.RETOUCH_STATE_DIR=path.join(fs.realpathSync(root),'.history-cache');const library=require('../../src/variable-library.cjs'),id=n=>'00000000-0000-4000-8000-'+String(n).padStart(12,'0'),data={version:1,collections:[{id:id(1),name:'Theme',defaultMode:id(2),modes:[{id:id(2),name:'Light'},{id:id(3),name:'Dark'}]}],variables:[{id:id(4),collectionId:id(1),name:'Brand',type:'color',values:{[id(2)]:'#123456',[id(3)]:'#cc3300'}},{id:id(5),collectionId:id(1),name:'Space',type:'number',values:{[id(2)]:24,[id(3)]:48}}]};library.commitPlan(root,library.planChange(root,{type:'replace',revision:null,library:data}));
  if(kind==='react'){
   const adapter=require('../../src/adapters/react.cjs'),bindings=require('../../src/jsx-variable-bindings.cjs'),extraFile=path.join(root,'app/Extra.jsx'),source='export default function Extra(){return <h2>Another source file</h2>}',elements=adapter.collect(source,'app/Extra.jsx').elements;
   const planned=bindings.plan({file:extraFile,relPath:'app/Extra.jsx',source,elements,element:elements.find(element=>element.kind==='host'),hash:adapter.contentHash(source)},{type:'applyVariable',scope:'md:',property:'color',binding:{id:id(4),modes:{[id(1)]:id(3)}}},data);assert.equal(planned.ok,true,planned.reason);fs.writeFileSync(extraFile,planned.edits[0].after);fs.writeFileSync(file,'import Extra from "./Extra";\n'+fs.readFileSync(file,'utf8').replace('</main>','<Extra/></main>'));
  }
 }
 if(process.env.RT_E2E_HOST_RENAME)process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');
 if(process.env.RT_E2E_COMPONENT_PROPERTY_EDIT){assert.equal(kind,'react');process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');fs.writeFileSync(file,'export default function Page(){return <main><HeadlineCard title="First" count={1} enabled/></main>}\n/** @retouch-component */\nfunction HeadlineCard({title="Default title",count=10,enabled=false}){return <h1 title={title} data-count={count} data-enabled={enabled} className="type-editorial">Headline</h1>}');}
 if(process.env.RT_E2E_COMPONENT_CHOICES){assert.ok(process.env.RT_E2E_COMPONENT_PROPERTY_EDIT);fs.writeFileSync(file,'type Compact="small"; type Expanded="large"; type AllSizes=Compact|Expanded|"small"|"hidden"; type Size=Exclude<Extract<AllSizes,string>,"hidden">; interface BaseProps {title?:string;count?:number;enabled?:boolean} interface VariantProps {size?:Size;tone?:"calm"|"bold"} interface InheritedProps extends BaseProps,VariantProps {} type Props=InheritedProps & {extra?:string}\n'+fs.readFileSync(file,'utf8').replace('count={1} enabled/>','count={1} enabled size="small"/>').replace('count=10,enabled=false})','count=10,enabled=false,size="small",tone}:Props)').replace('<h1 title={title}','<h1 style={{fontSize:size===\"large\"?48:32}} data-size={size} data-tone={tone||\"none\"} title={title}'));}
 if(process.env.RT_E2E_COMPONENT_CHOICES){fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</main>','<Badge/></main>')+'\ninterface BadgeBase {tone:"calm"|"bold";emphasized:boolean;label:string;width:number;color:string;cornerRadius:number;padding:number;tooltip:string;unused:string} interface BadgeOptions {tone:string;emphasized:boolean;label:string;width:number;color:string;cornerRadius:number;padding:number;tooltip:string} type BadgeProps=Readonly<Partial<Pick<BadgeBase,keyof BadgeOptions>>>\n/** @retouch-component */\nfunction Badge(props:BadgeProps){return <aside title={props.tooltip??""} data-tone={props.tone||"none"} style={{color:props.color??"inherit",borderRadius:props.cornerRadius??0,padding:props.padding??0,minHeight:24,width:props.width??"auto",fontWeight:props.emphasized?700:400,backgroundColor:props.tone==="bold"?"rgb(204, 51, 0)":"rgb(220, 220, 220)"}}>{props.label??"Badge"}</aside>}');}
 const importedTypeSources=new Map();
 if(process.env.RT_E2E_COMPONENT_IMPORTED_TYPES){
  assert.ok(process.env.RT_E2E_COMPONENT_CHOICES);
  let source=fs.readFileSync(file,'utf8'),headEnd=source.indexOf('\n'),head=source.slice(0,headEnd).replace('type Props=','export type Props=');source=source.slice(headEnd+1);
  const badgeStart=source.indexOf('interface BadgeBase'),badgeEnd=source.indexOf('/** @retouch-component */',badgeStart),badge=source.slice(badgeStart,badgeEnd).replace('type BadgeProps=','export type BadgeProps=');source=source.slice(0,badgeStart)+source.slice(badgeEnd);
  for(const [name,text] of [['headline-types.ts',head],['badge-types.ts',badge],['contracts.ts','export type {Props as HeadlineProps} from "./headline-types"; export type {BadgeProps} from "./badge-types";']]){const target=path.join(root,'app',name);fs.writeFileSync(target,text);importedTypeSources.set(target,text);}
  fs.writeFileSync(file,'import type {HeadlineProps as Props, BadgeProps} from "./contracts";\n'+source);
  if(process.env.RT_E2E_COMPONENT_NAMESPACE_TYPES){
   const barrel=path.join(root,'app/contracts.ts'),text='export * from "./headline-types"; export * from "./badge-types";';fs.writeFileSync(barrel,text);importedTypeSources.set(barrel,text);
   fs.writeFileSync(file,'import type * as Contracts from "./contracts";\n'+source.replace(':Props)',':Contracts.Props)').replace(':BadgeProps)',':Contracts.BadgeProps)'));
  }
  if(process.env.RT_E2E_COMPONENT_NESTED_NAMESPACE){
   for(const [name,text] of [['library.ts','export * as Headline from "./headline-types"; export * as Badge from "./badge-types";'],['contracts.ts','import type * as Library from "./library"; export type {Library as Design};']]){const target=path.join(root,'app',name);fs.writeFileSync(target,text);importedTypeSources.set(target,text);}
   fs.writeFileSync(file,'import type {Design} from "./contracts";\n'+source.replace(':Props)',':Design.Headline.Props)').replace(':BadgeProps)',':Design.Badge.BadgeProps)'));
  }
 }
 if(process.env.RT_E2E_COMPONENT_TYPE_PATHS){
  assert.ok(process.env.RT_E2E_COMPONENT_IMPORTED_TYPES);
  fs.mkdirSync(path.join(root,'config'));const base=path.join(root,'config/base.json'),text='{ "compilerOptions": { "paths": { "@/*": ["../preferred/*", "../app/*"] } } }';fs.writeFileSync(base,text);importedTypeSources.set(base,text);
  fs.writeFileSync(path.join(root,'tsconfig.json'),'{ "extends": "./config/base.json" }');fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('from "./contracts"','from "@/contracts"'));
 }
 if(process.env.RT_E2E_COMPONENT_LIBRARY){assert.ok(process.env.RT_E2E_COMPONENT_CHOICES);fs.mkdirSync(path.join(root,'app/other'));fs.writeFileSync(path.join(root,'app/other/page.tsx'),'/** @retouch-component */\nfunction RemoteBadge(){return <aside>Other page badge</aside>} export default function Other(){return <RemoteBadge/>}');}
 if(process.env.RT_E2E_UNUSED_COMPONENTS){assert.ok(process.env.RT_E2E_COMPONENT_LIBRARY);fs.mkdirSync(path.join(root,'app/components'));fs.writeFileSync(path.join(root,'app/components/UnusedButton.tsx'),'throw new Error("Unused component module must not execute");export function UnusedButton({label="Ready",tone}:{label?:string;tone?:"calm"|"bold"}){return <button>{label}</button>}');fs.writeFileSync(path.join(root,'app/components/SingleCard.tsx'),'export function SingleCard({label="Single default"}){return <footer title={label}>Single card</footer>}');fs.writeFileSync(file,'import {SingleCard} from "./components/SingleCard";\n'+fs.readFileSync(file,'utf8').replace('</main>','<SingleCard label="Only instance"/></main>'));}
 if(process.env.RT_E2E_PREVIEW_OCCURRENCES){assert.ok(process.env.RT_E2E_UNUSED_COMPONENTS);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('<SingleCard label="Only instance"/>','{["First occurrence","Second occurrence"].map(label=><SingleCard key={label} label={label}/>)}'));const target=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(target,fs.readFileSync(target,'utf8').replace('Single card','{label}'));}
 if(process.env.RT_E2E_COMPONENT_BRANCHES){assert.ok(process.env.RT_E2E_UNUSED_COMPONENTS);const target=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(target,fs.readFileSync(target,'utf8').replace('{label="Single default"}','{label="Single default",alternate=false}').replace('return <footer','if(alternate){return <section data-variant-card="alternate" title={label}>Alternate card</section>}return <footer'));}
 if(process.env.RT_E2E_COMPONENT_ARROWS){assert.ok(process.env.RT_E2E_COMPONENT_BRANCHES);const target=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(target,fs.readFileSync(target,'utf8').replace('export function SingleCard(', 'export const SingleCard = (').replace('alternate=false}){','alternate=false}) => {'));}
 if(process.env.RT_E2E_COMPONENT_FRAGMENTS){assert.ok(process.env.RT_E2E_COMPONENT_BRANCHES);const target=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(target,fs.readFileSync(target,'utf8').replace('return <footer','return <><footer').replace('</footer>','</footer><small data-fragment-sibling="true" title={label}>Fragment sibling</small></>'));}
 if(process.env.RT_E2E_NESTED_COMPONENT_NAVIGATION){assert.ok(process.env.RT_E2E_FRAGMENT_NAVIGATION&&process.env.RT_E2E_COMPONENT_FRAGMENTS);const target=path.join(root,'app/components/SingleCard.tsx'),source=fs.readFileSync(target,'utf8');fs.writeFileSync(target,source.replace('Fragment sibling</small>','Fragment sibling<InnerLabel/></small>')+'\n/** @retouch-component */\nfunction InnerLabel(){return <em data-inner-label="true">Inner label</em>}');}
 if(process.env.RT_E2E_PREVIEW_IDENTITIES){assert.ok(process.env.RT_E2E_PREVIEW_OCCURRENCES);const target=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(target,fs.readFileSync(target,'utf8').replace('<footer title={label}','<footer id={label.replaceAll(" ","-")} title={label}'));}
 if(process.env.RT_E2E_CONDITIONAL_FRAGMENT){assert.ok(process.env.RT_E2E_COMPONENT_FRAGMENTS);const target=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(target,fs.readFileSync(target,'utf8').replace('alternate=false','alternate=false,showDetails=false').replace('</footer><small','</footer>{showDetails ? <em data-fragment-optional="true" style={{display:"block",height:30}}>Details</em> : null}<small'));}
 if(process.env.RT_E2E_FRAGMENT_OPTIONAL_EDGE){assert.ok(process.env.RT_E2E_CONDITIONAL_FRAGMENT);const target=path.join(root,'app/components/SingleCard.tsx'),source=fs.readFileSync(target,'utf8'),optional=source.match(/\{showDetails \? <em.*?<\/em> : null\}/)?.[0];assert.ok(optional);const without=source.replace(optional,'');fs.writeFileSync(target,process.env.RT_E2E_FRAGMENT_OPTIONAL_EDGE==='prefix'?without.replace('return <><footer','return <>'+optional+'<footer'):without.replace('</small></>','</small>'+optional+'</>'));}
 if(process.env.RT_E2E_NAMED_FRAGMENT){assert.ok(process.env.RT_E2E_COMPONENT_FRAGMENTS);const target=path.join(root,'app/components/SingleCard.tsx'),namespace=process.env.RT_E2E_NAMED_FRAGMENT==='namespace',tag=namespace?'R.Fragment':'Group';fs.writeFileSync(target,(namespace?'import * as R from "react";':'import {Fragment as Group} from "react";')+fs.readFileSync(target,'utf8').replace('return <>','return <'+tag+' key="fragment">').replace('</>','</'+tag+'>'));}
 if(process.env.RT_E2E_REQUIRED_INSERT){assert.ok(process.env.RT_E2E_COMPONENT_INSERT);fs.writeFileSync(path.join(root,'app/components/required-types.ts'),'export interface RequiredProps {label:string;width:number;enabled:boolean;tone:"calm"|"bold";note?:string}');fs.writeFileSync(path.join(root,'app/components/RequiredCard.tsx'),'import type {RequiredProps} from "./required-types";export function RequiredCard({label,width,enabled,tone,note="Default"}:RequiredProps){return <section data-required-card="true" title={label} data-enabled={String(enabled)} data-tone={tone} data-note={note} style={{width}}>{label}</section>}');}
 if(process.env.RT_E2E_NAMESPACE_INSERT){assert.ok(process.env.RT_E2E_COMPONENT_INSERT);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('import {SingleCard} from "./components/SingleCard"','import * as ZCards from "./components/SingleCard"').replace('<SingleCard label="Only instance"/>','<ZCards.SingleCard label="Only instance"/>'));}
 if(process.env.RT_E2E_COMPONENT_SWAP){assert.ok(process.env.RT_E2E_COMPONENT_INSERT);const single=path.join(root,'app/components/SingleCard.tsx');fs.writeFileSync(single,fs.readFileSync(single,'utf8').replace('{label="Single default"','{label="Single default",legacy="Default legacy"'));fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('label="Only instance"','label="Only instance" legacy="Old override" key="single"'));fs.writeFileSync(path.join(root,'app/components/SwapCard.tsx'),'export function SwapCard({label,width,tone="bold"}:{label:string;width:number;tone?:"calm"|"bold"}){return <aside data-swap-card="true" title={label} data-tone={tone} style={{width}}>{label}</aside>}');}
 if(process.env.RT_E2E_ROOT_SWAP){assert.ok(process.env.RT_E2E_COMPONENT_SWAP);const source=fs.readFileSync(file,'utf8'),usage=source.match(/<(?:ZCards\.)?SingleCard label="Only instance"[^>]*\/>/)?.[0];assert.ok(usage);fs.writeFileSync(file,source.replace(usage,'<RootWrapper/>')+'\nfunction RootWrapper(){return '+usage+'}');}
 if(process.env.RT_E2E_COMPONENT_PROPS){assert.equal(kind,'react');fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('export default function Page(){return','\"use client\"; import {useState} from \"react\"; export default function Page(){const [label,setLabel]=useState(\"Headline\"), title=\"Local title\", onClick=()=>setLabel(\"Clicked\");return').replace('>Headline</h1>',' title={title} onClick={onClick}>{label}</h1>'));}
 if(process.env.RT_E2E_CREATE_COMPONENT){assert.equal(kind,'react');process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');}
 if(process.env.RT_E2E_TYPED_EXTRACTION){assert.ok(process.env.RT_E2E_COMPONENT_PROPS&&process.env.RT_E2E_CREATE_COMPONENT);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('const [label,setLabel]=useState("Headline"), title="Local title", onClick=()=>setLabel("Clicked");','const [stateLabel,setLabel]=useState("Headline");const label:string=stateLabel, title:string="Local title", onClick:()=>void=()=>setLabel("Clicked");'));}

 if(process.env.RT_E2E_OPTIONAL_PARAMETER_EXTRACTION){assert.ok(process.env.RT_E2E_TYPED_EXTRACTION&&!process.env.RT_E2E_TYPED_TUPLE_EXTRACTION);const source=fs.readFileSync(file,'utf8');assert.ok(source.includes('title:string="Local title"'));fs.writeFileSync(file,source.replace('function Page()','function Page({title}:{title?:string})').replace(', title:string="Local title"','').replace('title={title}','title={title??"Local title"}'));}
 if(process.env.RT_E2E_LITERAL_DEFAULT_EXTRACTION){assert.ok(process.env.RT_E2E_OPTIONAL_PARAMETER_EXTRACTION);const source=fs.readFileSync(file,'utf8');assert.ok(source.includes('function Page({title}:{title?:string})'));fs.writeFileSync(file,source.replace('function Page({title}:{title?:string})','function Page({title="Local title"}:{title?:string})').replace('title={title??"Local title"}','title={title}'));}
 if(process.env.RT_E2E_OPTIONAL_TUPLE_DEFAULT_EXTRACTION){assert.ok(process.env.RT_E2E_LITERAL_DEFAULT_EXTRACTION);const source=fs.readFileSync(file,'utf8'),before='function Page({title="Local title"}:{title?:string}){',after='function Page(){const [title="Local title"]:[title?:string]=[];';assert.ok(source.includes(before));fs.writeFileSync(file,source.replace(before,after));}
 if(process.env.RT_E2E_TYPED_TUPLE_EXTRACTION){assert.ok(process.env.RT_E2E_TYPED_EXTRACTION);const source=fs.readFileSync(file,'utf8'),before='const [stateLabel,setLabel]=useState("Headline");const label:string=stateLabel, title:string="Local title", onClick:()=>void=()=>setLabel("Clicked");',after='const [label,setLabel]:[text:string,update:(value:string)=>void]=useState("Headline");const {heading:title}:{heading:string}={heading:"Local title"};const onClick:()=>void=()=>setLabel("Clicked");';assert.ok(source.includes(before));fs.writeFileSync(file,source.replace(before,after));}
 if(process.env.RT_E2E_NAMED_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_TYPED_TUPLE_EXTRACTION);const source=fs.readFileSync(file,'utf8');fs.writeFileSync(file,source.replace(':[text:string,update:(value:string)=>void]',':ScreenState').replace(':{heading:string}',':HeadingProps').replace('onClick:()=>void','onClick:Click')+'\ntype ScreenState=[text:string,update:(value:string)=>void];interface HeadingProps {heading:string}type Click=()=>void;');}
 if(process.env.RT_E2E_INHERITED_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_NAMED_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8'),before='interface HeadingProps {heading:string}';assert.ok(source.includes(before));fs.writeFileSync(file,source.replace(before,'interface HeadingBase {heading:string}interface HeadingMiddle extends HeadingBase {}interface HeadingProps extends HeadingMiddle {}'));}
 if(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_INHERITED_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8'),before='const {heading:title}:HeadingProps={heading:"Local title"}';assert.ok(source.includes(before));fs.writeFileSync(file,source.replace(before,'const headingData:HeadingProps={heading:"Local title"}').replace('title={title}','title={headingData.heading}').replace('interface HeadingBase {heading:string}','type HeadingText=string;interface HeadingBase {heading:HeadingText}'));}
 if(process.env.RT_E2E_METHOD_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8'),before='const onClick:Click=()=>setLabel("Clicked");';assert.ok(source.includes(before));fs.writeFileSync(file,source.replace(before,'const {onClick}:ClickProps={onClick:()=>setLabel("Clicked")};')+'\ninterface ClickProps{onClick():void}');}
 if(process.env.RT_E2E_OPTIONAL_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8');assert.ok(source.includes('headingData:HeadingProps={heading:"Local title"}'));fs.writeFileSync(file,source.replace('headingData:HeadingProps={heading:"Local title"}','headingData:HeadingProps={}').replace('title={headingData.heading}','title={headingData.heading??"Local title"}').replace('heading:HeadingText','heading?:HeadingText'));}
 if(process.env.RT_E2E_INDEX_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION&&!process.env.RT_E2E_OPTIONAL_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8');assert.ok(source.includes('headingData:HeadingProps={heading:"Local title"}'));fs.writeFileSync(file,source.replace('headingData:HeadingProps={heading:"Local title"}','headingData:HeadingProps={}').replace('title={headingData.heading}','title={headingData["heading"]??"Local title"}').replace('heading:HeadingText','readonly [key:string]:HeadingText').replace('type ScreenState=[','type ScreenState=readonly ['));}
 if(process.env.RT_E2E_UNION_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION&&!process.env.RT_E2E_OPTIONAL_CONTRACT_EXTRACTION&&!process.env.RT_E2E_INDEX_CONTRACT_EXTRACTION&&!process.env.RT_E2E_INTERSECTION_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8');assert.ok(source.includes('type HeadingText=string;'));fs.writeFileSync(file,source.replace('type HeadingText=string;','type HeadingText=string|null;').replace('headingData:HeadingProps={heading:"Local title"}','headingData:HeadingProps={heading:null}').replace('title={headingData.heading}','title={headingData.heading??"Local title"}'));}
 if(process.env.RT_E2E_INTERSECTION_CONTRACT_EXTRACTION){assert.ok(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION&&!process.env.RT_E2E_OPTIONAL_CONTRACT_EXTRACTION&&!process.env.RT_E2E_INDEX_CONTRACT_EXTRACTION);const source=fs.readFileSync(file,'utf8'),before='interface HeadingProps extends HeadingMiddle {}';assert.ok(source.includes(before));fs.writeFileSync(file,source.replace(before,'type HeadingProps=HeadingMiddle & {note?:number};'));}
 if(process.env.RT_E2E_CREATE_COMPONENT_LOCKS){assert.ok(process.env.RT_E2E_CREATE_COMPONENT);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('>Headline</h1>',' data-rt-layer-name="Headline"><span>Headline</span></h1>'));}
 if(process.env.RT_E2E_SHARED_GRID_SPANS){assert.ok(['react','liquid'].includes(kind));process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');const attr=kind==='react'?'className':'class';fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('<main>','<main '+attr+'="md:grid md:grid-cols-4 '+(process.env.RT_E2E_SHARED_GRID_FRACTIONAL?'md:grid-rows-2':'md:grid-rows-[100px_100px]')+' md:auto-rows-[100px] md:w-[400px] md:h-[200px]">').replace('type-editorial font-bold','type-editorial font-bold w-[80px] h-[40px] md:w-auto md:h-auto min-w-0 md:!col-start-2 md:row-start-1').replace(attr+'="other-font',attr+'="other-font w-[90px] h-[60px] md:w-auto md:h-auto min-w-0 md:col-start-4 md:row-start-2').replace(attr+'="named-font',attr+'="named-font md:hidden'));}
 if(process.env.RT_E2E_SHARED_ALIGNMENT){assert.ok(['react','liquid'].includes(kind));process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');const attr=kind==='react'?'className':'class',layout=process.env.RT_E2E_SHARED_ALIGNMENT_GRID?'grid grid-cols-2':'flex';fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('<main>','<main '+attr+'="md:'+layout.replaceAll(' ',' md:')+' md:w-[400px] md:h-[200px]">').replace('type-editorial font-bold','type-editorial font-bold w-[80px] h-[40px] md:!place-self-start').replace(attr+'="other-font',attr+'="other-font w-[90px] h-[60px] md:place-self-end').replace(attr+'="named-font',attr+'="named-font md:hidden'));}
 if(process.env.RT_E2E_HTML_CONTENTS){assert.equal(kind,'html');fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</style>','.contents{display:contents}main{display:flex;width:150px;height:180px}h1,p.other-font{width:100px;min-width:0;flex:0 1 100px}.named-font{display:none}@media(min-width:1024px){main{width:400px}}</style>').replace(/<h1[^>]*>Headline<\/h1>|<p[^>]*other-font[^>]*>Other text<\/p>/g,el=>'<div class="contents">'+el+'</div>'));}
 if(process.env.RT_E2E_SHARED_COLUMNS){const classes='columns-2 gap-2 w-[240px] h-[180px] '+(process.env.RT_E2E_SHARED_COLUMNS_VERTICAL?'[writing-mode:vertical-rl] ':''),attr=kind==='react'?'className':'class';fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('type-editorial font-bold',classes+'type-editorial font-bold').replace(attr+'="other-font"',attr+'="'+classes+'other-font"'));}
 if(process.env.RT_E2E_SINGLE_ROTATION){
  process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');let source=fs.readFileSync(file,'utf8').replace('type-editorial font-bold','type-editorial font-bold !rotate-[-10deg]');if(kind==='html')source=source.replace('</style>','h1{rotate:-10deg!important}</style>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_SINGLE_ALIGN){
  process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');const attr=kind==='react'?'className':'class';let source=fs.readFileSync(file,'utf8').replace('<main>','<main '+attr+'="relative w-[300px] h-[240px] border-[4px] ml-[20px] mt-[20px]">').replace('type-editorial font-bold','type-editorial font-bold box-content w-[100px] h-[50px] p-[10px] border-[2px] md:absolute md:!right-[25px] md:top-[30px]');
  if(process.env.RT_E2E_SINGLE_CANVAS)source=source.replace('type-editorial font-bold','type-editorial font-bold max-w-[120px] max-h-[70px]');
  if(kind==='html')source=source.replace('</style>','main{box-sizing:border-box;position:relative;width:300px;height:240px;border:4px solid;margin:20px}h1{box-sizing:content-box;width:100px;height:50px;padding:10px;border:2px solid;margin:0;'+(process.env.RT_E2E_SINGLE_CANVAS?'max-width:120px;max-height:70px;':'')+'}@media(min-width:768px){h1{position:absolute;right:25px;top:30px}}</style>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_ROTATED_POSITION){
  assert.ok(process.env.RT_E2E_SINGLE_ALIGN);let source=fs.readFileSync(file,'utf8').replace('type-editorial font-bold','type-editorial font-bold md:rotate-[30deg] [transform-origin:20px_10px]');if(kind==='html')source=source.replace('</style>','h1{transform-origin:20px 10px}@media(min-width:768px){h1{rotate:30deg}}</style>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_ROTATED_RESIZE_PERCENT){assert.ok(process.env.RT_E2E_ROTATED_POSITION);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replaceAll('transform-origin:20px_10px','transform-origin:25%_75%').replaceAll('transform-origin:20px 10px','transform-origin:25% 75%'));}
 if(process.env.RT_E2E_SHARED_ROTATED_GEOMETRY){
  process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');const attr=kind==='react'?'className':'class';let source=fs.readFileSync(file,'utf8').replace('<main>','<main '+attr+'="relative w-[400px] h-[400px]">').replace('type-editorial font-bold','type-editorial font-bold md:absolute md:left-[20px] md:top-[40px] md:w-[80px] md:h-[50px] md:rotate-[30deg] [transform-origin:0px_0px]').replace(attr+'="other-font',attr+'="other-font md:absolute md:left-[160px] md:top-[140px] md:w-[90px] md:h-[70px] md:rotate-[-20deg] [transform-origin:20px_10px]');if(kind==='html')source=source.replace('</style>','main{position:relative;width:400px;height:400px}@media(min-width:768px){h1,p.other-font{position:absolute;margin:0}h1{left:20px;top:40px;width:80px;height:50px;rotate:30deg;transform-origin:0 0}p.other-font{left:160px;top:140px;width:90px;height:70px;rotate:-20deg;transform-origin:20px 10px}}</style>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_SHARED_ABSOLUTE){
  process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');const attr=kind==='react'?'className':'class';let source=fs.readFileSync(file,'utf8');
  source=source.replace(/<h1[^>]*>Headline<\/h1>|<p[^>]*other-font[^>]*>Other text<\/p>/g,element=>'<section '+attr+'="relative flow-root p-[12px] ml-[20px]">'+element+'</section>').replace('type-editorial font-bold','type-editorial font-bold w-[160px] h-[60px] m-[8px]').replace(attr+'="other-font',attr+'="other-font box-content w-[100px] h-[40px] p-[10px] border-[2px] m-[5px]');
  if(kind==='html')source=source.replace('</style>','section{position:relative;display:flow-root;padding:12px;margin-left:20px}h1{width:160px;height:60px;margin:8px}p.other-font{box-sizing:content-box;width:100px;height:40px;padding:10px;border:2px solid;margin:5px}</style>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_SHARED_ABSOLUTE_ROTATED){
  assert.ok(process.env.RT_E2E_SHARED_ABSOLUTE);const attr=kind==='react'?'className':'class';let source=fs.readFileSync(file,'utf8').replace('type-editorial font-bold','type-editorial font-bold md:rotate-[30deg] [transform-origin:0px_0px]').replace(attr+'="other-font',attr+'="other-font md:rotate-[-20deg] [transform-origin:20px_10px]');if(kind==='html')source=source.replace('</style>','h1{transform-origin:0 0}p.other-font{transform-origin:20px 10px}@media(min-width:768px){h1{rotate:30deg}p.other-font{rotate:-20deg}}</style>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_SHARED_ABSOLUTE_ROTATED_AUTO){assert.ok(process.env.RT_E2E_SHARED_ABSOLUTE_ROTATED);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('w-[160px]','w-auto').replace('width:160px;height:60px','width:auto;height:60px').replace('[transform-origin:0px_0px]','[transform-origin:25%_75%]').replace('h1{transform-origin:0 0}','h1{transform-origin:25% 75%}'));}
 if(process.env.RT_E2E_SHARED_FILL_MIXED){
  assert.ok(['react','liquid'].includes(kind));const attr=kind==='react'?'className':'class';let source=fs.readFileSync(file,'utf8');
  source=source.replace(/<h1([^>]*)>Headline<\/h1>/,(_,attrs)=>'<section '+attr+'="md:flex md:w-[300px] lg:w-[400px] md:h-[180px]" aria-label="Row parent"><h1'+attrs.replace('type-editorial','w-[80px] h-[40px] min-w-0 min-h-0 md:!size-[100px] type-editorial')+'>Headline</h1><span '+attr+'="w-[50px] shrink-0">Sibling</span></section>');
  source=source.replace(/<p([^>]*other-font[^>]*)>Other text<\/p>/,(_,attrs)=>'<section '+attr+'="md:grid md:w-[260px] lg:w-[360px] md:h-[180px] [writing-mode:vertical-rl]" aria-label="Vertical grid parent"><p'+attrs.replace('other-font','other-font w-[80px] h-[40px] min-w-0 min-h-0 box-content p-[10px] border-[2px] md:size-[100px]')+'>Other text</p></section>');if(process.env.RT_E2E_SHARED_FILL_FLOW)source=source.replace('md:grid md:w-[260px] lg:w-[360px] md:h-[180px] [writing-mode:vertical-rl]','md:flow-root md:w-[220px] lg:w-[320px] md:h-[180px]').replace('other-font w-[80px]','other-font m-[5px] w-[80px]');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_SHARED_FLEX){assert.ok(['react','liquid'].includes(kind));process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');const attr=kind==='react'?'className':'class';fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('<main>','<main '+attr+'="md:flex md:w-[150px] lg:w-[400px]">').replace('type-editorial font-bold','type-editorial font-bold w-[100px] min-w-0 md:!flex-[0_1_100px]').replace(attr+'="other-font',attr+'="other-font w-[100px] min-w-0 md:flex-[0_1_100px]').replace(attr+'="named-font',attr+'="named-font md:hidden'));}
 if(process.env.RT_E2E_SHARED_FLEX_CONTENTS){assert.ok(process.env.RT_E2E_SHARED_FLEX);const attr=kind==='react'?'className':'class';fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace(/<h1[^>]*>Headline<\/h1>|<p[^>]*other-font[^>]*>Other text<\/p>/g,element=>'<div '+attr+'="contents">'+element+'</div>'));}
 if(process.env.RT_E2E_SHARED_FILL_GRID){assert.ok(process.env.RT_E2E_SHARED_FLEX);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('md:flex md:w-[150px] lg:w-[400px]','md:grid md:grid-cols-2 md:h-[180px] md:w-[150px] lg:w-[400px]'));}
 if(process.env.RT_E2E_SHARED_FLEX_COLUMN){assert.ok(process.env.RT_E2E_SHARED_FLEX);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('md:flex md:w-[150px] lg:w-[400px]','md:flex md:flex-col md:w-[150px] lg:w-[400px] md:h-[150px] lg:h-[400px]').replaceAll('w-[100px] min-w-0','w-[100px] min-w-0 h-[100px] min-h-0'));}
 if(process.env.RT_E2E_SHARED_SECTIONS||process.env.RT_E2E_SHARED_SIZING||process.env.RT_E2E_SHARED_CONSTRAINTS||process.env.RT_E2E_SHARED_RATIO){assert.ok(['react','liquid'].includes(kind));process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('type-editorial font-bold','type-editorial font-bold size-[80px] md:!size-[120px]').replace((kind==='react'?'className':'class')+'="other-font',(kind==='react'?'className':'class')+'="other-font box-content size-[90px] md:size-[140px] md:h-[100px] md:p-[10px] md:border-[2px]'));}
 if(process.env.RT_E2E_LIQUID_SHARED_CLASSES){assert.equal(kind,'liquid');process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('type-editorial font-bold','type-editorial font-bold md:absolute md:left-10 md:top-10 md:w-40').replace('class="other-font','class="other-font md:absolute md:left-24 md:top-40 md:w-24'));}
 if(process.env.RT_E2E_LIQUID_SELECTION_STYLES){
  assert.equal(kind,'liquid');process.env.RETOUCH_STATE_DIR=path.join(root,'.history-cache');fs.mkdirSync(path.join(root,'.retouch'),{recursive:true});
  for(const [family,properties]of Object.entries({text:{'font-size':'28px'},color:{color:'#123456ff'},effect:{filter:'blur(2px)'}}))fs.writeFileSync(path.join(root,'.retouch',family+'-styles.json'),JSON.stringify({version:1,styles:[{id:'11111111-1111-4111-8111-111111111111',name:'Shared '+family,properties}]}));
 }
 if(process.env.RT_E2E_COMPONENT_REPARENT){assert.equal(kind,'react');const frame='<section aria-label="Destination frame"><p>Destination</p></section>',destination=process.env.RT_E2E_COMPONENT_REPARENT_CALLBACK?'{[1].map(() => ('+frame+'))}':frame;fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</main>',destination+'</main>'));}
 if(process.env.RT_E2E_PICKER_MULTI)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace(kind==='html'?'.other-font{font-family:':kind==='react'?'className="other-font"':'class="other-font"',kind==='html'?'.other-font{color:#ff8800;font-family:':kind==='react'?'className="other-font text-[#ff8800]"':'class="other-font text-[#ff8800]"'));
 if(process.env.RT_E2E_SHARED_GAPS){const source=fs.readFileSync(file,'utf8'),attribute=kind==='react'?'className':'class';fs.writeFileSync(file,source.replace('type-editorial font-bold','type-editorial font-bold flex gap-1').replace(attribute+'="other-font"',attribute+'="other-font grid grid-cols-2 gap-2 [writing-mode:vertical-rl]"').replace('>Headline</h1>','>Headline<span>A</span><span>B</span></h1>').replace('>Other text</p>','>Other text<span>A</span><span>B</span><span>C</span><span>D</span></p>'));}
 if(process.env.RT_E2E_SHARED_CLIP){assert.ok(process.env.RT_E2E_SHARED_GAPS);fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('other-font grid','other-font overflow-hidden grid'));}
 if(process.env.RT_E2E_SHARED_ALIGNMENT_GEOMETRY){
  assert.ok(process.env.RT_E2E_SHARED_GAPS);const attribute=kind==='react'?'className':'class',children=['A','B','C'].map(text=>'<span '+attribute+'="w-[50px] h-[30px] shrink-0">'+text+'</span>').join('');let source=fs.readFileSync(file,'utf8');
  source=source.replace(/<h1([^>]*)>[\s\S]*?<\/h1>/,(_,attrs)=>'<h1'+attrs.replace(attribute+'="',attribute+'="w-[120px] h-[100px] ')+' aria-label="Headline">'+children+'</h1>');source=source.replace(/<p([^>]*other-font[^>]*)>[\s\S]*?<\/p>/,(_,attrs)=>'<p'+attrs.replace(attribute+'="',attribute+'="w-[120px] h-[100px] ')+' aria-label="Other text">'+children+'</p>');fs.writeFileSync(file,source);
 }
 if(process.env.RT_E2E_PICKER_MULTI_SCOPE&&process.env.RT_E2E_SVG_COLORS)fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace('</svg>','<circle cx="60" cy="40" r="20" fill="orange" stroke="green"/><ellipse cx="50" cy="50" rx="20" ry="10" fill="black" stroke="purple"/></svg>'));
 let server,child,stopped,browser,url,logs='';const read=()=>fs.readFileSync(file,'utf8'),wait=async fn=>{for(let i=0;i<400;i++){try{if(await fn())return;}catch(error){if(!/Execution context was destroyed|fetch failed/.test(error.message))throw error;}await new Promise(r=>setTimeout(r,100));}throw Error('Timed out '+logs.slice(-1000));};
 try{
  if(kind==='react'){fs.symlinkSync(path.join(fixture,'node_modules'),path.join(root,'node_modules'),'dir');fs.copyFileSync(path.join(fixture,'postcss.config.mjs'),path.join(root,'postcss.config.mjs'));fs.writeFileSync(path.join(root,'package.json'),JSON.stringify({private:true,dependencies:{next:'16.2.5',react:'19.2.0','react-dom':'19.2.0'}}));fs.writeFileSync(path.join(root,'app/style.css'),'@import "tailwindcss";\n'+css+(process.env.RT_E2E_COMPONENT_SCOPE_ORDER?'@media(min-width:1200px){.alpha\\:flex{display:flex}}@media(min-width:400px){.zeta\\:flex{display:flex}}':'')+(process.env.RT_E2E_EFFECT_STYLES?'.type-editorial{box-shadow:0px 4px 8px 1px rgba(0,0,0,0.3);filter:blur(1px);backdrop-filter:blur(2px)}':'')+(process.env.RT_E2E_SHARED_RELATIVE?'.other-font{font-size:20px}.named-font{font-size:32px}':''));fs.writeFileSync(path.join(root,'app/layout.jsx'),'import "./style.css";export default function Layout({children}){return <html><body>{children}</body></html>}');child=spawn(process.execPath,[path.resolve(__dirname,'../../bin/retouch.cjs'),'--',process.execPath,path.join(fixture,'node_modules/next/dist/bin/next'),'dev','--webpack','-p','0'],{cwd:root,env:{...process.env,NEXT_TELEMETRY_DISABLED:'1'},stdio:['ignore','pipe','pipe']});stopped=new Promise(r=>child.once('exit',r));for(const stream of [child.stdout,child.stderr])stream.on('data',data=>{logs+=data;url=logs.match(/http:\/\/localhost:\d+/)?.[0];});await wait(()=>url);await wait(async()=>{try{return(await fetch(url+'/rt/__api/health')).ok;}catch{return false;}});
  }else if(kind==='liquid'){
   const liquid=require('../../src/adapters/liquid.cjs'),engine=new(require('liquidjs').Liquid)(),{Scanner}=require(path.join(fixture,'node_modules/@tailwindcss/oxide')),{compile}=require('node:module').createRequire(path.join(fixture,'package.json'))('@tailwindcss/node');
   const compiler=await compile('@import "tailwindcss";\n'+css,{base:fixture,onDependency(){}});
   server=require('../../src/server.cjs').startServer({appRoot:root,adapter:liquid,port:0,quiet:true,rendering:{reloadAfterWrite:true},serveSite:async(req,res)=>{try{const source=read(),candidates=new Scanner({}).scanFiles([{content:source,extension:'liquid'}]),styles=compiler.build(candidates),html=await engine.parseAndRender(liquid.stamp(source,file,root).code,{alternate:req.url.includes('alternate=1')});res.setHeader('content-type','text/html');res.end(html.replace('</head>','<style>'+styles+'</style></head>'));}catch(e){res.statusCode=500;res.end(e.stack);}}});await once(server,'listening');url='http://localhost:'+server.address().port;
  }else{server=require('../../src/html-site.cjs').start({root,port:0,quiet:true});if(!server.listening)await once(server,'listening');url='http://localhost:'+server.address().port;}
  browser=await browserType.launch();const page=await browser.newPage({viewport:{width:1600,height:1100}}),errors=[],snapshots=[];page.on('pageerror',e=>errors.push(e.message));if(process.env.RT_E2E_NETWORK_TRACE){
   const trace=(event,data)=>console.error(JSON.stringify({time:Date.now(),event,...data}));
   page.on('pageerror',error=>trace('pageerror',{message:error.message,stack:error.stack}));
   page.on('requestfailed',request=>trace('requestfailed',{url:request.url(),failure:request.failure(),resource:request.resourceType()}));
   page.on('response',response=>{if(response.status()>=400||response.request().method()==='POST'&&response.url().includes('/rt/__api/'))trace('response',{url:response.url(),status:response.status(),operation:response.request().postData()});});
   page.on('framenavigated',frame=>trace('framenavigated',{url:frame.url()}));
  }await page.goto(url+'/rt',{timeout:90000});const app=page.frameLocator('#app');await app.locator('h1').waitFor({timeout:90000});await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();const settled=()=>wait(async()=>await page.locator('#panelBody').getAttribute('aria-busy')!=='true'&&await page.evaluate(()=>!panelTasks&&!sourceRequests&&!undoBusy)),family=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).fontFamily);await settled();
  if(process.env.RT_E2E_HOST_RENAME){
   const beforeName=read(),tree=page.getByRole('tree',{name:'Site layers',exact:true}),originalRow=()=>tree.getByRole('treeitem',{name:'h1 · Headline',exact:true}),namedRow=()=>tree.getByRole('treeitem',{name:'h1 · Page heading',exact:true}),name=()=>page.getByLabel('Layer name',{exact:true}),beforeText=await app.locator('h1').textContent(),beforeLabel=await app.locator('h1').getAttribute('aria-label'),beforeClass=await app.locator('h1').getAttribute('class');
   await originalRow().press('F2');await wait(async()=>await name().evaluate(el=>el===el.ownerDocument.activeElement));await name().fill('Page heading');await name().press('Tab');await wait(()=>read()!==beforeName);await settled();await namedRow().waitFor();await wait(async()=>await app.locator('h1').getAttribute('data-rt-layer-name')==='Page heading');assert.equal(await app.locator('h1').textContent(),beforeText);assert.equal(await app.locator('h1').getAttribute('aria-label'),beforeLabel);assert.equal(await app.locator('h1').getAttribute('class'),beforeClass);const namedSource=read(),screenWidth=page.getByLabel('Screen width',{exact:true}),previousWidth=await screenWidth.inputValue();for(const width of ['375',previousWidth]){await screenWidth.fill(width);await screenWidth.press('Enter');await wait(async()=>await app.locator('html').evaluate(el=>el.ownerDocument.defaultView.innerWidth)===Number(width));await namedRow().waitFor();assert.equal(await app.locator('h1').getAttribute('data-rt-layer-name'),'Page heading');assert.equal(read(),namedSource);}if(kind==='liquid')assert.equal(await page.evaluate(()=>sel.info.classNameDynamic),false,'Naming retains rendered class context');if(process.env.RT_E2E_HOST_RENAME_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_HOST_RENAME_SCREENSHOT});await page.getByLabel('Find a layer',{exact:true}).fill('Page heading');await namedRow().waitFor();await page.getByLabel('Find a layer',{exact:true}).fill('');
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeName);await originalRow().waitFor();if(kind==='liquid')assert.equal(await page.evaluate(()=>sel.info.classNameDynamic),false,'Undo retains rendered class context');await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===namedSource);await namedRow().waitFor();if(kind==='liquid')assert.equal(await page.evaluate(()=>sel.info.classNameDynamic),false,'Redo retains rendered class context');await name().fill('');await name().press('Tab');await settled();await originalRow().waitFor();assert.equal(await app.locator('h1').getAttribute('data-rt-layer-name'),null);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===namedSource);await namedRow().waitFor();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeName);await originalRow().waitFor();await originalRow().click();await settled();console.log('HOST LAYER NAME/F2/SEARCH/CLEAR/UNDO/REDO PASS',kind,engine);
  }
  if(process.env.RT_E2E_COMPONENT_TYPE_PATHS){const config=path.join(root,'tsconfig.json');importedTypeSources.set(config,fs.readFileSync(config,'utf8'));}
  if(process.env.RT_E2E_COMPONENT_LIBRARY){
   const beforeLibrary=read(),openLibrary=async()=>{await page.getByRole('button',{name:'Components',exact:true}).click();const modal=page.getByRole('dialog',{name:'Project components',exact:true});await modal.getByRole('listitem').first().waitFor();return modal;};
   let library=await openLibrary();const search=()=>library.getByLabel('Search project components');await search().fill('no-such-component');await library.getByText('No components match.',{exact:true}).waitFor();await search().press('Escape');assert.deepEqual(await library.locator('li h3').allTextContents(),process.env.RT_E2E_UNUSED_COMPONENTS?['Badge','HeadlineCard',...(process.env.RT_E2E_NESTED_COMPONENT_NAVIGATION?['InnerLabel']:[]),'Layout','Page','RemoteBadge',...(process.env.RT_E2E_REQUIRED_INSERT?['RequiredCard']:[]),'SingleCard',...(process.env.RT_E2E_COMPONENT_SWAP?['SwapCard']:[]),'UnusedButton']:['Badge','HeadlineCard','Layout','Page','RemoteBadge']);
   if(process.env.RT_E2E_UNUSED_COMPONENTS){
    await search().fill('UnusedButton');const unused=library.getByRole('listitem',{name:'UnusedButton · app/components/UnusedButton.tsx',exact:true});assert.equal(await unused.getByRole('button',{name:'Select on canvas'}).isDisabled(),true);await unused.getByRole('button',{name:'View component'}).click();const detail=page.getByRole('dialog',{name:'UnusedButton component',exact:true});await detail.getByRole('columnheader',{name:'Type / options',exact:true}).waitFor();assert.equal(await detail.locator('iframe').count(),0);assert.match(await detail.locator('table').textContent(),/calm.*bold/);assert.match(await detail.locator('table').textContent(),/Ready/);assert.equal(await detail.locator('pre').isVisible(),true);await detail.getByRole('button',{name:'Close',exact:true}).click();assert.equal(read(),beforeLibrary);
    library=await openLibrary();await search().fill('SingleCard');if(process.env.RT_E2E_PREVIEW_OCCURRENCES)await library.getByLabel('Instance of SingleCard',{exact:true}).selectOption('1');await search().focus();await search().press('ArrowDown');await page.keyboard.press('ArrowRight');await page.keyboard.press('Enter');const previewDialog=page.getByRole('dialog',{name:'SingleCard component',exact:true});await previewDialog.frameLocator('iframe').locator('footer').last().waitFor({state:'attached'});await wait(async()=>await previewDialog.locator('iframe').getAttribute('aria-busy')==='false');assert.equal(await previewDialog.getByText('Loading component preview…',{exact:true}).count(),0);await wait(async()=>await previewDialog.frameLocator('iframe').locator('h1').evaluate(el=>getComputedStyle(el).display==='none'));if(process.env.RT_E2E_PREVIEW_OCCURRENCES){assert.equal(await previewDialog.frameLocator('iframe').locator('footer').nth(0).isVisible(),false,'First runtime occurrence stays hidden');assert.equal(await previewDialog.frameLocator('iframe').locator('footer').nth(1).isVisible(),true,'Selected second runtime occurrence stays visible');assert.equal(await previewDialog.frameLocator('iframe').locator('footer').nth(1).getAttribute('title'),'Second occurrence');}if(process.env.RT_E2E_COMPONENT_FRAGMENTS){const siblings=previewDialog.frameLocator('iframe').locator('[data-fragment-sibling]');assert.equal(await siblings.nth(process.env.RT_E2E_PREVIEW_OCCURRENCES?1:0).isVisible(),true,'Component preview retains every fragment root');if(process.env.RT_E2E_PREVIEW_OCCURRENCES)assert.equal(await siblings.first().isVisible(),false,'Unselected fragment roots stay hidden');}if(process.env.RT_E2E_PREVIEW_IDENTITIES){await previewDialog.frameLocator('iframe').locator('body').evaluate(body=>{const first=body.querySelector('footer'),second=body.querySelectorAll('footer')[1],sibling=second.nextElementSibling;first.parentElement.insertBefore(second,first);if(sibling?.hasAttribute('data-fragment-sibling'))first.parentElement.insertBefore(sibling,first);});await wait(async()=>await previewDialog.frameLocator('iframe').locator('footer').first().evaluate(el=>el.title==='Second occurrence'&&getComputedStyle(el).display!=='none'));assert.equal(await previewDialog.frameLocator('iframe').locator('footer').nth(1).isVisible(),false,'Reordering keeps the unselected occurrence hidden');await previewDialog.frameLocator('iframe').locator('body').evaluate(body=>{const root=body.querySelector('#Second-occurrence'),next=root.nextElementSibling;window.removedPreviewRoots=[root,...(next?.hasAttribute('data-fragment-sibling')?[next]:[])];window.previewParent=root.parentElement;for(const node of window.removedPreviewRoots)node.remove();});await previewDialog.getByText('Waiting for the selected component…',{exact:true}).waitFor();assert.equal(await previewDialog.locator('iframe').isVisible(),false,'Missing identity does not fall back to another instance');await previewDialog.frameLocator('iframe').locator('body').evaluate(()=>{for(const node of window.removedPreviewRoots)window.previewParent.append(node);});await wait(async()=>await previewDialog.locator('iframe').getAttribute('aria-busy')==='false');assert.equal(await previewDialog.frameLocator('iframe').locator('#Second-occurrence').isVisible(),true,'Reinserted identity recovers its preview');}
    await previewDialog.frameLocator('iframe').locator('html').evaluate(el=>el.dataset.previewReloadMarker='before');await previewDialog.getByRole('button',{name:'Refresh preview',exact:true}).click();await wait(async()=>await previewDialog.locator('iframe').evaluate(el=>el.getAttribute('aria-busy')==='false'&&!el.contentDocument.documentElement.dataset.previewReloadMarker));if(process.env.RT_E2E_PREVIEW_OCCURRENCES){assert.equal(await previewDialog.frameLocator('iframe').locator('footer').first().isVisible(),false);assert.equal(await previewDialog.frameLocator('iframe').locator('footer').nth(1).isVisible(),true,'Refresh preserves the selected occurrence');}assert.equal(read(),beforeLibrary,'Preview refresh leaves source unchanged');
    await page.setViewportSize({width:420,height:540});assert.equal(await previewDialog.evaluate(el=>{const box=el.getBoundingClientRect();return box.left>=0&&box.right<=innerWidth&&box.top>=0&&box.bottom<=innerHeight&&el.scrollWidth<=el.clientWidth&&getComputedStyle(el).backgroundColor==='rgb(255, 255, 255)';}),true,'Light component detail fits compact windows');assert.equal(await previewDialog.locator('iframe').evaluate(el=>el.getBoundingClientRect().width>250&&el.getBoundingClientRect().height>140),true,'Compact preview retains usable dimensions');if(process.env.RT_E2E_COMPONENT_DETAIL_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_DETAIL_SCREENSHOT+'.preview.png'});await previewDialog.getByText('Component definition',{exact:true}).click();await previewDialog.locator('pre').scrollIntoViewIfNeeded();assert.equal(await previewDialog.getByRole('button',{name:'Close',exact:true}).evaluate(el=>{const box=el.getBoundingClientRect();return box.top>=0&&box.bottom<=innerHeight;}),true,'Close remains visible while browsing source');if(process.env.RT_E2E_COMPONENT_DETAIL_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_DETAIL_SCREENSHOT});await previewDialog.getByRole('button',{name:'Close',exact:true}).click();await page.setViewportSize({width:1600,height:1100});assert.equal(read(),beforeLibrary);if(process.env.RT_E2E_PREVIEW_OCCURRENCES){assert.deepEqual(errors,[]);console.log('REPEATED COMPONENT PREVIEW OCCURRENCE PASS',engine);return;}
    library=await openLibrary();await search().fill('Page');const pageComponent=library.getByRole('listitem',{name:'Page · app/page.tsx',exact:true});await pageComponent.getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});await settled();assert.equal(await page.locator('.kindbadge').textContent(),'Main');assert.equal(read(),beforeLibrary);
    if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS){await page.getByRole('treeitem',{name:'SingleCard · component',exact:true}).click();await settled();await page.getByRole('button',{name:'Lock selection',exact:true}).click();await settled();await page.getByRole('treeitem',{name:'main',exact:true}).click();await settled();}
    const insertionLocks=()=>page.evaluate(()=>[...doc().querySelectorAll('footer')].every((el,i)=>layerLocks.direct(el)===(i===0))&&[...doc().querySelectorAll('[data-required-card]')].every(el=>!layerLocks.direct(el)));
    if(process.env.RT_E2E_COMPONENT_INSERT){
     library=await openLibrary();await search().fill('SingleCard');if(process.env.RT_E2E_COMPONENT_INSERT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_INSERT_SCREENSHOT});await library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true}).getByRole('button',{name:'Insert into frame',exact:true}).click();await library.waitFor({state:'hidden'});await settled();await wait(async()=>await app.locator('footer').count()===2);const insertedSource=read();if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);assert.equal((insertedSource.match(/import /g)||[]).length,(beforeLibrary.match(/import /g)||[]).length,'Insertion reuses the existing import');assert.match(insertedSource,process.env.RT_E2E_NAMESPACE_INSERT?/<ZCards\.SingleCard\/>/:/<SingleCard\/>/);const insertedLabel=page.getByLabel('Component property label',{exact:true});await insertedLabel.waitFor();assert.equal(await insertedLabel.inputValue(),'Single default');await insertedLabel.fill('Inserted copy');await insertedLabel.press('Control+Enter');await settled();await wait(async()=>await app.locator('footer').last().getAttribute('title')==='Inserted copy');assert.equal(await app.locator('footer').first().getAttribute('title'),'Only instance');
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===insertedSource);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await wait(async()=>await app.locator('footer').count()===1);assert.equal(await page.locator('.kindbadge').textContent(),'Main');await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===insertedSource);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await wait(async()=>await app.locator('footer').count()===2);await insertedLabel.waitFor();assert.equal(await insertedLabel.inputValue(),'Single default');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await wait(async()=>await app.locator('footer').count()===1);
    }
    if(process.env.RT_E2E_REQUIRED_INSERT){
     library=await openLibrary();await search().fill('RequiredCard');const add=()=>library.getByRole('listitem',{name:'RequiredCard · app/components/RequiredCard.tsx',exact:true}).getByRole('button',{name:'Insert into frame',exact:true}),dialog=()=>page.getByRole('dialog',{name:'Insert RequiredCard',exact:true}),initial=name=>dialog().getByLabel('Initial property '+name,{exact:true});
     await add().click();await dialog().waitFor();assert.equal(await initial('note').count(),0);await initial('label').fill('Cancelled draft');await initial('label').press('Escape');await dialog().waitFor({state:'hidden'});assert.equal(read(),beforeLibrary);await add().click();await dialog().waitFor();await dialog().getByRole('button',{name:'Insert component',exact:true}).click();assert.equal(await dialog().isVisible(),true);assert.equal(read(),beforeLibrary,'Missing number and choices cannot write source');
     const requiredLabel='Required "title" & <tag>\nSecond line',fill=async()=>{await initial('label').fill(requiredLabel);await initial('width').fill('180.5');await initial('enabled').selectOption('1');await initial('tone').selectOption('1');};await fill();
     const typeFile=path.join(root,'app/components/required-types.ts'),typeBefore=fs.readFileSync(typeFile,'utf8');fs.appendFileSync(typeFile,'\n// external contract edit');await dialog().getByRole('button',{name:'Insert component',exact:true}).click();await dialog().getByRole('alert').filter({hasText:/properties changed/}).waitFor();assert.equal(await initial('label').inputValue(),requiredLabel);assert.equal(read(),beforeLibrary);await dialog().getByRole('button',{name:'Cancel',exact:true}).click();await dialog().waitFor({state:'hidden'});fs.writeFileSync(typeFile,typeBefore);await add().click();await dialog().waitFor();await fill();await page.setViewportSize({width:720,height:800});assert.equal(await dialog().evaluate(el=>{const r=el.getBoundingClientRect();return r.left>=0&&r.right<=innerWidth&&el.scrollWidth<=el.clientWidth;}),true);if(process.env.RT_E2E_REQUIRED_INSERT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_REQUIRED_INSERT_SCREENSHOT});await page.setViewportSize({width:1600,height:1100});
     await dialog().getByRole('button',{name:'Insert component',exact:true}).click();await dialog().waitFor({state:'hidden'});await library.waitFor({state:'hidden'});await settled();await app.locator('[data-required-card]').waitFor();assert.deepEqual(await app.locator('[data-required-card]').evaluate(el=>[el.title,el.dataset.enabled,el.dataset.tone,el.dataset.note,el.style.width]),[requiredLabel,'false','bold','Default','180.5px']);const insertedRequired=read();if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);assert.match(insertedRequired,/enabled=\{false\}/);assert.match(insertedRequired,/width=\{180.5\}/);assert.equal(await page.getByLabel('Component property label',{exact:true}).inputValue(),requiredLabel);
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await wait(async()=>await app.locator('[data-required-card]').count()===0);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===insertedRequired);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await app.locator('[data-required-card]').waitFor();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS)await wait(insertionLocks);await wait(async()=>await app.locator('[data-required-card]').count()===0);assert.equal(fs.readFileSync(typeFile,'utf8'),typeBefore);
    }
    if(process.env.RT_E2E_COMPONENT_INSERT_LOCKS){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(async()=>await page.evaluate(()=>!layerLocks.direct(doc().querySelector('footer'))));console.log('COMPONENT INSERT/EXISTING LOCKS/UNLOCKED COPY/UNDO/REDO PASS',kind,engine);}
    library=await openLibrary();await search().fill('SingleCard');await library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true}).getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});const label=page.getByLabel('Component property label',{exact:true});await label.waitFor();assert.equal(await label.inputValue(),'Only instance');await label.fill('Edited single');await label.press('Control+Enter');await settled();await wait(async()=>await app.locator('footer').getAttribute('title')==='Edited single');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await wait(async()=>await app.locator('footer').getAttribute('title')==='Only instance');
    if(process.env.RT_E2E_COMPONENT_SWAP){
     if(process.env.RT_E2E_COMPONENT_SWAP_LOCKS){await page.getByRole('button',{name:'Lock selection',exact:true}).click();await settled();await page.getByRole('treeitem',{name:'SingleCard · component',exact:true}).click();await settled();}
     const swapLock=()=>page.evaluate(()=>layerLocks.direct(doc().querySelector('footer,[data-swap-card]')));
     const targetFile=path.join(root,'app/components/SwapCard.tsx'),targetSource=fs.readFileSync(targetFile,'utf8');library=await openLibrary();await search().fill('SwapCard');const change=()=>library.getByRole('listitem',{name:'SwapCard · app/components/SwapCard.tsx',exact:true}).getByRole('button',{name:'Swap selected instance',exact:true}),dialog=()=>page.getByRole('dialog',{name:'Swap to SwapCard',exact:true});await change().click();await dialog().waitFor();assert.match(await dialog().textContent(),/Kept: label/);assert.match(await dialog().textContent(),/Removed overrides: legacy/);assert.equal(await dialog().getByLabel('Initial property label',{exact:true}).count(),0);await dialog().getByRole('button',{name:'Cancel',exact:true}).click();await dialog().waitFor({state:'hidden'});assert.equal(read(),beforeLibrary);
     await change().click();await dialog().waitFor();await dialog().getByLabel('Initial property width',{exact:true}).fill('240');assert.equal(read(),beforeLibrary);await page.setViewportSize({width:720,height:800});assert.equal(await dialog().evaluate(el=>{const r=el.getBoundingClientRect();return r.left>=0&&r.right<=innerWidth&&el.scrollWidth<=el.clientWidth;}),true);if(process.env.RT_E2E_COMPONENT_SWAP_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_SWAP_SCREENSHOT});await page.setViewportSize({width:1600,height:1100});await dialog().getByRole('button',{name:'Swap component',exact:true}).click();await dialog().waitFor({state:'hidden'});await library.waitFor({state:'hidden'});await settled();await app.locator('[data-swap-card]').waitFor();if(process.env.RT_E2E_COMPONENT_SWAP_LOCKS)await wait(swapLock);assert.equal(await app.locator('footer').count(),0);assert.deepEqual(await app.locator('[data-swap-card]').evaluate(el=>[el.title,el.dataset.tone,el.style.width]),['Only instance','bold','240px']);const swappedSource=read();assert.match(swappedSource,/import "\.\/components\/SingleCard";/);assert.doesNotMatch(swappedSource,/import (?:\{SingleCard\}|\* as ZCards) from/);assert.match(swappedSource,/key="single"/);assert.doesNotMatch(swappedSource,/legacy=/);assert.equal(await page.getByLabel('Component property label',{exact:true}).inputValue(),'Only instance');assert.equal(fs.readFileSync(targetFile,'utf8'),targetSource);
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await app.locator('footer').waitFor();if(process.env.RT_E2E_COMPONENT_SWAP_LOCKS)await wait(swapLock);assert.equal(await app.locator('[data-swap-card]').count(),0);await page.getByLabel('Component property legacy',{exact:true}).waitFor();assert.equal(await page.getByLabel('Component property legacy',{exact:true}).inputValue(),'Old override');await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===swappedSource);await app.locator('[data-swap-card]').waitFor();if(process.env.RT_E2E_COMPONENT_SWAP_LOCKS)await wait(swapLock);assert.equal(await page.getByLabel('Component property width',{exact:true}).inputValue(),'240');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await app.locator('footer').waitFor();if(process.env.RT_E2E_COMPONENT_SWAP_LOCKS)await wait(swapLock);assert.equal(fs.readFileSync(targetFile,'utf8'),targetSource);
     if(process.env.RT_E2E_COMPONENT_SWAP_LOCKS){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(async()=>!await swapLock());await page.getByRole('treeitem',{name:'SingleCard · component',exact:true}).click();await settled();console.log('COMPONENT SWAP/LOCK/UNDO/REDO PASS',kind,engine);}
    }
    if(process.env.RT_E2E_COMPONENT_FRAGMENTS){assert.equal(await app.locator('[data-fragment-sibling]').count(),1);await wait(async()=>/1 instance at this usage/.test(await page.locator('.component-section').textContent()));await wait(async()=>await page.evaluate(()=>overlayLayer.querySelectorAll('.box.sel').length)===1);const grouped=await page.evaluate(()=>({roots:matchingInDocument(doc(),sel.info.id,sel.info).map(el=>{const r=el.getBoundingClientRect();return {left:r.left,top:r.top,right:r.right,bottom:r.bottom};}),box:(()=>{const el=overlayLayer.querySelector('.box.sel');return el?['left','top','width','height'].map(key=>parseFloat(el.style[key])):null;})()}));assert.ok(grouped.box);const left=Math.min(...grouped.roots.map(r=>r.left)),top=Math.min(...grouped.roots.map(r=>r.top));assert.deepEqual(grouped.box,[left,top,Math.max(...grouped.roots.map(r=>r.right))-left,Math.max(...grouped.roots.map(r=>r.bottom))-top]);for(const selector of ['footer','[data-fragment-sibling]']){await app.locator(selector).hover();await wait(async()=>await page.evaluate(()=>!!overlayLayer.querySelector('.box.hover.instance')));const hoverBounds=await page.evaluate(()=>{const el=overlayLayer.querySelector('.box.hover.instance');return ['left','top','width','height'].map(key=>parseFloat(el.style[key]));});assert.deepEqual(hoverBounds,grouped.box);assert.equal(await page.locator('.component-badge').isVisible(),true);}await app.locator('[data-fragment-sibling]').click();await settled();await page.getByLabel('Component property '+(process.env.RT_E2E_COMPONENT_SWAP?'legacy':'label'),{exact:true}).waitFor();assert.equal(await page.getByLabel('Component property label',{exact:true}).inputValue(),'Only instance');if(process.env.RT_E2E_INSTANCE_GROUP_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_INSTANCE_GROUP_SCREENSHOT});library=await openLibrary();await search().fill('SingleCard');const fragmentRow=library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true});assert.match(await fragmentRow.textContent(),/1 source usage · 1 on this page/);assert.equal(await fragmentRow.getByLabel('Instance of SingleCard').count(),0);await fragmentRow.getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});assert.equal(await app.locator('[data-fragment-sibling]').getAttribute('data-rt-i'),await app.locator('footer').getAttribute('data-rt-i'));assert.equal(await app.locator('[data-fragment-sibling]').getAttribute('data-rt-i-revision'),await app.locator('footer').getAttribute('data-rt-i-revision'));assert.equal(await app.locator('[data-fragment-sibling]').getAttribute('title'),'Only instance');}
    if(process.env.RT_E2E_COMPONENT_TREE){
     const tree=page.getByRole('tree',{name:'Site layers',exact:true}),component=()=>tree.getByRole('treeitem',{name:'SingleCard · component',exact:true});await component().waitFor();await component().click();await settled();await wait(async()=>await component().getAttribute('aria-selected')==='true');await tree.getByRole('button',{name:'Collapse SingleCard · component',exact:true}).click();assert.equal(await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).count(),0);await tree.getByRole('button',{name:'Expand SingleCard · component',exact:true}).click();await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');assert.equal(await component().getAttribute('aria-selected'),'false');await component().click();await settled();await page.getByLabel('Component property label',{exact:true}).waitFor();await component().press('ArrowLeft');assert.equal(await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).count(),0);await component().press('ArrowRight');await component().press('ArrowRight');assert.equal(await page.evaluate(()=>document.activeElement.textContent),'footer · Single card');await page.keyboard.press('Enter');await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');await component().click();await settled();await page.getByRole('button',{name:'Lock selection',exact:true}).click();await wait(async()=>await page.evaluate(()=>[...doc().querySelectorAll('footer,[data-fragment-sibling]')].every(el=>layerLocks.direct(el))));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(async()=>await page.evaluate(()=>[...doc().querySelectorAll('footer,[data-fragment-sibling]')].every(el=>!layerLocks.direct(el))));await component().click();await settled();if(process.env.RT_E2E_FRAGMENT_NAVIGATION){
      assert.ok(process.env.RT_E2E_COMPONENT_FRAGMENTS);const beforeNavigation=read(),sharedPath=path.join(root,'app/components/SingleCard.tsx'),sharedBefore=fs.readFileSync(sharedPath,'utf8');
      const navigate=async(key,target)=>{await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);await app.locator('body').evaluate(el=>el.tabIndex=-1);await app.locator('body').press(key);await wait(async()=>await tree.getByRole('treeitem',{name:target,exact:true}).getAttribute('aria-selected')==='true');};
      await navigate('Enter','footer · Single card');await navigate('Tab','small · Fragment sibling');if(process.env.RT_E2E_NESTED_COMPONENT_NAVIGATION){await navigate('Enter','InnerLabel · component');await navigate('Enter','em · Inner label');await navigate('Shift+Enter','InnerLabel · component');await navigate('Shift+Enter','small · Fragment sibling');}await navigate('Shift+Enter','SingleCard · component');await navigate('Enter','footer · Single card');await navigate('Shift+Tab','small · Fragment sibling');await navigate('Shift+Enter','SingleCard · component');assert.equal(read(),beforeNavigation);assert.equal(fs.readFileSync(sharedPath,'utf8'),sharedBefore);await page.getByLabel('Component property label',{exact:true}).waitFor();
     }
     if(process.env.RT_E2E_COMPONENT_TREE_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_TREE_SCREENSHOT});
    }
    if(process.env.RT_E2E_COMPONENT_RENAME){
     const beforeName=read(),tree=page.getByRole('tree',{name:'Site layers',exact:true}),original=()=>tree.getByRole('treeitem',{name:'SingleCard · component',exact:true}),named=()=>tree.getByRole('treeitem',{name:'Checkout summary · component',exact:true}),sharedFile=path.join(root,'app/components/SingleCard.tsx'),shared=fs.readFileSync(sharedFile,'utf8');
     await original().press('F2');await wait(async()=>await page.getByLabel('Layer name',{exact:true}).evaluate(el=>el===el.ownerDocument.activeElement));const name=()=>page.getByLabel('Layer name',{exact:true});await name().fill('Checkout summary');await name().press('Tab');await wait(()=>read()!==beforeName);await settled();await named().waitFor();assert.equal(await app.locator('footer').getAttribute('title'),'Only instance');assert.equal(fs.readFileSync(sharedFile,'utf8'),shared);const namedSource=read();
     library=await openLibrary();await search().fill('checkout SUMMARY');const namedRow=library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true});await namedRow.waitFor();assert.match(await namedRow.textContent(),/Checkout summary/);if(process.env.RT_E2E_LIBRARY_NAME_SCREENSHOT){await page.setViewportSize({width:720,height:800});assert.equal(await library.evaluate(el=>{const r=el.getBoundingClientRect();return r.left>=0&&r.right<=innerWidth&&el.scrollWidth<=el.clientWidth;}),true);await page.screenshot({path:process.env.RT_E2E_LIBRARY_NAME_SCREENSHOT});await page.setViewportSize({width:1600,height:1100});}await namedRow.getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});await settled();await wait(async()=>await name().inputValue()==='Checkout summary');assert.equal(read(),namedSource,'Library name search and selection do not write source');
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeName);await original().waitFor();await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===namedSource);await named().waitFor();await name().fill('');await name().press('Tab');await settled();await original().waitFor();assert.ok(!read().includes('@retouch-layer'));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===namedSource);await named().waitFor();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeName);await original().waitFor();await original().click();await settled();console.log('COMPONENT LAYER NAME/F2/CLEAR/UNDO/REDO PASS',kind,engine);
    }
    if(process.env.RT_E2E_COMPONENT_REPARENT){
     const beforeParent=read(),tree=page.getByRole('tree',{name:'Site layers',exact:true}),component=()=>tree.getByRole('treeitem',{name:'SingleCard · component',exact:true}),destination=()=>tree.getByRole('treeitem',{name:'section · Destination frame',exact:true}),inside=()=>app.locator('section[aria-label="Destination frame"] footer'),label=()=>page.getByLabel('Component property label',{exact:true}),sharedFile=path.join(root,'app/components/SingleCard.tsx'),sharedSource=fs.readFileSync(sharedFile,'utf8');
     await tree.getByRole('treeitem',{name:'p · Destination',exact:true}).click();await settled();await page.getByRole('button',{name:'Lock selection',exact:true}).click();await settled();const retainedLock=()=>page.evaluate(()=>layerLocks.direct(doc().querySelector('section[aria-label="Destination frame"] > p'))&&!layerLocks.direct(doc().querySelector('footer')));await wait(retainedLock);
     await component().click();await settled();await wait(async()=>await component().getAttribute('draggable')==='true');const box=await destination().boundingBox();await component().dragTo(destination(),{targetPosition:{x:20,y:box.height/2}});await settled();await inside().waitFor();await wait(retainedLock);const nestedSource=read();assert.notEqual(nestedSource,beforeParent);assert.equal(await label().inputValue(),'Only instance');assert.equal(Number(await component().getAttribute('aria-level')),Number(await destination().getAttribute('aria-level'))+1);if(process.env.RT_E2E_COMPONENT_REPARENT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_REPARENT_SCREENSHOT});
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeParent);await wait(async()=>await inside().count()===0);await wait(retainedLock);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===nestedSource);await inside().waitFor();await wait(retainedLock);
     await page.getByRole('button',{name:'Move into…',exact:true}).click();const dialog=page.getByRole('dialog',{name:'Move component into',exact:true});await dialog.waitFor();const picker=dialog.getByLabel('Destination container',{exact:true}),mainId=await app.locator('main').getAttribute('data-rt');await picker.selectOption(mainId);await dialog.getByRole('button',{name:'Move component',exact:true}).click();await dialog.waitFor({state:'hidden'});await settled();await wait(async()=>await inside().count()===0);await wait(retainedLock);await app.locator('main > footer').waitFor();const returnedSource=read();assert.notEqual(returnedSource,nestedSource);assert.equal(await label().inputValue(),'Only instance');
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===nestedSource);await inside().waitFor();await wait(retainedLock);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===returnedSource);await wait(async()=>await inside().count()===0);await wait(retainedLock);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===nestedSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeParent);await component().click();await settled();assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedSource);await wait(retainedLock);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(async()=>await page.evaluate(()=>!layerLocks.direct(doc().querySelector('section[aria-label="Destination frame"] > p'))));await component().click();await settled();console.log('COMPONENT REPARENT/DRAG/DIALOG/LOCKS/UNDO/REDO PASS',kind,engine);
    }
    if(process.env.RT_E2E_TREE_DUPLICATE){
     const beforeTreeCopy=read(),sharedFile=path.join(root,'app/components/SingleCard.tsx'),sharedSource=fs.readFileSync(sharedFile,'utf8'),tree=page.getByRole('tree',{name:'Site layers',exact:true}),rows=()=>tree.getByRole('treeitem',{name:'SingleCard · component',exact:true});await rows().click();await settled();if(process.env.RT_E2E_COMPONENT_ACTIONS){await page.getByRole('button',{name:'Actions',exact:true}).click();const commands=page.getByRole('dialog',{name:'Actions',exact:true});await commands.getByRole('combobox',{name:'Search actions'}).fill('duplicate component');await commands.getByRole('option',{name:'Duplicate component',exact:true}).click();await commands.waitFor({state:'hidden'});}else await page.getByRole('button',{name:'Duplicate component',exact:true}).click();await settled();await wait(async()=>await app.locator('footer').count()===2);await wait(async()=>await rows().count()===2);const copiedSource=read();assert.notEqual(copiedSource,beforeTreeCopy);const copyLabel=page.getByLabel('Component property label',{exact:true});await copyLabel.fill('Tree copy');await copyLabel.press('Control+Enter');await settled();await wait(async()=>await app.locator('footer[title="Tree copy"]').count()===1);assert.equal(await app.locator('footer[title="Only instance"]').count(),1);
     if(process.env.RT_E2E_COMPONENT_COPY_LOCKS){
      const before=read();await rows().nth(1).click();await settled();await page.getByRole('button',{name:'Lock selection',exact:true}).click();await settled();await rows().first().click();await settled();await page.getByRole('button',{name:'Duplicate component',exact:true}).click();await settled();await wait(async()=>await app.locator('footer').count()===3);
      const correctLocks=()=>page.evaluate(()=>[...doc().querySelectorAll('footer')].every(el=>layerLocks.direct(el)===(el.getAttribute('title')==='Tree copy')));await wait(correctLocks);const copied=read();
      await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(correctLocks);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===copied);await wait(correctLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(correctLocks);
      if(process.env.RT_E2E_COMPONENT_DELETE_LOCKS){
       await rows().nth(1).click();await settled();const deletedId=await app.locator('footer[title="Tree copy"]').getAttribute('data-rt-i');assert.ok(deletedId);await page.getByRole('button',{name:'Delete component',exact:true}).click();await settled();await wait(async()=>await app.locator('footer').count()===1);
       const deletedLockAbsent=()=>page.evaluate(id=>!layerLocks.direct({getAttribute:name=>name==='data-rt-i'?id:null}),deletedId);await wait(deletedLockAbsent);const deleted=read();
       await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(correctLocks);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===deleted);await wait(deletedLockAbsent);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(correctLocks);console.log('COMPONENT DELETE/REMOVED LOCK/UNDO/REDO PASS',kind,engine);
      }
      await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(async()=>await page.evaluate(()=>[...doc().querySelectorAll('footer')].every(el=>!layerLocks.direct(el))));await rows().nth(1).click();await settled();console.log('COMPONENT COPY/SIBLING LOCKS/UNDO/REDO PASS',kind,engine);
     }
     if(process.env.RT_E2E_TREE_MOVE){
      const beforeMove=read(),titles=()=>app.locator('footer').evaluateAll(nodes=>nodes.map(el=>el.getAttribute('title'))),selectedLabel=()=>page.getByLabel('Component property label',{exact:true});
      await rows().first().click();await settled();await page.getByRole('button',{name:'Lock selection',exact:true}).click();await settled();await rows().nth(1).click();await settled();const correctLocks=()=>page.evaluate(()=>layerLocks.direct(doc().querySelector('footer[title="Only instance"]'))&&!layerLocks.direct(doc().querySelector('footer[title="Tree copy"]')));await wait(correctLocks);
      await page.getByRole('button',{name:'Move component up',exact:true}).click();await settled();await wait(async()=>JSON.stringify(await titles())===JSON.stringify(['Tree copy','Only instance']));await selectedLabel().waitFor();assert.equal(await selectedLabel().inputValue(),'Tree copy');const movedUp=read();assert.notEqual(movedUp,beforeMove);await wait(correctLocks);if(process.env.RT_E2E_COMPONENT_MOVE_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_MOVE_SCREENSHOT});
      await page.getByRole('button',{name:'Move component down',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);await wait(async()=>JSON.stringify(await titles())===JSON.stringify(['Only instance','Tree copy']));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await wait(async()=>JSON.stringify(await titles())===JSON.stringify(['Tree copy','Only instance']));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);
      await page.getByRole('button',{name:'Send to back',exact:true}).click();await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await page.getByRole('button',{name:'Bring to front',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);await selectedLabel().waitFor();assert.equal(await selectedLabel().inputValue(),'Tree copy');assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedSource);
      if(process.env.RT_E2E_TREE_DRAG){
       await wait(async()=>await rows().nth(1).getAttribute('draggable')==='true');await rows().nth(1).dragTo(rows().first(),{targetPosition:{x:20,y:4}});await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await wait(async()=>JSON.stringify(await titles())===JSON.stringify(['Tree copy','Only instance']));assert.equal(await selectedLabel().inputValue(),'Tree copy');
       await wait(async()=>await rows().first().getAttribute('draggable')==='true');const noChangeResponse=page.waitForResponse(response=>response.url().includes('/rt/__api/op')&&response.request().method()==='POST'&&response.request().postDataJSON()?.type==='moveComponent');await rows().first().dragTo(rows().nth(1),{targetPosition:{x:20,y:4}});const noChange=await(await noChangeResponse).json();assert.equal(noChange.unchanged,true);assert.equal(noChange.undoId,undefined);await settled();assert.equal(read(),movedUp);
       await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await wait(async()=>await rows().first().getAttribute('draggable')==='true');const box=await rows().nth(1).boundingBox();await rows().first().dragTo(rows().nth(1),{targetPosition:{x:20,y:box.height-4}});await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===movedUp);await wait(correctLocks);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeMove);await wait(correctLocks);console.log('COMPONENT DRAG/LOCKS/UNDO/REDO PASS',kind,engine);
      }
      await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(async()=>await page.evaluate(()=>!layerLocks.direct(doc().querySelector('footer[title="Only instance"]'))));await rows().nth(1).click();await settled();console.log('COMPONENT ORDER/LOCKS/SELECTION/UNDO/REDO PASS',kind,engine);
     }if(process.env.RT_E2E_TREE_DELETE){const editedCopy=read();await page.getByRole('button',{name:'Delete component',exact:true}).click();await settled();await wait(async()=>await app.locator('footer[title="Tree copy"]').count()===0);assert.equal(await app.locator('footer[title="Only instance"]').count(),1);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===editedCopy);await app.locator('footer[title="Tree copy"]').waitFor();assert.equal(await app.locator('footer[title="Only instance"]').count(),1);}await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===copiedSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeTreeCopy);await wait(async()=>await rows().count()===1);await rows().click();await settled();await wait(async()=>!await rows().isDisabled());await rows().press('Control+d');await settled();await wait(async()=>await app.locator('footer').count()===2);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeTreeCopy);await wait(async()=>await rows().count()===1);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(async()=>await app.locator('footer').count()===2);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeTreeCopy);await wait(async()=>await rows().count()===1);assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedSource);await rows().click();await settled();console.log('LAYER TREE COMPONENT DUPLICATION PASS',kind,engine);
    }
    if(process.env.RT_E2E_CONDITIONAL_FRAGMENT){
     const beforeConditional=read(),details=()=>page.getByLabel('Component property showDetails',{exact:true});await details().check();await settled();await app.locator('[data-fragment-optional]').waitFor();await wait(async()=>/1 instance at this usage/.test(await page.locator('.component-section').textContent()));await app.locator('[data-fragment-optional]').hover();await wait(async()=>await page.evaluate(()=>{const group=RetouchComponentInstances.group(matchingInDocument(doc(),sel.info.id,sel.info),sel.info.rootGroups)[0],box=overlayLayer.querySelector('.box.hover.instance');return group?.elements.length===3&&box&&parseFloat(box.style.height)===RetouchComponentInstances.bounds(group.elements).height;}));if(process.env.RT_E2E_REPEAT_FRAGMENT){assert.ok(process.env.RT_E2E_ROOT_SWAP);const expandedSource=read();assert.ok(expandedSource.includes('<RootWrapper/>'));fs.writeFileSync(file,expandedSource.replace('<RootWrapper/>','<RootWrapper/><RootWrapper/>'));await wait(async()=>await app.locator('footer').count()===2);await wait(async()=>await app.locator('[data-fragment-optional]').count()===2);library=await openLibrary();await search().fill('SingleCard');const repeatedRow=library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true});assert.match(await repeatedRow.textContent(),/1 source usage · 2 on this page/);assert.equal(await repeatedRow.getByLabel('Instance of SingleCard').locator('option').count(),2);await repeatedRow.getByLabel('Instance of SingleCard').selectOption('1');await repeatedRow.getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});await wait(async()=>await page.evaluate(()=>RetouchComponentInstances.group(matchingInDocument(doc(),sel.info.id,sel.info),sel.info.rootGroups).map(group=>group.elements.length).join(',')==='3,3'));const primaryMatches=async index=>await page.evaluate(index=>{const groups=RetouchComponentInstances.group(matchingInDocument(doc(),sel.info.id,sel.info),sel.info.rootGroups),box=overlayLayer.querySelector('.box.sel.instance'),expected=RetouchComponentInstances.bounds(groups[index].elements);return box&&['left','top','width','height'].every(key=>parseFloat(box.style[key])===expected[key]);},index);await wait(()=>primaryMatches(1));if(process.env.RT_E2E_COMPONENT_TREE){const rows=page.getByRole('tree',{name:'Site layers',exact:true}).getByRole('treeitem',{name:'SingleCard · component',exact:true});await wait(async()=>await rows.count()===2);await rows.first().click();await settled();await wait(()=>primaryMatches(0));await rows.nth(1).click();await settled();await wait(()=>primaryMatches(1));assert.equal(await rows.nth(1).getAttribute('aria-selected'),'true');assert.equal(await rows.first().getAttribute('aria-selected'),'false');if(process.env.RT_E2E_REPEAT_NAVIGATION){
       const beforeNavigation=read(),sharedFile=path.join(root,'app/components/SingleCard.tsx'),sharedBefore=fs.readFileSync(sharedFile,'utf8'),tree=page.getByRole('tree',{name:'Site layers',exact:true});
       const navigate=async(key,name)=>{await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);await app.locator('body').evaluate(el=>el.tabIndex=-1);await app.locator('body').press(key);const targets=tree.getByRole('treeitem',{name,exact:true});await wait(async()=>await targets.nth(1).getAttribute('aria-selected')==='true').catch(async error=>{console.log('REPEATED NAVIGATION DIAGNOSTIC',key,name,await page.evaluate(()=>({selected:[...document.querySelectorAll('[role="treeitem"][aria-selected="true"]')].map(el=>el.textContent),id:sel?.info.id,kind:sel?.info.kind,anchor:renderedSelection&&{id:renderedSelection.id,connected:renderedSelection.element?.isConnected,tag:renderedSelection.element?.tagName},toasts:document.getElementById('toasts').textContent})));throw error;});assert.equal(await targets.first().getAttribute('aria-selected'),'false');};
       await navigate('Enter','footer · Single card');
       if(process.env.RT_E2E_COMPONENT_CONTEXT){
        const menu=page.getByRole('menu',{name:'Canvas actions'}),hostId=await page.evaluate(()=>sel.info.id);
        if(process.env.RT_E2E_LAYER_CONTEXT){await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).first().click();await wait(async()=>await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).first().getAttribute('aria-selected')==='true');await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).click({button:'right'});}else await app.locator('footer').nth(1).click({button:'right'});await menu.waitFor();assert.deepEqual(await page.evaluate(()=>({id:sel.info.id,kind:sel.info.kind})),{id:hostId,kind:'host'});assert.equal(await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).getAttribute('aria-selected'),'true');
        await menu.getByRole('menuitem',{name:'Select parent layer',exact:true}).click();await wait(()=>primaryMatches(1));await app.locator('footer').nth(1).click({button:'right'});await menu.waitFor();assert.equal(await page.evaluate(()=>sel.info.kind),'instance');await menu.getByRole('menuitem',{name:'Duplicate component',exact:true}).waitFor();await menu.getByRole('menuitem',{name:'Select child layer',exact:true}).click();await wait(async()=>await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).getAttribute('aria-selected')==='true');
        await app.locator('footer').first().click({button:'right'});await menu.waitFor();await wait(()=>primaryMatches(0));await page.keyboard.press('Escape');await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).click();await wait(async()=>await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).getAttribute('aria-selected')==='true');assert.equal(read(),beforeNavigation);assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedBefore);console.log('REACT CONTEXT MENU HOST/INSTANCE/OCCURRENCE SCOPE PASS',engine);
       }
       if(process.env.RT_E2E_REPEAT_EDIT_SELECTION){const opacity=page.getByLabel('Opacity (%)',{exact:true});await opacity.fill('40');await opacity.press('Enter');await wait(async()=>await app.locator('footer').evaluateAll(els=>els.length===2&&els.every(el=>getComputedStyle(el).opacity==='0.4')));await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);assert.equal(await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).getAttribute('aria-selected'),'true');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>fs.readFileSync(sharedFile,'utf8')===sharedBefore);await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);assert.equal(await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).getAttribute('aria-selected'),'true');}
       if(process.env.RT_E2E_REPEAT_RELOAD_SELECTION){await page.evaluate(()=>reloadFrame());await wait(async()=>await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).count()===2);assert.equal(await tree.getByRole('treeitem',{name:'footer · Single card',exact:true}).nth(1).getAttribute('aria-selected'),'true');}
       await page.evaluate(()=>{const original=RetouchZoom.toSelection;RetouchZoom.toSelection=async elements=>{window.__rtHostZoomOccurrence=elements.map(el=>matchingInDocument(doc(),sel.info.id,sel.info).indexOf(el));try{return await original(elements);}finally{RetouchZoom.toSelection=original;}};});await page.getByRole('button',{name:'Zoom to selection',exact:true}).click();await wait(async()=>await page.getByRole('button',{name:'Zoom to selection',exact:true}).getAttribute('aria-busy')==='false');assert.deepEqual(await page.evaluate(()=>window.__rtHostZoomOccurrence),[1]);
       await navigate('Tab','em · Details');await navigate('Tab','small · Fragment sibling');await navigate('Shift+Enter','SingleCard · component');await wait(()=>primaryMatches(1));await navigate('Enter','footer · Single card');await navigate('Shift+Tab','small · Fragment sibling');await navigate('Shift+Enter','SingleCard · component');await wait(()=>primaryMatches(1));assert.equal(read(),beforeNavigation);assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedBefore);console.log('REPEATED FRAGMENT NAVIGATION OCCURRENCE PASS',engine);
      }}await app.locator('footer').first().click();await settled();await wait(()=>primaryMatches(0));await app.locator('[data-fragment-optional]').nth(1).click();await settled();await wait(()=>primaryMatches(1));await page.evaluate(()=>{const original=RetouchZoom.toSelection;RetouchZoom.toSelection=async elements=>{window.__rtZoomOccurrence=elements.map(el=>matchingInDocument(doc(),sel.info.id,sel.info).indexOf(el));try{return await original(elements);}finally{RetouchZoom.toSelection=original;}};});await page.getByRole('button',{name:'Zoom to selection',exact:true}).click();await wait(async()=>await page.getByRole('button',{name:'Zoom to selection',exact:true}).getAttribute('aria-busy')==='false');assert.deepEqual(await page.evaluate(()=>window.__rtZoomOccurrence),[3,4,5]);fs.writeFileSync(file,expandedSource);await wait(async()=>await app.locator('footer').count()===1);await wait(async()=>await app.locator('[data-fragment-optional]').count()===1);library=await openLibrary();await search().fill('SingleCard');await library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true}).getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});console.log('REPEATED CONDITIONAL FRAGMENT GROUPS PASS',kind,engine);}await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeConditional);await wait(async()=>await app.locator('[data-fragment-optional]').count()===0);assert.equal(await details().isChecked(),false);
    }
    if(process.env.RT_E2E_TREE_DELETE){
     const beforeDelete=read(),sharedFile=path.join(root,'app/components/SingleCard.tsx'),sharedSource=fs.readFileSync(sharedFile,'utf8'),rows=()=>page.getByRole('tree',{name:'Site layers',exact:true}).getByRole('treeitem',{name:'SingleCard · component',exact:true});await rows().click();await settled();await page.getByRole('button',{name:'Delete component',exact:true}).click();await settled();await wait(async()=>await app.locator('footer').count()===0);await wait(async()=>await rows().count()===0);const deletedSource=read();assert.notEqual(deletedSource,beforeDelete);assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedSource);library=await openLibrary();await search().fill('SingleCard');assert.match(await library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true}).textContent(),/No authored usages/);await library.getByRole('button',{name:'Close',exact:true}).click();await library.waitFor({state:'hidden'});await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeDelete);await app.locator('footer').waitFor();await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===deletedSource);await wait(async()=>await app.locator('footer').count()===0);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeDelete);await rows().waitFor();await rows().click();await settled();await wait(async()=>!await rows().isDisabled());await rows().press('Delete');await settled();await wait(async()=>await app.locator('footer').count()===0);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeDelete);await rows().waitFor();await rows().click();await settled();assert.equal(fs.readFileSync(sharedFile,'utf8'),sharedSource);console.log('COMPONENT DELETE/UNDO/REDO/KEYBOARD PASS',kind,engine);
    }
    if(process.env.RT_E2E_ROOT_DETACH){
     const sharedFile=path.join(root,'app/components/SingleCard.tsx'),shared=fs.readFileSync(sharedFile,'utf8'),oldRoot=await app.locator('footer').getAttribute('data-rt'),instance=await app.locator('footer').getAttribute('data-rt-i'),copy=path.join(root,'app/components/SingleCard.retouch-'+instance+'.tsx');
     if(process.env.RT_E2E_COMPONENT_COMMANDS){await page.getByRole('button',{name:'Actions',exact:true}).click();const actions=page.getByRole('dialog',{name:'Actions',exact:true});await actions.getByRole('combobox').fill('Edit component definition');await actions.getByRole('option',{name:'Edit component definition',exact:true}).click();await actions.waitFor({state:'hidden'});await wait(async()=>await page.evaluate(id=>sel?.info.kind==='host'&&sel.info.id===id,oldRoot));assert.equal(read(),beforeLibrary);await app.locator('footer').click({button:'right'});const menu=page.getByRole('menu',{name:'Canvas actions'});await menu.getByRole('menuitem',{name:'View component',exact:true}).waitFor();await menu.getByRole('menuitem',{name:'Detach instance',exact:true}).click();await menu.waitFor({state:'hidden'});}else await page.getByRole('button',{name:'Detach instance',exact:true}).click();await settled();await wait(()=>fs.existsSync(copy));await wait(async()=>await app.locator('footer').getAttribute('data-rt')!==oldRoot);await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');const detachedSource=read(),newRoot=await app.locator('footer').getAttribute('data-rt');assert.notEqual(detachedSource,beforeLibrary);assert.equal(fs.readFileSync(sharedFile,'utf8'),shared);
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary&&!fs.existsSync(copy));await wait(async()=>await app.locator('footer').getAttribute('data-rt')===oldRoot);
     await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===detachedSource&&fs.existsSync(copy));await wait(async()=>await app.locator('footer').getAttribute('data-rt')===newRoot);
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary&&!fs.existsSync(copy));await wait(async()=>await app.locator('footer').getAttribute('data-rt')===oldRoot);assert.equal(fs.readFileSync(sharedFile,'utf8'),shared);console.log('ROOT DETACH UI/SOURCE/UNDO/REDO PASS',kind,engine);
    }
    if(process.env.RT_E2E_ROOT_SWAP){assert.deepEqual(errors,[]);console.log('ROOT COMPONENT SWAP UI/SOURCE/RENDER PASS',kind,engine);return;}
    if(process.env.RT_E2E_COMPONENT_BRANCHES){
     const definitionFile=path.join(root,'app/components/SingleCard.tsx'),definitionBefore=fs.readFileSync(definitionFile,'utf8'),alternate=()=>page.getByLabel('Component property alternate',{exact:true});
     await page.getByRole('button',{name:'Duplicate instance',exact:true}).click();await settled();await wait(async()=>await app.locator('footer').count()===2);const duplicatedSource=read();assert.notEqual(duplicatedSource,beforeLibrary);await page.getByRole('button',{name:'Component',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');await page.getByRole('button',{name:'This instance',exact:true}).click();await alternate().waitFor();await alternate().check();await settled();await app.locator('[data-variant-card="alternate"]').waitFor();assert.equal(await app.locator('footer').count(),1,'Changing the copy keeps the original branch');assert.equal(await app.locator('footer').getAttribute('title'),'Only instance');
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===duplicatedSource);await wait(async()=>await app.locator('footer').count()===2);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await wait(async()=>await app.locator('footer').count()===1);await page.getByRole('button',{name:'Component',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');await page.getByRole('button',{name:'This instance',exact:true}).click();await alternate().waitFor();
     await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===duplicatedSource);await wait(async()=>await app.locator('footer').count()===2);await page.getByRole('button',{name:'Component',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');await page.getByRole('button',{name:'This instance',exact:true}).click();await alternate().waitFor();assert.equal(await alternate().isChecked(),false);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await wait(async()=>await app.locator('footer').count()===1);assert.equal(fs.readFileSync(definitionFile,'utf8'),definitionBefore,'Duplicate and branch edits leave the shared definition unchanged');
     await page.getByRole('button',{name:'Edit definition',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');await page.getByRole('button',{name:'This instance',exact:true}).click();await alternate().waitFor();await alternate().check();await settled();await app.locator('[data-variant-card="alternate"]').waitFor();const variantSource=read();assert.notEqual(variantSource,beforeLibrary);assert.equal(fs.readFileSync(definitionFile,'utf8'),definitionBefore);
     library=await openLibrary();await search().fill('SingleCard');await library.getByRole('listitem',{name:'SingleCard · app/components/SingleCard.tsx',exact:true}).getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});await alternate().waitFor();assert.equal(await alternate().isChecked(),true);await page.getByRole('button',{name:'Component',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Section');await page.getByRole('button',{name:'This instance',exact:true}).click();await alternate().waitFor();
     await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await app.locator('footer').waitFor();await page.getByRole('button',{name:'Component',exact:true}).click();await settled();await wait(async()=>await page.locator('.kindbadge').textContent()==='Footer');await page.getByRole('button',{name:'This instance',exact:true}).click();await alternate().waitFor();assert.equal(await alternate().isChecked(),false);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===variantSource);await app.locator('[data-variant-card="alternate"]').waitFor();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeLibrary);await app.locator('footer').waitFor();assert.equal(fs.readFileSync(definitionFile,'utf8'),definitionBefore);
    }
    library=await openLibrary();
   }
   await search().fill('other/page');const remote=library.getByRole('listitem',{name:'RemoteBadge · app/other/page.tsx',exact:true});assert.equal(await remote.getByRole('button',{name:'Select on canvas'}).isDisabled(),true);await remote.getByRole('button',{name:'View component'}).click();const remoteView=page.getByRole('dialog',{name:'RemoteBadge component'});await remoteView.waitFor();assert.equal(await remoteView.locator('iframe').count(),0);assert.match(await remoteView.locator('pre').textContent(),/Other page badge/);assert.equal(await remoteView.getByRole('button',{name:'Edit definition'}).isDisabled(),true);await remoteView.getByRole('button',{name:'Close',exact:true}).click();
   library=await openLibrary();const extra=path.join(root,'LibraryRefresh.tsx');fs.writeFileSync(extra,'/** @retouch-component */\nfunction FreshComponent(){return <section>Fresh</section>}export const Usage=()=> <FreshComponent/>;');await library.getByRole('button',{name:'Refresh',exact:true}).click();await library.getByRole('listitem',{name:'FreshComponent · LibraryRefresh.tsx',exact:true}).waitFor();fs.unlinkSync(extra);await library.getByRole('button',{name:'Refresh',exact:true}).click();await library.getByRole('listitem',{name:'FreshComponent · LibraryRefresh.tsx',exact:true}).waitFor({state:'hidden'});
   await page.setViewportSize({width:720,height:800});assert.equal(await library.evaluate(el=>{const r=el.getBoundingClientRect();return r.left>=0&&r.right<=innerWidth&&el.scrollWidth<=el.clientWidth;}),true,'Component library fits a narrow workspace');if(process.env.RT_E2E_COMPONENT_LIBRARY_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_LIBRARY_SCREENSHOT});await page.setViewportSize({width:1600,height:1100});
   await search().fill('HeadlineCard');const headline=library.getByRole('listitem',{name:'HeadlineCard · app/page.tsx',exact:true});await headline.getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});await settled();assert.equal(await page.getByLabel('Component property title',{exact:true}).inputValue(),'First');assert.equal(read(),beforeLibrary,'Browsing and selecting components never writes page source');assert.equal(await page.getByRole('button',{name:'This instance',exact:true}).isVisible(),true,'Component editing scope stays visible after library selection');const editScope=page.getByRole('group',{name:'Component editing scope',exact:true});await editScope.getByRole('button',{name:'Component',exact:true}).click();await settled();await wait(async()=>await editScope.getByRole('button',{name:'Component',exact:true}).getAttribute('aria-pressed')==='true');await editScope.getByRole('button',{name:'This instance',exact:true}).click();await settled();await wait(async()=>await editScope.getByRole('button',{name:'This instance',exact:true}).getAttribute('aria-pressed')==='true');assert.equal(read(),beforeLibrary,'Changing component scope does not write source');
   assert.equal(await page.locator('.selection-heading > .kindbadge').textContent(),'HeadlineCard','Inspector identifies the selected component by name');
   await wait(async()=>await page.locator('.component-badge > span').textContent()==='HeadlineCard');
   assert.match(await page.locator('.component-badge').evaluate(el=>getComputedStyle(el).fontFamily),/^Inter/);
   await app.locator('aside').hover();await wait(async()=>await page.locator('.component-badge > span').textContent()==='Badge');
   await page.getByLabel('Find a layer',{exact:true}).hover();await wait(async()=>await page.locator('.component-badge > span').textContent()==='HeadlineCard');
   const longComponentName='ResponsiveProductRecommendationCardWithLocalizedContentAndOptionalActions';
   await page.evaluate(name=>{sel.info.tag=name;},longComponentName);
   await wait(async()=>await page.locator('.component-badge > span').textContent()===longComponentName);
   const badgeGeometry=await page.locator('.component-badge').evaluate(el=>{const label=el.querySelector('span'),button=el.querySelector('button'),r=el.getBoundingClientRect(),b=button.getBoundingClientRect();return {width:r.width,truncated:label.scrollWidth>label.clientWidth,title:label.title,buttonInside:b.right<=r.right&&b.left>=r.left,buttonWidth:b.width};});
   assert.ok(badgeGeometry.width<=240&&badgeGeometry.truncated&&badgeGeometry.buttonInside&&badgeGeometry.buttonWidth>=20,'Long component name truncates without shrinking its action');assert.equal(badgeGeometry.title,longComponentName);
   const originalHeadingStyle=await app.locator('h1').getAttribute('style');
   await app.locator('h1').evaluate(el=>{el.style.width='20px';el.style.marginLeft=(el.ownerDocument.defaultView.innerWidth-20)+'px';});
   for(const percent of [50,200,100]){
    await page.locator('#canvasZoom').fill(String(percent));await page.locator('#canvasZoom').press('Enter');
    await wait(async()=>await page.locator('.component-badge').evaluate(el=>{const badge=el.getBoundingClientRect(),frame=document.getElementById('app').getBoundingClientRect();return badge.width>0&&badge.left>=frame.left-1&&badge.right<=frame.right+1;}));
   }
   await app.locator('h1').evaluate((el,style)=>style===null?el.removeAttribute('style'):el.setAttribute('style',style),originalHeadingStyle);
   if(process.env.RT_E2E_LONG_BADGE_SCREENSHOT)await page.locator('.component-badge').screenshot({path:process.env.RT_E2E_LONG_BADGE_SCREENSHOT});
   await page.evaluate(()=>{sel.info.tag='HeadlineCard';});await wait(async()=>await page.locator('.component-badge > span').textContent()==='HeadlineCard');
   const componentLayer=page.getByRole('tree',{name:'Site layers',exact:true}).getByRole('treeitem',{name:'HeadlineCard · component',exact:true});
   await componentLayer.waitFor();await componentLayer.hover();
   assert.equal(await componentLayer.getAttribute('data-layer-kind'),'component');
   assert.equal(await componentLayer.getAttribute('aria-selected'),'true');
   assert.equal(await componentLayer.evaluate(el=>getComputedStyle(el).color),'rgb(151, 71, 255)','Component selection keeps the component accent');
   assert.equal(await componentLayer.evaluate(el=>getComputedStyle(el.parentElement).backgroundColor),'rgb(241, 229, 255)','Component selection fills the row while hovered');
   assert.notEqual(await componentLayer.evaluate(el=>getComputedStyle(el,'::before').maskImage),'none');
   const componentDisclosure=page.getByRole('button',{name:'Collapse HeadlineCard · component',exact:true});
   await componentDisclosure.click();assert.equal(await componentLayer.getAttribute('aria-expanded'),'false');
   assert.notEqual(await componentLayer.evaluate(el=>getComputedStyle(el.parentElement.querySelector('.layer-toggle'),'::before').transform),'none');
   await page.getByRole('button',{name:'Expand HeadlineCard · component',exact:true}).click();
   assert.equal(await componentLayer.getAttribute('aria-selected'),'true');
   assert.equal(read(),beforeLibrary,'Layer disclosure preserves component selection and source');
   if(process.env.RT_E2E_COMPONENT_LAYER_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_LAYER_SCREENSHOT});
   if(process.env.RT_E2E_COMPONENT_LIBRARY_ONLY){assert.deepEqual(errors,[]);console.log(engine+' '+kind+': PASS component library search, navigation, refresh, compact bounds and enabled insertion workflows');return;}
  }
  if(process.env.RT_E2E_COMPONENT_PROPERTY_EDIT){
   await page.getByRole('button',{name:'This instance',exact:true}).click();await settled();
   if(process.env.RT_E2E_FOCUS_TRACE)await page.evaluate(()=>{window.__rtFocusTrace=[];const record=event=>{window.__rtFocusTrace.push({time:Date.now(),event,active:document.activeElement?.getAttribute('aria-label')||document.activeElement?.tagName,pending:pendingPanelFocus?{...pendingPanelFocus}:null,selection:panelSelectionKey(),disabled:panelBody.disabled});if(window.__rtFocusTrace.length>300)window.__rtFocusTrace.shift();};const previousRender=renderPanel;renderPanel=function(...args){record('render:'+new Error().stack);return previousRender(...args);};for(const type of ['keydown','pointerdown','focus','blur'])window.addEventListener(type,event=>{record(type+':'+(event.key||event.target?.getAttribute?.('aria-label')||event.target?.tagName||'window'));queueMicrotask(()=>record('after:'+type));},true);new MutationObserver(()=>record('panel mutation')).observe(panelBody,{childList:true,subtree:true,attributes:true,attributeFilter:['disabled','aria-busy']});});
   const editedTitle='New \"title\" & <tag>\nSecond line',before=read(),definition=before.slice(before.indexOf('/** @retouch-component */')),input=name=>page.getByLabel('Component property '+name,{exact:true}),state=()=>app.locator('h1').evaluate(el=>[el.title,el.dataset.count,el.dataset.enabled]);
   await input('title').waitFor();
   for(const name of ['title','count','size']){
    await wait(async()=>await input(name).evaluate(el=>{
     const reset=el.closest('td').querySelector('button[aria-label^="Reset "]');if(!reset)return false;
     const fieldBounds=el.getBoundingClientRect(),resetBounds=reset.getBoundingClientRect();
     return fieldBounds.width>0&&resetBounds.width>0&&resetBounds.x>=fieldBounds.right&&Math.abs(resetBounds.y-fieldBounds.y)<2;
    }));
   }
   const checkboxReset=page.getByRole('button',{name:'Reset enabled to default',exact:true});
   assert.equal(await checkboxReset.evaluate(el=>{const a=el.getBoundingClientRect(),b=el.closest('td').getBoundingClientRect();return a.top>=b.top&&a.bottom<=b.bottom;}),true,'Checkbox reset stays within its property row');
   assert.equal(await input('title').evaluate(el=>{const table=el.closest('table');return table.classList.contains('component-props-editable')&&getComputedStyle(table.querySelector('tbody tr td:last-child')).display==='none';}),true,'Editable component properties use compact two-column controls');assert.match(await input('title').getAttribute('aria-description'),/Default:/);assert.equal(await input('title').evaluate(el=>el.tagName),'TEXTAREA');await input('title').fill('Discarded');await input('title').press('Escape');assert.equal(read(),before);assert.equal(await input('title').inputValue(),'First');await input('title').fill('New \"title\" & <tag>');await input('title').press('End');await input('title').press('Enter');await input('title').pressSequentially('Second line');assert.equal(read(),before,'Enter adds a newline without committing');assert.equal(await input('title').inputValue(),editedTitle);assert.equal(await page.evaluate(async()=>{const draft=document.activeElement;await loadScope();return draft.isConnected&&document.activeElement===draft;}),true,'Late scope metadata keeps the current text draft focused');assert.equal(read(),before,'Metadata refresh must not commit the draft');assert.equal(await input('title').inputValue(),editedTitle);await input('title').press('Control+Enter');await settled();await wait(async()=> (await state())[0]===editedTitle);await input('title').waitFor();assert.equal(await input('title').evaluate(el=>el===document.activeElement),true,'Explicit keyboard commit retains text property focus'+(process.env.RT_E2E_FOCUS_TRACE?' '+JSON.stringify(await page.evaluate(()=>window.__rtFocusTrace)):''));const titled=read();await input('title').press('Control+Enter');assert.equal(read(),titled);assert.equal(await input('title').evaluate(el=>el===document.activeElement),true,'Unchanged keyboard commit retains focus without writing');
   await input('count').fill('2.5');await input('count').press('Tab');await settled();await wait(async()=> (await state())[1]==='2.5');const numbered=read();
   await input('enabled').uncheck();await settled();await wait(async()=> (await state())[2]==='false');const edited=read();assert.equal(edited.slice(edited.indexOf('/** @retouch-component */')),definition);
   for(const expected of [numbered,titled,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);await input('title').waitFor();}await wait(async()=>JSON.stringify(await state())===JSON.stringify(['First','1','true']));
   for(const expected of [titled,numbered,edited]){await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===expected);await input('title').waitFor();}await wait(async()=>JSON.stringify(await state())===JSON.stringify([editedTitle,'2.5','false']));
   await page.getByRole('button',{name:'Duplicate instance',exact:true}).click();await settled();await wait(async()=>await app.locator('h1').count()===2);const duplicated=read();await input('title').fill('Copy only');await input('title').press('Meta+Enter');await settled();await wait(async()=>JSON.stringify(await app.locator('h1').evaluateAll(nodes=>nodes.map(el=>el.title)))===JSON.stringify([editedTitle,'Copy only']));const isolated=read();assert.equal(isolated.slice(isolated.indexOf('/** @retouch-component */')),definition);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===duplicated);await wait(async()=>await app.locator('h1').evaluateAll((nodes,title)=>nodes.every(el=>el.title===title),editedTitle));await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===isolated);await wait(async()=>JSON.stringify(await app.locator('h1').evaluateAll(nodes=>nodes.map(el=>el.title)))===JSON.stringify([editedTitle,'Copy only']));
   const overridden=read();await page.getByRole('button',{name:'Reset title to default',exact:true}).click();await settled();await wait(async()=>await app.locator('h1').last().getAttribute('title')==='Default title');assert.equal(await input('title').inputValue(),'Default title');await wait(async()=>await input('title').evaluate(el=>el===document.activeElement));const resetSource=read();assert.equal(await app.locator('h1').first().getAttribute('title'),editedTitle);assert.equal(await page.getByRole('button',{name:'Reset title to default',exact:true}).count(),0);
   await input('title').fill('Override default');await input('title').press('Control+Enter');await settled();await wait(async()=>await app.locator('h1').last().getAttribute('title')==='Override default');const reoverridden=read();
   for(const expected of [resetSource,overridden]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await wait(async()=>await app.locator('h1').last().getAttribute('title')==='Copy only');
   for(const expected of [resetSource,reoverridden]){await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===expected);}await wait(async()=>await app.locator('h1').last().getAttribute('title')==='Override default');assert.equal(read().slice(read().indexOf('/** @retouch-component */')),definition);
   const measured=()=>input('title').evaluate(el=>({height:el.getBoundingClientRect().height,client:el.clientHeight,scroll:el.scrollHeight,line:parseFloat(getComputedStyle(el).lineHeight)}));await wait(async()=>{const box=await measured();return box.scroll<=box.client+1&&box.height>=box.line;});
   await input('title').fill('A long paragraph '.repeat(50));await wait(async()=>{const box=await measured();return box.scroll>box.client&&box.height<=box.line*5+14;});assert.equal(read(),reoverridden);await input('title').press('Escape');assert.equal(await input('title').inputValue(),'Override default');await wait(async()=>{const box=await measured();return box.scroll<=box.client+1;});assert.equal(read(),reoverridden);
   if(process.env.RT_E2E_COMPONENT_CHOICES){const beforeChoice=read();assert.equal(await input('size').evaluate(el=>el.tagName),'SELECT');assert.deepEqual(await input('size').locator('option').allTextContents(),['small','large']);await input('size').focus();await input('size').selectOption({label:'large'});await settled();await wait(async()=>await app.locator('h1').last().getAttribute('data-size')==='large');assert.equal(await app.locator('h1').first().getAttribute('data-size'),'small');assert.deepEqual(await app.locator('h1').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).fontSize)),['32px','48px']);await input('size').waitFor();assert.equal(await input('size').evaluate(el=>el===document.activeElement),true,'Variant control retains keyboard focus after a source write');const chosen=read();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeChoice);await wait(async()=>await app.locator('h1').last().getAttribute('data-size')==='small');await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===chosen);await wait(async()=>await app.locator('h1').last().getAttribute('data-size')==='large');}
   if(process.env.RT_E2E_COMPONENT_CHOICES){
    assert.equal(await input('tone').inputValue(),'unset');assert.deepEqual(await input('tone').locator('option').allTextContents(),['Not set','calm','bold']);const beforeTone=read();await input('tone').selectOption({label:'bold'});await settled();await wait(async()=>await app.locator('h1').last().getAttribute('data-tone')==='bold');assert.equal(await app.locator('h1').first().getAttribute('data-tone'),'none');const setTone=read();await input('tone').selectOption({label:'Not set'});await settled();await wait(async()=>await app.locator('h1').last().getAttribute('data-tone')==='none');const clearedTone=read();assert.equal(await input('tone').inputValue(),'unset');
    for(const expected of [setTone,beforeTone]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await wait(async()=>await input('tone').inputValue()==='unset');for(const expected of [setTone,clearedTone]){await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===expected);}await wait(async()=>await input('tone').inputValue()==='unset');assert.equal(read().slice(read().indexOf('/** @retouch-component */')),definition);
   }
   if(process.env.RT_E2E_COMPONENT_CHOICES){await page.getByRole('treeitem',{name:'aside · Badge',exact:true}).click();await settled();await page.getByRole('button',{name:'This instance',exact:true}).click();await settled();assert.equal(await input('tone').inputValue(),'unset');const beforeBadge=read(),propertySearch=page.getByLabel('Search component properties',{exact:true});await propertySearch.fill('no-such-property');await page.getByText('No properties match.',{exact:true}).waitFor();assert.equal(await input('tone').isVisible(),false);assert.equal(read(),beforeBadge);await propertySearch.fill(' TONE ');assert.equal(await input('tone').isVisible(),true);assert.equal(await input('label').isVisible(),false);await propertySearch.focus();assert.equal(await propertySearch.evaluate(async search=>{const before={focused:document.activeElement===search,key:renderedPanelSelection,current:JSON.stringify([sel.info.file,sel.scope,sel.instanceId,(sel.multiple||[sel.info]).map(info=>info.id).sort()]),busy:[panelTasks,sourceRequests,undoBusy]};search.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,button:0,pointerId:771}));renderPanel();window.dispatchEvent(new PointerEvent('pointerup',{pointerId:771}));await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));const state={before,connected:search.isConnected,focused:document.activeElement===search,value:search.value,active:document.activeElement?.getAttribute('aria-label'),busy:[panelTasks,sourceRequests,undoBusy],deferred:panelRenderDeferred};if(!state.connected||!state.focused||state.value!==' TONE ')throw Error('PROPERTY SEARCH STATE '+JSON.stringify(state));return state.connected&&state.focused&&state.value===' TONE ';}),true,'Deferred metadata refresh preserves the focused search draft');assert.equal(await propertySearch.evaluate(search=>{sourceRequests++;try{renderPanel();return search.isConnected&&document.activeElement===search&&search.value===' TONE ';}finally{sourceRequests--;}}),true,'Source activity must not discard the focused property filter');await propertySearch.press('Tab');assert.equal(await input('tone').evaluate(el=>el===document.activeElement),true,'Filtered property Tab focus: '+await page.evaluate(()=>document.activeElement?.tagName+' '+document.activeElement?.getAttribute('aria-label')));await input('tone').press('Shift+Tab');assert.equal(await propertySearch.evaluate(el=>el===document.activeElement),true,'Shift+Tab returns to property search: '+await page.evaluate(()=>document.activeElement?.tagName+' '+document.activeElement?.getAttribute('aria-label')));await propertySearch.press('Tab');assert.equal(await input('tone').evaluate(el=>el===document.activeElement),true,'Repeated Tab returns to property: '+await page.evaluate(()=>document.activeElement?.tagName+' '+document.activeElement?.getAttribute('aria-label')));await input('tone').selectOption({label:'bold'});await settled();await wait(async()=>await app.locator('aside').evaluate(el=>getComputedStyle(el).backgroundColor)==='rgb(204, 51, 0)');const badgeSet=read();assert.equal(await propertySearch.inputValue(),' TONE ');assert.equal(await input('label').isVisible(),false);await propertySearch.press('Escape');assert.equal(await input('label').isVisible(),true);assert.equal(read(),badgeSet);assert.ok(badgeSet.includes('<Badge tone={"bold"}/>'));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===beforeBadge);await wait(async()=>await app.locator('aside').getAttribute('data-tone')==='none');await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===badgeSet);await wait(async()=>await app.locator('aside').getAttribute('data-tone')==='bold');}
   if(process.env.RT_E2E_COMPONENT_CHOICES){assert.equal(await input('emphasized').inputValue(),'unset');assert.deepEqual(await input('emphasized').locator('option').allTextContents(),['Not set','true','false']);const beforeBoolean=read();await input('emphasized').selectOption({label:'true'});await settled();await wait(async()=>await app.locator('aside').evaluate(el=>getComputedStyle(el).fontWeight)==='700');const enabledBoolean=read();await input('emphasized').selectOption({label:'false'});await settled();await wait(async()=>await app.locator('aside').evaluate(el=>getComputedStyle(el).fontWeight)==='400');const disabledBoolean=read();assert.ok(disabledBoolean.includes('emphasized={false}'));await input('emphasized').selectOption({label:'Not set'});await settled();await wait(()=>!read().includes('emphasized={'));const unsetBoolean=read();for(const expected of [disabledBoolean,enabledBoolean,beforeBoolean]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}for(const expected of [enabledBoolean,disabledBoolean,unsetBoolean]){await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===expected);}await wait(async()=>await input('emphasized').inputValue()==='unset');}
   if(process.env.RT_E2E_COMPONENT_CHOICES){
    const beforePrimitive=read();assert.equal(await input('label').getAttribute('placeholder'),'Not set');assert.equal(await input('width').inputValue(),'');await input('label').fill('Discarded');await input('label').press('Escape');assert.equal(await input('label').inputValue(),'');assert.equal(read(),beforePrimitive);
    await page.getByRole('button',{name:'Set label to empty text',exact:true}).focus();await page.getByRole('button',{name:'Set label to empty text',exact:true}).press('Enter');await settled();await wait(async()=>await app.locator('aside').textContent()==='');await wait(async()=>await input('label').evaluate(el=>el===document.activeElement));const emptyLabel=read();assert.ok(emptyLabel.includes('label={""}'));await input('label').fill('Custom badge');await input('label').press('Control+Enter');await settled();await wait(async()=>await app.locator('aside').textContent()==='Custom badge');const customLabel=read();
    await input('width').fill('160');await input('width').press('Tab');await settled();await wait(async()=>await app.locator('aside').evaluate(el=>el.getBoundingClientRect().width)===160);const fixedWidth=read();await input('width').fill('');await input('width').press('Tab');assert.equal(read(),fixedWidth);assert.equal(await app.locator('aside').evaluate(el=>el.getBoundingClientRect().width),160);assert.equal(await input('width').getAttribute('aria-invalid'),'true');await page.getByRole('alert').filter({hasText:'Enter a number, or use Unset to remove it.'}).waitFor();await input('width').press('Escape');assert.equal(await input('width').inputValue(),'160');assert.equal(await input('width').getAttribute('aria-invalid'),null);assert.equal(await page.locator('.property-error').count(),0);assert.equal(read(),fixedWidth);await page.getByRole('button',{name:'Unset property label',exact:true}).click();await settled();await wait(async()=>await app.locator('aside').textContent()==='Badge');await wait(async()=>await input('label').evaluate(el=>el===document.activeElement));const unsetLabel=read();await page.getByRole('button',{name:'Unset property width',exact:true}).focus();await page.getByRole('button',{name:'Unset property width',exact:true}).press('Enter');await settled();await wait(async()=>await app.locator('aside').evaluate(el=>el.getBoundingClientRect().width)>300);await wait(async()=>await input('width').evaluate(el=>el===document.activeElement));const unsetWidth=read();
    for(const expected of [unsetLabel,fixedWidth,customLabel,emptyLabel,beforePrimitive]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}for(const expected of [emptyLabel,customLabel,fixedWidth,unsetLabel,unsetWidth]){await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===expected);}await wait(async()=>await input('label').getAttribute('placeholder')==='Not set');assert.equal(await input('width').inputValue(),'');
   }
   if(process.env.RT_E2E_COMPONENT_LIBRARY){
    const beforeSelect=read(),secondTitle=await app.locator('h1').nth(1).getAttribute('title');await page.getByRole('button',{name:'Components',exact:true}).click();const library=page.getByRole('dialog',{name:'Project components',exact:true});await library.getByLabel('Search project components').fill('HeadlineCard');const row=library.getByRole('listitem',{name:'HeadlineCard · app/page.tsx',exact:true}),picker=row.getByLabel('Instance of HeadlineCard');await picker.waitFor();assert.equal(await picker.locator('option').count(),2);await picker.selectOption('1');await row.getByRole('button',{name:'Select on canvas'}).click();await library.waitFor({state:'hidden'});await settled();await page.getByLabel('Component property title',{exact:true}).waitFor();assert.equal(await page.getByLabel('Component property title',{exact:true}).inputValue(),secondTitle);assert.equal(read(),beforeSelect);
   }
   if(process.env.RT_E2E_COMPONENT_PROPERTY_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COMPONENT_PROPERTY_SCREENSHOT});assert.deepEqual(errors,[]);for(const [target,source] of importedTypeSources)assert.equal(fs.readFileSync(target,'utf8'),source);if(process.env.RT_E2E_COMPONENT_TYPE_PATHS)assert.equal(fs.existsSync(path.join(root,'preferred')),false);console.log('COMPONENT PROPERTY UI/SOURCE/RENDER PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_CREATE_COMPONENT){
   const correctCreationLocks=()=>page.evaluate(()=>{const d=doc();return layerLocks.direct(d.querySelector('h1 span'))&&layerLocks.direct(d.querySelector('p.other-font'))&&!layerLocks.direct(d.querySelector('h1'))&&!layerLocks.direct(d.querySelector('p.named-font'));});
   if(process.env.RT_E2E_CREATE_COMPONENT_LOCKS){
    for(const name of ['span · Headline','p · Other text']){await page.getByRole('treeitem',{name,exact:true}).click();await settled();await page.getByRole('button',{name:'Lock selection',exact:true}).click();await settled();}
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await wait(correctCreationLocks);
   }
   const before=read(),id=await app.locator('h1').first().getAttribute('data-rt'),appearance=()=>app.locator('h1').first().evaluate(el=>{const css=getComputedStyle(el),r=el.getBoundingClientRect();return {text:el.textContent,className:el.className,font:css.fontFamily,size:css.fontSize,color:css.color,box:[r.x,r.y,r.width,r.height]};}),originalAppearance=await appearance();
   await page.getByText('Create component',{exact:true}).click();await page.getByLabel('Component name',{exact:true}).fill('Page');await page.getByRole('button',{name:'Create component from layer',exact:true}).click();await settled();await page.getByText('That name already appears in this module. Choose a different component name.',{exact:true}).waitFor();assert.equal(read(),before);await page.getByLabel('Component name',{exact:true}).fill('HeadlineCard');await page.getByRole('button',{name:'Create component from layer',exact:true}).click();await settled();await wait(()=>read()!==before);const after=read();if(process.env.RT_E2E_COMPONENT_PROPS){assert.ok(after.includes(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION?'<HeadlineCard headingData={headingData} onClick={onClick} label={label} />':'<HeadlineCard title={title} onClick={onClick} label={label} />'));assert.equal(await app.locator('h1').first().getAttribute('title'),'Local title');}
   if(!process.env.RT_E2E_COMPONENT_PROPS)await page.getByText('No instance properties to edit here. Edit the definition for shared styles and content.',{exact:true}).waitFor();
   assert.equal(await page.getByRole('button',{name:'Duplicate component',exact:true}).isEnabled(),true,'New component can be duplicated from Layers without reselection');
   assert.equal(await page.getByRole('button',{name:'Delete component',exact:true}).isEnabled(),true,'New component can be deleted from Layers without reselection');
   const canvasCommand=async key=>{await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);await app.locator('body').evaluate(el=>el.tabIndex=-1);await app.locator('body').press(key);};
   if(!process.env.RT_E2E_CREATE_COMPONENT_LOCKS){if(process.env.RT_E2E_COMPONENT_CANVAS_SHORTCUTS)await canvasCommand('Control+d');else await page.getByRole('button',{name:'Duplicate component',exact:true}).click();await settled();await wait(async()=>await app.locator('h1').count()===2);if(process.env.RT_E2E_COMPONENT_CANVAS_SHORTCUTS)await canvasCommand('Control+z');else await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===after);await wait(async()=>await app.locator('h1').count()===1);}
   if(process.env.RT_E2E_COMPONENT_CANVAS_SHORTCUTS){
    assert.ok(!process.env.RT_E2E_CREATE_COMPONENT_LOCKS);const definition=after.slice(after.indexOf('function HeadlineCard'));assert.ok(definition.startsWith('function HeadlineCard'));
    await canvasCommand('Delete');await wait(async()=>await app.locator('h1').count()===0);await settled();const deleted=read();assert.notEqual(deleted,after);assert.equal(deleted.slice(deleted.indexOf('function HeadlineCard')),definition);await canvasCommand('Control+z');await wait(()=>read()===after);await wait(async()=>await app.locator('h1').count()===1);await settled();await canvasCommand('Control+y');await wait(()=>read()===deleted);await wait(async()=>await app.locator('h1').count()===0);await settled();await canvasCommand('Control+z');await wait(()=>read()===after);await wait(async()=>await app.locator('h1').count()===1);await settled();await page.getByRole('button',{name:'Edit definition',exact:true}).waitFor();console.log('REACT COMPONENT CANVAS DUPLICATE/DELETE/UNDO/REDO PASS',engine);
   }
   if(process.env.RT_E2E_TYPED_EXTRACTION){assert.ok(after.includes(process.env.RT_E2E_OBJECT_CONTRACT_EXTRACTION?(process.env.RT_E2E_UNION_CONTRACT_EXTRACTION?'"headingData": ({ heading:(string|null) })':process.env.RT_E2E_INTERSECTION_CONTRACT_EXTRACTION?'"headingData": ({ heading:(string); note?:number })':process.env.RT_E2E_INDEX_CONTRACT_EXTRACTION?'"headingData": ({ readonly [key:string]:(string) })':process.env.RT_E2E_OPTIONAL_CONTRACT_EXTRACTION?'"headingData": ({ heading?:(string) })':'"headingData": ({ heading:(string) })'):process.env.RT_E2E_OPTIONAL_PARAMETER_EXTRACTION&&!process.env.RT_E2E_LITERAL_DEFAULT_EXTRACTION?'"title": ((string) | undefined)':'"title": (string)'));assert.ok(after.includes('"label": (string)'));assert.ok(after.includes('"onClick": (()=>void)'));}
   if(process.env.RT_E2E_LITERAL_DEFAULT_EXTRACTION)assert.ok(after.includes(process.env.RT_E2E_OPTIONAL_TUPLE_DEFAULT_EXTRACTION?'function Page(){const [title="Local title"]:[title?:string]=[];':'function Page({title="Local title"}:{title?:string})'),'literal defaults remain in the original component');
   const info=await page.evaluate(async id=>(await fetch('/rt/__api/component?id='+id,{headers:{'x-retouch-token':window.__RT_TOKEN}})).json(),id);assert.ok(info.ok,info.reason);assert.equal(info.name,'HeadlineCard');await wait(async()=>await app.locator('h1').first().getAttribute('data-rt')===info.definitionId);await wait(async()=>JSON.stringify(await appearance())===JSON.stringify(originalAppearance));if(process.env.RT_E2E_CREATE_COMPONENT_LOCKS)await wait(correctCreationLocks);await page.getByRole('button',{name:'Edit definition',exact:true}).waitFor();assert.equal(await app.locator('h1').first().getAttribute('data-rt-i'),id);if(process.env.RT_E2E_CREATE_COMPONENT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_CREATE_COMPONENT_SCREENSHOT});
   if(process.env.RT_E2E_COMPONENT_NAVIGATION){
    await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);await app.locator('body').evaluate(el=>el.tabIndex=-1);await app.locator('body').press('Enter');await wait(async()=>await page.evaluate(definitionId=>sel?.info?.kind==='host'&&sel.info.id===definitionId,info.definitionId));
    assert.equal(read(),after,'entering a component definition does not change source');await page.waitForFunction(()=>!undoBusy&&!sourceRequests&&!panelTasks);await app.locator('body').press('Shift+Enter');await wait(async()=>await page.evaluate(instanceId=>sel?.info?.kind==='instance'&&sel.info.id===instanceId,id));await page.getByRole('button',{name:'Edit definition',exact:true}).waitFor();assert.equal(read(),after,'returning to the component instance does not change source');await wait(async()=>JSON.stringify(await appearance())===JSON.stringify(originalAppearance));
   }
   if(process.env.RT_E2E_COMPONENT_PROPS){const preview=await browser.newPage();preview.on('pageerror',error=>errors.push(error.message));try{await preview.goto(url,{timeout:90000});await preview.locator('h1').click();await wait(async()=>await preview.locator('h1').textContent()==='Clicked');assert.equal(await preview.locator('h1').getAttribute('title'),'Local title');}finally{await preview.close();}}
   let working=after;
   if(process.env.RT_E2E_DUPLICATE_COMPONENT){await page.getByRole('button',{name:'Duplicate instance',exact:true}).click();await settled();await wait(()=>read()!==after);working=read();await wait(async()=>await app.locator('h1').count()===2);const roots=await app.locator('h1').evaluateAll(nodes=>nodes.map(el=>[el.getAttribute('data-rt'),el.getAttribute('data-rt-i')]));assert.ok(roots.every(([host])=>host===info.definitionId));assert.equal(new Set(roots.map(([,instance])=>instance)).size,2);}
   await page.getByRole('button',{name:'Edit definition',exact:true}).click();await settled();
   if(process.env.RT_E2E_COMPONENT_BREAKPOINT_COMPARE){
    const priorSize=await page.evaluate(()=>RetouchScreens.get()),priorWidth=await app.locator('body').evaluate(()=>innerWidth);
    if(process.env.RT_E2E_COMPONENT_SCOPE_ORDER){const width=page.getByLabel('Screen width',{exact:true});await width.fill('900');await width.press('Enter');await wait(async()=>await app.locator('body').evaluate(()=>innerWidth)===900);await wait(async()=>JSON.stringify(await page.getByLabel('Style screen scope').locator('option').evaluateAll(options=>options.map(option=>option.value)))===JSON.stringify(['','zeta:','md:','min-[56.25rem]:','alpha:']));assert.equal(read(),working);console.log('REACT NAMED/GENERATED SCOPE WIDTH ORDER PASS',engine);}
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>await app.locator('body').evaluate(()=>innerWidth)===768);await page.getByLabel('Style screen scope').selectOption('md:');await page.getByLabel('Opacity (%)',{exact:true}).fill('50');await page.getByLabel('Opacity (%)',{exact:true}).press('Tab');await settled();await wait(()=>read()!==working);const scoped=read();assert.match(scoped,/md:opacity-\[0\.5\]/);assert.equal(scoped.slice(0,scoped.indexOf('function HeadlineCard')),working.slice(0,working.indexOf('function HeadlineCard')));
    await page.getByRole('button',{name:'Compare across breakpoint',exact:true}).click();const outside=page.frameLocator('iframe[title="Outside md comparison preview"]'),inside=page.frameLocator('iframe[title="Tablet comparison preview"]');await outside.locator('h1').waitFor();await inside.locator('h1').waitFor();const values=async value=>{await wait(async()=>await inside.locator('h1').evaluate(el=>getComputedStyle(el).opacity)===value);assert.equal(await outside.locator('h1').evaluate(el=>getComputedStyle(el).opacity),'1');};await values('0.5');assert.deepEqual(await outside.locator('body').evaluate(()=>[innerWidth,innerHeight]),[767,1024]);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===working);await values('0.9');await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===scoped);await values('0.5');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===working);await values('0.9');await page.getByRole('button',{name:'Compare screens',exact:true}).click();await page.waitForFunction(()=>!document.getElementById('compareScreens').disabled);await page.getByLabel('Style screen scope').selectOption('');await page.getByLabel('Screen size',{exact:true}).focus();await page.evaluate(size=>RetouchScreens.set(size),priorSize);await wait(async()=>await app.locator('body').evaluate(()=>innerWidth)===priorWidth);await settled();console.log('REACT SHARED DEFINITION BREAKPOINT COMPARISON/HMR/UNDO/REDO PASS',engine);
   }
   await page.getByLabel('Font size (px)',{exact:true}).fill('40');await page.getByLabel('Font size (px)',{exact:true}).press('Tab');await settled();await wait(()=>read()!==working);await wait(async()=>await app.locator('h1').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).fontSize==='40px')));assert.equal(await app.locator('h1').first().getAttribute('data-rt-i'),id);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===working);await wait(async()=>JSON.stringify(await appearance())===JSON.stringify(originalAppearance));if(process.env.RT_E2E_CREATE_COMPONENT_LOCKS)await wait(correctCreationLocks);
   if(process.env.RT_E2E_DUPLICATE_COMPONENT){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===after);await wait(async()=>await app.locator('h1').count()===1);}
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(async()=>await app.locator('h1').first().getAttribute('data-rt')===id);await wait(async()=>JSON.stringify(await appearance())===JSON.stringify(originalAppearance));if(process.env.RT_E2E_CREATE_COMPONENT_LOCKS)await wait(correctCreationLocks);
   await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===after);await wait(async()=>await app.locator('h1').first().getAttribute('data-rt')===info.definitionId);await wait(async()=>JSON.stringify(await appearance())===JSON.stringify(originalAppearance));if(process.env.RT_E2E_CREATE_COMPONENT_LOCKS)await wait(correctCreationLocks);if(process.env.RT_E2E_DUPLICATE_COMPONENT){await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===working);await wait(async()=>await app.locator('h1').count()===2);await page.getByRole('button',{name:'Edit definition',exact:true}).waitFor();if(process.env.RT_E2E_DUPLICATE_COMPONENT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_DUPLICATE_COMPONENT_SCREENSHOT});}assert.deepEqual(errors,[]);console.log('CREATE COMPONENT UI/SOURCE/RENDER PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_SECTIONS){
   const before=read(),group=key=>page.locator('[data-shared-section="'+key+'"]'),summary=key=>group(key).locator(':scope > summary'),opened=async(key,value)=>wait(async()=>await group(key).evaluate(el=>el.open)===value),select=async()=>{await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();};
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await select();
   for(const key of ['layout','typography','appearance','fill','stroke','effects']){await summary(key).click();await opened(key,false);}assert.equal(await page.getByLabel('Shared Width (px)',{exact:true}).isVisible(),true);assert.equal(await page.getByLabel('Shared Font size (px)',{exact:true}).isVisible(),false);
   await page.getByLabel('Shared Width (px)',{exact:true}).fill('200');await page.getByLabel('Shared Width (px)',{exact:true}).press('Tab');await settled();await wait(()=>read()!==before);const sized=read();await wait(async()=>await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.every(el=>el.getBoundingClientRect().width===200)));for(const key of ['layout','typography','appearance','fill','stroke','effects'])await opened(key,false);
   await summary('typography').focus();await summary('typography').press('Enter');await opened('typography',true);await summary('size').focus();await summary('size').press('Space');await opened('size',false);await page.getByLabel('Shared Font size (px)',{exact:true}).fill('24');await page.getByLabel('Shared Font size (px)',{exact:true}).press('Tab');await settled();await wait(()=>read()!==sized);await opened('size',false);await opened('typography',true);
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await app.locator('h1').evaluate(el=>el.getBoundingClientRect().width===80));await opened('size',false);await opened('appearance',false);
   for(const expected of [sized,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await page.reload();await app.locator('h1').waitFor({timeout:90000});await select();await opened('size',false);await opened('layout',false);assert.equal(await group('item').count(),0);await opened('typography',true);await opened('appearance',false);await opened('fill',false);await opened('stroke',false);await opened('effects',false);
   if(process.env.RT_E2E_SHARED_SECTIONS_SCREENSHOT){await summary('fill').click();await summary('stroke').click();await summary('typography').click();await opened('typography',false);await summary('size').focus();await page.screenshot({path:process.env.RT_E2E_SHARED_SECTIONS_SCREENSHOT});}
   assert.equal(read(),before);assert.deepEqual(errors,[]);console.log('SHARED INSPECTOR SECTIONS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_GRID_SPANS){
   const before=read(),states=[before],rects=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const p=el.parentElement.getBoundingClientRect(),r=el.getBoundingClientRect();return [r.x-p.x,r.y-p.y,r.width,r.height];})),expect=async expected=>wait(async()=>JSON.stringify(await rects())===JSON.stringify(expected));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await expect([[100,0,100,100],[300,100,100,100]]);
   const edit=async(label,value,expected)=>{await page.getByLabel('Shared Grid '+label+' span',{exact:true}).selectOption(value);await settled();await wait(()=>read()!==states.at(-1));states.push(read());await expect(expected);};
   await edit('column','2',[[0,0,200,100],[0,100,200,100]]);await edit('row','2',[[0,0,200,200],[200,0,200,200]]);await edit('column','full',process.env.RT_E2E_SHARED_GRID_FRACTIONAL?[[0,0,400,0],[0,0,400,200]]:[[0,0,400,200],[0,200,400,200]]);
   await page.getByRole('button',{name:'Reset shared grid column span',exact:true}).click();await settled();await wait(()=>read()!==states.at(-1));states.push(read());await expect([[0,0,100,200],[100,0,100,200]]);
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([[0,0,80,40],[0,40,90,60]]);await wait(async()=>await page.getByLabel('Shared Grid column span',{exact:true}).count()===0);await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([[0,0,100,200],[100,0,100,200]]);
   if(process.env.RT_E2E_SHARED_GRID_SPANS_SCREENSHOT){await wait(async()=>await page.getByLabel('Shared Grid row span',{exact:true}).inputValue()==='2');await page.getByLabel('Shared Grid row span',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_GRID_SPANS_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await expect([[100,0,100,100],[300,100,100,100]]);assert.deepEqual(errors,[]);console.log('SHARED GRID SPANS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_ALIGNMENT){
   const before=read(),states=[before],grid=!!process.env.RT_E2E_SHARED_ALIGNMENT_GRID,rects=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const p=el.parentElement.getBoundingClientRect(),r=el.getBoundingClientRect();return [r.x-p.x,r.y-p.y,r.width,r.height];})),expect=async expected=>wait(async()=>JSON.stringify(await rects())===JSON.stringify(expected));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await expect(grid?[[0,0,80,40],[310,140,90,60]]:[[0,0,80,40],[80,140,90,60]]);
   const edit=async(label,value,expected)=>{await page.getByLabel('Shared '+label,{exact:true}).selectOption(value);await settled();await wait(()=>read()!==states.at(-1));states.push(read());await expect(expected);};
   await edit('Item alignment','center',grid?[[0,80,80,40],[310,70,90,60]]:[[0,80,80,40],[80,70,90,60]]);
   if(grid){await edit('Grid inline alignment','center',[[60,80,80,40],[255,70,90,60]]);await edit('Grid inline alignment','end',[[120,80,80,40],[310,70,90,60]]);}else assert.equal(await page.getByLabel('Shared Grid inline alignment',{exact:true}).count(),0);
   await edit('Item alignment','end',grid?[[120,160,80,40],[310,140,90,60]]:[[0,160,80,40],[80,140,90,60]]);
   const automatic=async axis=>{await page.getByLabel('Shared '+axis[0].toUpperCase()+axis.slice(1)+' sizing',{exact:true}).selectOption('auto');await settled();await wait(()=>read()!==states.at(-1));states.push(read());};await automatic('height');await edit('Item alignment','stretch',grid?[[120,0,80,200],[310,0,90,200]]:[[0,0,80,200],[80,0,90,200]]);
   if(grid){await automatic('width');await edit('Grid inline alignment','stretch',[[0,0,200,200],[200,0,200,200]]);}
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([[0,0,80,40],[0,40,90,60]]);await wait(async()=>await page.getByLabel('Shared Item alignment',{exact:true}).count()===0);await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>await page.getByLabel('Shared Item alignment',{exact:true}).isEnabled());
   if(process.env.RT_E2E_SHARED_ALIGNMENT_SCREENSHOT){await page.getByLabel('Shared Item alignment',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_ALIGNMENT_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await expect(grid?[[0,0,80,40],[310,140,90,60]]:[[0,0,80,40],[80,140,90,60]]);assert.deepEqual(errors,[]);console.log('SHARED ITEM ALIGNMENT PASS',kind,engine,grid?'grid':'flex');return;
  }
  if(process.env.RT_E2E_HTML_CONTENTS){
   const before=read(),sizes=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.getBoundingClientRect().width)),expect=async values=>wait(async()=>JSON.stringify(await sizes())===JSON.stringify(values));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('min-[768px]:');await settled();await expect([75,75]);await page.getByRole('button',{name:'Fill available space',exact:true}).click();await settled();await wait(()=>read()!==before);const fill=read();await expect([50,100]);await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await settled();await expect([300,100]);await page.getByRole('button',{name:'Hug contents',exact:true}).click();await settled();await wait(()=>read()!==fill);assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).flexGrow),'0');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===fill);await expect([300,100]);await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await expect([75,75]);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);assert.deepEqual(errors,[]);console.log('HTML CONTENTS FLEX PASS',engine);return;
  }
  if(process.env.RT_E2E_SINGLE_CONTENTS){
   assert.ok(process.env.RT_E2E_SHARED_FLEX_CONTENTS);const before=read(),grid=!!process.env.RT_E2E_SHARED_FILL_GRID,sizes=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.getBoundingClientRect().width)),expect=async values=>wait(async()=>JSON.stringify(await sizes())===JSON.stringify(values)),undo=async source=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);};
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();await expect(grid?[100,100]:[75,75]);
   await page.getByLabel('Width behavior',{exact:true}).selectOption('fill');await settled();await wait(()=>read()!==before);const fill=read();await expect(grid?[75,100]:[50,100]);
   if(grid){assert.equal(await page.getByLabel('Span columns',{exact:true}).isVisible(),true);await page.getByLabel('Show grid guides',{exact:true}).check();await wait(async()=>await page.locator('.grid-guide.column').count()===3);assert.deepEqual(await page.evaluate(()=>{const r=document.getElementById('app').contentDocument.querySelector('main').getBoundingClientRect();return [...document.querySelectorAll('.grid-guide.column')].map(el=>parseFloat(el.style.left)-r.left);}),[0,75,150]);await page.getByLabel('Height behavior',{exact:true}).selectOption('fill');await settled();assert.equal(await app.locator('h1').evaluate(el=>el.getBoundingClientRect().height),180);await undo(fill);}
   await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await settled();await expect(grid?[200,100]:[300,100]);await page.getByLabel('Width behavior',{exact:true}).selectOption('fixed');await settled();await wait(()=>read()!==fill);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await expect(grid?[200,100]:[300,0]);await undo(fill);await expect(grid?[75,100]:[50,100]);
   await page.getByLabel('Width (px)',{exact:true}).fill('90');await page.getByLabel('Width (px)',{exact:true}).press('Tab');await settled();await expect(grid?[90,100]:[90,60]);await undo(fill);await undo(before);await expect(grid?[100,100]:[75,75]);assert.deepEqual(errors,[]);console.log('SINGLE CONTENTS SIZING PASS',kind,engine,grid?'grid':'flex');return;
  }
  if(process.env.RT_E2E_SHARED_COLUMNS){
   const before=read(),vertical=!!process.env.RT_E2E_SHARED_COLUMNS_VERTICAL,label=vertical?'Vertical':'Horizontal',other=vertical?'Horizontal':'Vertical';await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();assert.equal(await page.getByLabel('Shared '+other+' gap',{exact:true}).count(),0);assert.equal(await page.getByLabel('Shared Wrap children',{exact:true}).count(),0);const input=page.getByLabel('Shared '+label+' gap',{exact:true});await input.fill('12');await input.press('Enter');await settled();await wait(()=>read()!==before);const gaps=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).columnGap));assert.deepEqual(await gaps(),['12px','12px']);await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.deepEqual(await gaps(),['8px','8px']);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);assert.deepEqual(errors,[]);console.log('SHARED COLUMNS GAP PASS',kind,engine,label);return;
  }
  if(process.env.RT_E2E_SINGLE_ROTATION){
   const before=read(),rotation=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).rotate),input=page.getByLabel('Rotation (°)',{exact:true}),scope=kind==='html'?'min-[768px]:':'md:';
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption(scope);await settled();assert.equal(await input.inputValue(),'-10');
   if(process.env.RT_E2E_CANVAS_ROTATION){
    if(process.env.RT_E2E_ROTATION_CORNERS){
     const corners=page.locator('.canvas-rotation-corner:visible');await wait(async()=>await corners.count()>0);const corner=corners.first(),r=await corner.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.getByLabel('Rotate layer on canvas',{exact:true}).waitFor();await page.evaluate(()=>{const field=document.querySelector('input[aria-label="Rotation (°)"]');renderPanel();if(!field.isConnected)throw Error('Inspector refresh replaced the active rotation draft');});await page.mouse.move(r.x+r.width/2+30,r.y+r.height/2+30,{steps:5});await wait(async()=>await rotation()!=='-10deg');assert.equal(read(),before);await page.screenshot({path:'/private/tmp/retouch-rotation-corners-'+kind+'.png'});await page.mouse.up();await settled();await wait(()=>read()!==before);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);
     await wait(async()=>await corners.count()>0);const c=await corners.last().boundingBox();await page.mouse.move(c.x+c.width/2,c.y+c.height/2);await page.mouse.down();await page.mouse.move(c.x+c.width/2+20,c.y+c.height/2-20,{steps:4});await page.keyboard.press('Escape');await page.mouse.up();await settled();assert.equal(read(),before);await wait(async()=>await rotation()==='-10deg');
     await page.getByRole('button',{name:'Hand',exact:true}).click();await wait(async()=>await corners.count()===0);await page.keyboard.press('Escape');
    }
    const start=async()=>{await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));await page.getByRole('button',{name:'Rotate on canvas',exact:true}).click();await page.getByLabel('Rotate layer on canvas',{exact:true}).waitFor();};
    await start();await page.keyboard.press('ArrowRight');await wait(async()=>await rotation()==='-9deg');assert.equal(read(),before);await page.keyboard.press('Escape');await wait(async()=>await rotation()==='-10deg');assert.equal(read(),before);
    await start();await page.keyboard.press('Shift+ArrowRight');await wait(async()=>await rotation()==='5deg');await page.keyboard.press('Enter');await settled();await wait(()=>read()!==before);assert.equal(await app.locator('h1').evaluate(el=>el.style.rotate),'');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);
    await start();const h=await page.getByRole('button',{name:'Drag to rotate layer',exact:true}).boundingBox();await page.mouse.move(h.x+h.width/2,h.y+h.height/2);await page.mouse.down();await page.mouse.move(h.x+h.width/2+15,h.y+h.height/2+30,{steps:5});assert.equal(read(),before);await page.screenshot({path:'/private/tmp/retouch-canvas-rotation-'+kind+'.png'});await page.mouse.up();await settled();await wait(()=>read()!==before);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);
    await start();const handle=page.getByRole('button',{name:'Drag to rotate layer',exact:true}),box=await handle.boundingBox(),pivot=await page.evaluate(()=>{const el=document.querySelector('#app').contentDocument.querySelector('h1'),g=RetouchInspector.geometry(el,{allowRotation:true,layoutOnly:true}),f=document.querySelector('#app').getBoundingClientRect(),scale=f.width/document.querySelector('#app').contentWindow.innerWidth,o=g.transformOrigin.split(' ').map(parseFloat);return {x:f.x+(g.layoutLeft+o[0])*scale,y:f.y+(g.layoutTop+o[1])*scale};});
    const hx=box.x+box.width/2,hy=box.y+box.height/2,dx=hx-pivot.x,dy=hy-pivot.y,rad=Math.PI/4;await page.mouse.move(hx,hy);await page.mouse.down();await page.keyboard.down('Shift');await page.mouse.move(pivot.x+dx*Math.cos(rad)-dy*Math.sin(rad),pivot.y+dx*Math.sin(rad)+dy*Math.cos(rad),{steps:5});await wait(async()=>await rotation()==='30deg');assert.equal(read(),before);await page.keyboard.press('Escape');await page.mouse.up();await page.keyboard.up('Shift');await wait(async()=>await rotation()==='-10deg');
    await start();await page.keyboard.press('ArrowRight');await app.locator('h1').evaluate(el=>el.style.width='123px');await page.getByLabel('Rotate layer on canvas',{exact:true}).waitFor({state:'detached'});assert.equal(read(),before);assert.equal(await app.locator('h1').evaluate(el=>el.style.rotate),'');await app.locator('h1').evaluate(el=>el.style.removeProperty('width'));await page.evaluate(()=>renderPanel());await settled();
    for(const zoom of [50,125,100]){await page.getByLabel('Canvas zoom (%)',{exact:true}).fill(String(zoom));await page.getByLabel('Canvas zoom (%)',{exact:true}).press('Enter');await page.locator('#frameWrap').evaluate(el=>el.scrollTo(0,0));await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));await start();await page.keyboard.press('ArrowRight');await wait(async()=>await rotation()==='-9deg');await page.keyboard.press('Escape');await wait(async()=>await rotation()==='-10deg');assert.equal(read(),before);}
    await start();await page.keyboard.press('ArrowRight');await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await page.getByLabel('Rotate layer on canvas',{exact:true}).waitFor({state:'detached'});assert.equal(read(),before);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>await rotation()==='-10deg');
   }
   await input.fill('30');await input.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>await rotation()==='30deg');const edited=read();
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await rotation()==='-10deg');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>await rotation()==='30deg');
   await page.getByRole('button',{name:kind==='html'?'Reset rotate':'Reset rotation',exact:true}).click();await settled();await wait(()=>read()!==edited);await wait(async()=>await rotation()==='-10deg');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===edited);await wait(async()=>await rotation()==='30deg');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);
   const drag=async cancel=>{const label=input.locator('..').locator('span').first();await label.scrollIntoViewIfNeeded();const r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+20,r.y+r.height/2,{steps:3});await wait(async()=>await rotation()==='10deg');assert.equal(read(),before);if(cancel)await page.keyboard.press('Escape');await page.mouse.up();await settled();};await drag(true);assert.equal(read(),before);await wait(async()=>await rotation()==='-10deg');await drag(false);await wait(()=>read()!==before);await wait(async()=>await rotation()==='10deg');assert.equal(await app.locator('h1').evaluate(el=>el.style.rotate),'');
   await page.screenshot({path:'/private/tmp/retouch-single-rotation-'+kind+'.png'});await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);assert.deepEqual(errors,[]);console.log('SINGLE ROTATION PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_ROTATED_POSITION){
   const before=read(),scope=kind==='html'?'min-[768px]:':'md:',bounds=()=>app.locator('h1').evaluate(el=>{const r=el.getBoundingClientRect(),css=getComputedStyle(el);return {x:r.x,y:r.y,width:r.width,height:r.height,rotate:css.rotate,cssWidth:css.width,cssHeight:css.height,right:css.right};}),close=(a,b)=>assert.ok(Math.abs(a-b)<.6,`${a} differs from ${b}`);
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption(scope);await settled();const initial=await bounds(),parentWidth=await app.locator('main').evaluate(el=>el.clientWidth),x=page.getByLabel('X',{exact:true}),y=page.getByLabel('Y',{exact:true});close(Number(await x.inputValue()),parentWidth-124-25);close(Number(await y.inputValue()),30);
   if(process.env.RT_E2E_ROTATED_OUTLINE){
    const aligned=()=>page.evaluate(()=>{const frame=document.querySelector('#app'),el=frame.contentDocument.querySelector('h1'),outline=document.querySelector('#overlayLayer .box.sel');if(!outline)return false;const r=el.getBoundingClientRect(),b=outline.getBoundingClientRect(),f=frame.getBoundingClientRect(),scale=f.width/frame.contentWindow.innerWidth;return outline.style.rotate==='30deg'&&outline.style.width==='124px'&&outline.style.height==='74px'&&outline.style.transformOrigin==='20px 10px'&&Math.abs(b.x-(f.x+r.x*scale))<.6&&Math.abs(b.y-(f.y+r.y*scale))<.6&&Math.abs(b.width-r.width*scale)<.6&&Math.abs(b.height-r.height*scale)<.6;});
    await wait(aligned);for(const percent of [50,125,100]){await page.getByLabel('Canvas zoom (%)',{exact:true}).fill(String(percent));await page.getByLabel('Canvas zoom (%)',{exact:true}).press('Enter');await wait(aligned);}assert.equal(read(),before);
    const mutations=await page.evaluate(async()=>{const el=document.querySelector('#app').contentDocument.querySelector('h1'),style=el.getAttribute('style');el.style.position='static';let changes=0;const observer=new MutationObserver(records=>changes+=records.length);observer.observe(el,{attributes:true,attributeFilter:['style']});for(let i=0;i<5;i++)await new Promise(resolve=>requestAnimationFrame(resolve));observer.disconnect();if(style===null)el.removeAttribute('style');else el.setAttribute('style',style);return changes;});assert.equal(mutations,0);await page.evaluate(()=>renderPanel());await settled();await wait(aligned);await page.locator('#frameWrap').evaluate(el=>{el.scrollTop=0;});await page.screenshot({path:'/private/tmp/retouch-rotated-outline-'+kind+'.png'});
   }
   const undo=async()=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(async()=>Math.abs((await bounds()).x-initial.x)<.6);};
   await x.fill('40');await x.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await bounds()).x-initial.x-(40-(parentWidth-124-25)))<.6);const moved=await bounds();close(moved.y,initial.y);close(moved.width,initial.width);close(moved.height,initial.height);assert.equal(moved.rotate,'30deg');assert.equal(moved.cssWidth,'100px');assert.equal(moved.cssHeight,'50px');close(parseFloat(moved.right),parentWidth-40-124);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).position)==='static');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>Math.abs((await bounds()).x-moved.x)<.6);await undo();
   await y.fill('55');await y.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await bounds()).y-initial.y-25)<.6);close((await bounds()).x,initial.x);await undo();
   for(const [label,axis,size,fraction]of [['Align left','x','width',0],['Align horizontal centers','x','width',.5],['Align right','x','width',1],['Align top','y','height',0],['Align vertical centers','y','height',.5],['Align bottom','y','height',1]]){const frame=await app.locator('main').evaluate(el=>{const r=el.getBoundingClientRect();return {x:r.x+el.clientLeft,y:r.y+el.clientTop,width:el.clientWidth,height:el.clientHeight};});await page.getByRole('button',{name:label,exact:true}).click();await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await bounds())[axis]-(frame[axis]+(frame[size]-initial[size])*fraction))<.6);const current=await bounds();close(current.width,initial.width);close(current.height,initial.height);assert.equal(current.rotate,'30deg');await undo();}
   const drag=async cancel=>{const label=x.locator('..').locator('span').first();await label.scrollIntoViewIfNeeded();const r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+10,r.y+r.height/2,{steps:3});await wait(async()=>Math.abs((await bounds()).x-initial.x-10)<.6);assert.equal(read(),before);assert.equal((await bounds()).rotate,'30deg');if(cancel)await page.keyboard.press('Escape');await page.mouse.up();await settled();};await drag(true);assert.equal(read(),before);await wait(async()=>Math.abs((await bounds()).x-initial.x)<.6);await drag(false);await wait(()=>read()!==before);await wait(async()=>Math.abs((await bounds()).x-initial.x-10)<.6);assert.equal(await app.locator('h1').evaluate(el=>el.style.translate),'');await undo();
   if(process.env.RT_E2E_ROTATED_RESIZE){
    const layout=()=>page.evaluate(()=>{const el=document.querySelector('#app').contentDocument.querySelector('h1'),g=RetouchInspector.geometry(el,{allowRotation:true,layoutOnly:true}),o=g.transformOrigin.split(' ').map(parseFloat),a=g.rotation*Math.PI/180;return {...g,leftMid:{x:g.layoutLeft+o[0]-o[0]*Math.cos(a)-(g.height/2-o[1])*Math.sin(a),y:g.layoutTop+o[1]-o[0]*Math.sin(a)+(g.height/2-o[1])*Math.cos(a)},center:{x:g.layoutLeft+o[0]+(g.width/2-o[0])*Math.cos(a)-(g.height/2-o[1])*Math.sin(a),y:g.layoutTop+o[1]+(g.width/2-o[0])*Math.sin(a)+(g.height/2-o[1])*Math.cos(a)}};}),original=await layout(),open=async()=>{await page.getByRole('button',{name:'Resize on canvas',exact:true}).click();await page.locator('.canvas-move-surface').waitFor();};
    await open();const h=await page.getByRole('button',{name:'Resize right',exact:true}).boundingBox();await page.mouse.move(h.x+h.width/2,h.y+h.height/2);await page.mouse.down();await page.mouse.move(h.x+h.width/2+20*Math.cos(Math.PI/6),h.y+h.height/2+10,{steps:5});assert.equal(read(),before);close((await layout()).width,124);const preview=await page.locator('.canvas-move-preview').evaluate(el=>({width:parseFloat(el.style.width),rotate:el.style.rotate}));close(preview.width,144);assert.equal(preview.rotate,'30deg');await page.screenshot({path:'/private/tmp/retouch-rotated-resize-'+kind+'.png'});await page.mouse.up();await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await layout()).width-144)<.6);const resized=await layout();close(resized.height,74);close(resized.leftMid.x,original.leftMid.x);close(resized.leftMid.y,original.leftMid.y);assert.ok(Math.abs(parseFloat((await bounds()).cssWidth)-(preview.width-24))<.04);assert.equal(resized.rotation,30);await undo();
    await open();await page.getByRole('button',{name:'Resize right',exact:true}).focus();await page.keyboard.press('ArrowRight');assert.equal(read(),before);await page.keyboard.press('Escape');await settled();assert.equal(read(),before);close((await layout()).width,124);
    await open();await page.getByRole('button',{name:'Resize right',exact:true}).focus();await page.keyboard.press('ArrowRight');await page.keyboard.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await layout()).width-125)<.6);close((await layout()).leftMid.x,original.leftMid.x);close((await layout()).leftMid.y,original.leftMid.y);await undo();
    await open();await page.getByRole('button',{name:'Resize bottom right',exact:true}).focus();await page.keyboard.down('Alt');await page.keyboard.down('Shift');await page.keyboard.press('ArrowRight');await page.keyboard.press('Enter');await page.keyboard.up('Shift');await page.keyboard.up('Alt');await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await layout()).width-126)<.6);const proportional=await layout();close(proportional.height,74*126/124);close(proportional.center.x,original.center.x);close(proportional.center.y,original.center.y);await undo();
    await open();await page.getByRole('button',{name:'Resize right',exact:true}).focus();await page.keyboard.press('ArrowRight');await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await page.locator('.canvas-move-surface').waitFor({state:'detached'});assert.equal(read(),before);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>Math.abs((await layout()).width-124)<.6);
   }
   if(process.env.RT_E2E_ROTATED_CANVAS){
    const move=()=>page.getByRole('button',{name:'Move on canvas',exact:true}).click();
    if(process.env.RT_E2E_ROTATED_MOVE_PREVIEW){for(const percent of [50,125,100]){await page.getByLabel('Canvas zoom (%)',{exact:true}).fill(String(percent));await page.getByLabel('Canvas zoom (%)',{exact:true}).press('Enter');await settled();await page.locator('#frameWrap').evaluate(el=>el.scrollTo(0,0));await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));await move();await wait(async()=>await page.evaluate(()=>{const frame=document.querySelector('#app'),target=frame.contentDocument.querySelector('h1'),ghost=document.querySelector('.canvas-group-layer');if(!ghost){window.__rtMovePreview={missing:true};return false;}const r=target.getBoundingClientRect(),b=ghost.getBoundingClientRect(),f=frame.getBoundingClientRect(),scale=f.width/frame.contentWindow.innerWidth;window.__rtMovePreview={ghost:[b.x,b.y,b.width,b.height],expected:[f.x+r.x*scale,f.y+r.y*scale,r.width*scale,r.height*scale],style:ghost.style.cssText,scale};return ghost.style.rotate==='30deg'&&Math.abs(b.x-f.x-r.x*scale)<.6&&Math.abs(b.y-f.y-r.y*scale)<.6&&Math.abs(b.width-r.width*scale)<.6&&Math.abs(b.height-r.height*scale)<.6;})).catch(async error=>{console.log('MOVE PREVIEW',JSON.stringify(await page.evaluate(()=>window.__rtMovePreview)));throw error;});if(percent===100)await page.screenshot({path:'/private/tmp/retouch-rotated-move-preview-'+kind+'.png'});await page.keyboard.press('Escape');await page.locator('.canvas-move-surface').waitFor({state:'detached'});assert.equal(read(),before);}}
    await move();await page.keyboard.press('ArrowRight');await page.keyboard.press('Shift+ArrowDown');assert.equal(read(),before);close((await bounds()).x,initial.x);close((await bounds()).y,initial.y);await page.keyboard.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await bounds()).x-initial.x-1)<.6&&Math.abs((await bounds()).y-initial.y-10)<.6);assert.equal((await bounds()).rotate,'30deg');assert.equal((await bounds()).cssWidth,'100px');await undo();
    await move();await page.keyboard.press('ArrowRight');await page.keyboard.press('Escape');await page.locator('.canvas-move-surface').waitFor({state:'detached'});assert.equal(read(),before);close((await bounds()).x,initial.x);
    await move();const preview=page.getByLabel('Drag selected layer',{exact:true}),r=await preview.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.keyboard.down('Alt');await page.mouse.move(r.x+r.width/2+20,r.y+r.height/2+15,{steps:3});assert.equal(read(),before);close((await bounds()).x,initial.x);await page.mouse.up();await page.keyboard.up('Alt');await settled();await wait(()=>read()!==before);await wait(async()=>Math.abs((await bounds()).x-initial.x-20)<.6&&Math.abs((await bounds()).y-initial.y-15)<.6);close((await bounds()).width,initial.width);close((await bounds()).height,initial.height);await undo();
    await move();await page.keyboard.press('ArrowRight');await app.locator('h1').evaluate(el=>el.style.rotate='390deg');await page.keyboard.press('Enter');await settled();assert.equal(read(),before);await app.locator('h1').evaluate(el=>el.style.removeProperty('rotate'));await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).rotate)==='30deg');
   }
   await page.screenshot({path:'/private/tmp/retouch-rotated-position-'+kind+'.png'});assert.deepEqual(errors,[]);console.log('ROTATED POSITION PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SINGLE_ALIGN){
   if(process.env.RT_E2E_ZERO_ROTATION){
    const probe=await page.evaluate(()=>{const d=document.querySelector('#app').contentDocument,body=d.body,original=body.getAttribute('style'),el=d.createElement('div');try{body.style.cssText+=';rotate:0deg;width:400px;height:600px;margin:20px;padding:10px;border:5px solid;position:static';el.style.cssText='position:absolute;left:20px;top:40px;width:30px;height:50px';body.append(el);body.style.rotate='none';const cancel=RetouchCanvasMove.mount({target:el,frame:document.querySelector('#app'),canvas:canvasSurface,mode:'resize',onCommit(){},onEnd(){},onError(message){throw Error(message);}});if(!cancel)throw Error('Viewport resize did not mount');cancel();body.style.rotate='0deg';const g=RetouchInspector.geometry(el);return {x:g.x,y:g.y,width:g.width,height:g.height,parentWidth:g.parentWidth,expectedWidth:body.clientWidth,parent:g.parentLabel};}finally{el.remove();if(original===null)body.removeAttribute('style');else body.setAttribute('style',original);}});
    assert.equal(probe.x,20);assert.equal(probe.y,40);assert.equal(probe.width,30);assert.equal(probe.height,50);assert.equal(probe.parentWidth,probe.expectedWidth);assert.equal(probe.parent,'<body>');
    await app.locator('h1').evaluate(el=>el.style.rotate='0deg');await page.evaluate(()=>renderPanel());await settled();
   }
   const before=read(),bounds=()=>app.locator('h1').evaluate(el=>{const r=el.getBoundingClientRect();return [r.left,r.top,r.width,r.height];}),expect=async wanted=>{try{await wait(async()=>{const actual=await bounds();return process.env.RT_E2E_SHARED_ABSOLUTE_ROTATED?actual.every((r,i)=>r.every((value,j)=>Math.abs(value-wanted[i][j])<.04)):JSON.stringify(actual)===JSON.stringify(wanted);});}catch(error){throw Error('Single alignment expected '+JSON.stringify(wanted)+', received '+JSON.stringify(await bounds()),{cause:error});}},undo=async()=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);};
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await settled();const toolbar=page.getByRole('toolbar',{name:'Align layer to frame',exact:true});try{await toolbar.waitFor({timeout:10000});}catch(error){console.log('SINGLE ALIGN PANEL',await page.locator('#panelBody').innerText(),await page.evaluate(()=>({revision:sel.info.renderRevisionAttribute,classes:sel.info.className,scope:styleScope,keys:Object.keys(sel.info)})));throw error;}assert.equal(await toolbar.getByRole('button').count(),6);const baseline=await bounds(),frame=await app.locator('main').evaluate(el=>{const r=el.getBoundingClientRect();return [r.left+el.clientLeft,r.top+el.clientTop,el.clientWidth,el.clientHeight];});assert.deepEqual(baseline.slice(2),[124,74]);if(kind!=='html')assert.equal(await page.locator('#panelBody [data-section=position] .control-grid input').evaluateAll(inputs=>inputs.length===4&&inputs.every(el=>el.getBoundingClientRect().width>=60)),true);
   if(process.env.RT_E2E_SINGLE_XY){
    const x=page.getByLabel('X',{exact:true}),y=page.getByLabel('Y',{exact:true});assert.equal(Number(await x.inputValue()),baseline[0]-frame[0]);assert.equal(Number(await y.inputValue()),baseline[1]-frame[1]);
    await x.fill('40');await x.press('Enter');await settled();await wait(()=>read()!==before);await expect([frame[0]+40,...baseline.slice(1)]);await undo();await expect(baseline);
    await y.fill('55');await y.press('Enter');await settled();await wait(()=>read()!==before);await expect([baseline[0],frame[1]+55,...baseline.slice(2)]);await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).position)==='static');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([baseline[0],frame[1]+55,...baseline.slice(2)]);await undo();await expect(baseline);
    await x.fill('900');await x.press('Escape');await settled();assert.equal(read(),before);await expect(baseline);
    const drag=async cancel=>{const label=x.locator('..').locator('span').first();await label.scrollIntoViewIfNeeded();const r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+10,r.y+r.height/2,{steps:3});assert.equal(read(),before);await expect([baseline[0]+10,...baseline.slice(1)]);if(cancel)await page.keyboard.press('Escape');await page.mouse.up();await settled();};
    await drag(true);assert.equal(read(),before);await expect(baseline);await drag(false);await wait(()=>read()!==before);await expect([baseline[0]+10,...baseline.slice(1)]);assert.equal(await app.locator('h1').evaluate(el=>el.style.getPropertyValue('translate')),'');await undo();await expect(baseline);
    await x.fill('100001');await x.press('Enter');await settled();assert.equal(read(),before);assert.equal(await x.evaluate(el=>el.checkValidity()),false);await x.press('Escape');
    const label=x.locator('..').locator('span').first(),r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+7,r.y+r.height/2);await expect([baseline[0]+7,...baseline.slice(1)]);await page.getByLabel('Style screen scope',{exact:true}).selectOption('');await page.mouse.up();await settled();assert.equal(read(),before);await expect(baseline);assert.equal(await app.locator('h1').evaluate(el=>el.style.getPropertyValue('translate')),'');await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await settled();
   }
   if(process.env.RT_E2E_SINGLE_CANVAS){
    const move=page.getByRole('button',{name:'Move on canvas',exact:true}),resize=page.getByRole('button',{name:'Resize on canvas',exact:true}),surface=page.locator('.canvas-move-surface');
    if(kind!=='html')assert.equal(await page.evaluate(()=>{const target=matchingEls(sel.info.id)[0],g=RetouchInspector.geometry(target);return writeClassLayerGeometry(sel.info,target,{...g,x:g.x+.01},g);}),false);assert.equal(read(),before);
    await move.click();await surface.waitFor();await page.keyboard.press('ArrowRight');await page.keyboard.press('Shift+ArrowDown');assert.equal(read(),before);await expect(baseline);await page.keyboard.press('Enter');await settled();await wait(()=>read()!==before);await expect([baseline[0]+1,baseline[1]+10,...baseline.slice(2)]);await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).position)==='static');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([baseline[0]+1,baseline[1]+10,...baseline.slice(2)]);await undo();await expect(baseline);
    await resize.click();await page.keyboard.press('Shift+ArrowRight');await page.keyboard.press('Escape');await surface.waitFor({state:'detached'});assert.equal(read(),before);await expect(baseline);
    await resize.click();const handle=await page.locator('[data-resize-handle=e]').boundingBox();await page.mouse.move(handle.x+5,handle.y+5);await page.mouse.down();await page.keyboard.down('Control');await page.mouse.move(handle.x+25,handle.y+5,{steps:4});assert.equal(read(),before);await expect(baseline);await page.mouse.up();await page.keyboard.up('Control');await settled();await wait(()=>read()!==before);await expect([baseline[0],baseline[1],baseline[2]+20,baseline[3]]);assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).boxSizing),'content-box');await undo();await expect(baseline);
    await move.click();await page.keyboard.press('ArrowRight');await app.locator('h1').evaluate(el=>el.style.left='5px');await page.keyboard.press('Enter');await settled();assert.equal(read(),before);await app.locator('h1').evaluate(el=>el.style.removeProperty('left'));await expect(baseline);
   }
   for(const [label,key,axis,size,fraction]of [['Align left','A',0,2,0],['Align horizontal centers','H',0,2,.5],['Align right','D',0,2,1],['Align top','W',1,3,0],['Align vertical centers','V',1,3,.5],['Align bottom','S',1,3,1]]){
    const expected=baseline.map((value,i)=>i===axis?frame[axis]+(frame[size]-baseline[size])*fraction:value);
    if(axis===0)await toolbar.getByRole('button',{name:label,exact:true}).click();else{await app.locator('h1').evaluate(el=>{el.tabIndex=-1;el.focus();});await page.keyboard.press('Alt+Key'+key);}await settled();await wait(()=>read()!==before);await expect(expected);assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).boxSizing),'content-box');
    if(label==='Align bottom'){if(process.env.RT_E2E_SINGLE_ALIGN_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SINGLE_ALIGN_SCREENSHOT});await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).position)==='static');await wait(async()=>await page.getByRole('toolbar',{name:'Align layer to frame',exact:true}).count()===0);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect(expected);}
    await undo();await expect(baseline);
   }
   assert.deepEqual(errors,[]);console.log('SINGLE FRAME ALIGNMENT PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_ROTATED_GEOMETRY){
   const before=read(),bounds=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect(),css=getComputedStyle(el);return {x:r.x,y:r.y,width:r.width,height:r.height,rotate:css.rotate};})),close=(a,b)=>assert.ok(Math.abs(a-b)<.6,`${a} differs from ${b}`);
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();const initial=await bounds();
   if(process.env.RT_E2E_ROTATED_OUTLINE)await wait(async()=>JSON.stringify(await page.locator('#overlayLayer .box.sel, #overlayLayer .box.co').evaluateAll(nodes=>nodes.map(el=>el.style.rotate).sort()))===JSON.stringify(['-20deg','30deg']));
   const undo=async()=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(async()=>(await bounds()).every((r,i)=>Math.abs(r.x-initial[i].x)<.6&&Math.abs(r.y-initial[i].y)<.6));};
   assert.equal(await page.getByRole('button',{name:'Resize selection on canvas',exact:true}).isDisabled(),true);
   for(const [label,axis,size,fraction]of [['Align left','x','width',0],['Align horizontal centers','x','width',.5],['Align right','x','width',1],['Align top','y','height',0],['Align vertical centers','y','height',.5],['Align bottom','y','height',1]]){const start=Math.min(...initial.map(r=>r[axis])),end=Math.max(...initial.map(r=>r[axis]+r[size]));await page.getByRole('button',{name:label,exact:true}).click();await settled();await wait(()=>read()!==before);await wait(async()=>(await bounds()).every(r=>Math.abs(r[axis]-(start+(end-start-r[size])*fraction))<.6));for(const [i,r]of (await bounds()).entries()){close(r.width,initial[i].width);close(r.height,initial[i].height);assert.equal(r.rotate,initial[i].rotate);}await undo();}
   for(const [label,axis,size,fraction]of [['Align left','x','width',0],['Align horizontal centers','x','width',.5],['Align right','x','width',1],['Align top','y','height',0],['Align vertical centers','y','height',.5],['Align bottom','y','height',1]]){const frame=await app.locator('main').evaluate(el=>{const r=el.getBoundingClientRect();return {x:r.x+el.clientLeft,y:r.y+el.clientTop,width:el.clientWidth,height:el.clientHeight};}),start=Math.min(...initial.map(r=>r[axis])),end=Math.max(...initial.map(r=>r[axis]+r[size])),delta=frame[axis]+(frame[size]-(end-start))*fraction-start;await page.getByRole('button',{name:label,exact:true}).click({modifiers:['Shift']});await settled();await wait(()=>read()!==before);await wait(async()=>(await bounds()).every((r,i)=>Math.abs(r[axis]-initial[i][axis]-delta)<.6));await undo();}
   const x=page.getByLabel('Shared X',{exact:true});close(Number(await x.inputValue()),20);await x.fill('35');await x.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>(await bounds()).every((r,i)=>Math.abs(r.x-initial[i].x-15)<.6));await undo();
   await page.getByRole('button',{name:'Move selection on canvas',exact:true}).click();if(process.env.RT_E2E_ROTATED_MOVE_PREVIEW)assert.deepEqual(await page.locator('.canvas-group-layer').evaluateAll(nodes=>nodes.map(el=>el.style.rotate).sort()),['-20deg','30deg']);await page.keyboard.press('ArrowRight');await page.keyboard.press('Shift+ArrowDown');assert.equal(read(),before);await page.keyboard.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>(await bounds()).every((r,i)=>Math.abs(r.x-initial[i].x-1)<.6&&Math.abs(r.y-initial[i].y-10)<.6));await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).position==='static'&&getComputedStyle(el).rotate==='none')));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>(await bounds()).every((r,i)=>Math.abs(r.x-initial[i].x-1)<.6&&Math.abs(r.y-initial[i].y-10)<.6));await undo();
   await page.getByLabel('Vertical gap (px)',{exact:true}).fill('20');await page.getByLabel('Vertical gap (px)',{exact:true}).press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>{const r=await bounds();return Math.abs(r[1].y-r[0].y-r[0].height-20)<.6;});await undo();
   await page.getByRole('button',{name:'Move selection on canvas',exact:true}).click();if(process.env.RT_E2E_ROTATED_MOVE_PREVIEW)assert.deepEqual(await page.locator('.canvas-group-layer').evaluateAll(nodes=>nodes.map(el=>el.style.rotate).sort()),['-20deg','30deg']);await page.keyboard.press('ArrowRight');await app.locator('h1').evaluate(el=>el.style.rotate='390deg');await page.keyboard.press('Enter');await settled();assert.equal(read(),before);await app.locator('h1').evaluate(el=>el.style.removeProperty('rotate'));
   await page.screenshot({path:'/private/tmp/retouch-shared-rotated-'+kind+'.png'});assert.deepEqual(errors,[]);console.log('SHARED ROTATED GEOMETRY PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_ABSOLUTE){
   const before=read(),bounds=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect();return [r.left,r.top,r.width,r.height];})),positions=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).position)),expect=async wanted=>{try{await wait(async()=>{const actual=await bounds();return process.env.RT_E2E_SHARED_ABSOLUTE_ROTATED?actual.every((r,i)=>r.every((value,j)=>Math.abs(value-wanted[i][j])<.04)):JSON.stringify(actual)===JSON.stringify(wanted);});}catch(error){throw Error('Absolute bounds expected '+JSON.stringify(wanted)+', received '+JSON.stringify(await bounds()),{cause:error});}},undo=async source=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);};
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();assert.equal(await page.evaluate(()=>sel.multiple.length),2);const originalBounds=await bounds(),originalStyles=await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el);return [css.rotate,css.width,css.height,css.transformOrigin];}));
   if(process.env.RT_E2E_SHARED_ABSOLUTE_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SHARED_ABSOLUTE_SCREENSHOT});
   await page.getByRole('button',{name:'Use absolute positioning',exact:true}).click();await settled();await wait(()=>read()!==before);await expect(originalBounds);assert.deepEqual(await positions(),['absolute','absolute']);const absolute=read();if(process.env.RT_E2E_SHARED_ABSOLUTE_ROTATED)assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el);return [css.rotate,css.width,css.height,css.transformOrigin];})),originalStyles);
   if(process.env.RT_E2E_SHARED_XY){
    const x=page.getByLabel('Shared X',{exact:true}),y=page.getByLabel('Shared Y',{exact:true}),originX=Math.min(...originalBounds.map(r=>r[0])),originY=Math.min(...originalBounds.map(r=>r[1])),shift=(axis,delta)=>originalBounds.map(r=>r.map((v,i)=>i===axis?v+delta:v));assert.equal(Number(await x.inputValue()),originX);assert.equal(Number(await y.inputValue()),originY);
    await x.fill(String(originX+12));await x.press('Enter');await settled();await wait(()=>read()!==absolute);await expect(shift(0,12));await undo(absolute);await expect(originalBounds);
    await y.fill(String(originY+9));await y.press('Enter');await settled();await wait(()=>read()!==absolute);await expect(shift(1,9));await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>JSON.stringify(await positions())===JSON.stringify(['static','static']));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect(shift(1,9));await undo(absolute);await expect(originalBounds);
    const drag=async cancel=>{const label=x.locator('..').locator('span').first();await label.scrollIntoViewIfNeeded();const r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+10,r.y+r.height/2,{steps:3});assert.equal(read(),absolute);await expect(shift(0,10));if(cancel)await page.keyboard.press('Escape');await page.mouse.up();await settled();};await drag(true);assert.equal(read(),absolute);await expect(originalBounds);await drag(false);await wait(()=>read()!==absolute);await expect(shift(0,10));assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.style.getPropertyValue('translate'))),['','']);await undo(absolute);await expect(originalBounds);
    const label=x.locator('..').locator('span').first(),r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+7,r.y+r.height/2);await expect(shift(0,7));await page.getByLabel('Style screen scope',{exact:true}).selectOption('');await page.mouse.up();await settled();assert.equal(read(),absolute);await expect(originalBounds);assert.equal(await page.getByLabel('Shared X',{exact:true}).isVisible(),true);assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.style.getPropertyValue('translate'))),['','']);await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await settled();
   }
   for(const [label,axis,size,fraction]of [['Align left',0,2,0],['Align horizontal centers',0,2,.5],['Align right',0,2,1],['Align top',1,3,0],['Align vertical centers',1,3,.5],['Align bottom',1,3,1]]){
    const frames=await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const p=el.offsetParent,r=p.getBoundingClientRect();return [r.left+p.clientLeft,r.top+p.clientTop,p.clientWidth,p.clientHeight];})),expected=originalBounds.map((r,i)=>r.map((value,key)=>key===axis?frames[i][axis]+(frames[i][size]-r[size])*fraction:value));
    await page.getByRole('button',{name:label,exact:true}).click({modifiers:['Shift']});await settled();await wait(()=>read()!==absolute);await expect(expected);assert.equal(await page.getByLabel('Align to',{exact:true}).inputValue(),'selection');if(label==='Align bottom'){await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>JSON.stringify(await positions())===JSON.stringify(['static','static']));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect(expected);}await undo(absolute);await expect(originalBounds);
   }
   if(process.env.RT_E2E_ALIGNMENT_KEYS){
    for(const [key,axis,size,fraction]of [['A',0,2,0],['H',0,2,.5],['D',0,2,1],['W',1,3,0],['V',1,3,.5],['S',1,3,1]]){
     const start=Math.min(...originalBounds.map(r=>r[axis])),end=Math.max(...originalBounds.map(r=>r[axis]+r[size])),expected=originalBounds.map(r=>r.map((v,i)=>i===axis?start+(end-start)*fraction-r[size]*fraction:v));
     await app.locator('h1').evaluate(el=>{el.tabIndex=-1;el.focus();});await page.keyboard.press('Alt+Key'+key);await settled();await wait(()=>read()!==absolute);await expect(expected);assert.equal(await page.evaluate(()=>document.activeElement.id),'app');await undo(absolute);
    }
    const field=page.getByLabel('Shared Width (px)',{exact:true});await field.focus();await field.dispatchEvent('keydown',{key:'å',code:'KeyA',altKey:true});await settled();assert.equal(read(),absolute);
    await page.locator('#canvasHand').click();await page.keyboard.press('Alt+KeyA');await settled();assert.equal(read(),absolute);await page.locator('#canvasHand').click();
    await page.locator('#quickActions').click();const search=page.getByRole('combobox',{name:'Search actions'});await search.fill('Distribute horizontal spacing');assert.equal(await page.getByRole('dialog',{name:'Actions',exact:true}).getByRole('option').getAttribute('aria-disabled'),'true');await search.fill('Align right');await search.dispatchEvent('keydown',{key:'å',code:'KeyA',altKey:true});await settled();assert.equal(read(),absolute);await search.press('Enter');await settled();await wait(()=>read()!==absolute);const edge=Math.max(...originalBounds.map(r=>r[0]+r[2]));await expect(originalBounds.map(r=>[edge-r[2],...r.slice(1)]));await undo(absolute);
   }
   const toolbar=page.getByRole('toolbar',{name:'Align selected layers',exact:true}),left=toolbar.getByRole('button',{name:'Align left',exact:true});assert.equal(await toolbar.locator('svg').count(),8);assert.equal(await toolbar.getByRole('button',{name:'Distribute horizontal spacing',exact:true}).isDisabled(),true);await left.focus();await left.press('End');assert.equal(await toolbar.getByRole('button',{name:'Align bottom',exact:true}).evaluate(el=>el===document.activeElement),true);await page.keyboard.press('ArrowRight');assert.equal(await left.evaluate(el=>el===document.activeElement),true);await page.keyboard.press('ArrowLeft');assert.equal(await toolbar.getByRole('button',{name:'Align bottom',exact:true}).evaluate(el=>el===document.activeElement),true);await page.keyboard.press('Home');assert.equal(read(),absolute);await expect(originalBounds);
   assert.equal(await toolbar.evaluate(el=>{const rects=[...el.children].map(el=>el.getBoundingClientRect());return rects.every(r=>r.top===rects[0].top)&&el.scrollWidth<=el.clientWidth;}),true);
   if(process.env.RT_E2E_SHARED_ABSOLUTE_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SHARED_ABSOLUTE_SCREENSHOT.replace('.png','-aligned-controls.png')});
   await left.press('Enter');await settled();await wait(()=>read()!==absolute);const aligned=originalBounds.map(r=>[Math.min(...originalBounds.map(r=>r[0])),...r.slice(1)]);await expect(aligned);await wait(async()=>await left.evaluate(el=>el===document.activeElement));assert.equal(await toolbar.locator('button[tabindex="0"]').count(),1);await undo(absolute);await expect(originalBounds);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>JSON.stringify(await positions())===JSON.stringify(['static','static']));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect(originalBounds);await undo(before);await expect(originalBounds);assert.deepEqual(await positions(),['static','static']);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===absolute);await expect(originalBounds);assert.deepEqual(await positions(),['absolute','absolute']);await undo(before);assert.deepEqual(errors,[]);console.log('SHARED ABSOLUTE POSITIONING PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_LAYOUT_CONTEXT){
   const before=read(),states=[before];await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();
   const controls=async kind=>{const names=['Wrap children','Align children','Distribute children','Horizontal gap','Vertical gap','Grid columns'],expected=names.map(name=>name==='Grid columns'?Number(kind==='grid'):kind==='flow'?0:name==='Wrap children'&&kind==='grid'?0:1);await wait(async()=>JSON.stringify(await Promise.all(names.map(name=>page.getByLabel('Shared '+name,{exact:true}).count())))===JSON.stringify(expected));},mode=async value=>{await page.getByLabel('Shared Arrange children',{exact:true}).selectOption(value);await settled();await wait(()=>read()!==states.at(-1));states.push(read());await controls(value==='row'?'flex':value);};
   await controls('flow');await mode('row');await page.getByLabel('Shared Wrap children',{exact:true}).selectOption('wrap');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await controls('flex');await mode('grid');await mode('flow');
   if(process.env.RT_E2E_SHARED_LAYOUT_CONTEXT_SCREENSHOT){await page.getByLabel('Shared Arrange children',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_LAYOUT_CONTEXT_SCREENSHOT});}
   for(const [source,kind]of [[states[3],'grid'],[states[2],'flex'],[states[1],'flex'],[before,'flow']]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);await controls(kind);}
   await mode('row');await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await controls('flow');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await controls('flex');await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);assert.deepEqual(errors,[]);console.log('SHARED LAYOUT CONTEXT PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_FILL_MIXED){
   const flow=!!process.env.RT_E2E_SHARED_FILL_FLOW,otherWidth=flow?210:260,otherHeight=flow?170:180,before=read(),sizes=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect();return [r.width,r.height];})),expect=async expected=>{try{await wait(async()=>JSON.stringify(await sizes())===JSON.stringify(expected));}catch(error){throw Error('Mixed Fill bounds: expected '+JSON.stringify(expected)+', received '+JSON.stringify(await sizes()),{cause:error});}},undo=async source=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===source);};
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();assert.equal(await page.evaluate(()=>sel.multiple.length),2);await expect([[100,100],[124,124]]);
   await page.getByLabel('Shared Width sizing',{exact:true}).selectOption('fill');await settled();await wait(()=>read()!==before);const widthFilled=read();console.log('MIXED FILL WIDTH',await sizes(),await app.locator('p.other-font').evaluate(el=>({classes:el.className,width:getComputedStyle(el).width,parent:getComputedStyle(el.parentElement).width})));await expect([[250,100],[otherWidth,124]]);
   await page.getByLabel('Shared Height sizing',{exact:true}).selectOption('fill');await settled();await wait(()=>read()!==widthFilled);const filled=read();await expect([[250,180],[otherWidth,otherHeight]]);if(!flow)assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>[getComputedStyle(el).alignSelf,getComputedStyle(el).justifySelf])),[['stretch','auto'],['stretch','stretch']]);
   await page.getByLabel('Shared Width sizing',{exact:true}).selectOption('fixed');await settled();await wait(()=>read()!==filled);const fixed=read();await expect([[250,180],[otherWidth,otherHeight]]);await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await expect([[250,180],[otherWidth,otherHeight]]);await undo(filled);await expect([[350,180],[otherWidth+100,otherHeight]]);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([[80,40],[104,64]]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([[250,180],[otherWidth,otherHeight]]);
   if(process.env.RT_E2E_SHARED_FILL_MIXED_SCREENSHOT){await page.getByLabel('Shared Width (px)',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_FILL_MIXED_SCREENSHOT});}
   await undo(widthFilled);await expect([[250,100],[otherWidth,124]]);await undo(before);await expect([[100,100],[124,124]]);assert.deepEqual(errors,[]);console.log('SHARED MIXED PARENT FILL PASS',kind,engine,flow?'normal flow':'vertical grid');return;
  }
  if(process.env.RT_E2E_SHARED_FLEX){
   const before=read(),states=[before],widths=()=>app.locator('h1,p.other-font').evaluateAll((nodes,column)=>nodes.map(el=>el.getBoundingClientRect()[column?'height':'width']),!!process.env.RT_E2E_SHARED_FLEX_COLUMN),expect=async expected=>wait(async()=>JSON.stringify(await widths())===JSON.stringify(expected));
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await expect(process.env.RT_E2E_SHARED_FILL_GRID?[100,100]:[75,75]);
   if(process.env.RT_E2E_SHARED_FILL){
    const grid=!!process.env.RT_E2E_SHARED_FILL_GRID,column=!!process.env.RT_E2E_SHARED_FLEX_COLUMN,axis=column?'Height':'Width',mode=()=>page.getByLabel('Shared '+axis+' sizing',{exact:true}),undo=async expected=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);};
    await mode().selectOption('fill');await settled();await wait(()=>read()!==before);const filled=read();await expect([75,75]);if(!grid)assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).flexGrow)),['1','1']);
    if(grid){await page.getByLabel('Shared Height sizing',{exact:true}).selectOption('fill');await settled();assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.getBoundingClientRect().height)),[180,180]);await undo(filled);}
    const dragInput=page.getByLabel('Shared '+axis+' (px)',{exact:true}),label=dragInput.locator('..').locator(':scope > span');await label.scrollIntoViewIfNeeded();const b=await label.boundingBox();await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();await page.mouse.move(b.x+b.width/2+10,b.y+b.height/2);await expect([85,85]);assert.equal(read(),filled);await page.keyboard.press('Escape');await page.mouse.up();await expect([75,75]);assert.equal(read(),filled);
    await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await expect([200,200]);await mode().selectOption('fixed');await settled();await wait(()=>read()!==filled);const fixed=read();await expect([200,200]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([200,200]);await undo(filled);await expect([75,75]);
    const input=page.getByLabel('Shared '+axis+' (px)',{exact:true});await input.fill('90');await input.press('Enter');await settled();await expect([90,90]);await undo(filled);await expect([75,75]);
    await mode().selectOption('hug');await settled();await wait(()=>read()!==filled);assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).flexGrow)),['0','0']);await undo(filled);
    await page.getByRole('button',{name:'Reset shared '+axis.toLowerCase()+' (px)',exact:true}).click();await settled();await wait(()=>read()!==filled);await expect(grid?[100,100]:[75,75]);await undo(filled);
    await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([100,100]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([75,75]);await undo(before);await expect(grid?[100,100]:[75,75]);assert.deepEqual(errors,[]);console.log('SHARED FILL SIZING PASS',kind,engine,axis);return;
   }
   const edit=async(label,value,expected)=>{await page.getByLabel('Shared '+label,{exact:true}).fill(String(value));await page.getByLabel('Shared '+label,{exact:true}).press('Tab');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await expect(expected);};
   await edit('Shrink',0,[100,100]);await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await expect([100,100]);await edit('Grow',1,[200,200]);assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).flexBasis)),['100px','100px']);
   const item=page.locator('[data-shared-section="item"]'),itemSummary=item.locator(':scope > summary');await itemSummary.click();await wait(async()=>!(await item.evaluate(el=>el.open)));
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([100,100]);await wait(async()=>await page.getByLabel('Shared Grow',{exact:true}).count()===0);assert.equal(await page.locator('[data-shared-section="item"]').count(),0);await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([100,100]);await wait(async()=>await page.getByLabel('Shared Shrink',{exact:true}).isEnabled());assert.equal(await item.evaluate(el=>el.open),false);await itemSummary.focus();await itemSummary.press('Enter');await edit('Shrink',1,[75,75]);
   const basis=page.getByLabel('Shared Flex basis',{exact:true});await basis.fill('-1px');await basis.press('Tab');assert.equal(await basis.evaluate(el=>el.checkValidity()),false);assert.equal(read(),states.at(-1));await edit('Flex basis','100%',[75,75]);assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).flexBasis)),['100%','100%']);await edit('Shrink',0,[150,150]);
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await expect([400,400]);await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([150,150]);await wait(async()=>await page.getByLabel('Shared Flex basis',{exact:true}).inputValue()==='100%');
   await page.getByRole('button',{name:'Automatic shared flex basis',exact:true}).click();await settled();await wait(()=>read()!==states.at(-1));states.push(read());await expect([100,100]);
   if(process.env.RT_E2E_SHARED_FLEX_SCREENSHOT){await wait(async()=>await page.getByLabel('Shared Shrink',{exact:true}).inputValue()==='0');await page.getByLabel('Shared Shrink',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_FLEX_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await expect([75,75]);assert.deepEqual(errors,[]);console.log('SHARED FLEX PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_RATIO){
   const before=read(),states=[before],sizes=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect();return [r.width,r.height];})),expect=async expected=>wait(async()=>JSON.stringify(await sizes())===JSON.stringify(expected)),snapshot=async()=>{await settled();await wait(()=>read()!==states.at(-1));states.push(read());};
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await expect([[120,120],[164,124]]);
   let ratio=page.getByLabel('Shared Aspect ratio',{exact:true});await ratio.fill('1 / 0');await ratio.press('Tab');assert.equal(await ratio.evaluate(el=>el.checkValidity()),false);assert.equal(read(),before);await ratio.fill('2:1');await ratio.press('Tab');await snapshot();await expect([[120,60],[164,94]]);
   await page.getByLabel('Shared Width (px)',{exact:true}).fill('200');await page.getByLabel('Shared Width (px)',{exact:true}).press('Tab');await snapshot();await expect([[200,100],[200,112]]);
   await page.getByRole('button',{name:'Square shared aspect ratio',exact:true}).click();await snapshot();await expect([[200,200],[200,200]]);
   await page.getByLabel('Shared size limits',{exact:true}).locator(':scope > summary').click();
   await page.getByLabel('Shared Maximum height (px)',{exact:true}).fill('80');await page.getByLabel('Shared Maximum height (px)',{exact:true}).press('Tab');await snapshot();await expect([[200,80],[200,80]]);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([[80,80],[90,90]]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([[200,80],[200,80]]);
   await page.getByRole('button',{name:'Reset shared aspect ratio',exact:true}).click();await snapshot();await wait(async()=>(await sizes()).every(size=>size[0]===200&&size[1]<80));
   if(process.env.RT_E2E_SHARED_RATIO_SCREENSHOT){await wait(async()=>await page.getByLabel('Shared Aspect ratio',{exact:true}).inputValue()==='auto');await page.getByLabel('Shared Aspect ratio',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_RATIO_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await expect([[120,120],[164,124]]);assert.deepEqual(errors,[]);console.log('SHARED ASPECT RATIO PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_CONSTRAINTS){
   const before=read(),states=[before],sizes=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect();return [r.width,r.height];})),expect=async expected=>wait(async()=>JSON.stringify(await sizes())===JSON.stringify(expected));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await expect([[120,120],[164,124]]);
   const limits=page.getByLabel('Shared size limits',{exact:true}),summary=limits.locator(':scope > summary');assert.equal(await limits.evaluate(el=>el.open),false);assert.equal(await page.getByLabel('Shared Minimum width (px)',{exact:true}).isVisible(),false);await summary.focus();await summary.press('Enter');await wait(async()=>await limits.evaluate(el=>el.open));
   assert.equal(await page.getByLabel('Shared Maximum width (px)',{exact:true}).getAttribute('placeholder'),'No limit');assert.equal(await page.getByLabel('Shared Maximum width (px)',{exact:true}).isEnabled(),true);
   const edit=async(label,value,expected)=>{const input=page.getByLabel('Shared '+label+' (px)',{exact:true});await input.fill(String(value));await input.press('Tab');await settled();await expect(expected);await wait(()=>read()!==states.at(-1));states.push(read());};
   const preset=async(name,expected)=>{await page.getByRole('button',{name,exact:true}).click();await settled();await expect(expected);await wait(()=>read()!==states.at(-1));states.push(read());};
   await edit('Minimum width',180,[[180,120],[180,124]]);await edit('Maximum width',220,[[180,120],[180,124]]);await edit('Width',300,[[220,120],[220,124]]);
   await preset('Automatic shared minimum width',[[220,120],[220,124]]);await preset('No shared maximum width',[[300,120],[300,124]]);
   await edit('Minimum height',80,[[300,120],[300,124]]);await edit('Maximum height',100,[[300,100],[300,100]]);await edit('Minimum height',110,[[300,110],[300,110]]);await edit('Minimum height',80,[[300,100],[300,100]]);await edit('Height',60,[[300,80],[300,80]]);
   await preset('No shared maximum height',[[300,80],[300,80]]);await preset('Automatic shared minimum height',[[300,60],[300,60]]);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([[80,80],[90,90]]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await expect([[300,60],[300,60]]);
   await wait(async()=>await page.getByLabel('Shared Width (px)',{exact:true}).inputValue()==='300'&&await page.getByLabel('Shared Height (px)',{exact:true}).inputValue()==='60');
   if(process.env.RT_E2E_SHARED_CONSTRAINTS_SCREENSHOT){await page.getByLabel('Shared Maximum height (px)',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_CONSTRAINTS_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}await expect([[120,120],[164,124]]);await summary.focus();await summary.press('Space');await wait(async()=>!(await limits.evaluate(el=>el.open)));const width=page.getByLabel('Shared Width (px)',{exact:true});await width.fill('200');await width.press('Enter');await settled();assert.equal(await limits.evaluate(el=>el.open),false);if(process.env.RT_E2E_SHARED_LIMITS_CLOSED_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SHARED_LIMITS_CLOSED_SCREENSHOT});await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);assert.deepEqual(errors,[]);console.log('SHARED CONSTRAINTS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_SIZING){
   const before=read(),states=[before],sizes=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const box=el.getBoundingClientRect();return {width:box.width+'px',height:box.height+'px'};})),expect=async expected=>wait(async()=>JSON.stringify(await sizes())===JSON.stringify(expected));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await expect([{width:'120px',height:'120px'},{width:'164px',height:'124px'}]);assert.equal(await page.getByLabel('Shared Width (px)',{exact:true}).getAttribute('placeholder'),'Mixed');
   await page.getByLabel('Shared Width (px)',{exact:true}).fill('200');await page.getByLabel('Shared Width (px)',{exact:true}).press('Tab');await settled();await expect([{width:'200px',height:'120px'},{width:'200px',height:'124px'}]);states.push(read());await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([{width:'80px',height:'80px'},{width:'90px',height:'90px'}]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');
   if(process.env.RT_E2E_SHARED_SIZE_SCRUB){
    await settled();const source=read(),width=page.getByLabel('Shared Width (px)',{exact:true}),label=width.locator('..').locator(':scope > span');await wait(async()=>await width.inputValue()==='200');await label.scrollIntoViewIfNeeded();
    const start=async()=>{const b=await label.boundingBox();assert.ok(b);await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();return {x:b.x+b.width/2,y:b.y+b.height/2};};
    let p=await start();await page.mouse.move(p.x+10,p.y);await expect([{width:'210px',height:'120px'},{width:'210px',height:'124px'}]);assert.equal(read(),source,'mixed box sizing previews without a source write');await page.keyboard.press('Escape');await page.mouse.up();await expect([{width:'200px',height:'120px'},{width:'200px',height:'124px'}]);assert.equal(read(),source);
    p=await start();await page.mouse.move(p.x-300,p.y);await expect([{width:'24px',height:'120px'},{width:'24px',height:'124px'}]);assert.equal(await width.inputValue(),'24','shared size clamps to largest padding and border floor');assert.equal(read(),source);await page.mouse.up();await settled();await expect([{width:'24px',height:'120px'},{width:'24px',height:'124px'}]);assert.notEqual(read(),source);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),source);await expect([{width:'200px',height:'120px'},{width:'200px',height:'124px'}]);
   }
   if(process.env.RT_E2E_SHARED_SIZE_SCRUB){
    for(const [name,property,initial,expected]of [['Opacity (%)','opacity','50','0.6'],['Font size (px)','font-size','20','30px']]){
     const beforeNumeric=read(),input=page.getByLabel('Shared '+name,{exact:true});await input.fill(initial);await input.press('Enter');await settled();await wait(()=>read()!==beforeNumeric);const saved=read(),label=input.locator('..').locator(':scope > span');await label.scrollIntoViewIfNeeded();const b=await label.boundingBox();await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();await page.mouse.move(b.x+b.width/2+10,b.y+b.height/2);await wait(async()=>await app.locator('h1,p.other-font').evaluateAll((nodes,[property,expected])=>nodes.every(el=>getComputedStyle(el).getPropertyValue(property)===expected),[property,expected]));assert.equal(read(),saved);await page.mouse.up();await settled();assert.notEqual(read(),saved);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),saved);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),beforeNumeric);
    }
   }
   await page.getByLabel('Shared Height (px)',{exact:true}).fill('60');await page.getByLabel('Shared Height (px)',{exact:true}).press('Tab');await settled();await expect([{width:'200px',height:'60px'},{width:'200px',height:'60px'}]);states.push(read());
   await page.getByRole('button',{name:'Reset shared width (px)',exact:true}).click();await settled();await expect([{width:'120px',height:'60px'},{width:'164px',height:'60px'}]);states.push(read());
   await page.getByLabel('Shared Height sizing',{exact:true}).selectOption('auto');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>(await sizes()).every(size=>parseFloat(size.height)<60));assert.deepEqual((await sizes()).map(size=>size.width),['120px','164px']);
   await page.getByLabel('Shared Width sizing',{exact:true}).selectOption('hug');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.every(el=>{const range=el.ownerDocument.createRange();range.selectNodeContents(el);const css=getComputedStyle(el),extra=['padding-left','padding-right','border-left-width','border-right-width'].reduce((sum,key)=>sum+(parseFloat(css.getPropertyValue(key))||0),0);return Math.abs(el.getBoundingClientRect().width-range.getBoundingClientRect().width-extra)<1;})));
   const hugSource=read(),hugSizes=await sizes();await page.getByLabel('Shared Width sizing',{exact:true}).selectOption('fixed');await settled();await wait(()=>read()!==hugSource);await expect(hugSizes);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===hugSource);await expect(hugSizes);
   const fitted=await sizes();await page.getByLabel('Shared Height sizing',{exact:true}).selectOption('hug');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await expect(fitted);
   await page.getByLabel('Shared Width sizing',{exact:true}).selectOption('auto');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>(await sizes()).every(size=>Math.abs(parseFloat(size.width)-768)<1));await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await expect([{width:'80px',height:'80px'},{width:'90px',height:'90px'}]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>(await sizes()).every(size=>Math.abs(parseFloat(size.width)-768)<1));
   if(process.env.RT_E2E_SHARED_SIZING_SCREENSHOT){await page.getByLabel('Shared Height (px)',{exact:true}).focus();await page.screenshot({path:process.env.RT_E2E_SHARED_SIZING_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}assert.equal(read(),before);await expect([{width:'120px',height:'120px'},{width:'164px',height:'124px'}]);assert.deepEqual(errors,[]);console.log('SHARED SIZING PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_ROTATION){
   const before=read(),rotations=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).rotate));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();
   const input=page.getByLabel('Shared Rotation (°)',{exact:true});assert.equal(await input.inputValue(),'0');assert.equal(await input.locator('xpath=ancestor::section[1]').getAttribute('data-section'),'position');
   await input.fill('30');await input.press('Enter');await settled();await wait(()=>read()!==before);await wait(async()=>JSON.stringify(await rotations())===JSON.stringify(['30deg','30deg']));
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>(await rotations()).every(value=>value==='none'));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>(await rotations()).every(value=>value==='30deg'));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await wait(async()=>(await rotations()).every(value=>value==='none'));
   const drag=async cancel=>{const label=input.locator('..').locator('span').first();await label.scrollIntoViewIfNeeded();const r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+10,r.y+r.height/2,{steps:3});await wait(async()=>(await rotations()).every(value=>value==='10deg'));assert.equal(read(),before);if(cancel)await page.keyboard.press('Escape');await page.mouse.up();await settled();};
   await drag(true);assert.equal(read(),before);await wait(async()=>(await rotations()).every(value=>value==='none'));await drag(false);await wait(()=>read()!==before);await wait(async()=>(await rotations()).every(value=>value==='10deg'));assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.style.rotate)),['','']);
   await page.screenshot({path:'/private/tmp/retouch-shared-rotation-'+kind+'.png'});
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);
   await input.fill('361');await input.press('Enter');await settled();assert.equal(read(),before);await input.press('Escape');
   const label=input.locator('..').locator('span').first();await label.scrollIntoViewIfNeeded();const r=await label.boundingBox();await page.mouse.move(r.x+r.width/2,r.y+r.height/2);await page.mouse.down();await page.mouse.move(r.x+r.width/2+7,r.y+r.height/2);await wait(async()=>(await rotations()).every(value=>value==='7deg'));await page.getByLabel('Style screen scope',{exact:true}).selectOption('');await page.mouse.up();await settled();assert.equal(read(),before);await wait(async()=>(await rotations()).every(value=>value==='none'));assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>el.style.rotate)),['','']);
   assert.deepEqual(errors,[]);console.log('SHARED ROTATION PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_LIQUID_SHARED_CLASSES){
   const before=read(),states=[before],values=property=>app.locator('h1,p.other-font').evaluateAll((nodes,property)=>nodes.map(el=>getComputedStyle(el).getPropertyValue(property)),property),rects=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect();return {x:r.x,y:r.y,width:r.width,height:r.height,right:r.right};}));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});
   for(const [label,input,property,expected]of [['Shared Opacity (%)','50','opacity','0.5'],['Shared Font size (px)','24','font-size','24px'],['Shared Text color with alpha','#123456ff','color','rgb(18, 52, 86)'],['Shared Layer blur (px)','2','filter','blur(2px)']]){
    await page.getByLabel(label,{exact:true}).fill(input);await page.getByLabel(label,{exact:true}).press('Tab');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>(await values(property)).every(value=>value===expected));await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>(await values(property)).some(value=>value!==expected));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>(await values(property)).every(value=>value===expected));
   }
   const old=await rects();assert.ok(Math.abs(old[0].right-old[1].right)>1);await page.getByRole('button',{name:'Align right',exact:true}).click();await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>{const current=await rects();return Math.abs(current[0].right-current[1].right)<.6;});
   const aligned=await rects();await page.getByRole('button',{name:'Move selection on canvas',exact:true}).click();await page.keyboard.press('ArrowRight');assert.equal(read(),states.at(-1));await page.keyboard.press('Escape');await page.locator('.canvas-move-surface').waitFor({state:'detached'});assert.deepEqual(await rects(),aligned);
   await page.getByRole('button',{name:'Move selection on canvas',exact:true}).click();await page.keyboard.press('Shift+ArrowDown');await page.keyboard.press('Enter');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>(await rects()).every((rect,i)=>Math.abs(rect.y-aligned[i].y-10)<.6&&Math.abs(rect.x-aligned[i].x)<.6));
   const moved=await rects(),bounds=rects=>({left:Math.min(...rects.map(r=>r.x)),right:Math.max(...rects.map(r=>r.right))});await page.getByRole('button',{name:'Resize selection on canvas',exact:true}).click();await page.keyboard.press('ArrowRight');assert.equal(read(),states.at(-1));await page.keyboard.press('Escape');await page.locator('.canvas-move-surface').waitFor({state:'detached'});assert.deepEqual(await rects(),moved);
   await page.getByRole('button',{name:'Resize selection on canvas',exact:true}).click();await page.keyboard.press('Shift+ArrowRight');await page.keyboard.press('Enter');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>{const b=bounds(await rects()),a=bounds(moved);return Math.abs(b.left-a.left)<.6&&Math.abs(b.right-a.right-1)<.6;});const resized=await rects(),scale=(bounds(moved).right-bounds(moved).left+1)/(bounds(moved).right-bounds(moved).left);resized.forEach((r,i)=>{assert.ok(Math.abs(r.width-moved[i].width*scale)<.6);assert.ok(Math.abs(r.height-moved[i].height*scale)<.6);});
   const beforeGap=await rects();await page.getByLabel('Vertical gap (px)',{exact:true}).fill('32');await page.getByLabel('Vertical gap (px)',{exact:true}).press('Tab');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>{const r=await rects();return Math.abs(r[1].y-r[0].y-r[0].height-32)<.6&&Math.abs(r[0].y-beforeGap[0].y)<.6;});
   await page.getByLabel('Canvas gap adjustment',{exact:true}).selectOption('individual');const gapBefore=await rects();await page.getByRole('button',{name:'Adjust vertical gaps on canvas',exact:true}).click();await page.keyboard.press('ArrowDown');assert.equal(read(),states.at(-1));await page.keyboard.press('Escape');await page.locator('.canvas-move-surface').waitFor({state:'detached'});assert.deepEqual(await rects(),gapBefore);
   await page.getByRole('button',{name:'Adjust vertical gaps on canvas',exact:true}).click();await page.keyboard.press('ArrowDown');await page.keyboard.press('Enter');await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>{const r=await rects();return Math.abs(r[1].y-r[0].y-r[0].height-33)<.6&&Math.abs(r[0].y-gapBefore[0].y)<.6;});
   if(process.env.RT_E2E_LIQUID_SHARED_SCREENSHOT){await page.getByRole('button',{name:'Align right',exact:true}).scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_LIQUID_SHARED_SCREENSHOT});}
   for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);await page.getByText('2 layers',{exact:true}).waitFor();}assert.equal(read(),before);assert.deepEqual((await rects()).map(({x,width})=>({x,width})),old.map(({x,width})=>({x,width})));assert.deepEqual(errors,[]);console.log('LIQUID SHARED CLASSES PASS',engine);return;
  }
  if(process.env.RT_E2E_LIQUID_SELECTION_STYLES){
   const before=read(),computed=property=>app.locator('h1,p.other-font').evaluateAll((els,property)=>els.map(el=>getComputedStyle(el).getPropertyValue(property)),property);
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});
   for(const [family,property,value]of [['text','font-size','28px'],['color','color','rgb(18, 52, 86)'],['effect','filter','blur(2px)']]){
    const open=async()=>{const summary=page.getByText('Saved '+family+' styles',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();};await open();await page.getByLabel('Saved '+family+' style',{exact:true}).selectOption('11111111-1111-4111-8111-111111111111');await page.getByRole('button',{name:'Apply '+family+' style',exact:true}).click();await settled();await wait(async()=>(await computed(property)).every(item=>item===value));const applied=read();
    await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>(await computed(property)).some(item=>item!==value));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>(await computed(property)).every(item=>item===value));
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();const override=family==='text'?['Font size (px)','40','40px']:family==='color'?['Text color with alpha','#00ff00ff','rgb(0, 255, 0)']:['Layer blur (px)','4','blur(4px)'];await page.getByLabel(override[0],{exact:true}).fill(override[1]);await page.getByLabel(override[0],{exact:true}).press('Tab');await settled();await wait(async()=>(await computed(property))[0]===override[2]);const overridden=read();
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await open();await page.getByRole('button',{name:family==='color'?'Reset selected color overrides':'Reset selected '+family+' style overrides',exact:true}).click();await settled();await wait(async()=>(await computed(property)).every(item=>item===value));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===overridden);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===applied);await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});
    await open();if(process.env.RT_E2E_LIQUID_STYLE_SELECTION_SCREENSHOT&&family==='effect')await page.screenshot({path:process.env.RT_E2E_LIQUID_STYLE_SELECTION_SCREENSHOT});await page.getByRole('button',{name:family==='color'?'Detach selected colors':'Detach selected '+family+' styles',exact:true}).click();await settled();assert.deepEqual(await computed(property),[value,value]);assert.ok(!read().includes('data-rt-'+family+'-styles'));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===applied);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>!read().includes('data-rt-'+family+'-styles'));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===applied);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===before);await page.getByText('2 layers',{exact:true}).waitFor();
   }
   assert.deepEqual(errors,[]);console.log('LIQUID SAVED STYLE SELECTION PASS',engine);return;
  }
  if(process.env.RT_E2E_COLLECTION_BINDINGS){
   const id=n=>'00000000-0000-4000-8000-'+String(n).padStart(12,'0'),before=read(),color=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).color),catalog=path.join(root,'.retouch/variables.json'),libraryBefore=fs.readFileSync(catalog,'utf8');const extraFile=path.join(root,'app/Extra.jsx'),extraBefore=kind==='react'?fs.readFileSync(extraFile,'utf8'):null;
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await page.getByText('Collection bindings',{exact:true}).click();const choose=page.getByLabel('Bound collection variable',{exact:true});await choose.selectOption(id(4));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>await color()==='rgb(18, 52, 86)');const light=read();assert.ok(light.includes('md:![color:#123456ff]'));
   await page.getByLabel('Binding mode for Theme',{exact:true}).selectOption(id(3));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>await color()==='rgb(204, 51, 0)');const dark=read();
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await color()!=='rgb(204, 51, 0)');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>await color()==='rgb(204, 51, 0)');
   await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await page.getByLabel('Style screen scope',{exact:true}).selectOption({label:'1440 px and larger'});await page.getByText(/^Inherited: Brand · md/).waitFor();assert.equal(await choose.inputValue(),id(4));assert.equal(await page.getByLabel('Binding mode for Theme',{exact:true}).inputValue(),id(3));
   if(process.env.RT_E2E_CLASS_INHERITANCE_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_CLASS_INHERITANCE_SCREENSHOT});
   await page.getByRole('button',{name:'Override collection binding here',exact:true}).click();await settled();const copied=read(),desktopScope=await page.getByLabel('Style screen scope',{exact:true}).inputValue();const meta=await app.locator('h1').evaluate(el=>JSON.parse(el.getAttribute('data-rt-variables')));assert.equal(meta[desktopScope].color.modes[id(1)],id(3));
   await page.getByLabel('Binding mode for Theme',{exact:true}).selectOption(id(2));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>await color()==='rgb(18, 52, 86)');const desktop=read();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>await color()==='rgb(204, 51, 0)');await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await page.getByRole('button',{name:'Use smaller-screen binding',exact:true}).click();await settled();await wait(async()=>await color()==='rgb(204, 51, 0)');
   for(const expected of [desktop,copied,dark]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
   await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');
   if(process.env.RT_E2E_REACT_BINDING_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_REACT_BINDING_SCREENSHOT});
   await page.getByRole('button',{name:'Variables',exact:true}).click();const dialog=page.getByRole('dialog',{name:'Variable collections',exact:true});await dialog.getByLabel('Collection variable',{exact:true}).selectOption(id(4));await dialog.getByLabel('Dark value',{exact:true}).fill('#9933ff');await dialog.getByRole('button',{name:'Save collection variable',exact:true}).click();await dialog.getByText('Saved in this project.',{exact:true}).waitFor();await dialog.getByRole('button',{name:'Close variable collections',exact:true}).click();await wait(async()=>await color()==='rgb(153, 51, 255)');if(kind==='react')await wait(async()=>await app.locator('h2').evaluate(el=>getComputedStyle(el).color)==='rgb(153, 51, 255)');const liveDocument=await app.locator('h1').evaluate(el=>{el.ownerDocument.__rtHistoryProbe='collection-undo';return el.ownerDocument.__rtHistoryProbe;});await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===dark);await settled();await wait(async()=>await color()==='rgb(204, 51, 0)');if(kind==='react')assert.equal(await app.locator('h1').evaluate(el=>el.ownerDocument.__rtHistoryProbe),liveDocument,'Collection Undo should preserve the live React document');assert.equal(fs.readFileSync(catalog,'utf8'),libraryBefore);
   if(kind==='react'){
    assert.equal(fs.readFileSync(extraFile,'utf8'),extraBefore);await wait(async()=>await app.locator('h2').evaluate(el=>getComputedStyle(el).color)==='rgb(204, 51, 0)');
    await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(async()=>await color()==='rgb(153, 51, 255)'&&await app.locator('h2').evaluate(el=>getComputedStyle(el).color)==='rgb(153, 51, 255)');assert.equal(await app.locator('h1').evaluate(el=>el.ownerDocument.__rtHistoryProbe),liveDocument);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===dark);await wait(async()=>await color()==='rgb(204, 51, 0)'&&await app.locator('h2').evaluate(el=>getComputedStyle(el).color)==='rgb(204, 51, 0)');assert.equal(fs.readFileSync(extraFile,'utf8'),extraBefore);assert.equal(fs.readFileSync(catalog,'utf8'),libraryBefore);assert.equal(await app.locator('h1').evaluate(el=>el.ownerDocument.__rtHistoryProbe),liveDocument);
   }
   await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('button',{name:'Detach collection binding',exact:true}).click();await settled();await wait(()=>!read().includes('data-rt-variables'));assert.equal(await color(),'rgb(204, 51, 0)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===dark);await settled();
   await page.getByLabel('Collection binding target',{exact:true}).selectOption('padding');await choose.selectOption(id(5));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).padding)==='24px');
   for(const expected of [dark,light,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
   if(kind==='react'){
   await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByLabel('Collection binding target',{exact:true}).selectOption('color');await choose.selectOption(id(4));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>await color()==='rgb(18, 52, 86)');const firstOnly=read();
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await choose.selectOption(id(4));await page.getByLabel('Binding mode for Theme',{exact:true}).selectOption(id(3));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();const colors=()=>app.locator('h1,p.other-font').evaluateAll(els=>els.map(el=>getComputedStyle(el).color));await wait(async()=>(await colors())[1]==='rgb(204, 51, 0)');const mixed=read();
   await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click({modifiers:['Shift']});await page.getByText('2 of 2 layers bound in this scope · Mixed bindings.',{exact:true}).waitFor();
   await choose.selectOption(id(4));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>(await colors()).every(value=>value==='rgb(18, 52, 86)'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===mixed);await settled();
   const overrideColor=page.getByLabel('Shared Text color with alpha',{exact:true});await overrideColor.fill('#00ff00ff');await overrideColor.press('Tab');await settled();await wait(async()=>(await colors()).every(value=>value==='rgb(0, 255, 0)'));const localOverrides=read();
   await page.getByRole('button',{name:'Reset collection binding',exact:true}).click();await settled();await wait(async()=>JSON.stringify(await colors())===JSON.stringify(['rgb(18, 52, 86)','rgb(204, 51, 0)']));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===localOverrides);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===mixed);await settled();
   if(process.env.RT_E2E_REACT_MULTI_BINDING_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_REACT_MULTI_BINDING_SCREENSHOT});
   await page.getByRole('button',{name:'Detach collection binding',exact:true}).click();await settled();await wait(()=>!read().includes('data-rt-variables'));assert.deepEqual(await colors(),['rgb(18, 52, 86)','rgb(204, 51, 0)']);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===mixed);await settled();
   for(const expected of [firstOnly,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
   }
   if(kind==='liquid'){
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByLabel('Collection binding target',{exact:true}).selectOption('color');await choose.selectOption(id(4));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();const firstOnly=read();
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await choose.selectOption(id(4));await page.getByLabel('Binding mode for Theme',{exact:true}).selectOption(id(3));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();const mixed=read(),colors=()=>app.locator('h1,p.other-font').evaluateAll(els=>els.map(el=>getComputedStyle(el).color));await wait(async()=>JSON.stringify(await colors())===JSON.stringify(['rgb(18, 52, 86)','rgb(204, 51, 0)']));
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click({modifiers:['Shift']});await page.getByText('2 of 2 layers bound in this scope · Mixed bindings.',{exact:true}).waitFor();
    assert.equal(await page.getByRole('button',{name:'Reset overrides at this size',exact:true}).count(),0);await app.locator('p.other-font').click();await app.locator('h1').click({modifiers:['Shift']});await page.getByText('2 of 2 layers bound in this scope · Mixed bindings.',{exact:true}).waitFor();
    await choose.selectOption(id(4));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();await wait(async()=>(await colors()).every(value=>value==='rgb(18, 52, 86)'));const uniform=read();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===mixed);await wait(async()=>JSON.stringify(await colors())===JSON.stringify(['rgb(18, 52, 86)','rgb(204, 51, 0)']));await page.getByText('2 of 2 layers bound in this scope · Mixed bindings.',{exact:true}).waitFor();
    await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();await wait(()=>read()===uniform);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===mixed);
    await page.getByRole('button',{name:'Reset collection binding',exact:true}).click();await settled();assert.equal(read(),mixed);assert.deepEqual(await colors(),['rgb(18, 52, 86)','rgb(204, 51, 0)']);
    if(process.env.RT_E2E_LIQUID_MULTI_BINDING_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_LIQUID_MULTI_BINDING_SCREENSHOT});
    await page.getByRole('button',{name:'Detach collection binding',exact:true}).click();await settled();await wait(()=>!read().includes('data-rt-variables'));assert.deepEqual(await colors(),['rgb(18, 52, 86)','rgb(204, 51, 0)']);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===mixed);
    for(const expected of [firstOnly,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}
   }
   await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await page.getByLabel('Collection binding target',{exact:true}).selectOption('width');await choose.selectOption(id(5));await page.getByRole('button',{name:'Apply collection binding',exact:true}).click();await settled();const widthBound=read(),widths=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).width));await wait(async()=>(await widths()).every(width=>width==='24px'));
   await page.getByLabel('Shared Width sizing',{exact:true}).selectOption('auto');await settled();const widthAuto=read();await wait(async()=>(await widths()).every(width=>width==='768px'));await page.getByRole('button',{name:'Reset collection binding',exact:true}).click();await settled();await wait(async()=>(await widths()).every(width=>width==='24px'));for(const expected of [widthAuto,widthBound,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);}
   assert.equal(await page.getByText('History is available for this session only.',{exact:true}).isVisible(),false,await page.locator('#toolbar > span.hint[role=status]').getAttribute('title'));assert.deepEqual(errors,[]);console.log('COLLECTION BINDINGS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_IMPORTED_GRADIENT){
   assert.ok(['react','liquid'].includes(kind));const before=read();await page.getByText('Gradient fills',{exact:true}).click();const position=page.getByLabel('Gradient 1 stop 4 Position (%)',{exact:true});assert.equal(await position.inputValue(),'62.5');assert.equal(read(),before,'opening imported fills leaves source untouched');await position.fill('70');await position.press('Tab');await wait(()=>read()!==before);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage.includes('rgb(0, 128, 0) 70%')));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(errors,[]);console.log('IMPORTED GRADIENT PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SITE_VARIABLES){
   const before=read(),color=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).color);
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();
   await page.getByText('Site variables',{exact:true}).click();await page.getByLabel('Site variable',{exact:true}).selectOption('--accent');await page.getByRole('button',{name:'Apply site variable',exact:true}).click();await wait(()=>read().includes('md:![color:var(--accent)]'));await settled();await wait(async()=>await color()==='rgb(204, 51, 0)');const linked=read();
   await page.getByRole('button',{name:'Detach site variable',exact:true}).click();await wait(()=>read()!==linked);await settled();await wait(async()=>await color()==='rgb(204, 51, 0)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await wait(async()=>await color()!=='rgb(204, 51, 0)');
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   if(kind==='react'){
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await page.getByLabel('Site variable',{exact:true}).selectOption('--accent');assert.match(await page.getByLabel('Site variable',{exact:true}).locator('option[value="--accent"]').textContent(),/Varies by layer/);
    await page.getByRole('button',{name:'Apply site variable',exact:true}).click();await wait(()=>read().split('md:![color:var(--accent)]').length===3);await settled();const batch=read(),colors=()=>app.locator('h1,p.other-font').evaluateAll(els=>els.map(el=>getComputedStyle(el).color));await wait(async()=>JSON.stringify(await colors())===JSON.stringify(['rgb(204, 51, 0)','rgb(0, 136, 68)']));
    await page.getByRole('button',{name:'Detach site variable',exact:true}).click();await wait(()=>read()!==batch);await settled();assert.deepEqual(await colors(),['rgb(204, 51, 0)','rgb(0, 136, 68)']);assert.equal(read().includes('color:var(--accent)'),false);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   }
   await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
   await page.getByText('Define a variable',{exact:true}).click();await page.getByLabel('Variable name',{exact:true}).fill('--local');await page.getByLabel('Variable definition',{exact:true}).fill('#ff8800');await page.getByRole('button',{name:'Save variable on layer',exact:true}).click();await wait(()=>read().includes('md:![--local:#ff8800]'));await settled();const defined=read();
   await page.getByLabel('Site variable',{exact:true}).selectOption('--local');await page.getByRole('button',{name:'Apply site variable',exact:true}).click();await wait(async()=>await color()==='rgb(255, 136, 0)');await settled();const localBound=read();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await page.getByLabel('Style screen scope',{exact:true}).selectOption({label:'1440 px and larger'});await settled();await page.getByText('Inherited binding: --local',{exact:true}).waitFor();await page.getByRole('button',{name:'Override inherited binding here',exact:true}).click();await wait(()=>read()!==localBound);await settled();const desktopBinding=read();
   await page.getByText('Define a variable',{exact:true}).click();await page.getByLabel('Inherited variable definitions',{exact:true}).selectOption('--local');assert.equal(await page.getByLabel('Variable definition',{exact:true}).inputValue(),'#ff8800');await page.getByLabel('Variable definition',{exact:true}).fill('#22aa44');await page.getByRole('button',{name:'Save variable on layer',exact:true}).click();await wait(async()=>await color()==='rgb(34, 170, 68)');await settled();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await wait(async()=>await color()==='rgb(255, 136, 0)');for(const expected of [desktopBinding,localBound]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();
   await page.getByText('Define a variable',{exact:true}).click();await page.getByLabel('Variable name',{exact:true}).fill('--alias');await page.getByLabel('Variable definition',{exact:true}).fill('var(--local)');await page.getByRole('button',{name:'Save variable on layer',exact:true}).click();await wait(()=>read().includes('md:![--alias:var(--local)]'));await settled();const aliased=read();
   await page.getByText('Define a variable',{exact:true}).click();await page.getByLabel('Defined in this scope',{exact:true}).selectOption('--local');await page.getByLabel('Variable definition',{exact:true}).fill('var(--alias)');await page.getByRole('button',{name:'Save variable on layer',exact:true}).click();await page.locator('#status').filter({hasText:'Variable alias cycle:'}).waitFor();assert.equal(read(),aliased);assert.equal(await color(),'rgb(255, 136, 0)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===localBound);await settled();
   await page.getByText('Define a variable',{exact:true}).click();await page.getByLabel('Defined in this scope',{exact:true}).selectOption('--local');await page.getByLabel('Variable definition',{exact:true}).fill('#0088ff');await page.getByRole('button',{name:'Save variable on layer',exact:true}).click();await wait(async()=>await color()==='rgb(0, 136, 255)');await settled();
   for(const expected of [localBound,defined,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
   assert.deepEqual(errors,[]);console.log('CLASS SITE VARIABLES PASS',kind);return;
  }
  if(process.env.RT_E2E_PICKER_CREATE_EMPTY){
   const file=path.join(root,'.retouch/color-styles.json'),before=read(),label=kind==='html'?'Text color (CSS)':'Text color with alpha';assert.equal(fs.existsSync(file),false);await page.getByRole('button',{name:'Edit '+label,exact:true}).click();const picker=page.getByRole('dialog',{name:'Edit '+label,exact:true});await picker.getByText('Color styles',{exact:true}).click();await picker.getByRole('status',{name:'Picker color styles status',exact:true}).filter({hasText:'No saved color styles yet.'}).waitFor();await picker.getByLabel('Color value',{exact:true}).fill('#12345680');await picker.getByText('Create color style',{exact:true}).click();await picker.getByLabel('New color style name',{exact:true}).fill('First color');await picker.getByRole('button',{name:'Save color style',exact:true}).click();await wait(()=>fs.existsSync(file));await settled();const catalog=JSON.parse(fs.readFileSync(file,'utf8'));assert.equal(catalog.styles.length,1);assert.equal(catalog.styles[0].properties.color,'#12345680');assert.equal(read(),before);await page.keyboard.press('Escape');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>!fs.existsSync(file));await settled();assert.equal(read(),before);assert.deepEqual(errors,[]);console.log('FIRST PICKER STYLE PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_PICKER_STYLES){
   const paletteFile=path.join(root,'.retouch/color-styles.json'),palette=()=>JSON.parse(fs.readFileSync(paletteFile,'utf8')),openLibrary=async()=>{const summary=page.getByText('Saved color styles',{exact:true}),parents=summary.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await page.getByLabel('Color style name',{exact:true}).waitFor();};await openLibrary();await page.getByLabel('Color style name',{exact:true}).fill('Brand blue');await page.getByLabel('Color value with alpha',{exact:true}).fill('#33669980');await page.getByRole('button',{name:'Create color style',exact:true}).click();await wait(()=>fs.existsSync(paletteFile));await settled();const saved=palette().styles[0],before=read(),style=await app.locator('h1').getAttribute('style'),label=kind==='html'?'Text color (CSS)':'Text color with alpha',open=async()=>{await page.getByRole('button',{name:'Edit '+label,exact:true}).click();const dialog=page.getByRole('dialog',{name:'Edit '+label,exact:true});await dialog.getByText('Color styles',{exact:true}).click();await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).waitFor();return dialog;};
   if(process.env.RT_E2E_PICKER_CREATE){
    for(const [name,color,alpha]of [['Picker exact','rgb(12 34 56 / 0.2345)',.2345],['Picker P3','color(display-p3 0.9 0.2 0.1 / 0.3456)',.3456]]){
     let picker=await open();await picker.getByLabel('Color value',{exact:true}).fill(color);await picker.getByText('Create color style',{exact:true}).click();const newName=picker.getByLabel('New color style name',{exact:true});await picker.getByRole('button',{name:'Save color style',exact:true}).click();assert.equal(palette().styles.length,1);await newName.fill(name);
     const reject=async route=>{const data=route.request().postDataJSON();if(data?.type==='create')await route.fulfill({status:409,contentType:'application/json',body:JSON.stringify({ok:false,reason:'Create conflict test'})});else await route.continue();};await page.route('**/rt/__api/color-styles',reject);await newName.press('Enter');await picker.getByRole('status',{name:'Color style application',exact:true}).filter({hasText:'Create conflict test'}).waitFor();assert.equal(read(),before);assert.equal(palette().styles.length,1);await page.unroute('**/rt/__api/color-styles',reject);await picker.getByRole('button',{name:'Save color style',exact:true}).click();await wait(()=>palette().styles.length===2);await settled();const created=palette().styles.find(style=>style.name===name);assert.equal(require('../../shell/palette-values.js').parse(created.properties.color).alpha,alpha);assert.equal(await picker.getByLabel('Color value',{exact:true}).inputValue(),created.properties.color);assert.ok(await picker.getByRole('button',{name:'Apply color style',exact:true}).isEnabled());assert.equal(read(),before);if(process.env.RT_E2E_PICKER_CREATE_SCREENSHOT)await picker.screenshot({path:process.env.RT_E2E_PICKER_CREATE_SCREENSHOT});await page.keyboard.press('Escape');assert.equal(read(),before);assert.equal(palette().styles.length,2);
     await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>palette().styles.length===1);await settled();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>palette().styles.length===2);await settled();picker=await open();await picker.getByRole('button',{name:'Use color style '+name,exact:true}).click();await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes(created.id));assert.ok(read().includes(String(alpha)));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>palette().styles.length===1);await settled();
    }assert.deepEqual(errors,[]);console.log('PICKER CREATE STYLE PASS',kind,engine);return;
   }
   if(process.env.RT_E2E_PALETTE_PRECISION){
    await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);await page.getByLabel('Color opacity (%)',{exact:true}).fill('23.45');await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>palette().styles[0].properties.color==='color(srgb 0.2 0.4 0.6 / 0.2345)');await settled();assert.equal(await page.getByLabel('Color opacity (%)',{exact:true}).inputValue(),'23.45');assert.equal(await page.getByLabel('Color RGB',{exact:true}).inputValue(),'#336699');await page.getByLabel('Color RGB',{exact:true}).fill('#123456');await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>palette().styles[0].properties.color.includes('0.070588'));await settled();const exact=palette().styles[0].properties.color;assert.ok(exact.endsWith('/ 0.2345)'));assert.equal(read(),before);
    const picker=await open();await picker.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();assert.equal(await picker.getByLabel('Color value',{exact:true}).inputValue(),exact);await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes('0.2345'));assert.ok(read().includes(saved.id));await wait(async()=>app.locator('h1').evaluate(el=>getComputedStyle(el).color.startsWith('color(srgb ')));const computed=await app.locator('h1').evaluate(el=>getComputedStyle(el).color),expected=require('../../shell/palette-values.js').parse(computed),linkedSource=read();await openLibrary();await page.getByRole('button',{name:'Use selected layer color',exact:true}).click();assert.ok(await page.getByLabel('Color value with alpha',{exact:true}).evaluate(el=>el===document.activeElement));const captured=require('../../shell/palette-values.js').parse(await page.getByLabel('Color value with alpha',{exact:true}).inputValue());assert.deepEqual(captured.channels,expected.channels);assert.equal(captured.alpha,expected.alpha);assert.equal(read(),linkedSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await openLibrary();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),exact);assert.equal(await page.getByLabel('Color opacity (%)',{exact:true}).inputValue(),'23.45');assert.deepEqual(errors,[]);console.log('PALETTE EXACT SRGB OPACITY PASS',kind,engine);return;
   }
   if(process.env.RT_E2E_PICKER_MULTI_SCOPE){
    const svg=!!process.env.RT_E2E_SVG_COLORS;await page.getByRole('treeitem',{name:svg?'rect':'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:svg?'circle':'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();
    const screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true});await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(kind==='html'?'min-[768px]:':'md:');await screen.focus();await screen.selectOption('390x844');await settled();
    for(const [name,property]of (svg?[['SVG fill','fill'],['SVG stroke','stroke']]:[['Text color','color'],['Background color','background-color'],['Border color','border-top-color']]))for(const linked of [false,true]){
     const paintLabel='Shared '+name+(kind==='html'?'':' with alpha'),appearance=()=>app.locator(svg?'main > svg > *':'h1,p').evaluateAll((els,p)=>els.map(el=>({color:getComputedStyle(el).getPropertyValue(p),style:el.getAttribute('style')})),property),initial=await appearance(),source=read(),parents=page.getByLabel(paintLabel,{exact:true}).locator('xpath=ancestor::details');
     for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}
     await page.getByRole('button',{name:'Edit '+paintLabel,exact:true}).click();const picker=page.getByRole('dialog',{name:'Edit '+paintLabel,exact:true});await picker.getByRole('status',{name:'Color edit range',exact:true}).waitFor();
     if(linked){await picker.getByText('Color styles',{exact:true}).click();await picker.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();}else await picker.getByLabel('Color value',{exact:true}).fill('#33669980');
     if(process.env.RT_E2E_PICKER_MULTI_SCOPE_SCREENSHOT&&linked&&property==='fill')await picker.screenshot({path:process.env.RT_E2E_PICKER_MULTI_SCOPE_SCREENSHOT});assert.deepEqual(await appearance(),initial,'smaller preview '+paintLabel);assert.equal(read(),source);await picker.getByRole('button',{name:linked?'Apply color style':'Apply color',exact:true}).click();await wait(()=>read()!==source);await settled();assert.deepEqual(await appearance(),initial,'smaller save '+paintLabel);if(linked)assert.equal(read().split(saved.id).length-1,2);
     await screen.focus();await screen.selectOption('768x1024');await settled();await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color.startsWith('rgba(51, 102, 153,')));assert.deepEqual((await appearance())[2],initial[2]);await screen.focus();await screen.selectOption('390x844');await settled();assert.deepEqual(await appearance(),initial);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(await appearance(),initial);
    }
    assert.equal(read(),before);assert.deepEqual(errors,[]);console.log('MULTI PICKER RESPONSIVE PASS',kind,engine);return;
   }
   if(process.env.RT_E2E_PICKER_STYLES_SCOPE){
    assert.ok(process.env.RT_E2E_SVG_COLORS);const screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true}),scopeValue=kind==='html'?'min-[768px]:':'md:',cases=[['h1 · Headline','h1',label,'color'],['h1 · Headline','h1',kind==='html'?'Background color (CSS)':'Background color with alpha','background-color'],['h1 · Headline','h1',kind==='html'?'Border color (CSS)':'Border color with alpha','border-top-color'],['rect','main > svg > rect','SVG fill','fill'],['rect','main > svg > rect','SVG stroke','stroke']];
    for(const [layer,selector,paintLabel,property]of cases){await page.getByRole('treeitem',{name:layer,exact:true}).click();await settled();await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(scopeValue);await screen.focus();await screen.selectOption('390x844');await settled();const target=app.locator(selector),appearance=()=>target.evaluate((el,property)=>({color:getComputedStyle(el).getPropertyValue(property),style:el.getAttribute('style')}),property),initial=await appearance(),source=read(),parents=page.getByLabel(paintLabel,{exact:true}).locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await page.getByRole('button',{name:'Edit '+paintLabel,exact:true}).click();const picker=page.getByRole('dialog',{name:'Edit '+paintLabel,exact:true});assert.ok(await picker.getByRole('status',{name:'Color edit range',exact:true}).isVisible());await picker.getByText('Color styles',{exact:true}).click();await picker.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();assert.deepEqual(await appearance(),initial,'out-of-range preview '+paintLabel);assert.equal(read(),source);await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==source);await settled();assert.ok(read().includes(saved.id));assert.deepEqual(await appearance(),initial,'out-of-range save '+paintLabel);await screen.focus();await screen.selectOption('768x1024');await settled();await wait(async()=>(await appearance()).color.startsWith('rgba(51, 102, 153,'));await screen.focus();await screen.selectOption('390x844');await settled();assert.deepEqual(await appearance(),initial,'return to smaller screen '+paintLabel);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(await appearance(),initial);}
    assert.equal(read(),before);assert.deepEqual(errors,[]);console.log('PICKER STYLE SCOPE AND SVG PASS',kind,engine);return;
   }
   let dialog=await open();await dialog.getByLabel('Search picker color styles',{exact:true}).fill('missing');assert.equal(await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).count(),0);await dialog.getByLabel('Search picker color styles',{exact:true}).fill('blue');await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),'#33669980');assert.ok(await dialog.getByRole('button',{name:'Apply color style',exact:true}).isVisible());assert.equal(read(),before);await page.keyboard.press('Escape');assert.equal(await app.locator('h1').getAttribute('style'),style);assert.equal(read(),before);
   dialog=await open();await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();await dialog.getByLabel('Color value',{exact:true}).fill('#aa3355');assert.equal(await dialog.getByRole('button',{name:'Apply color style',exact:true}).count(),0);assert.ok(await dialog.getByRole('button',{name:'Apply color',exact:true}).isVisible());await page.keyboard.press('Escape');
   if(process.env.RT_E2E_PICKER_MULTI){
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();
    const sharedLabel=kind==='html'?'Shared Text color':'Shared Text color with alpha',field=page.getByLabel(sharedLabel,{exact:true}),parents=field.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}
    const appearance=()=>app.locator('h1,p').evaluateAll(els=>els.map(el=>({color:getComputedStyle(el).color,style:el.getAttribute('style')}))),originalPaint=await appearance(),openShared=async()=>{await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();const picker=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await picker.getByText('Color styles',{exact:true}).click();await picker.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();return picker;};
    assert.notEqual(originalPaint[0].color,originalPaint[1].color);assert.equal(await field.inputValue(),'');let picker=await openShared();await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color.startsWith('rgba(51, 102, 153,')));assert.deepEqual((await appearance())[2],originalPaint[2]);assert.equal(read(),before);await page.keyboard.press('Escape');assert.deepEqual(await appearance(),originalPaint);assert.equal(read(),before);
    picker=await openShared();await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();assert.equal(read().split(saved.id).length-1,2);await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color.startsWith('rgba(51, 102, 153,')));assert.deepEqual((await appearance())[2],originalPaint[2]);
    if(process.env.RT_E2E_PICKER_DETACH){
     const linkedSource=read(),linkedPaint=await appearance();await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();picker=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await picker.getByRole('button',{name:'Detach style',exact:true}).click();await wait(()=>read()!==linkedSource);await settled();assert.equal(read().includes(saved.id),false);assert.deepEqual(await appearance(),linkedPaint);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();assert.deepEqual(await appearance(),linkedPaint);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await appearance(),originalPaint);assert.deepEqual(errors,[]);console.log('MULTI PICKER DETACH PASS',kind,engine);return;
    }
    if(process.env.RT_E2E_PICKER_MULTI_INHERITED){
     const baseSource=read(),screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true});await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(kind==='html'?'min-[768px]:':'md:');await screen.focus();await screen.selectOption('390x844');await settled();await wait(()=>page.getByRole('status',{name:'Edit range status',exact:true}).evaluate(el=>el.dataset.match==='false'));const smaller=await appearance(),inspect=async()=>{await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();const d=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await wait(async()=>/Inherited: Brand blue.*All sizes.*2 layers/.test(await d.getByRole('status',{name:'Linked paint style',exact:true}).textContent()));assert.equal(await d.getByRole('button',{name:'Detach style',exact:true}).count(),0);return d;};
     picker=await inspect();await picker.getByRole('button',{name:'Use inherited color here',exact:true}).click();assert.deepEqual(await appearance(),smaller);assert.equal(read(),baseSource);await page.keyboard.press('Escape');assert.deepEqual(await appearance(),smaller);picker=await inspect();if(process.env.RT_E2E_PICKER_MULTI_INHERITED_SCREENSHOT)await picker.screenshot({path:process.env.RT_E2E_PICKER_MULTI_INHERITED_SCREENSHOT});await picker.getByRole('button',{name:'Use inherited color here',exact:true}).click();await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==baseSource);await settled();assert.equal(read().split(saved.id).length-1,4);assert.deepEqual(await appearance(),smaller);await screen.focus();await screen.selectOption('768x1024');await settled();await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color.startsWith('rgba(51, 102, 153,')));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===baseSource);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(errors,[]);console.log('MULTI PICKER INHERITED PASS',kind,engine);return;
    }
    if(process.env.RT_E2E_PICKER_MULTI_CONTEXT){
     const linkedSource=read(),inspect=async()=>{await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();const d=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await d.getByRole('status',{name:'Linked paint style',exact:true}).waitFor();return d;},status=d=>d.getByRole('status',{name:'Linked paint style',exact:true});
     picker=await inspect();await wait(async()=>/Linked: Brand blue.*2 layers/.test(await status(picker).textContent()));assert.ok(await picker.getByRole('button',{name:'Use linked color',exact:true}).isDisabled());await picker.getByLabel('Color value',{exact:true}).fill('#ff0000');await picker.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==linkedSource);await settled();const overrideSource=read(),overridden=await appearance();picker=await inspect();await wait(async()=>/2 local overrides/.test(await status(picker).textContent()));if(process.env.RT_E2E_PICKER_MULTI_CONTEXT_SCREENSHOT)await picker.screenshot({path:process.env.RT_E2E_PICKER_MULTI_CONTEXT_SCREENSHOT});await picker.getByRole('button',{name:'Use linked color',exact:true}).click();assert.equal(read(),overrideSource);await page.keyboard.press('Escape');assert.deepEqual(await appearance(),overridden);picker=await inspect();await wait(async()=>await picker.getByRole('button',{name:'Use linked color',exact:true}).isEnabled());await picker.getByRole('button',{name:'Use linked color',exact:true}).click();await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==overrideSource);await settled();for(const source of [overrideSource,linkedSource,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();}
     await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();picker=await open();await picker.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();await picker.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();const partial=read();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();picker=await inspect();assert.match(await status(picker).textContent(),/1 of 2 layers linked in this range/);assert.equal(await picker.getByRole('button',{name:'Use linked color',exact:true}).count(),0);assert.equal(read(),partial);const partialPaint=await appearance();await picker.getByRole('button',{name:'Detach style',exact:true}).click();await wait(()=>read()!==partial);await settled();assert.equal(read().includes(saved.id),false);assert.deepEqual(await appearance(),partialPaint);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===partial);await settled();assert.deepEqual(await appearance(),partialPaint);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await appearance(),originalPaint);assert.deepEqual(errors,[]);console.log('MULTI PICKER CONTEXT PASS',kind,engine);return;
    }
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await appearance(),originalPaint);
    await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();picker=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await picker.getByRole('status',{name:'Selected paint values',exact:true}).waitFor();if(process.env.RT_E2E_PICKER_MULTI_SCREENSHOT)await picker.screenshot({path:process.env.RT_E2E_PICKER_MULTI_SCREENSHOT});assert.ok(await picker.getByRole('button',{name:'Apply color',exact:true}).isDisabled());assert.equal(await picker.locator('.paint-notation-row').isVisible(),false);const selectedColors=picker.getByRole('toolbar',{name:'Selection colors',exact:true}),choices=selectedColors.getByRole('button');assert.equal(await choices.count(),2);assert.deepEqual(await appearance(),originalPaint);await choices.first().focus();await page.keyboard.press('End');assert.equal(await choices.last().evaluate(el=>el===document.activeElement),true);await page.keyboard.press('Enter');assert.equal(read(),before);assert.ok(await picker.getByRole('slider',{name:'Saturation and brightness',exact:true}).isVisible());assert.ok(await picker.getByRole('button',{name:'Apply color',exact:true}).isEnabled());await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color===originalPaint[1].color));await picker.getByLabel('Color value',{exact:true}).fill('not-a-color');assert.ok(await picker.getByRole('button',{name:'Apply color',exact:true}).isDisabled());assert.equal(read(),before);await page.keyboard.press('Escape');assert.deepEqual(await appearance(),originalPaint);await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();picker=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await picker.getByRole('toolbar',{name:'Selection colors',exact:true}).getByRole('button').last().click();await picker.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color===originalPaint[1].color));assert.deepEqual((await appearance())[2],originalPaint[2]);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await appearance(),originalPaint);await page.getByRole('button',{name:'Edit '+sharedLabel,exact:true}).click();picker=page.getByRole('dialog',{name:'Edit '+sharedLabel,exact:true});await picker.getByLabel('Color value',{exact:true}).fill('rgb(12 34 56 / 0.2345)');assert.equal(read(),before);await picker.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes('0.2345'));await wait(async()=>(await appearance()).slice(0,2).every(item=>item.color==='rgba(12, 34, 56, 0.235)'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await appearance(),originalPaint);assert.deepEqual(errors,[]);console.log('MULTI PICKER STYLE PASS',kind,engine);return;
   }
   dialog=await open();await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();if(process.env.RT_E2E_PICKER_STYLES_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_PICKER_STYLES_SCREENSHOT});await dialog.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes(saved.id));const linked=read();if(process.env.RT_E2E_PICKER_DETACH){
    const appearance=()=>app.locator('h1').evaluate(el=>({color:getComputedStyle(el).color,style:el.getAttribute('style')})),inspect=async()=>{await page.getByRole('button',{name:'Edit '+label,exact:true}).click();return page.getByRole('dialog',{name:'Edit '+label,exact:true});};
    dialog=await inspect();await dialog.getByLabel('Color value',{exact:true}).fill('rgb(12 34 56 / 0.2345)');await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==linked);await settled();const overriddenSource=read(),overriddenPaint=await appearance();assert.ok(overriddenSource.includes(saved.id));assert.ok(overriddenSource.includes('0.2345'));
    dialog=await inspect();const detach=dialog.getByRole('button',{name:'Detach style',exact:true});assert.ok(await detach.isEnabled());if(process.env.RT_E2E_PICKER_DETACH_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_PICKER_DETACH_SCREENSHOT});await dialog.getByLabel('Color value',{exact:true}).fill('#ff0000');assert.ok(await detach.isDisabled());await page.keyboard.press('Escape');assert.equal(read(),overriddenSource);assert.deepEqual(await appearance(),overriddenPaint);
    dialog=await inspect();const reject=async route=>{const data=route.request().postDataJSON();if(data?.type==='detachColorStyle')await route.fulfill({status:409,contentType:'application/json',body:JSON.stringify({ok:false,reason:'Detach conflict test'})});else await route.continue();};await page.route('**/rt/__api/op',reject);await dialog.getByRole('button',{name:'Detach style',exact:true}).click();await dialog.getByRole('status',{name:'Color style application',exact:true}).filter({hasText:'Detach conflict test'}).waitFor();assert.equal(read(),overriddenSource);await page.unroute('**/rt/__api/op',reject);
    await dialog.getByRole('button',{name:'Detach style',exact:true}).click();await wait(()=>read()!==overriddenSource);await settled();assert.equal(read().includes(saved.id),false);assert.ok(read().includes('0.2345'));assert.deepEqual(await appearance(),overriddenPaint);
    for(const source of [overriddenSource,linked,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();if(source===overriddenSource)assert.deepEqual(await appearance(),overriddenPaint);}
    assert.deepEqual(errors,[]);console.log('PICKER DETACH OVERRIDE RETRY PASS',kind,engine);return;
   }if(process.env.RT_E2E_PICKER_LINK_CONTEXT){
    if(process.env.RT_E2E_PICKER_RESIZE_CLOSED){
     const selected=await page.evaluate(()=>sel.info.id),field=page.getByLabel(label,{exact:true});await field.focus();await field.fill('#ff0000');
     for(const viewport of [{width:1024,height:600},{width:1600,height:1100},{width:1024,height:600}]){await page.setViewportSize(viewport);await wait(()=>page.locator('#main').evaluate((el,compact)=>el.classList.contains('compact-workspace')===compact,viewport.width<1100));assert.ok(await field.isVisible());assert.equal(await field.inputValue(),'#ff0000');assert.equal(await field.evaluate(el=>el===document.activeElement),true);assert.equal(await page.evaluate(()=>sel.info.id),selected);assert.equal(read(),linked);}
     await field.press('Escape');dialog=await open();await page.keyboard.press('Escape');assert.equal(read(),linked);assert.deepEqual(errors,[]);console.log('PICKER CLOSED RESIZE PASS',kind,engine);return;
    }
    if(process.env.RT_E2E_PICKER_SMALL_WINDOW){
     const compact=await open();await page.setViewportSize({width:1024,height:600});await wait(()=>page.locator('#main').evaluate(el=>el.classList.contains('compact-workspace')));assert.ok(await page.locator('#panel').isVisible(),'Picker keeps its originating inspector open');
     const visible=async selector=>compact.locator(selector).evaluate(el=>{const r=el.getBoundingClientRect(),d=el.closest('dialog').getBoundingClientRect(),x=r.left+r.width/2,y=r.top+r.height/2;return r.top>=d.top&&r.bottom<=d.bottom&&r.left>=0&&r.right<=innerWidth&&el.contains(document.elementFromPoint(x,y));});
     for(const fraction of [0,0.5,1]){await compact.evaluate((el,f)=>el.scrollTop=(el.scrollHeight-el.clientHeight)*f,fraction);await wait(()=>visible('.paint-close'));await wait(()=>visible('.paint-picker-actions button:last-child'));}
     await compact.getByLabel('Color value',{exact:true}).fill('#ff0000');assert.equal(read(),linked);if(process.env.RT_E2E_PICKER_SMALL_SCREENSHOT)await compact.screenshot({path:process.env.RT_E2E_PICKER_SMALL_SCREENSHOT});await compact.locator('.paint-close').click();assert.equal(read(),linked);assert.deepEqual(errors,[]);console.log('PICKER SMALL WINDOW PASS',kind,engine);return;
    }

    const context=()=>dialog.getByRole('status',{name:'Linked paint style',exact:true}),chooseReset=()=>dialog.getByRole('button',{name:'Use linked color',exact:true});await wait(()=>page.evaluate(label=>document.activeElement?.getAttribute('aria-label')===label,label));const refreshedColor=await page.getByLabel(label,{exact:true}).evaluate(el=>RetouchPaletteValues.parse(RetouchColorStyles.fromComputed(el.value)));assert.deepEqual(refreshedColor.channels,[.2,.4,.6]);assert.ok(Math.abs(refreshedColor.alpha-128/255)<.002);dialog=await open();assert.ok(await context().isVisible(),JSON.stringify(await dialog.evaluate(el=>({current:sel.info.colorStyleLinks,panel:el.retouchSourceInput.closest('[data-retouch-color-style-scope]').retouchColorStyleOptions.links}))));assert.match(await context().textContent(),/Linked: Brand blue/);assert.ok(await chooseReset().isDisabled());await dialog.getByLabel('Color value',{exact:true}).fill('#ff0000');assert.match(await context().textContent(),/Unsaved color change/);assert.ok(await chooseReset().isEnabled());await page.keyboard.press('Escape');assert.equal(read(),linked);
    dialog=await open();await dialog.getByLabel('Color value',{exact:true}).fill('#ff0000');await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==linked);await settled();const overridden=read(),appearance=()=>app.locator('h1').evaluate(el=>({color:getComputedStyle(el).color,style:el.getAttribute('style')})),localPaint=await appearance();assert.ok(overridden.includes(saved.id));dialog=await open();assert.match(await context().textContent(),/Local override/);await chooseReset().click();assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),'#33669980');assert.equal(read(),overridden);await page.keyboard.press('Escape');assert.deepEqual(await appearance(),localPaint);dialog=await open();await chooseReset().click();if(process.env.RT_E2E_PICKER_LINK_CONTEXT_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_PICKER_LINK_CONTEXT_SCREENSHOT});await dialog.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==overridden);await settled();assert.ok(read().includes(saved.id));await wait(async()=>(await appearance()).color.startsWith('rgba(51, 102, 153,'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===overridden);await settled();assert.deepEqual(await appearance(),localPaint);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();
    const screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true});await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(kind==='html'?'min-[768px]:':'md:');await screen.focus();await screen.selectOption('390x844');await settled();const smaller=await appearance();dialog=await open();assert.match(await context().textContent(),/Inherited: Brand blue.*All sizes/i);await dialog.getByRole('button',{name:'Use inherited color here',exact:true}).click();assert.deepEqual(await appearance(),smaller);assert.equal(read(),linked);await dialog.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==linked);await settled();assert.equal(read().split(saved.id).length-1,2,'base and selected-range links');assert.deepEqual(await appearance(),smaller);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(errors,[]);console.log('PICKER LINK CONTEXT PASS',kind,engine);return;
   }await openLibrary();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);await page.getByLabel('Color value with alpha',{exact:true}).fill('#008844cc');await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>read()!==linked);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).color)==='rgba(0, 136, 68, 0.8)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.equal(await app.locator('h1').getAttribute('style'),style);
   dialog=await open();await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();const stale=palette();stale.styles[0].properties.color='#ff0000ff';fs.writeFileSync(paletteFile,JSON.stringify(stale,null,2)+'\n');await dialog.getByRole('button',{name:'Apply color style',exact:true}).click();await dialog.getByRole('status',{name:'Color style application',exact:true}).waitFor();assert.equal(read(),before);assert.ok(await dialog.isVisible());assert.ok(await dialog.getByRole('button',{name:'Apply color style',exact:true}).isEnabled());await dialog.getByRole('button',{name:'Reload picker color styles',exact:true}).click();await wait(async()=>await dialog.getByRole('button',{name:'Apply color',exact:true}).count()===1);await dialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),'#ff0000ff');await dialog.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes(saved.id));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();for(const [base,property]of [['Background color','backgroundColor'],['Border color','borderTopColor']]){const colorLabel=base+(kind==='html'?' (CSS)':' with alpha'),parents=page.getByLabel(colorLabel,{exact:true}).locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await page.getByRole('button',{name:'Edit '+colorLabel,exact:true}).click();const paintDialog=page.getByRole('dialog',{name:'Edit '+colorLabel,exact:true});await paintDialog.getByText('Color styles',{exact:true}).click();await paintDialog.getByRole('button',{name:'Use color style Brand blue',exact:true}).click();await paintDialog.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes(saved.id));await wait(async()=>await app.locator('h1').evaluate((el,property)=>getComputedStyle(el)[property],property)==='rgb(255, 0, 0)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();}assert.deepEqual(errors,[]);console.log('PICKER STYLES PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_HSL_DRAFT){
   const before=read(),target=app.locator('h1'),style=await target.getAttribute('style'),label=kind==='html'?'Text color (CSS)':'Text color with alpha',open=async()=>{await page.getByRole('button',{name:'Edit '+label,exact:true}).click();return page.getByRole('dialog',{name:'Edit '+label,exact:true});};
   for(const space of ['srgb','display-p3'])for(const level of [0,1,0.5]){
    const dialog=await open(),value=dialog.getByLabel('Color value',{exact:true}),model=dialog.getByLabel('Color model',{exact:true}),hue=dialog.getByLabel('Hue (deg)',{exact:true}),saturation=dialog.getByLabel('Saturation (%)',{exact:true}),lightness=dialog.getByLabel('Lightness (%)',{exact:true}),color=()=>value.evaluate(el=>{const normalized=RetouchColorStyles.fromComputed(el.value),parsed=RetouchPaletteValues.parse(normalized);return parsed;});
    await value.fill(space==='display-p3'?'color(display-p3 '+[level,level,level].join(' ')+' / 0.2345)':'rgb('+[level*255,level*255,level*255].join(' ')+' / 0.2345)');await model.selectOption('hsl');await hue.fill('240');await saturation.fill('100');await dialog.getByLabel('Opacity value (%)',{exact:true}).fill('23.45');await model.selectOption('css');await model.selectOption('hsl');assert.equal(await hue.inputValue(),'240','remember hue at lightness '+level+' in '+space);assert.equal(await saturation.inputValue(),'100');await lightness.fill('50');assert.deepEqual((await color()).channels,[0,0,1]);assert.equal((await color()).space,space);assert.ok((await value.inputValue()).includes('0.2345'));assert.equal(read(),before);
    await dialog.getByRole('slider',{name:'Hue',exact:true}).press('Home');assert.equal(await hue.inputValue(),'0');assert.deepEqual((await color()).channels,[1,0,0]);await value.fill(space==='display-p3'?'color(display-p3 0 1 0 / 0.2345)':'rgb(0 255 0 / 0.2345)');assert.equal(await hue.inputValue(),'120');await model.selectOption('hsb');await dialog.getByLabel('Hue (deg)',{exact:true}).fill('240');await model.selectOption('hsl');assert.equal(await hue.inputValue(),'240');if(process.env.RT_E2E_HSL_DRAFT_SCREENSHOT&&space==='display-p3'&&level===0.5)await dialog.screenshot({path:process.env.RT_E2E_HSL_DRAFT_SCREENSHOT});
    if(level===0.5){await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes('0.2345'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();}else if(level===0)await dialog.getByRole('button',{name:'Close color picker',exact:true}).click();else await page.keyboard.press('Escape');assert.equal(await target.getAttribute('style'),style);assert.equal(read(),before);
   }
   assert.deepEqual(errors,[]);console.log('HSL DRAFT PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_P3_PICKER){
   const before=read(),target=app.locator('h1'),style=await target.getAttribute('style'),label=kind==='html'?'Text color (CSS)':'Text color with alpha',open=async()=>{await page.getByRole('button',{name:'Edit '+label,exact:true}).click();return page.getByRole('dialog',{name:'Edit '+label,exact:true});};
   let dialog=await open();const value=()=>dialog.getByLabel('Color value',{exact:true}),model=()=>dialog.getByLabel('Color model',{exact:true}),profile=()=>dialog.getByLabel('Color profile',{exact:true}),plane=()=>dialog.getByRole('slider',{name:'Saturation and brightness',exact:true}),initial='color(display-p3 1 0.2 0.3 / 0.2345)',parsed=()=>value().evaluate(el=>RetouchPaletteValues.parse(el.value));await value().fill(initial);
   for(const name of ['hex','hsl','hsb','css','rgb']){await model().selectOption(name);assert.equal(await value().inputValue(),initial);assert.equal(read(),before);assert.equal(await profile().inputValue(),'display-p3');}
   assert.ok(await plane().isVisible());assert.match(await plane().evaluate(el=>el.style.backgroundColor),/display-p3/);assert.match(await plane().evaluate(el=>el.style.backgroundImage),/in display-p3/);assert.match(await dialog.getByRole('slider',{name:'Hue',exact:true}).evaluate(el=>el.style.backgroundImage),/in display-p3/);await plane().press('ArrowLeft');assert.equal((await parsed()).space,'display-p3');assert.equal((await parsed()).alpha,0.2345);assert.notEqual(await value().inputValue(),initial);assert.equal(read(),before);await dialog.getByRole('slider',{name:'Hue',exact:true}).press('End');assert.equal((await parsed()).space,'display-p3');
   const box=await plane().boundingBox();await page.mouse.click(box.x+box.width*.4,box.y+box.height*.3);assert.equal((await parsed()).space,'display-p3');assert.equal((await parsed()).alpha,0.2345);assert.notEqual(await target.getAttribute('style'),style);await page.keyboard.press('Escape');assert.equal(await target.getAttribute('style'),style);assert.equal(read(),before);
   dialog=await open();await value().fill(initial);await model().selectOption('hex');assert.equal(await dialog.getByLabel('Hex color',{exact:true}).getAttribute('title'),'Hex channels in Display P3');await dialog.getByLabel('Hex color',{exact:true}).fill('00FF00');assert.deepEqual((await parsed()).channels,[0,1,0]);assert.equal((await parsed()).space,'display-p3');assert.equal((await parsed()).alpha,0.2345);await dialog.getByLabel('Hex color',{exact:true}).fill('FF000040');assert.deepEqual((await parsed()).channels,[1,0,0]);assert.equal((await parsed()).alpha,64/255);
   await model().selectOption('hsl');await dialog.getByLabel('Hue (deg)',{exact:true}).fill('120');assert.deepEqual((await parsed()).channels,[0,1,0]);await dialog.getByLabel('Lightness (%)',{exact:true}).fill('25');assert.deepEqual((await parsed()).channels,[0,0.5,0]);assert.equal((await parsed()).space,'display-p3');
   await model().selectOption('hsb');await dialog.getByLabel('Brightness (%)',{exact:true}).fill('0');await dialog.getByLabel('Hue (deg)',{exact:true}).fill('240');await dialog.getByLabel('Saturation (%)',{exact:true}).fill('100');await dialog.getByLabel('Opacity value (%)',{exact:true}).fill('23.45');await dialog.getByLabel('Brightness (%)',{exact:true}).fill('100');assert.equal(await value().inputValue(),'color(display-p3 0 0 1 / 0.2345)');if(process.env.RT_E2E_P3_PICKER_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_P3_PICKER_SCREENSHOT});assert.equal(read(),before);await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes('0.2345'));dialog=await open();assert.equal(await profile().inputValue(),'display-p3');assert.deepEqual((await parsed()).channels,[0,0,1]);await page.keyboard.press('Escape');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.equal(await target.getAttribute('style'),style);
   dialog=await open();await value().fill(initial);await value().fill('#336699');assert.equal(await plane().evaluate(el=>el.style.backgroundImage),'');assert.equal(await dialog.getByRole('slider',{name:'Hue',exact:true}).evaluate(el=>el.style.backgroundImage),'');await page.keyboard.press('Escape');assert.equal(read(),before);assert.deepEqual(errors,[]);console.log('P3 PICKER PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_CLIP){
   assert.ok(process.env.RT_E2E_SHARED_GAPS);const before=read();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();assert.equal(await page.evaluate(()=>sel.multiple.length),2);
   const clip=()=>page.getByRole('checkbox',{name:'Shared Clip content',exact:true}),overflow=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el);return [css.overflowX,css.overflowY];})),expectOverflow=async value=>assert.deepEqual(await overflow(),[[value,value],[value,value]]);
   assert.equal(await clip().isChecked(),false);assert.equal(await clip().evaluate(el=>el.indeterminate),true);await clip().click();await wait(()=>read()!==before);await settled();const clipped=read();await expectOverflow('clip');assert.equal(await clip().isChecked(),true);assert.equal(await clip().evaluate(el=>el.indeterminate),false);
   assert.deepEqual(await app.locator('p.named-font').evaluate(el=>[getComputedStyle(el).overflowX,getComputedStyle(el).overflowY]),['visible','visible']);
   await clip().focus();await page.keyboard.press('Space');await wait(()=>read()!==clipped);await settled();const visible=read();await expectOverflow('visible');assert.equal(await clip().isChecked(),false);
   await page.getByRole('button',{name:'Reset shared clip content',exact:true}).click();await wait(()=>read()!==visible);await settled();assert.deepEqual(await overflow(),[['visible','visible'],['hidden','hidden']]);assert.equal(await clip().evaluate(el=>el.indeterminate),true);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===visible);await settled();await expectOverflow('visible');
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.deepEqual(await overflow(),[['visible','visible'],['hidden','hidden']]);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await expectOverflow('visible');
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===clipped);await settled();await expectOverflow('clip');
   if(process.env.RT_E2E_SHARED_CLIP_SCREENSHOT){await clip().scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_SHARED_CLIP_SCREENSHOT});}
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.equal(await clip().evaluate(el=>el.indeterminate),true);assert.deepEqual(errors,[]);console.log('SHARED CLIPPING PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_ALIGNMENT_GEOMETRY){
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();assert.equal(await page.evaluate(()=>sel.multiple.length),2);
   const bounds=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const r=el.getBoundingClientRect(),children=[...el.children].map(child=>child.getBoundingClientRect()),left=Math.min(...children.map(c=>c.left)),right=Math.max(...children.map(c=>c.right)),top=Math.min(...children.map(c=>c.top)),bottom=Math.max(...children.map(c=>c.bottom));return {x:left-r.left,y:top-r.top,width:right-left,height:bottom-top,containerWidth:r.width,containerHeight:r.height};}));
   for(const [direction,wrap]of [['row','nowrap'],['column','wrap'],['row-reverse','wrap-reverse'],['column-reverse','wrap']]){
    await page.getByLabel('Shared Arrange children',{exact:true}).selectOption(direction);await settled();await page.getByLabel('Shared Wrap children',{exact:true}).selectOption(wrap);await settled();
    for(const [x,y,horizontal,vertical]of ['top','middle','bottom'].flatMap((vertical,y)=>['left','center','right'].map((horizontal,x)=>[x,y,horizontal,vertical]))){
     const button=page.getByRole('button',{name:'Shared Align children '+vertical+' '+horizontal,exact:true});await button.click();await settled();assert.equal(await button.getAttribute('aria-pressed'),'true');const actual=await bounds();for(const box of actual){assert.ok(Math.abs(box.x-(box.containerWidth-box.width)*x/2)<1,JSON.stringify({direction,wrap,x,y,box}));assert.ok(Math.abs(box.y-(box.containerHeight-box.height)*y/2)<1,JSON.stringify({direction,wrap,x,y,box}));}console.log('GEOMETRY',direction,wrap,horizontal,vertical,JSON.stringify(actual));
    }
   }
   assert.deepEqual(errors,[]);console.log('SHARED PHYSICAL ALIGNMENT GEOMETRY PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_GAPS){
   assert.notEqual(kind,'html');const before=read();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Meta']});await settled();
   if(process.env.RT_E2E_SHARED_CONTAINERS){
    assert.equal(await page.evaluate(()=>sel.multiple?.length),2,'initial container selection');const snapshot=read(),states=[snapshot],layout=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el);return [css.display,css.flexDirection,css.flexWrap,css.alignItems,css.justifyContent];})),initial=await layout();
    for(const [label,value,index,expected]of [['Arrange children','column',1,'column'],['Wrap children','wrap',2,'wrap'],['Align children','center',3,'center'],['Distribute children','between',4,'space-between']]){await page.getByLabel('Shared '+label,{exact:true}).selectOption(value);await settled();await wait(()=>read()!==states.at(-1));states.push(read());await wait(async()=>(await layout()).every(row=>row[0]==='flex'&&row[index]===expected));assert.deepEqual(await page.evaluate(()=>sel.multiple.map(info=>info.tag)),['h1','p'],'selection after '+label+' SOURCE '+read());}
    if(process.env.RT_E2E_SHARED_ALIGNMENT_PICKER){
     const source=read(),button=page.getByRole('button',{name:'Shared Align children bottom right',exact:true});await button.click();await settled();await wait(()=>read()!==source);states.push(read());assert.equal(await button.getAttribute('aria-pressed'),'true');assert.deepEqual(await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el);return [css.justifyContent,css.alignItems,css.alignContent];})),[['flex-end','flex-end','flex-end'],['flex-start','flex-end','flex-end']]);
     const aligned=read();await page.getByRole('button',{name:'Shared Align children top left',exact:true}).click();await settled();assert.notEqual(read(),aligned);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),aligned);assert.equal(await button.getAttribute('aria-pressed'),'true');
    }
    if(process.env.RT_E2E_SHARED_CONTAINERS_SCREENSHOT){await page.getByLabel('Shared Arrange children',{exact:true}).scrollIntoViewIfNeeded();await page.locator('#panel').screenshot({path:process.env.RT_E2E_SHARED_CONTAINERS_SCREENSHOT});}
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.deepEqual(await layout(),initial,'container layout changes stay in their edit range');await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
    await page.getByRole('button',{name:'Reset shared arrange children',exact:true}).click();await settled();await wait(async()=>JSON.stringify((await layout()).map(row=>row.slice(0,2)))===JSON.stringify(initial.map(row=>row.slice(0,2))));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),states.at(-1));
    for(const source of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),source);}assert.deepEqual(await layout(),initial);assert.equal(await app.locator('p.named-font').evaluate(el=>getComputedStyle(el).display),'block');
    if(process.env.RT_E2E_SHARED_GRID_CONTAINERS){
     const saved=[read()];await page.getByLabel('Shared Arrange children',{exact:true}).selectOption('grid');await settled();saved.push(read());
     for(const [axis,value]of [['columns',3],['rows',4]]){const input=page.getByLabel('Shared Grid '+axis,{exact:true});await input.fill(String(value));await input.press('Enter');await settled();await wait(()=>read()!==saved.at(-1));saved.push(read());await wait(async()=>await app.locator('h1,p.other-font').evaluateAll((nodes,[axis,value])=>nodes.every(el=>getComputedStyle(el).getPropertyValue('grid-template-'+axis).split(/\s+/).length===value),[axis,value]));}
     await page.getByLabel('Shared Grid flow',{exact:true}).selectOption('col-dense');await settled();saved.push(read());await wait(async()=>await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).gridAutoFlow==='column dense')));
     if(process.env.RT_E2E_SHARED_GRID_CONTAINERS_SCREENSHOT){await page.getByLabel('Shared Grid flow',{exact:true}).scrollIntoViewIfNeeded();await page.locator('#panel').screenshot({path:process.env.RT_E2E_SHARED_GRID_CONTAINERS_SCREENSHOT});}
     if(process.env.RT_E2E_SHARED_CUSTOM_TRACKS){
      const original=read();await page.locator('[data-shared-section=layout]').getByText('Custom grid tracks',{exact:true}).click();
      for(const [label,value,property,expected]of [['Column sizes','40px 80px 120px','grid-template-columns','40px 80px 120px'],['Row sizes','repeat(4, 30px)','grid-template-rows','30px 30px 30px 30px']]){const input=page.getByLabel('Shared '+label,{exact:true});await input.fill(value);await input.press('Enter');await settled();await wait(async()=>await app.locator('h1,p.other-font').evaluateAll((nodes,[property,expected])=>nodes.every(el=>getComputedStyle(el).getPropertyValue(property)===expected),[property,expected]));assert.equal(await input.inputValue(),value);}
      const customSource=read(),input=page.getByLabel('Shared Column sizes',{exact:true});await input.fill('repeat(');await input.press('Enter');assert.equal(await input.evaluate(el=>el.checkValidity()),false);assert.equal(read(),customSource);await input.press('Escape');assert.equal(await input.inputValue(),'40px 80px 120px');
      if(process.env.RT_E2E_SHARED_CUSTOM_TRACKS_SCREENSHOT){await input.scrollIntoViewIfNeeded();await page.locator('#panel').screenshot({path:process.env.RT_E2E_SHARED_CUSTOM_TRACKS_SCREENSHOT});}
      await page.getByRole('button',{name:'Reset shared column sizes',exact:true}).click();await settled();assert.notEqual(read(),customSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),customSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),original);
     }
     const count=page.getByLabel('Shared Grid columns',{exact:true});await count.fill('0');await count.press('Enter');assert.equal(await count.evaluate(el=>el.checkValidity()),false);assert.equal(read(),saved.at(-1));await count.press('Escape');
     await page.getByRole('button',{name:'Reset shared grid columns',exact:true}).click();await settled();assert.notEqual(read(),saved.at(-1));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),saved.at(-1));
     await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.deepEqual(await layout(),initial);await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
     for(const source of saved.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),source);}assert.deepEqual(await layout(),initial);
    }
   }
   const horizontal=page.getByLabel('Shared Horizontal gap',{exact:true}),vertical=page.getByLabel('Shared Vertical gap',{exact:true});
   const gaps=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el),vertical=css.writingMode!=='horizontal-tb';return vertical?[css.rowGap,css.columnGap]:[css.columnGap,css.rowGap];}));
   assert.equal(await horizontal.inputValue(),'');assert.equal(await horizontal.getAttribute('placeholder'),'Mixed');
   await horizontal.fill('12');await horizontal.press('Enter');await settled();await wait(async()=>JSON.stringify(await gaps())===JSON.stringify([['12px','4px'],['12px','8px']]));const first=read();
   await vertical.fill('20');await vertical.press('Enter');await settled();await wait(async()=>(await gaps()).every(values=>values.join(',')==='12px,20px'));const second=read();
   if(process.env.RT_E2E_SHARED_SPACING_SCRUB){
    const label=horizontal.locator('..').locator(':scope > span');await label.scrollIntoViewIfNeeded();
    const begin=async()=>{const box=await label.boundingBox();assert.ok(box);await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();return box.x+box.width/2;};
    let x=await begin();await page.mouse.move(x+8,(await label.boundingBox()).y+8);await wait(async()=>(await gaps()).every(v=>v[0]==='20px'));assert.equal(read(),second,'drag previews do not write source');await page.keyboard.press('Escape');await page.mouse.up();await wait(async()=>(await gaps()).every(v=>v[0]==='12px'));assert.equal(read(),second,'cancel preserves source');
    x=await begin();await page.mouse.move(x+6,(await label.boundingBox()).y+8);await page.mouse.up();await settled();await wait(async()=>(await gaps()).every(v=>v[0]==='18px'));assert.notEqual(read(),second);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),second,'one Undo restores drag source');await wait(async()=>(await gaps()).every(v=>v[0]==='12px'));
   }
   assert.ok(Math.abs(await app.locator('h1').evaluate(el=>{const [a,b]=[...el.children].map(e=>e.getBoundingClientRect());return b.left-a.right;})-12)<.1);
   assert.deepEqual(await app.locator('p.other-font').evaluate(el=>{const [a,b,c]=[...el.children].map(e=>e.getBoundingClientRect());return [Math.round(a.left-c.right),Math.round(a.top-b.bottom)];}),[12,20]);
   assert.equal(await app.locator('p.named-font').evaluate(el=>getComputedStyle(el).gap),'normal');
   if(process.env.RT_E2E_SHARED_GAPS_SCREENSHOT){await page.locator('#panel').evaluate(panel=>{panel.scrollTop+=panel.querySelector('[data-shared-section=layout]').getBoundingClientRect().top-panel.getBoundingClientRect().top-50;});await page.locator('#panel').screenshot({path:process.env.RT_E2E_SHARED_GAPS_SCREENSHOT});}
   await page.getByRole('button',{name:'Reset selected horizontal gap',exact:true}).click();await settled();await wait(async()=>JSON.stringify(await gaps())===JSON.stringify([['4px','20px'],['8px','20px']]));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),second);
   await horizontal.fill('-1');await horizontal.press('Enter');assert.equal(await horizontal.evaluate(el=>el.checkValidity()),false);assert.equal(read(),second);await horizontal.press('Escape');
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>JSON.stringify(await gaps())===JSON.stringify([['4px','4px'],['8px','8px']]));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),first);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),before);assert.deepEqual(errors,[]);
   console.log('SHARED GAPS AXES/GEOMETRY/SCOPE/RESET/UNDO PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SHARED_PADDING){
   assert.notEqual(kind,'html');const before=read();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
   await page.getByLabel('Style screen scope',{exact:true}).selectOption('md:');await settled();
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();
   const field=page.getByLabel('Shared Padding',{exact:true}),padding=()=>app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>['top','right','bottom','left'].map(side=>getComputedStyle(el).getPropertyValue('padding-'+side))));
   await field.fill('12');await field.press('Enter');await wait(()=>read()!==before);await settled();await wait(async()=>(await padding()).every(row=>row.every(v=>v==='12px')));const uniform=read();
   if(process.env.RT_E2E_SHARED_SPACING_SCRUB){
    const label=field.locator('..').locator(':scope > span');await label.scrollIntoViewIfNeeded();
    const begin=async()=>{const b=await label.boundingBox();assert.ok(b);await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();return b;};
    let b=await begin();await page.keyboard.down('Shift');await page.mouse.move(b.x+b.width/2+1,b.y+b.height/2);await page.keyboard.up('Shift');await wait(async()=>(await padding()).every(row=>row.every(v=>v==='22px')));assert.equal(read(),uniform);await page.keyboard.press('Escape');await page.mouse.up();await wait(async()=>(await padding()).every(row=>row.every(v=>v==='12px')));
    b=await begin();await page.keyboard.down('Alt');await page.mouse.move(b.x+b.width/2+5,b.y+b.height/2);await page.keyboard.up('Alt');await page.mouse.up();await settled();await wait(async()=>(await padding()).every(row=>row.every(v=>v==='12.5px')));assert.notEqual(read(),uniform);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),uniform);await wait(async()=>(await padding()).every(row=>row.every(v=>v==='12px')));
   }
   if(process.env.RT_E2E_SHARED_SPACING_CONTEXT){
    const label=field.locator('..').locator(':scope > span'),start=async()=>{await wait(async()=>{try{await label.scrollIntoViewIfNeeded();return true;}catch(error){if(/not attached/.test(error.message))return false;throw error;}});const b=await label.boundingBox();await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();return {x:b.x+b.width/2,y:b.y+b.height/2};};
    const emExpected=await app.locator('h1,p.other-font').evaluateAll(nodes=>nodes.map(el=>parseFloat(getComputedStyle(el).fontSize)*1.5));
    await field.fill('1em');let point=await start();await page.keyboard.down('Alt');await page.mouse.move(point.x+5,point.y);await page.keyboard.up('Alt');await wait(async()=>(await padding()).every((row,i)=>row.every(v=>Math.abs(parseFloat(v)-emExpected[i])<.01)));assert.equal(await field.inputValue(),'1.5em');assert.equal(read(),uniform);await page.mouse.up();await settled();assert.match(read(),/1\.5em/);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),uniform);
    point=await start();await page.mouse.move(point.x+7,point.y);await wait(async()=>(await padding()).every(row=>row.every(v=>v==='19px')));const viewport=page.viewportSize();await page.evaluate(()=>{window.__spacingResizeObserved=false;window.addEventListener('resize',()=>{window.__spacingResizeObserved=true;},{once:true});});await page.setViewportSize({width:viewport.width-40,height:viewport.height});await wait(()=>page.evaluate(()=>window.__spacingResizeObserved));await page.mouse.up();await settled();assert.equal(read(),uniform,'window resize cancels drag');await wait(async()=>(await padding()).every(row=>row.every(v=>v==='12px')));await page.setViewportSize(viewport);
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.equal(await page.getByLabel('Style screen scope',{exact:true}).inputValue(),'md:');
    point=await start();await page.mouse.move(point.x+5,point.y);assert.equal(await field.inputValue(),'5');assert.ok((await padding()).every(row=>row.every(v=>v==='0px')),'inactive range does not preview on phone');assert.equal(read(),uniform);await page.mouse.up();await settled();assert.notEqual(read(),uniform);assert.ok((await padding()).every(row=>row.every(v=>v==='0px')));await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>(await padding()).every(row=>row.every(v=>v==='5px')));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),uniform);await wait(async()=>(await padding()).every(row=>row.every(v=>v==='12px')));
   }
   assert.equal(await app.locator('p.named-font').evaluate(el=>getComputedStyle(el).padding),'0px');
   await page.getByLabel('Shared Padding left',{exact:true}).fill('24');await page.getByLabel('Shared Padding left',{exact:true}).press('Enter');await settled();await wait(async()=>(await padding()).every(row=>row.join(',')==='12px,12px,12px,24px'));
   assert.equal(await field.inputValue(),'');assert.equal(await field.getAttribute('placeholder'),'Mixed');
   if(process.env.RT_E2E_SHARED_PADDING_SCREENSHOT){await page.locator('#panel').evaluate(panel=>{panel.scrollTop+=panel.querySelector('[data-shared-section=layout]').getBoundingClientRect().top-panel.getBoundingClientRect().top-50;});await page.locator('#panel').screenshot({path:process.env.RT_E2E_SHARED_PADDING_SCREENSHOT});}
   const edged=read();await page.getByRole('button',{name:'Reset selected padding',exact:true}).click();await settled();await wait(async()=>(await padding()).every(row=>row.every(v=>v==='0px')));assert.doesNotMatch(read(),/md:!?p[trbl]-\[/);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),edged);
   await field.fill('-1');await field.press('Enter');assert.equal(await field.evaluate(el=>el.checkValidity()),false);await field.press('Escape');
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>(await padding()).every(row=>row.every(v=>v==='0px')));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),uniform);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),before);
   console.log('SHARED PADDING SCOPED EDIT/MIXED/VALIDATION/UNDO PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_PAINT_MODELS){
   const before=read(),target=app.locator('h1'),style=await target.getAttribute('style'),label=kind==='html'?'Text color (CSS)':'Text color with alpha',open=async()=>{await page.getByRole('button',{name:'Edit '+label,exact:true}).click();return page.getByRole('dialog',{name:'Edit '+label,exact:true});};
   let dialog=await open();const value=()=>dialog.getByLabel('Color value',{exact:true}),model=()=>dialog.getByLabel('Color model',{exact:true}),profile=()=>dialog.getByLabel('Color profile',{exact:true});
   await value().fill('rgb(255 0 0 / 0.2345)');for(const name of ['hex','hsl','hsb','css','rgb']){await model().selectOption(name);assert.equal(await value().inputValue(),'rgb(255 0 0 / 0.2345)');assert.equal(read(),before);}
   await model().selectOption('hsb');assert.equal(await dialog.getByLabel('Brightness (%)',{exact:true}).inputValue(),'100');await dialog.getByLabel('Hue (deg)',{exact:true}).fill('180');await dialog.getByLabel('Saturation (%)',{exact:true}).fill('50');await dialog.getByLabel('Brightness (%)',{exact:true}).fill('80');assert.equal(await value().inputValue(),'rgb(102 204 204 / 0.2345)');assert.equal(read(),before);
   await dialog.getByLabel('Brightness (%)',{exact:true}).fill('0');await dialog.getByLabel('Hue (deg)',{exact:true}).fill('240');await dialog.getByLabel('Saturation (%)',{exact:true}).fill('100');await dialog.getByLabel('Opacity value (%)',{exact:true}).fill('23.45');await dialog.getByLabel('Brightness (%)',{exact:true}).fill('100');assert.equal(await value().inputValue(),'rgb(0 0 255 / 0.2345)');
   await model().selectOption('css');assert.equal(await dialog.getByLabel('Red (0–255)',{exact:true}).isVisible(),false);assert.ok(await value().isVisible());await model().selectOption('hex');await dialog.getByLabel('Hex color',{exact:true}).fill('1234');assert.equal(await value().inputValue(),'#11223344');await dialog.getByLabel('Hex color',{exact:true}).fill('AABBCC80');assert.equal(await value().inputValue(),'#aabbcc80');await dialog.getByLabel('Hex color',{exact:true}).fill('zzzz');await dialog.getByRole('button',{name:'Apply color',exact:true}).click();assert.ok(await dialog.isVisible());assert.equal(read(),before);await value().fill('rgb(18 52 86 / 0.2345)');await dialog.getByLabel('Hex color',{exact:true}).fill('ABCDEF');assert.equal(await value().inputValue(),'rgb(171 205 239 / 0.2345)');await page.keyboard.press('Escape');assert.equal(await target.getAttribute('style'),style);assert.equal(read(),before);
   dialog=await open();await value().fill('rgb(18 52 86 / 0.2345)');await profile().selectOption('display-p3');const p3=await value().inputValue();assert.match(p3,/^color\(display-p3 .* \/ 0.2345\)$/);assert.ok(await dialog.getByLabel('Display P3 red',{exact:true}).isVisible());await model().selectOption('css');assert.equal(await dialog.getByLabel('Display P3 red',{exact:true}).isVisible(),false);assert.equal(await value().inputValue(),p3);await profile().selectOption('srgb');assert.equal(await value().inputValue(),'rgb(18 52 86 / 0.2345)');await profile().selectOption('display-p3');assert.equal(read(),before);await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes('0.2345'));dialog=await open();assert.equal(await profile().inputValue(),'display-p3');await page.keyboard.press('Escape');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   dialog=await open();const wide='color(display-p3 1 0 0 / 0.3456)';await value().fill(wide);await profile().selectOption('srgb');assert.equal(await profile().inputValue(),'display-p3');assert.equal(await value().inputValue(),wide);assert.equal(read(),before);await dialog.getByRole('button',{name:'Keep Display P3',exact:true}).click();assert.equal(await value().inputValue(),wide);assert.equal(await dialog.getByRole('button',{name:'Convert to sRGB',exact:true}).count(),0);await profile().selectOption('srgb');await value().fill('color(display-p3 0.2 0.4 0.6 / 0.3456)');assert.equal(await dialog.getByRole('button',{name:'Convert to sRGB',exact:true}).count(),0);await value().fill(wide);await profile().selectOption('srgb');if(process.env.RT_E2E_PAINT_MODELS_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_PAINT_MODELS_SCREENSHOT});await dialog.getByRole('button',{name:'Convert to sRGB',exact:true}).click();assert.equal(await value().inputValue(),'rgb(255 0 0 / 0.3456)');assert.equal(await profile().inputValue(),'srgb');assert.equal(read(),before);await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();assert.ok(read().includes('0.3456'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.equal(await target.getAttribute('style'),style);assert.deepEqual(errors,[]);console.log('PAINT MODELS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_EYEDROPPER){
   const before=read(),target=app.locator('h1'),originalStyle=await target.getAttribute('style'),label=kind==='html'?'Text color (CSS)':'Text color with alpha',open=async()=>{await page.getByRole('button',{name:'Edit '+label,exact:true}).click();return page.getByRole('dialog',{name:'Edit '+label,exact:true});},sample=dialog=>dialog.getByRole('button',{name:'Pick color from screen',exact:true});
   const nativeAvailable=await page.evaluate(()=>typeof EyeDropper==='function');
   await page.evaluate(()=>{window.originalEyeDropper=window.EyeDropper;window.EyeDropper=undefined;});let dialog=await open();assert.ok(await sample(dialog).isDisabled());assert.match(await sample(dialog).getAttribute('title'),/unavailable/);await page.keyboard.press('Escape');
   if(process.env.RT_E2E_NATIVE_SAMPLER){
    const swift=fs.readFileSync(path.resolve(__dirname,'../../../desktop/Sources/Retouch.swift'),'utf8'),script=/static let colorSamplerScript = #"""([\s\S]*?)"""#/.exec(swift)?.[1];assert.ok(script);
    await page.evaluate(script=>{window.eyeCalls=0;window.EyeDropper=undefined;window.webkit={messageHandlers:{retouchColorSampler:{postMessage(){window.eyeCalls++;return new Promise(resolve=>{window.resolveEye=resolve;window.rejectEye=error=>resolve({error:error.name});});}}}};(0,eval)(script);const open=window.RetouchNativeEyeDropper.prototype.open;window.RetouchNativeEyeDropper.prototype.open=function(options){window.eyeSignal=options.signal;return open.call(this,options);};},script);
   }else await page.evaluate(()=>{window.eyeCalls=0;window.EyeDropper=class{open({signal}){window.eyeCalls++;window.eyeSignal=signal;return new Promise((resolve,reject)=>{window.resolveEye=resolve;window.rejectEye=reject;});}};});
   dialog=await open();await dialog.getByLabel('Color value',{exact:true}).fill('rgb(18 52 86 / 0.2345)');await sample(dialog).click();assert.ok(await sample(dialog).isDisabled());assert.equal(await page.evaluate(()=>eyeCalls),1);assert.equal(read(),before);await page.evaluate(()=>resolveEye({sRGBHex:'#aabbcc'}));await wait(async()=>!await sample(dialog).isDisabled());assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),'rgb(170 187 204 / 0.2345)');assert.equal(read(),before);assert.notEqual(await target.getAttribute('style'),originalStyle);await page.keyboard.press('Escape');assert.equal(await target.getAttribute('style'),originalStyle);assert.equal(read(),before);
   dialog=await open();const initial=await dialog.getByLabel('Color value',{exact:true}).inputValue();await sample(dialog).click();await page.evaluate(()=>rejectEye(new DOMException('Cancelled','AbortError')));await wait(async()=>!await sample(dialog).isDisabled());assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),initial);assert.equal(await dialog.getByRole('status',{name:'Screen color sampling',exact:true}).isVisible(),false);assert.equal(await sample(dialog).evaluate(el=>el===document.activeElement),true);
   await sample(dialog).click();await page.evaluate(()=>rejectEye(new DOMException('Failed','OperationError')));await dialog.getByRole('status',{name:'Screen color sampling',exact:true}).waitFor();assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),initial);await sample(dialog).click();await page.evaluate(()=>resolveEye({sRGBHex:'invalid'}));await wait(async()=>!await sample(dialog).isDisabled());assert.ok(await dialog.getByRole('status',{name:'Screen color sampling',exact:true}).isVisible());assert.equal(read(),before);
   await sample(dialog).click();await page.evaluate(()=>resolveEye({sRGBHex:'#123456'}));await wait(async()=>!await sample(dialog).isDisabled());assert.equal(await dialog.getByRole('status',{name:'Screen color sampling',exact:true}).isVisible(),false);if(process.env.RT_E2E_EYEDROPPER_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_EYEDROPPER_SCREENSHOT});await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();await wait(async()=>await target.evaluate(el=>getComputedStyle(el).color)==='rgb(18, 52, 86)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   dialog=await open();await dialog.getByLabel('Color value',{exact:true}).fill('color(display-p3 1 0.2 0.3 / 0.2345)');await sample(dialog).click();await page.evaluate(()=>resolveEye({sRGBHex:'#123456'}));await wait(async()=>!await sample(dialog).isDisabled());const sampledP3=await dialog.getByLabel('Color value',{exact:true}).evaluate(el=>RetouchPaletteValues.parse(el.value)),expectedP3=await page.evaluate(()=>RetouchPaletteValues.parse(RetouchPaletteValues.convert('#123456','display-p3').value));assert.equal(sampledP3.space,'display-p3');assert.equal(sampledP3.alpha,0.2345);assert.deepEqual(sampledP3.channels,expectedP3.channels);assert.equal(read(),before);await page.keyboard.press('Escape');assert.equal(await target.getAttribute('style'),originalStyle);
   dialog=await open();await sample(dialog).click();await page.keyboard.press('Escape');await wait(()=>page.evaluate(()=>eyeSignal.aborted));await page.evaluate(()=>resolveEye({sRGBHex:'#ff0000'}));assert.equal(await page.getByRole('dialog').count(),0);assert.equal(read(),before);assert.equal(await target.getAttribute('style'),originalStyle);
   dialog=await open();await sample(dialog).click();await dialog.evaluate(el=>el.retouchSourceInput.remove());await wait(()=>page.evaluate(()=>eyeSignal.aborted));await page.evaluate(()=>resolveEye({sRGBHex:'#ff0000'}));assert.equal(await page.getByRole('dialog').count(),0);assert.equal(read(),before);assert.equal(await target.getAttribute('style'),originalStyle);await page.evaluate(()=>{window.EyeDropper=window.originalEyeDropper;});assert.deepEqual(errors,[]);console.log(process.env.RT_E2E_NATIVE_SAMPLER?'NATIVE SAMPLER ADAPTER PASS':'EYEDROPPER INTEGRATION PASS',kind,engine,'native API exposed:',nativeAvailable,'sampling responses simulated');return;
  }
  if(process.env.RT_E2E_RECENT_COLORS){
   const storage='retouch:recent-colors:v1',target=app.locator('h1'),label=kind==='html'?'Text color (CSS)':'Text color with alpha',trigger=()=>page.getByRole('button',{name:'Edit '+label,exact:true}),open=async()=>{await trigger().click();return page.getByRole('dialog',{name:'Edit '+label,exact:true});},apply=async color=>{const before=read(),dialog=await open();await dialog.getByLabel('Color value',{exact:true}).fill(color);await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();},recent=dialog=>dialog.getByRole('toolbar',{name:'Recent colors',exact:true}).getByRole('button'),reload=async()=>{await page.reload();await app.locator('h1').waitFor();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();};
   await page.evaluate(key=>localStorage.setItem(key,'{broken'),storage);let dialog=await open();assert.equal(await recent(dialog).count(),0);await page.keyboard.press('Escape');await apply('rgb(18 52 86 / 0.2345)');dialog=await open();assert.equal(await recent(dialog).count(),1);const beforeCancel=read();await dialog.getByLabel('Color value',{exact:true}).fill('#aa3355');await page.keyboard.press('Escape');assert.equal(read(),beforeCancel);dialog=await open();assert.equal(await recent(dialog).count(),1);await page.keyboard.press('Escape');await apply('#aa3355');const secondSource=read(),secondColor=await target.evaluate(el=>getComputedStyle(el).color);
   dialog=await open();assert.equal(await recent(dialog).count(),2);await recent(dialog).nth(0).focus();await page.keyboard.press('ArrowRight');assert.equal(await recent(dialog).nth(1).evaluate(el=>el===document.activeElement),true);await page.keyboard.press('Enter');assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),'rgb(18 52 86 / 0.2345)');assert.equal(read(),secondSource);assert.notEqual(await target.evaluate(el=>getComputedStyle(el).color),secondColor);await page.keyboard.press('Escape');assert.equal(await target.evaluate(el=>getComputedStyle(el).color),secondColor);
   dialog=await open();await recent(dialog).nth(1).click();await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==secondSource);await settled();await apply('rgb(170 51 85)');dialog=await open();assert.equal(await recent(dialog).count(),2);assert.equal(await recent(dialog).nth(0).getAttribute('data-color'),'rgb(170 51 85)');await page.keyboard.press('Escape');await reload();dialog=await open();assert.equal(await recent(dialog).count(),2);assert.equal(await recent(dialog).nth(1).getAttribute('data-color'),'rgb(18 52 86 / 0.2345)');await page.keyboard.press('Escape');
   await apply('color(display-p3 0.2 0.4 0.6 / 0.3456)');dialog=await open();assert.equal(await recent(dialog).nth(0).getAttribute('data-color'),'color(display-p3 0.2 0.4 0.6 / 0.3456)');await recent(dialog).nth(0).click();assert.equal(await dialog.getByLabel('Color value',{exact:true}).inputValue(),'color(display-p3 0.2 0.4 0.6 / 0.3456)');await page.keyboard.press('Escape');
   await page.evaluate(key=>{window.originalRecentSetItem=Storage.prototype.setItem;Storage.prototype.setItem=function(name,value){if(name===key)throw new DOMException('Storage full','QuotaExceededError');return originalRecentSetItem.call(this,name,value);};},storage);await apply('#aabbcc');dialog=await open();assert.equal(await recent(dialog).nth(0).getAttribute('data-color'),'#aabbcc');await page.keyboard.press('Escape');await page.evaluate(()=>{Storage.prototype.setItem=originalRecentSetItem;delete window.originalRecentSetItem;});
   await page.evaluate(key=>localStorage.setItem(key,JSON.stringify([null,'not-a-color',...Array.from({length:20},(_,i)=>'hsl('+i*18+' 70% 50% / '+(i%3===0?'0.4':'1')+')'),'hsl(0 70% 50% / 0.4)'])),storage);await reload();dialog=await open();assert.equal(await recent(dialog).count(),12);await recent(dialog).nth(0).focus();await page.keyboard.press('End');assert.equal(await recent(dialog).nth(11).evaluate(el=>el===document.activeElement),true);await page.keyboard.press('Home');assert.equal(await recent(dialog).nth(0).evaluate(el=>el===document.activeElement),true);if(process.env.RT_E2E_RECENT_COLORS_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_RECENT_COLORS_SCREENSHOT});const beforeClear=read();await dialog.getByRole('button',{name:'Clear recent colors',exact:true}).click();assert.equal(await recent(dialog).count(),0);assert.equal(await page.evaluate(key=>localStorage.getItem(key),storage),null);assert.equal(read(),beforeClear);await page.keyboard.press('Escape');assert.deepEqual(errors,[]);console.log('RECENT COLORS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_PAINT_SCOPE){
   const before=read(),target=app.locator('h1'),screen=page.getByLabel('Screen size',{exact:true}),prefix=kind==='liquid'?'Gradient':'Fill';
   for(const [label,property] of [[kind==='html'?'Text color (CSS)':'Text color with alpha','color'],[kind==='html'?'Background color (CSS)':'Background color with alpha','background-color'],[kind==='html'?'Border color (CSS)':'Border color with alpha','border-top-color'],['Shadow 1 color','box-shadow'],[prefix+' 1 stop 1 color','background-image']]){
    await screen.focus();await screen.selectOption('768x1024');await settled();await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='html'?'min-[768px]:':'md:');await screen.focus();await screen.selectOption('390x844');await settled();const appearance=()=>target.evaluate((el,p)=>({value:getComputedStyle(el).getPropertyValue(p),style:el.getAttribute('style')}),property),original=await appearance(),trigger=page.getByRole('button',{name:'Edit '+label,exact:true}),miniature=page.getByLabel(prefix+' 1 preview',{exact:true}),initialMini=property==='background-image'?await miniature.evaluate(el=>el.style.backgroundImage):null;
    const open=async()=>{const parents=page.getByLabel(label,{exact:true}).locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await trigger.click();const dialog=page.getByRole('dialog',{name:'Edit '+label,exact:true});assert.ok(await dialog.getByRole('status',{name:'Color edit range',exact:true}).isVisible());assert.match(await dialog.getByRole('status',{name:'Color edit range',exact:true}).textContent(),/current canvas is outside this range/);await dialog.getByLabel('Color value',{exact:true}).fill('rgb(88 66 44 / 0.3456)');assert.deepEqual(await appearance(),original,'picker outside edit range must not override canvas');assert.equal(read(),before);if(initialMini!==null)assert.notEqual(await miniature.evaluate(el=>el.style.backgroundImage),initialMini);if(process.env.RT_E2E_PAINT_SCOPE_SCREENSHOT)await dialog.screenshot({path:process.env.RT_E2E_PAINT_SCOPE_SCREENSHOT});return dialog;};
    let dialog=await open();await page.keyboard.press('Escape');await dialog.waitFor({state:'detached'});assert.deepEqual(await appearance(),original);if(initialMini!==null)assert.equal(await miniature.evaluate(el=>el.style.backgroundImage),initialMini);dialog=await open();await dialog.getByRole('button',{name:'Apply color',exact:true}).click();await wait(()=>read()!==before);await settled();assert.deepEqual(await appearance(),original);await screen.focus();await screen.selectOption('768x1024');await settled();assert.notEqual((await appearance()).value,original.value);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await appearance(),original);
   }
   assert.deepEqual(errors,[]);console.log('PAINT SCOPE PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_GRADIENT_SCOPE){
   const before=read(),target=app.locator('h1'),image=()=>target.evaluate(el=>getComputedStyle(el).backgroundImage),originalImage=await image(),originalStyle=await target.getAttribute('style'),prefix=kind==='liquid'?'Gradient':'Fill',screen=page.getByLabel('Screen size',{exact:true});
   for(const [name,index] of [[prefix+' 1 stop 1 handle',1],[prefix+' 1 angle',1],[prefix+' 2 center',2],[prefix+' 1 Angle '+(kind==='liquid'?'(deg)':'(°)'),1],[prefix+' 1 stop 1 '+(kind==='liquid'?'Position':'position')+' (%)',1],[prefix+' 2 Center X (%)',2]]){
    await screen.focus();await screen.selectOption('768x1024');await settled();await page.getByLabel('Style screen scope',{exact:true}).selectOption(kind==='liquid'?'md:':'min-[768px]:');await screen.focus();await screen.selectOption('390x844');await settled();await wait(async()=>await page.getByRole('status',{name:'Edit range status',exact:true}).getAttribute('data-match')==='false');
    const miniature=page.getByLabel(prefix+' '+index+' preview',{exact:true}),originalMini=await miniature.evaluate(el=>el.style.backgroundImage),drag=async cancel=>{const control=page.getByLabel(name,{exact:true}),handle=await control.evaluate(el=>el.tagName==='INPUT')?control.locator('xpath=..').locator('[data-numeric-scrub]'):control,parents=handle.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}let box;await wait(async()=>{await handle.hover();box=await handle.boundingBox();return !!box;});await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+20,box.y+box.height/2+10,{steps:3});assert.notEqual(await miniature.evaluate(el=>el.style.backgroundImage),originalMini);assert.equal(await image(),originalImage,'out-of-range gradient drag must not override the canvas');assert.equal(await target.getAttribute('style'),originalStyle);assert.equal(read(),before);if(cancel)await page.keyboard.press('Escape');await page.mouse.up();};
    await drag(true);assert.equal(await miniature.evaluate(el=>el.style.backgroundImage),originalMini);assert.equal(read(),before);await drag(false);await wait(()=>read()!==before);await settled();assert.equal(await image(),originalImage);await screen.focus();await screen.selectOption('768x1024');await settled();assert.notEqual(await image(),originalImage);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.equal(await image(),originalImage);
   }
   assert.deepEqual(errors,[]);console.log('GRADIENT SCOPE PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_GRADIENT_INTERRUPTION){
   const target=app.locator('h1'),before=read(),style=await target.getAttribute('style'),image=()=>target.evaluate(el=>getComputedStyle(el).backgroundImage),originalImage=await image(),prefix=kind==='liquid'?'Gradient':'Fill';
   for(const name of [prefix+' 1 stop 1 handle',prefix+' 1 angle',prefix+' 2 center'])for(const interruption of ['screen','resize','zoom','runtime']){
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();const handle=page.getByLabel(name,{exact:true}),parents=handle.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}let box;await wait(async()=>{await handle.hover();box=await handle.boundingBox();return !!box;});await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+20,box.y+box.height/2+10,{steps:3});assert.notEqual(await image(),originalImage);assert.equal(read(),before);
    if(interruption==='screen')await page.evaluate(()=>RetouchScreens.set({width:390,height:844}));else if(interruption==='resize')await page.setViewportSize({width:page.viewportSize().width-10,height:1000});else if(interruption==='zoom')await page.locator('#canvasZoom').evaluate(el=>{el.value=Number(el.value)===80?'100':'80';el.dispatchEvent(new Event('change',{bubbles:true}));});else{await target.evaluate(el=>el.style.setProperty('background-image','linear-gradient(lime, blue)','important'));await page.keyboard.press('Escape');}
    if(interruption!=='runtime')await wait(async()=>await target.getAttribute('style')===style);await page.mouse.up();await settled();assert.equal(read(),before,name+' '+interruption);if(interruption==='runtime'){assert.equal(await target.evaluate(el=>el.style.backgroundImage),'linear-gradient(lime, blue)');await target.evaluate((el,value)=>{if(value===null)el.removeAttribute('style');else el.setAttribute('style',value);},style);}else{assert.equal(await target.getAttribute('style'),style);assert.equal(await image(),originalImage);}
   }
   assert.deepEqual(errors,[]);console.log('GRADIENT INTERRUPTION PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_NUMERIC_RESPONSIVE){
   assert.equal(kind,'html');const before=read(),target=app.locator('h1'),originalStyle=await target.getAttribute('style'),field=page.getByLabel('Width (CSS)',{exact:true}),width=()=>target.evaluate(el=>getComputedStyle(el).width),screen=page.getByLabel('Screen size',{exact:true});await screen.focus();await screen.selectOption('768x1024');await settled();await page.getByLabel('Style screen scope',{exact:true}).selectOption('min-[768px]:');await settled();
   const start=async(source=before)=>{const parents=field.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}const handle=field.locator('xpath=..').locator('[data-numeric-scrub]');await handle.hover();const box=await handle.boundingBox();await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+20,box.y+box.height/2,{steps:3});assert.equal(read(),source);};
   await start();assert.equal(await width(),'120px');await page.evaluate(()=>RetouchScreens.set({width:390,height:844}));assert.equal(await target.getAttribute('style'),originalStyle,'screen change cancels the active preview');await page.mouse.up();await settled();assert.equal(await width(),'100px');assert.equal(read(),before);
   await screen.focus();await screen.selectOption('768x1024');await settled();
   for(const change of [()=>page.setViewportSize({width:1550,height:1000}),()=>page.locator('#canvasZoom').evaluate(el=>{el.value='80';el.dispatchEvent(new Event('change',{bubbles:true}));})]){await start();assert.equal(await width(),'120px');await change();await wait(async()=>await target.getAttribute('style')===originalStyle);await page.mouse.up();assert.equal(await width(),'100px');assert.equal(read(),before);}
   await start();assert.equal(await width(),'120px');await page.mouse.up();await wait(()=>read()!==before);await settled();const saved=read();assert.match(saved,/min-width:\s*768px/);assert.equal(await width(),'120px');await screen.focus();await screen.selectOption('390x844');await settled();assert.equal(await width(),'100px');await start(saved);assert.equal(await width(),'100px');assert.equal(await field.inputValue(),'140px');await page.mouse.up();await wait(()=>read()!==saved);await settled();await screen.focus();await screen.selectOption('768x1024');await settled();assert.equal(await width(),'140px');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===saved);await settled();assert.equal(await width(),'120px');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await screen.focus();await screen.selectOption('768x1024');await settled();assert.equal(await width(),'100px');assert.deepEqual(errors,[]);console.log('NUMERIC RESPONSIVE PASS',engine);return;
  }
  if(process.env.RT_E2E_HTML_EFFECT_NUMERIC){
   assert.equal(kind,'html');const before=read(),target=app.locator('h1'),originalStyle=await target.getAttribute('style'),transform=await target.evaluate(el=>getComputedStyle(el).transform),get=property=>target.evaluate((el,p)=>getComputedStyle(el).getPropertyValue(p),property),shadows=async()=>page.evaluate(value=>RetouchHTMLCSSValues.parseShadows(value),await get('box-shadow'));
   for(const [label,property,delta,key] of [['Opacity (%)','opacity',-12],['Rotation (°)','rotate',15],['Layer blur (px)','filter',3],['Background blur (px)','backdrop-filter',4],['Shadow 1 X (px)','box-shadow',5,'x'],['Shadow 1 Y (px)','box-shadow',-2,'y'],['Shadow 1 Blur (px)','box-shadow',4,'blur'],['Shadow 1 Spread (px)','box-shadow',-3,'spread']]){
    const field=page.getByLabel(label,{exact:true}),originalValue=await get(property),originalShadow=await shadows(),value=Number(await field.inputValue());let expected;
    if(property==='box-shadow')expected=originalShadow.map((item,i)=>i===0?{...item,[key]:value+delta}:item);else if(property.endsWith('filter'))expected=await page.evaluate(({raw,n})=>RetouchHTMLCSSValues.withBlur(raw,n),{raw:originalValue,n:value+delta});else expected=property==='opacity'?String((value+delta)/100):(value+delta)+'deg';
    const state=()=>property==='box-shadow'?shadows():get(property),scrub=async cancel=>{const groups=field.locator('xpath=ancestor::details');for(let i=0;i<await groups.count();i++){const group=groups.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await field.scrollIntoViewIfNeeded();const handle=field.locator('xpath=..').locator('[data-numeric-scrub]');await handle.hover();const box=await handle.boundingBox();await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+delta,box.y+box.height/2,{steps:3});assert.equal(read(),before);assert.deepEqual(await state(),expected);assert.equal(await get('transform'),transform);if(cancel)await page.keyboard.press('Escape');await page.mouse.up();};
    await scrub(true);assert.equal(await get(property),originalValue);assert.equal(await target.getAttribute('style'),originalStyle);assert.equal(read(),before);await scrub(false);await wait(()=>read()!==before);await settled();await wait(async()=>JSON.stringify(await state())===JSON.stringify(expected));assert.equal(await target.getAttribute('style'),originalStyle);assert.equal(await get('transform'),transform);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.equal(await get(property),originalValue);
   }
   for(const [label,property,raw,delta,result] of [['Width (CSS)','width','50%',10,'60%'],['Height (CSS)','height','2em',1,'3em'],['Padding (CSS)','padding','1em',1,'2em'],['Padding top (CSS)','padding-top','1em',-4,'0em'],['Margin (CSS)','margin','-1em',-1,'-2em'],['Border width (CSS)','border-width','1px',2,'3px'],['Corner radius (CSS)','border-radius','1em',1,'2em'],['Font size (CSS)','font-size','2em',1,'3em'],['Font size (CSS)','font-size','50%',10,'60%'],['Letter spacing (CSS)','letter-spacing','0.125rem',1,'1.125rem'],['Line height (CSS)','line-height','1.5',1,'2.5'],['Letter spacing (CSS)','letter-spacing','0.125em',1,'1.125em'],['Font weight (CSS)','font-weight','500',100,'600']]){
    const field=page.getByLabel(label,{exact:true}),parents=field.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await field.fill(raw);await field.press('Tab');await wait(()=>read()!==before);await settled();const typed=read(),prior=await get(property);
    const scrub=async(cancel,during=null)=>{const details=field.locator('xpath=ancestor::details');for(let i=0;i<await details.count();i++){const group=details.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await field.scrollIntoViewIfNeeded();const handle=field.locator('xpath=..').locator('[data-numeric-scrub]');await handle.hover();const box=await handle.boundingBox();await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+delta,box.y+box.height/2,{steps:3});assert.equal(await field.inputValue(),result);assert.equal(read(),typed);const rendered=await get(property);assert.notEqual(rendered,prior);if(during)await during();if(cancel)await page.keyboard.press('Escape');await page.mouse.up();return rendered;};
    await scrub(true);assert.equal(await field.inputValue(),raw);assert.equal(await get(property),prior);assert.equal(read(),typed);if(property==='padding'){await scrub(true,()=>target.evaluate(el=>el.style.setProperty('padding-left','7px','important')));assert.equal(await get('padding-left'),'7px');assert.equal(await get('padding-top'),prior);assert.equal(await get('padding-right'),prior);assert.equal(await get('padding-bottom'),prior);assert.equal(read(),typed);await target.evaluate((el,style)=>{if(style===null)el.removeAttribute('style');else el.setAttribute('style',style);},originalStyle);}const expected=await scrub(false);await wait(()=>read()!==typed);await settled();await wait(async()=>await get(property)===expected);assert.equal(await field.inputValue(),result);assert.equal(await target.getAttribute('style'),originalStyle);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===typed);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   }
   const automatic=page.getByLabel('Line height (CSS)',{exact:true});assert.equal(await automatic.inputValue(),'normal');const automaticSource=read(),automaticStyle=await get('line-height'),handle=automatic.locator('xpath=..').locator('[data-numeric-scrub]');await handle.hover();const box=await handle.boundingBox();await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+8,box.y+box.height/2);await page.mouse.up();assert.equal(await automatic.inputValue(),'normal');assert.equal(read(),automaticSource);assert.equal(await get('line-height'),automaticStyle);assert.equal(read(),before);
   assert.deepEqual(errors,[]);console.log('HTML EFFECT NUMERIC PASS',engine);return;
  }
  if(process.env.RT_E2E_HTML_GRADIENT_NUMERIC){
   const before=read(),target=app.locator('h1'),originalStyle=await target.getAttribute('style'),state=async()=>page.evaluate(value=>RetouchHTMLCSSValues.parseGradients(value),await target.evaluate(el=>getComputedStyle(el).backgroundImage)),baseline=await state();assert.equal(baseline.length,2);
   for(const [rawName,index,key,delta] of [['Fill 1 Angle (°)',0,'angle',25],['Fill 1 stop 1 position (%)',0,'stop',60],['Fill 2 Center X (%)',1,'x',10],['Fill 2 Center Y (%)',1,'y',-10]]){
    const prefix=kind==='liquid'?'Gradient':'Fill',name=kind==='liquid'?rawName.replace('Fill','Gradient').replace('(°)','(deg)').replace(' position',' Position'):rawName,field=page.getByLabel(name,{exact:true}),expected=structuredClone(baseline);assert.equal(Number(await field.inputValue()),key==='stop'?baseline[index].stops[0].position:baseline[index][key]);if(key==='stop'){expected[index].stops[0].position+=delta;expected[index].stops.sort((a,b)=>a.position-b.position);}else expected[index][key]+=delta;
    const scrub=async cancel=>{const groups=field.locator('xpath=ancestor::details');for(let i=0;i<await groups.count();i++){const group=groups.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await field.scrollIntoViewIfNeeded();const handle=field.locator('xpath=..').locator('[data-numeric-scrub]');await handle.hover();const box=await handle.boundingBox();await page.mouse.move(box.x+box.width/2,box.y+box.height/2);await page.mouse.down();await page.mouse.move(box.x+box.width/2+delta,box.y+box.height/2,{steps:3});assert.equal(read(),before);assert.deepEqual(await state(),expected);assert.deepEqual(await page.evaluate(value=>RetouchHTMLCSSValues.parseGradients(value),await page.getByLabel(prefix+' '+(index+1)+' preview',{exact:true}).evaluate(el=>el.style.backgroundImage)),[expected[index]]);if(cancel)await page.keyboard.press('Escape');await page.mouse.up();};
    await scrub(true);assert.deepEqual(await state(),baseline);assert.equal(await target.getAttribute('style'),originalStyle);assert.equal(read(),before);await scrub(false);await wait(()=>read()!==before);await settled();await wait(async()=>JSON.stringify(await state())===JSON.stringify(expected));assert.equal(await target.getAttribute('style'),originalStyle);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await state(),baseline);
   }
   assert.deepEqual(errors,[]);console.log('HTML GRADIENT NUMERIC PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_GRADIENTS){
   assert.ok(['react','liquid'].includes(kind));const before=read(),states=[before],open=async()=>{const summary=page.getByText('Gradient fills',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();},checkpoint=async()=>{await wait(()=>read()!==states.at(-1));await settled();states.push(read());await open();},appearance=()=>app.locator('h1').evaluate(el=>{const css=getComputedStyle(el);return [css.backgroundColor,css.backgroundSize,css.backgroundPosition,css.backgroundRepeat];}),baseline=await appearance();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('md:');await settled();assert.equal(await page.getByText('Gradient fills',{exact:true}).evaluate(el=>el.parentElement.open),false);assert.equal(await page.getByRole('button',{name:'Clear background images',exact:true}).isVisible(),false);await page.getByRole('button',{name:'Add gradient fill',exact:true}).click();await wait(async()=>await page.getByLabel('Gradient 1 type',{exact:true}).isVisible());await checkpoint();const beforeLastFillRemoval=read();await page.getByRole('button',{name:'Remove gradient 1',exact:true}).press('Enter');await wait(()=>read()!==beforeLastFillRemoval);await settled();await wait(async()=>await page.getByRole('button',{name:'Add gradient fill',exact:true}).evaluate(el=>el===document.activeElement));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeLastFillRemoval);await settled();const pickerCanvas=()=>app.locator('h1').evaluate(el=>({image:getComputedStyle(el).backgroundImage,style:el.getAttribute('style')})),pickerCanvasBefore=await pickerCanvas();const beforePicker=read(),pickerButton=page.getByRole('button',{name:'Edit Gradient 1 stop 1 color',exact:true});await pickerButton.click();const picker=page.getByRole('dialog',{name:'Edit Gradient 1 stop 1 color',exact:true});await picker.getByLabel('Color value',{exact:true}).fill('hsl(210deg 50% 40% / 50%)');await page.evaluate(()=>renderPanel());assert.ok(await picker.isVisible());assert.equal(await picker.getByLabel('Color value',{exact:true}).inputValue(),'hsl(210deg 50% 40% / 50%)');await picker.getByRole('slider',{name:'Saturation and brightness',exact:true}).press('ArrowRight');assert.notEqual((await pickerCanvas()).image,pickerCanvasBefore.image);assert.ok((await pickerCanvas()).style.includes('background-image:'));assert.equal(read(),beforePicker);await page.keyboard.press('Escape');await picker.waitFor({state:'detached'});assert.equal(read(),beforePicker);assert.deepEqual(await pickerCanvas(),pickerCanvasBefore);await pickerButton.click();await picker.getByLabel('Color value',{exact:true}).fill('not-a-color');await picker.getByRole('button',{name:'Apply color',exact:true}).click();assert.ok(await picker.isVisible());assert.equal(read(),beforePicker);await picker.getByLabel('Color value',{exact:true}).fill('color(display-p3 1 0.2 0.3 / 0.4)');assert.ok(await picker.getByLabel('Display P3 red',{exact:true}).isVisible());await picker.getByRole('slider',{name:'Opacity (%)',exact:true}).press('Home');assert.ok((await picker.getByLabel('Color value',{exact:true}).inputValue()).includes('display-p3 1 0.2 0.3 / 0'));await picker.getByLabel('Color value',{exact:true}).fill('#33669980');await picker.getByLabel('Opacity value (%)',{exact:true}).fill('50');assert.equal(await picker.getByLabel('Color value',{exact:true}).inputValue(),'rgb(51 102 153 / 0.5)');await picker.getByLabel('Red (0–255)',{exact:true}).fill('90');assert.equal(await picker.getByLabel('Color value',{exact:true}).inputValue(),'rgb(90 102 153 / 0.5)');await picker.getByLabel('Color model',{exact:true}).selectOption('hsl');assert.equal(await picker.getByLabel('Color value',{exact:true}).inputValue(),'rgb(90 102 153 / 0.5)');await picker.getByLabel('Hue (deg)',{exact:true}).fill('120');if(process.env.RT_E2E_PICKER_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_PICKER_SCREENSHOT});await picker.getByLabel('Color value',{exact:true}).press('Enter');await wait(()=>read()!==beforePicker);await settled();assert.equal(await page.getByLabel('Gradient 1 stop 1 color',{exact:true}).evaluate(el=>RetouchColorStyles.fromComputed(el.value)),'#5a995a80');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforePicker);await settled();assert.deepEqual(await pickerCanvas(),pickerCanvasBefore);const stopGeometry=await page.getByLabel('Gradient 1 stop 1 color',{exact:true}).evaluate(el=>{const row=el.closest('.gradient-stop-row'),color=el.getBoundingClientRect(),position=row.querySelector('input[type=number]').getBoundingClientRect(),remove=row.querySelector(':scope > button').getBoundingClientRect();return {tabOrder:row.querySelector('input').type==='number',height:row.getBoundingClientRect().height,ordered:position.right<=color.left+1&&color.right<=remove.left+1,aligned:Math.abs(position.top-color.top)<2};});assert.equal(stopGeometry.tabOrder,true);assert.ok(stopGeometry.height<=36);assert.equal(stopGeometry.ordered,true);assert.equal(stopGeometry.aligned,true);if(process.env.RT_E2E_STOP_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_STOP_SCREENSHOT});await page.getByLabel('Gradient 1 Angle (deg)',{exact:true}).fill('45');await page.getByLabel('Gradient 1 Angle (deg)',{exact:true}).press('Tab');await checkpoint();const angleHandle=page.getByRole('slider',{name:'Gradient 1 angle',exact:true}),beforeRotation=read();await angleHandle.scrollIntoViewIfNeeded();let angleBox=await angleHandle.boundingBox(),anglePreview=await page.getByLabel('Gradient 1 preview',{exact:true}).boundingBox();await page.mouse.move(Math.round(angleBox.x+angleBox.width/2),Math.round(angleBox.y+angleBox.height/2));await page.mouse.down();await page.keyboard.down('Shift');await page.mouse.move(anglePreview.x+anglePreview.width/2+30,anglePreview.y+anglePreview.height/2,{steps:5});assert.equal(await angleHandle.getAttribute('aria-valuenow'),'90');assert.equal(read(),beforeRotation);await page.keyboard.press('Escape');await page.mouse.up();await page.keyboard.up('Shift');assert.equal(read(),beforeRotation);assert.equal(await app.locator('h1').getAttribute('style'),null);angleBox=await angleHandle.boundingBox();await page.mouse.move(Math.round(angleBox.x+angleBox.width/2),Math.round(angleBox.y+angleBox.height/2));await page.mouse.down();await page.keyboard.down('Shift');await page.mouse.move(anglePreview.x+anglePreview.width/2,anglePreview.y+anglePreview.height/2+30,{steps:5});await page.mouse.up();await page.keyboard.up('Shift');await checkpoint();assert.equal(await angleHandle.getAttribute('aria-valuenow'),'180');await wait(async()=>await angleHandle.evaluate(el=>el===document.activeElement));await page.keyboard.press('ArrowRight');await checkpoint();assert.equal(await angleHandle.getAttribute('aria-valuenow'),'181');for(let i=0;i<2;i++){states.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===states.at(-1));await settled();}assert.equal(read(),beforeRotation);
   const draftColor=page.getByLabel('Gradient 1 stop 1 color',{exact:true}),draftBefore=read(),initialColor=await draftColor.inputValue();const swatch=draftColor.locator('xpath=..').locator('.gradient-stop-swatch');assert.equal(await swatch.isVisible(),true);const initialSwatch=await swatch.evaluate(el=>el.style.backgroundImage);await draftColor.fill('not-a-color');await draftColor.press('Enter');assert.ok(await draftColor.evaluate(el=>el.validationMessage));assert.equal(read(),draftBefore);await draftColor.press('Escape');assert.equal(await draftColor.inputValue(),initialColor);assert.equal(await draftColor.evaluate(el=>el.validationMessage),'');await draftColor.fill('#123456');assert.ok((await swatch.evaluate(el=>el.style.backgroundImage)).includes('rgb(18, 52, 86)'));assert.equal(read(),draftBefore);await draftColor.press('Escape');await settled();assert.equal(await swatch.evaluate(el=>el.style.backgroundImage),initialSwatch);assert.equal(read(),draftBefore);const draftPosition=page.getByLabel('Gradient 1 stop 1 Position (%)',{exact:true}),initialPosition=await draftPosition.inputValue();await draftPosition.fill('17');await draftPosition.press('Escape');await settled();assert.equal(await draftPosition.inputValue(),initialPosition);assert.equal(read(),draftBefore);   await page.getByLabel('Gradient 1 stop 1 color',{exact:true}).fill('color(display-p3 1 .2 .1 / .4)');await page.getByLabel('Gradient 1 stop 1 color',{exact:true}).press('Enter');await checkpoint();await page.getByRole('button',{name:'Add gradient 1 stop',exact:true}).click();await checkpoint();await page.getByLabel('Gradient 1 stop 2 Position (%)',{exact:true}).fill('35');await page.getByLabel('Gradient 1 stop 2 Position (%)',{exact:true}).press('Enter');await checkpoint();await page.getByRole('button',{name:'Add gradient fill',exact:true}).click();await checkpoint();await page.getByLabel('Gradient 2 type',{exact:true}).selectOption('radial');await checkpoint();await page.getByLabel('Gradient 2 Center X (%)',{exact:true}).fill('25');await page.getByLabel('Gradient 2 Center X (%)',{exact:true}).press('Tab');await checkpoint();await page.getByLabel('Gradient 2 Size',{exact:true}).selectOption('closest-side');await checkpoint();await page.getByLabel('Gradient 2 Size',{exact:true}).selectOption('custom');await checkpoint();await page.getByLabel('Gradient 2 Radius X',{exact:true}).fill('35%');await page.getByLabel('Gradient 2 Radius X',{exact:true}).press('Tab');await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage.includes('35% 50% at 25% 50%')));if(process.env.RT_E2E_RADIAL_SCREENSHOT){await page.getByLabel('Gradient 2 Size',{exact:true}).scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_RADIAL_SCREENSHOT});}const center=page.getByRole('button',{name:'Gradient 2 center',exact:true}),beforeCenter=read();await center.scrollIntoViewIfNeeded();const centerBox=await center.boundingBox(),previewBox=await page.getByLabel('Gradient 2 preview',{exact:true}).boundingBox();await page.mouse.move(Math.round(centerBox.x+centerBox.width/2),Math.round(centerBox.y+centerBox.height/2));await page.mouse.down();await page.mouse.move(Math.round(centerBox.x+centerBox.width/2)+Math.round(previewBox.width*.1),Math.round(centerBox.y+centerBox.height/2)+Math.round(previewBox.height*.1),{steps:5});assert.equal(read(),beforeCenter);assert.equal(await center.getAttribute('data-x'),'35');assert.equal(await center.getAttribute('data-y'),'60');await page.keyboard.press('Escape');await page.mouse.up();assert.equal(read(),beforeCenter);assert.equal(await app.locator('h1').getAttribute('style'),null);await center.press('ArrowRight');await checkpoint();assert.equal(await center.getAttribute('data-x'),'26');await wait(async()=>await center.evaluate(el=>el===document.activeElement));await page.keyboard.press('Shift+ArrowDown');await checkpoint();assert.equal(await center.getAttribute('data-y'),'60');for(let i=0;i<2;i++){states.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===states.at(-1));await settled();}assert.equal(read(),beforeCenter);await center.scrollIntoViewIfNeeded();const commitBox=await center.boundingBox();await page.mouse.move(Math.round(commitBox.x+commitBox.width/2),Math.round(commitBox.y+commitBox.height/2));await page.mouse.down();await page.mouse.move(Math.round(commitBox.x+commitBox.width/2)+Math.round(previewBox.width*.1),Math.round(commitBox.y+commitBox.height/2)+Math.round(previewBox.height*.1),{steps:5});assert.equal(read(),beforeCenter);await page.mouse.up();await checkpoint();assert.equal(await center.getAttribute('data-x'),'35');assert.equal(await center.getAttribute('data-y'),'60');states.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeCenter);await settled();
   await page.getByRole('button',{name:'Move gradient 2 up',exact:true}).click();await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>{const value=getComputedStyle(el).backgroundImage;return value.startsWith('radial-gradient(')&&value.includes('linear-gradient(45deg')&&value.includes('color(display-p3');}));const beforeDrag=read();let handle=page.getByRole('slider',{name:'Gradient 2 stop 2 handle',exact:true});await handle.scrollIntoViewIfNeeded();let railBox=await page.getByRole('group',{name:'Gradient 2 stop positions',exact:true}).boundingBox(),handleBox=await handle.boundingBox();await page.mouse.move(handleBox.x+handleBox.width/2,handleBox.y+handleBox.height/2);await page.mouse.down();await page.mouse.move(railBox.x+railBox.width*.6,handleBox.y+handleBox.height/2,{steps:8});await wait(async()=>await handle.getAttribute('aria-valuenow')==='60');assert.equal(read(),beforeDrag);await page.mouse.up();await checkpoint();const dragged=read(),paintAfterDrag=await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage);handle=page.getByRole('slider',{name:'Gradient 2 stop 2 handle',exact:true});await handle.scrollIntoViewIfNeeded();railBox=await page.getByRole('group',{name:'Gradient 2 stop positions',exact:true}).boundingBox();handleBox=await handle.boundingBox();await page.mouse.move(handleBox.x+handleBox.width/2,handleBox.y+handleBox.height/2);await page.mouse.down();await page.mouse.move(railBox.x+railBox.width*.2,handleBox.y+handleBox.height/2,{steps:8});await page.keyboard.press('Escape');await page.mouse.up();assert.equal(read(),dragged);assert.equal(await app.locator('h1').getAttribute('style'),null);await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage)===paintAfterDrag);await handle.press('ArrowRight');await checkpoint();handle=page.getByRole('slider',{name:'Gradient 2 stop 2 handle',exact:true});assert.equal(await handle.getAttribute('aria-valuenow'),'61');await wait(async()=>await handle.evaluate(el=>el===document.activeElement));await page.keyboard.press('Shift+ArrowLeft');await checkpoint();assert.equal(await handle.getAttribute('aria-valuenow'),'51');await wait(async()=>await handle.evaluate(el=>el===document.activeElement));
   await page.getByLabel('Gradient 1 type',{exact:true}).selectOption('conic');await checkpoint();await page.getByLabel('Gradient 1 Angle (deg)',{exact:true}).fill('90');await page.getByLabel('Gradient 1 Angle (deg)',{exact:true}).press('Tab');await checkpoint();await page.getByLabel('Gradient 1 Center Y (%)',{exact:true}).fill('60');await page.getByLabel('Gradient 1 Center Y (%)',{exact:true}).press('Tab');await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>{const value=getComputedStyle(el).backgroundImage;return value.startsWith('conic-gradient(from 90deg at 25% 60%')&&value.includes('linear-gradient(45deg');}));
   assert.equal(await page.getByLabel('Gradient 1 Repeat',{exact:true}).isVisible(),false);await page.getByLabel('Gradient 1 options',{exact:true}).click();   await page.getByLabel('Gradient 1 Repeat',{exact:true}).check();await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage.startsWith('repeating-conic-gradient(')));await page.getByLabel('Gradient 1 stop 2 Position (%)',{exact:true}).fill('25');await page.getByLabel('Gradient 1 stop 2 Position (%)',{exact:true}).press('Tab');await checkpoint();await wait(async()=>await page.evaluate(()=>{const fill=RetouchHTMLCSSValues.parseGradients(getComputedStyle(document.querySelector('#app').contentDocument.querySelector('h1')).backgroundImage)[0];return fill.repeat&&fill.stops.at(-1).position===25;}));
   await page.getByLabel('Gradient 1 Color blending',{exact:true}).selectOption('oklch');await checkpoint();assert.equal(await page.getByLabel('Gradient 1 Hue direction',{exact:true}).isVisible(),true,'gradient options remain expanded after source refresh');await page.getByLabel('Gradient 1 Hue direction',{exact:true}).selectOption('longer');await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage.includes('in oklch longer hue')));
   await page.getByRole('slider',{name:'Gradient 1 angle',exact:true}).press('Shift+ArrowRight');await checkpoint();assert.equal(await page.getByRole('slider',{name:'Gradient 1 angle',exact:true}).getAttribute('aria-valuenow'),'105');
   const duplicateBefore=read(),stack=()=>page.evaluate(()=>RetouchHTMLCSSValues.parseGradients(getComputedStyle(document.querySelector('#app').contentDocument.querySelector('h1')).backgroundImage)),stackBefore=await stack();await page.getByRole('button',{name:'Duplicate gradient 1',exact:true}).click();await wait(()=>read()!==duplicateBefore);await settled();assert.deepEqual(await stack(),[stackBefore[0],stackBefore[0],...stackBefore.slice(1)]);await wait(async()=>await page.getByLabel('Gradient 2 type',{exact:true}).evaluate(el=>el===document.activeElement));const beforeFillRemoval=read();await page.getByRole('button',{name:'Remove gradient 1',exact:true}).press('Enter');await wait(()=>read()!==beforeFillRemoval);await settled();await wait(async()=>await page.getByLabel('Gradient 1 type',{exact:true}).evaluate(el=>el===document.activeElement));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeFillRemoval);await settled();const actionsGeometry=await page.getByRole('button',{name:'Move gradient 1 down',exact:true}).evaluate(el=>{const bar=el.closest('.gradient-actions'),legend=bar.closest('legend').getBoundingClientRect(),rect=bar.getBoundingClientRect();return {count:bar.querySelectorAll('button').length,height:rect.height,fits:rect.right<=legend.right+1&&rect.left>=legend.left,icons:[...bar.querySelectorAll('button')].every(button=>button.querySelector('svg')&&button.getAttribute('aria-label')&&button.title)};});assert.equal(actionsGeometry.count,3);assert.ok(actionsGeometry.height<=28);assert.ok(actionsGeometry.fits);assert.ok(actionsGeometry.icons);const keyboardBefore=read(),actionBar=page.getByRole('toolbar',{name:'Gradient 1 actions',exact:true}),moveDown=page.getByRole('button',{name:'Move gradient 1 down',exact:true});assert.equal(await actionBar.locator('button[tabindex="0"]').count(),1);await moveDown.focus();await page.keyboard.press('ArrowRight');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Duplicate gradient 1');await page.keyboard.press('End');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Remove gradient 1');await page.keyboard.press('ArrowRight');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Move gradient 1 down');await page.keyboard.press('ArrowLeft');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Remove gradient 1');await page.keyboard.press('Home');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Move gradient 1 down');assert.equal(read(),keyboardBefore);if(process.env.RT_E2E_ACTIONS_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_ACTIONS_SCREENSHOT});const beforeReverse=read(),reverseStack=await stack();await page.getByRole('button',{name:'Reverse gradient 1 stops',exact:true}).press('Enter');await wait(()=>read()!==beforeReverse);await settled();const reversedStack=await stack();assert.deepEqual(reversedStack,[{...reverseStack[0],stops:reverseStack[0].stops.map(stop=>({...stop,position:100-stop.position})).reverse()},...reverseStack.slice(1)]);await wait(async()=>await page.getByRole('button',{name:'Reverse gradient 1 stops',exact:true}).evaluate(el=>el===document.activeElement));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeReverse);await settled();const duplicatedSource=read();await page.getByLabel('Gradient 2 stop 1 color',{exact:true}).fill('#ffee00');await page.getByLabel('Gradient 2 stop 1 color',{exact:true}).press('Enter');await wait(()=>read()!==duplicatedSource);await settled();const changed=await stack();assert.deepEqual(changed[0],stackBefore[0]);assert.notDeepEqual(changed[1],stackBefore[0]);assert.deepEqual(changed.slice(2),stackBefore.slice(1));const beforeMove=read();await page.getByRole('button',{name:'Move gradient 1 down',exact:true}).press('Enter');await wait(()=>read()!==beforeMove);await settled();assert.deepEqual(await stack(),[changed[1],changed[0],...changed.slice(2)]);await wait(async()=>await page.getByLabel('Gradient 2 type',{exact:true}).evaluate(el=>el===document.activeElement));await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),beforeMove);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),duplicatedSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),duplicateBefore);await page.getByRole('button',{name:'Redo',exact:true}).click();await settled();assert.equal(read(),duplicatedSource);await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),duplicateBefore);
   const renderedBackground=await appearance();assert.equal(renderedBackground[0],baseline[0]);for(let i=1;i<4;i++)assert.ok(renderedBackground[i].split(',').every(value=>value.trim()===baseline[i]));if(process.env.RT_E2E_GRADIENT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_GRADIENT_SCREENSHOT});await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundImage),'none');await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}assert.deepEqual(errors,[]);console.log('GRADIENT FILLS PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_FILTER_STACK){
   const before=read(),open=async()=>{const summary=page.getByText('Layer filter stack',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();},snapshots=[before],checkpoint=async()=>{await wait(()=>read()!==snapshots.at(-1));await settled();snapshots.push(read());await open();};
   await open();await page.getByLabel('New layer effect',{exact:true}).selectOption('brightness');await page.getByRole('button',{name:'Add layer effect',exact:true}).click();await checkpoint();await page.getByLabel('Layer effect 2 Brightness (%)',{exact:true}).fill('60');await page.getByLabel('Layer effect 2 Brightness (%)',{exact:true}).press('Tab');await checkpoint();await page.getByLabel('New layer effect',{exact:true}).selectOption('blur');await page.getByRole('button',{name:'Add layer effect',exact:true}).click();await checkpoint();await page.getByLabel('Layer effect 3 Blur (px)',{exact:true}).fill('5');await page.getByLabel('Layer effect 3 Blur (px)',{exact:true}).press('Tab');await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).filter==='blur(1px) brightness(0.6) blur(5px)'));assert.equal(await page.getByLabel('Layer blur (px)',{exact:true}).isDisabled(),true);await page.getByRole('button',{name:'Move layer effect 3 up',exact:true}).click();await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).filter==='blur(1px) blur(5px) brightness(0.6)'));await page.getByRole('button',{name:'Remove layer effect 1',exact:true}).click();await checkpoint();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).filter==='blur(5px) brightness(0.6)'));for(const expected of snapshots.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}await open();await page.getByLabel('New layer effect',{exact:true}).selectOption('drop-shadow');await page.getByRole('button',{name:'Add layer effect',exact:true}).click();await wait(()=>read()!==before);await settled();const dropAdded=read();await page.getByLabel('Layer effect 2 Shadow X (px)',{exact:true}).fill('-3');await page.getByLabel('Layer effect 2 Shadow X (px)',{exact:true}).press('Tab');await wait(()=>read()!==dropAdded);await settled();const dropOffset=read();await page.getByLabel('Layer effect 2 Shadow Blur (px)',{exact:true}).fill('7');await page.getByLabel('Layer effect 2 Shadow Blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==dropOffset);await settled();const dropBlurred=read();await page.getByLabel('Layer effect 2 Shadow color',{exact:true}).fill('color(display-p3 1 .2 .1 / .4)');await page.getByLabel('Layer effect 2 Shadow color',{exact:true}).press('Tab');await wait(()=>read()!==dropBlurred);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>{const filter=getComputedStyle(el).filter;return filter.startsWith('blur(1px) drop-shadow(')&&filter.includes('color(display-p3')&&filter.includes('-3px')&&filter.includes('7px');}));for(const expected of [dropBlurred,dropOffset,dropAdded,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
   const backdropSummary=page.getByText('Backdrop filter stack',{exact:true});if(!await backdropSummary.evaluate(el=>el.parentElement.open))await backdropSummary.click();await page.getByLabel('New backdrop effect',{exact:true}).selectOption('contrast');await page.getByRole('button',{name:'Add backdrop effect',exact:true}).click();await wait(()=>read()!==before);await settled();const backdropAdded=read();await page.getByLabel('Backdrop effect 2 Contrast (%)',{exact:true}).fill('150');await page.getByLabel('Backdrop effect 2 Contrast (%)',{exact:true}).press('Tab');await wait(()=>read()!==backdropAdded);await settled();const backdropEdited=read();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backdropFilter==='blur(2px) contrast(1.5)'));await page.getByRole('button',{name:'Clear backdrop filters',exact:true}).click();await wait(()=>read()!==backdropEdited);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backdropFilter==='none'));for(const expected of [backdropEdited,backdropAdded,before]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}assert.deepEqual(errors,[]);console.log('FILTER STACK PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_LIQUID_PATH){
   if(process.env.RT_E2E_VECTOR_SMALL_WINDOW)await page.setViewportSize({width:1100,height:480});
   assert.equal(kind,'liquid');await page.getByRole('treeitem',{name:'path · Vector path',exact:true}).click();await settled();const source=read(),shape=app.locator('main > svg > path'),original=await shape.getAttribute('d'),model=()=>page.evaluate(()=>RetouchSVGPath.parseCompound(document.querySelector('#app').contentDocument.querySelector('main > svg > path').getAttribute('d'))),baseline=await model();assert.ok(await page.getByLabel('Shape Path data',{exact:true}).isVisible());
   if(process.env.RT_E2E_VECTOR_ENTRY){
    await shape.click({position:{x:30,y:20}});await settled();assert.equal(await page.getByRole('group',{name:'Edit vector points',exact:true}).count(),0);await page.keyboard.press('Enter');await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor();assert.equal(read(),source);await page.keyboard.press('Escape');await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor({state:'detached'});
    await shape.dblclick({position:{x:30,y:20}});await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor();assert.equal(read(),source);await page.keyboard.press('Escape');await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor({state:'detached'});
   }
   const start=async()=>{await page.getByRole('button',{name:'Edit vector points',exact:true}).click();await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor();},point=()=>page.getByRole('button',{name:'Vector point 2',exact:true});await start();if(process.env.RT_E2E_POINT_POSITION){const x=page.getByLabel('Vector X',{exact:true});await x.fill('12.5');assert.equal(read(),source);await x.press('Enter');await wait(()=>read()!==source);await settled();assert.equal((await model()).subpaths[0].nodes[0].x,12.5);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();await start();}const toolbar=page.getByRole('toolbar',{name:'Vector editing actions',exact:true});assert.equal(await toolbar.evaluate(el=>getComputedStyle(el).backgroundColor),'rgb(255, 255, 255)');assert.equal(await toolbar.getByRole('button',{name:'Done',exact:true}).evaluate(el=>getComputedStyle(el).backgroundColor),'rgb(13, 153, 255)');const vectorOptions=toolbar.locator('.svg-vector-options'),more=vectorOptions.locator('summary');assert.equal(await vectorOptions.evaluate(el=>el.open),false);await more.click();await toolbar.getByRole('button',{name:'Duplicate contour',exact:true}).waitFor({state:'visible'});const popupBox=await vectorOptions.getByRole('group',{name:'More vector actions',exact:true}).boundingBox(),surfaceBox=await page.locator('.svg-vertex-surface').boundingBox();assert.ok(popupBox.x>=surfaceBox.x&&popupBox.x+popupBox.width<=surfaceBox.x+surfaceBox.width);assert.ok(popupBox.y>=surfaceBox.y&&popupBox.y+popupBox.height<=surfaceBox.y+surfaceBox.height);await toolbar.getByRole('button',{name:'Open contour',exact:true}).scrollIntoViewIfNeeded();await page.keyboard.press('Escape');assert.equal(await vectorOptions.evaluate(el=>el.open),false);assert.equal(await more.evaluate(el=>el===document.activeElement),true);assert.equal(await page.getByRole('group',{name:'Edit vector points',exact:true}).count(),1);const beforeToolbarKeys=await point().boundingBox();await toolbar.getByRole('button',{name:'Make corner',exact:true}).focus();await page.keyboard.press('ArrowRight');assert.equal(await toolbar.getByRole('button',{name:'Make smooth',exact:true}).evaluate(el=>el===document.activeElement),true);assert.deepEqual(await point().boundingBox(),beforeToolbarKeys);assert.equal(read(),source);if(process.env.RT_E2E_VECTOR_LIGHT_SCREENSHOT){await page.locator('#toasts .toast').waitFor({state:'detached'});await page.screenshot({path:process.env.RT_E2E_VECTOR_LIGHT_SCREENSHOT});}await point().focus();const pointBefore=await point().boundingBox();await page.keyboard.press('ArrowRight');assert.equal(read(),source);assert.ok((await point().boundingBox()).x>pointBefore.x);assert.equal(await shape.getAttribute('d'),original);await page.keyboard.press('Escape');await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor({state:'detached'});assert.equal(read(),source);assert.equal(await shape.getAttribute('d'),original);
   await start();await point().focus();await page.keyboard.press('ArrowRight');await page.keyboard.press('Enter');await wait(()=>read()!==source);await settled();const saved=read(),changed=await model();assert.equal(changed.subpaths[0].nodes[1].x,baseline.subpaths[0].nodes[1].x+1);assert.deepEqual(changed.subpaths.slice(1),baseline.subpaths.slice(1));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(await model(),baseline);await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===saved);await settled();assert.deepEqual(await model(),changed);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(errors,[]);console.log('LIQUID PATH PASS '+engine);return;
  }
  if(process.env.RT_E2E_SVG_CONVERT){
   const tag=process.env.RT_E2E_SVG_CONVERT_KIND||'rect',shape=()=>app.locator('main > svg > '+tag),source=read();await page.getByRole('treeitem',{name:new RegExp('^'+tag+'(?: ·|$)')}).click();await settled();
   const accessible=async()=>{if(process.env.RT_E2E_SVG_METADATA){const image=app.getByRole('img',{name:'Accessible shape',exact:true});assert.equal(await image.count(),1);assert.equal(await image.getAttribute('aria-describedby'),'shape-description');assert.equal(await image.locator('desc').textContent(),'A shape with a preserved description');}};await accessible();
   const snapshot=el=>{const b=el.getBBox(),s=getComputedStyle(el),length=el.getTotalLength();return {box:{x:b.x,y:b.y,width:b.width,height:b.height},fill:s.fill,stroke:s.stroke,length,samples:Array.from({length:21},(_,i)=>{const p=el.getPointAtLength(length*i/20);return {x:p.x,y:p.y};})};},before=await shape().evaluate(snapshot);
   await shape().evaluate(el=>{const style=el.ownerDocument.createElement('style');style.id='conversion-refusal-test';style.textContent=el.tagName+'{fill:green}';el.ownerDocument.head.append(style);});await page.getByRole('button',{name:'Convert to vector path',exact:true}).click();await wait(async()=>/CSS changes/.test(await page.locator('#status').textContent()));assert.equal(read(),source);await shape().evaluate(el=>el.ownerDocument.getElementById('conversion-refusal-test').remove());if(process.env.RT_E2E_SVG_METADATA)await shape().evaluate(el=>{const style=el.ownerDocument.createElement('style');style.textContent='svg path:empty{fill:green}';el.ownerDocument.head.append(style);});
   await page.getByRole('button',{name:'Convert to vector path',exact:true}).click();await wait(()=>read()!==source);await settled();const path=app.locator('main > svg > path');await path.waitFor();const after=await path.evaluate(snapshot);await accessible();for(const key of ['x','y','width','height'])assert.ok(Math.abs(before.box[key]-after.box[key])<.001);assert.equal(after.fill,before.fill);assert.equal(after.stroke,before.stroke);assert.ok(Math.abs(before.length-after.length)<Math.max(.1,before.length*.002),JSON.stringify({before,after}));for(let i=0;i<21;i++)assert.ok(Math.hypot(before.samples[i].x-after.samples[i].x,before.samples[i].y-after.samples[i].y)<.1);const converted=read();await page.getByRole('button',{name:'Edit vector points',exact:true}).click();await page.getByRole('group',{name:'Edit vector points',exact:true}).waitFor();await page.keyboard.press('Escape');assert.equal(read(),converted);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.equal(await shape().count(),1);await accessible();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===converted);await settled();assert.equal(await path.count(),1);await accessible();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(errors,[]);console.log('SVG CONVERSION PASS',kind,engine,tag);return;
  }
  if(process.env.RT_E2E_SVG_PICKER){
   await app.locator('main > svg > rect').click();await settled();await page.getByLabel('SVG fill',{exact:true}).waitFor();assert.ok(await page.getByLabel('Shape Width',{exact:true}).isVisible());assert.equal(await page.getByLabel('Shape Width',{exact:true}).evaluate(el=>el.closest('.inspector-section').dataset.section),'geometry');if(process.env.RT_E2E_LIQUID_GEOMETRY_DYNAMIC)assert.ok(await page.getByLabel('Shape X',{exact:true}).isDisabled());const initial=read();await page.getByLabel('Shape Width',{exact:true}).fill('90');await page.getByLabel('Shape Width',{exact:true}).press('Tab');await wait(()=>read()!==initial);await settled();await wait(async()=>await app.locator('main > svg > rect').evaluate(el=>el.getBBox().width)===90);const resized=read();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===initial);await settled();assert.equal(await app.locator('main > svg > rect').evaluate(el=>el.getBBox().width),80);await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===resized);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===initial);await settled();
   for(const property of ['fill','stroke']){const label='SVG '+property,field=page.getByLabel(label,{exact:true});if(property==='stroke'){await field.fill('none');await field.press('Enter');await wait(()=>read()!==initial);await settled();}const source=read(),appearance=()=>app.locator('main > svg > rect').evaluate((el,p)=>({paint:getComputedStyle(el).getPropertyValue(p),style:el.getAttribute('style')}),property),original=await appearance(),trigger=page.getByRole('button',{name:'Edit '+label,exact:true});assert.equal(await trigger.evaluate(el=>el.closest('.inspector-section').dataset.section),property);await trigger.click();const picker=page.getByRole('dialog',{name:'Edit '+label,exact:true}),value=picker.getByLabel('Color value',{exact:true});if(property==='stroke')assert.ok(await picker.getByRole('slider',{name:'Saturation and brightness',exact:true}).isVisible());await value.fill('rgb(30 90 180 / 0.2345)');assert.notEqual((await appearance()).paint,original.paint);assert.equal(read(),source);await page.keyboard.press('Escape');await picker.waitFor({state:'detached'});assert.deepEqual(await appearance(),original);await trigger.click();await value.fill('rgb(30 90 180 / 0.2345)');await picker.getByLabel('Color model',{exact:true}).selectOption('hex');await picker.getByLabel('Hex color',{exact:true}).fill('123ABC');if(process.env.RT_E2E_SVG_PICKER_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SVG_PICKER_SCREENSHOT});await value.press('Enter');await wait(()=>read()!==source);await settled();assert.ok(read().includes('0.2345'));assert.notEqual((await appearance()).paint,original.paint);assert.equal((await appearance()).style,original.style);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(await appearance(),original);if(property==='stroke'){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===initial);await settled();}}
   assert.equal(read(),initial);assert.deepEqual(errors,[]);console.log('SVG PICKER PASS '+kind+' '+engine);return;
  }
  if(process.env.RT_E2E_SOLID_PICKER){
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();
   for(const [baseLabel,property]of [['Background color (CSS)','background-color'],['Border color (CSS)','border-top-color'],['Text color (CSS)','color']]){
    const label=kind==='html'?baseLabel:baseLabel.replace(' (CSS)',' with alpha');const source=read(),appearance=()=>app.locator('h1').evaluate((el,property)=>({color:getComputedStyle(el).getPropertyValue(property),style:el.getAttribute('style')}),property),original=await appearance(),trigger=page.getByRole('button',{name:'Edit '+label,exact:true});assert.equal(await trigger.evaluate(el=>el.closest('.inspector-section').dataset.section),property==='background-color'?'fill':property==='border-top-color'?'stroke':'typography');assert.ok(await trigger.isVisible());const direct=page.getByLabel(label,{exact:true}),originalInput=await direct.inputValue();await direct.fill('#ff3300');await direct.press('Escape');assert.equal(await direct.inputValue(),originalInput);assert.equal(read(),source);await direct.fill('not-a-color');await direct.press('Enter');assert.ok(await direct.evaluate(el=>!!el.validationMessage));assert.equal(read(),source);await direct.press('Escape');assert.equal(await direct.inputValue(),originalInput);await direct.fill('rgb(80 100 120 / 0.5678)');await direct.dispatchEvent('keydown',{key:'Enter',isComposing:true});assert.equal(read(),source);await direct.press('Enter');await wait(()=>read()!==source);await settled();assert.ok(read().includes('0.5678'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(await appearance(),original);await trigger.click();const picker=page.getByRole('dialog',{name:'Edit '+label,exact:true}),value=picker.getByLabel('Color value',{exact:true});await value.fill('rgb(25 90 170 / 0.1234)');assert.notEqual((await appearance()).color,original.color);assert.equal(read(),source);await page.evaluate(()=>renderPanel());assert.ok(await picker.isVisible());await page.keyboard.press('Escape');await picker.waitFor({state:'detached'});assert.deepEqual(await appearance(),original);await trigger.click();await value.fill('rgb(25 90 170 / 0.1234)');await picker.getByLabel('Color model',{exact:true}).selectOption('hex');await picker.getByLabel('Hex color',{exact:true}).fill('1A2B3C');assert.equal(await value.inputValue(),'rgb(26 43 60 / 0.1234)');if(process.env.RT_E2E_SOLID_SCREENSHOT&&property==='background-color')await page.screenshot({path:process.env.RT_E2E_SOLID_SCREENSHOT});await value.press('Enter');await wait(()=>read()!==source);await settled();assert.ok(read().includes('0.1234'));assert.notEqual((await appearance()).color,original.color);assert.equal((await appearance()).style,original.style);if(kind!=='html'){const applied=read(),clear=page.getByRole('button',{name:'Clear local '+baseLabel.replace(' (CSS)','').toLowerCase(),exact:true});await clear.click();await wait(()=>read()!==applied);await settled();assert.equal((await appearance()).color,original.color);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===applied);await settled();if(property!=='border-top-color'){const section=page.locator('[data-section="'+(property==='background-color'?'fill':'typography')+'"]'),summary=section.getByText('Color presets',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();const palette=section.getByRole('toolbar',{name:property==='background-color'?'Fill color presets':'Text color presets',exact:true}),white=section.getByRole('button',{name:property==='background-color'?'bg-white':'text-white',exact:true});assert.equal(await palette.locator('.palbtn[tabindex="0"]').count(),1);await white.focus();await page.keyboard.press('ArrowRight');assert.ok((await page.evaluate(()=>document.activeElement.getAttribute('aria-label'))).endsWith('-black'));await page.keyboard.press('ArrowDown');assert.equal(await palette.evaluate(el=>document.activeElement===el.querySelectorAll('.palrow')[1].querySelectorAll('.palbtn')[1]),true);await page.keyboard.press('End');assert.equal(await palette.evaluate(el=>document.activeElement===[...el.querySelectorAll('.palbtn')].at(-1)),true);await page.keyboard.press('ArrowRight');assert.ok((await page.evaluate(()=>document.activeElement.getAttribute('aria-label'))).endsWith('-white'));await page.keyboard.press('Tab');assert.equal(await section.getByLabel(property==='background-color'?'Fill hex color':'Text hex color',{exact:true}).evaluate(el=>el===document.activeElement),true);assert.equal(read(),applied);await white.press('Enter');await wait(()=>read()!==applied);await settled();assert.equal((await appearance()).color,'rgb(255, 255, 255)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===applied);await settled();}}await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();assert.deepEqual(await appearance(),original);
   }
   assert.deepEqual(errors,[]);console.log('SOLID PICKER PASS '+kind+' '+engine);return;
  }
  if(process.env.RT_E2E_SHADOW_PICKER){
   const states=[read()],open=async()=>{if(kind==='html')return;const summary=page.getByText('Shadow stack',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();},checkpoint=async()=>{await wait(()=>read()!==states.at(-1));await settled();states.push(read());await open();};
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await open();
   if(kind!=='html')await page.getByText('Shadow stack',{exact:true}).evaluate(el=>{el.parentElement.open=false;});const addShadow=page.getByRole('button',{name:'Add shadow',exact:true});assert.ok(await addShadow.evaluate(el=>el.classList.contains('section-add')&&el.textContent==='+'&&el.closest('.inspector-section').dataset.section==='effects'));assert.equal(await page.getByRole('button',{name:'Clear shadows',exact:true}).isVisible(),false);assert.equal(await page.getByRole('button',{name:'Reset shadows',exact:true}).isVisible(),false);
   for(let i=0;i<2;i++){await addShadow.press('Enter');if(kind!=='html')await wait(async()=>await page.getByText('Shadow stack',{exact:true}).evaluate(el=>el.parentElement.open));await checkpoint();await wait(async()=>await page.getByLabel('Shadow '+(i+1)+' type',{exact:true}).evaluate(el=>el===document.activeElement));}

   await page.getByLabel('Shadow 2 type',{exact:true}).selectOption('inner');await checkpoint();
   const appearance=()=>app.locator('h1').evaluate(el=>({shadow:getComputedStyle(el).boxShadow,style:el.getAttribute('style')})),beforeAppearance=await appearance(),beforePicker=read(),stack=()=>page.evaluate(()=>RetouchHTMLCSSValues.parseShadows(getComputedStyle(document.querySelector('#app').contentDocument.querySelector('h1')).boxShadow)),beforeStack=await stack();
   const shadowLayout=await page.getByLabel('Shadow 1 X (px)',{exact:true}).evaluate(el=>{const group=el.closest('.shadow-controls'),rect=label=>group.querySelector('[aria-label="Shadow 1 '+label+' (px)"]').getBoundingClientRect(),x=rect('X'),y=rect('Y'),blur=rect('Blur'),spread=rect('Spread');return {pairs:group.querySelectorAll(':scope > .property-pair').length,aligned:Math.abs(x.y-y.y)<1&&Math.abs(blur.y-spread.y)<1,ordered:x.right<=y.left&&blur.right<=spread.left&&x.bottom<=blur.top,labels:[...group.querySelectorAll('.property-pair .inspector-field > span')].map(el=>el.textContent),fits:group.scrollWidth<=group.clientWidth+1};});assert.deepEqual(shadowLayout,{pairs:2,aligned:true,ordered:true,labels:['X','Y','Blur','Spread'],fits:true});if(process.env.RT_E2E_SHADOW_LAYOUT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SHADOW_LAYOUT_SCREENSHOT});
   const actionBar=page.getByRole('toolbar',{name:'Shadow 1 actions',exact:true});assert.equal(await actionBar.locator('button[tabindex="0"]').count(),1);await page.getByRole('button',{name:'Move shadow 1 down',exact:true}).focus();await page.keyboard.press('ArrowRight');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Duplicate shadow 1');await page.keyboard.press('End');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Remove shadow 1');await page.keyboard.press('ArrowRight');assert.equal(await page.evaluate(()=>document.activeElement.getAttribute('aria-label')),'Move shadow 1 down');assert.equal(read(),beforePicker);
   await page.getByRole('button',{name:'Duplicate shadow 1',exact:true}).press('Enter');await wait(()=>read()!==beforePicker);await settled();await open();const duplicated=read();assert.deepEqual(await stack(),[beforeStack[0],beforeStack[0],beforeStack[1]]);await wait(async()=>await page.getByLabel('Shadow 2 type',{exact:true}).evaluate(el=>el===document.activeElement));await page.getByLabel('Shadow 2 Blur (px)',{exact:true}).fill('19');await page.getByLabel('Shadow 2 Blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==duplicated);await settled();await open();const edited=read(),editedStack=await stack();assert.deepEqual(editedStack[0],beforeStack[0]);assert.equal(editedStack[1].blur,19);assert.deepEqual(editedStack[2],beforeStack[1]);await page.getByRole('button',{name:'Move shadow 2 down',exact:true}).press('Enter');await wait(()=>read()!==edited);await settled();await open();const moved=read();assert.deepEqual(await stack(),[editedStack[0],editedStack[2],editedStack[1]]);await wait(async()=>await page.getByLabel('Shadow 3 type',{exact:true}).evaluate(el=>el===document.activeElement));await page.getByRole('button',{name:'Move shadow 3 up',exact:true}).press('Enter');await wait(()=>read()!==moved);await settled();await open();assert.deepEqual(await stack(),editedStack);await wait(async()=>await page.getByLabel('Shadow 2 type',{exact:true}).evaluate(el=>el===document.activeElement));for(const source of [moved,edited,duplicated,beforePicker]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();await open();}assert.deepEqual(await stack(),beforeStack);
   const removalStates=[];for(let i=0;i<2;i++){removalStates.push(read());const remove=page.getByRole('button',{name:'Remove shadow 1',exact:true});assert.ok(await remove.evaluate(el=>{const legend=el.closest('legend'),r=el.getBoundingClientRect(),l=legend?.getBoundingClientRect();return !!el.querySelector('svg')&&el.title==='Remove shadow 1'&&r.width<=28&&r.height<=28&&r.left>=l.left&&r.right<=l.right+1;}));await remove.press('Enter');await wait(()=>read()!==removalStates.at(-1));await settled();await open();await wait(async()=>await (i===0?page.getByLabel('Shadow 1 type',{exact:true}):page.getByRole('button',{name:'Add shadow',exact:true})).evaluate(el=>el===document.activeElement));assert.equal(await page.getByLabel('Shadow 1 type',{exact:true}).count(),i===0?1:0);}for(const source of removalStates.reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===source);await settled();await open();}assert.deepEqual(await stack(),beforeStack);
   const trigger=page.getByRole('button',{name:'Edit Shadow 1 color',exact:true});assert.ok(await trigger.evaluate(el=>{const control=el.parentElement,input=control.querySelector('input').getBoundingClientRect(),button=el.getBoundingClientRect();return button.left>=input.left&&button.right<=input.right&&control.scrollWidth<=control.clientWidth+1;}));
   await trigger.click();const picker=page.getByRole('dialog',{name:'Edit Shadow 1 color',exact:true}),value=picker.getByLabel('Color value',{exact:true});await value.fill('#ff000080');assert.notEqual((await appearance()).shadow,beforeAppearance.shadow);assert.equal(read(),beforePicker);await page.evaluate(()=>renderPanel());assert.ok(await picker.isVisible());await page.keyboard.press('Escape');await picker.waitFor({state:'detached'});assert.deepEqual(await appearance(),beforeAppearance);
   await trigger.click();await value.fill('#12345680');if(process.env.RT_E2E_SHADOW_PICKER_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SHADOW_PICKER_SCREENSHOT});await value.press('Enter');await wait(()=>read()!==beforePicker);await settled();await open();const afterStack=await stack();assert.deepEqual(afterStack.slice(1),beforeStack.slice(1));const {color:beforeColor,...beforeGeometry}=beforeStack[0],{color:afterColor,...afterGeometry}=afterStack[0];assert.deepEqual(afterGeometry,beforeGeometry);assert.equal(await page.evaluate(color=>RetouchColorStyles.fromComputed(color),afterColor),'#12345680');assert.equal((await appearance()).style,beforeAppearance.style);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforePicker);await settled();assert.deepEqual(await appearance(),beforeAppearance);while(states.length>1){states.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===states.at(-1));await settled();}assert.equal(read(),states[0]);assert.deepEqual(errors,[]);console.log('SHADOW PICKER PASS '+kind+' '+engine);return;
  }
  if(process.env.RT_E2E_EFFECT_STYLES&&['react','liquid'].includes(kind)){
   const before=read(),catalogFile=path.join(root,'.retouch/effect-styles.json'),catalog=()=>JSON.parse(fs.readFileSync(catalogFile,'utf8')),open=async()=>{const summary=page.getByText('Saved effect styles',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();await page.getByLabel('Effect style name',{exact:true}).waitFor();};
   await open();await page.getByLabel('Effect style name',{exact:true}).fill('Floating');await page.getByRole('button',{name:'Save current effects',exact:true}).click();await wait(()=>fs.existsSync(catalogFile));await settled();const saved=catalog().styles[0],catalogBefore=fs.readFileSync(catalogFile,'utf8');assert.equal(saved.properties.filter,'blur(1px)');assert.equal(saved.properties['backdrop-filter'],'blur(2px)');
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('md:');await settled();await open();await page.getByLabel('Saved effect style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Apply effect style',exact:true}).click();await wait(()=>read().includes('data-rt-effect-styles'));await settled();const linked=read(),other=path.join(root,kind==='react'?'app/unvisited.jsx':'sections/unvisited.liquid');fs.writeFileSync(other,linked);
   const filter=()=>app.locator('p').first().evaluate(el=>getComputedStyle(el).filter);await wait(async()=>await filter()==='blur(1px)');
   for(const amount of ['5','0']){await page.getByLabel('Backdrop blur (px)',{exact:true}).fill(amount);await page.getByLabel('Backdrop blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==linked);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).backdropFilter)===(amount==='0'?'none':'blur(5px)'));assert.equal(await filter(),'blur(1px)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();}

   const openShadows=async()=>{const summary=page.getByText('Shadow stack',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();};await openShadows();await page.getByRole('button',{name:'Add shadow',exact:true}).click();await wait(()=>read()!==linked);await settled();const two=read();await openShadows();await page.getByLabel('Shadow 2 type',{exact:true}).selectOption('inner');await wait(()=>read()!==two);await settled();const inner=read();await openShadows();await page.getByLabel('Shadow 2 color',{exact:true}).fill(process.env.RT_E2E_SHADOW_COLOR||'color(display-p3 1 0.25 0.1 / 0.4)');await page.getByLabel('Shadow 2 color',{exact:true}).press('Tab');await wait(()=>read()!==inner);await settled();const colored=read();await wait(async()=>await app.locator('p').first().evaluate((el,functionName)=>getComputedStyle(el).boxShadow.includes(functionName),(process.env.RT_E2E_SHADOW_COLOR||'color(display-p3').split('(')[0]+'('));await openShadows();await page.getByLabel('Shadow 2 Blur (px)',{exact:true}).fill('11');await page.getByLabel('Shadow 2 Blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==colored);await settled();const blurred=read();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).boxShadow.includes('11px')&&getComputedStyle(el).boxShadow.includes('inset')));await openShadows();await page.getByRole('button',{name:'Move shadow 2 up',exact:true}).click();await wait(()=>read()!==blurred);await settled();const reordered=read();await openShadows();assert.equal(await page.getByLabel('Shadow 1 type',{exact:true}).inputValue(),'inner');await page.getByRole('button',{name:'Remove shadow 2',exact:true}).click();await wait(()=>read()!==reordered);await settled();await openShadows();assert.equal(await page.getByLabel('Shadow 2 type',{exact:true}).count(),0);for(const expected of [reordered,blurred,colored,inner,two,linked]){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
   const linkedShadow=await app.locator('p').first().evaluate(el=>getComputedStyle(el).boxShadow);await page.getByLabel('Shadow preset',{exact:true}).selectOption('shadow-lg');await wait(()=>read()!==linked);await settled();assert.ok(read().includes('md:!shadow-lg'));await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).boxShadow)!==linkedShadow);await open();await page.getByText('1 local override in this screen scope.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset effect style overrides',exact:true}).click();await settled();await wait(async()=>!await app.locator('p').first().evaluate(el=>el.classList.contains('md:!shadow-lg')));await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).boxShadow)===linkedShadow);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read().includes('md:!shadow-lg'));await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();

   await page.getByLabel('Layer blur (px)',{exact:true}).fill('3');await page.getByLabel('Layer blur (px)',{exact:true}).press('Tab');await settled();const local=read();await wait(async()=>await filter()==='blur(3px)');await open();await page.getByText('1 local override in this screen scope.',{exact:true}).waitFor();
   await page.getByRole('button',{name:'Update style from this layer',exact:true}).click();await wait(()=>catalog().styles[0].properties.filter==='blur(3px)');await settled();assert.ok(fs.readFileSync(other,'utf8').includes('![filter:blur(3px)]'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===local);await settled();assert.equal(fs.readFileSync(other,'utf8'),linked);assert.equal(fs.readFileSync(catalogFile,'utf8'),catalogBefore);fs.unlinkSync(other);
   await open();await page.getByRole('button',{name:'Reset effect style overrides',exact:true}).click();await wait(()=>read()!==local);await settled();await wait(async()=>await filter()==='blur(1px)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===local);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await filter()==='none');await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await open();await page.getByRole('button',{name:'Detach effect style',exact:true}).click();await wait(()=>!read().includes('data-rt-effect-styles'));await settled();assert.equal(await filter(),'blur(1px)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   if(kind==='liquid'){assert.deepEqual(errors,[]);console.log('LIQUID EFFECT STYLES PASS',engine);return;}
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByLabel('Saved effect style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Apply effect style',exact:true}).click();await wait(()=>read()!==before);await settled();const batch=read();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).filter==='blur(1px)')));await open();await page.getByText('2 of 2 layers linked in this screen scope.',{exact:true}).waitFor();
   await page.evaluate(()=>window.setReactClassesSelection(Object.fromEntries(sel.multiple.map((info,i)=>[info.id,RetouchResponsive.replaceScope(info.className,RetouchInspector.filterClasses(RetouchResponsive.project(info.className,'md:'),'filter',i?'contrast(1.25) blur(6px)':'brightness(0.8) blur(2px)'),'md:')]))));await settled();const seeded=read();await page.getByLabel('Shared Layer blur (px)',{exact:true}).fill('4');await page.getByLabel('Shared Layer blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==seeded);await settled();await wait(async()=>{const filters=await app.locator('p').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).filter));return filters.includes('brightness(0.8) blur(4px)')&&filters.includes('contrast(1.25) blur(4px)');});const shared=read();await page.getByLabel('Shared Backdrop blur (px)',{exact:true}).fill('0');await page.getByLabel('Shared Backdrop blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==shared);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backdropFilter==='none')));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===shared);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===seeded);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await open();
   await page.getByRole('button',{name:'Detach selected effect styles',exact:true}).click();await wait(()=>!read().includes('data-rt-effect-styles'));await settled();assert.equal(await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).filter==='blur(1px)')),true);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(errors,[]);console.log('REACT EFFECT STYLES PASS',engine);return;
  }
  if(process.env.RT_E2E_EFFECT_STYLES){
   assert.equal(kind,'html');const before=read(),catalogFile=path.join(root,'.retouch/effect-styles.json'),catalog=()=>JSON.parse(fs.readFileSync(catalogFile,'utf8')),open=async()=>{const summary=page.getByText('Saved effect styles',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();await page.getByLabel('Effect style name',{exact:true}).waitFor();};
   await open();await page.getByLabel('Effect style name',{exact:true}).fill('Floating');await page.getByRole('button',{name:'Save current effects',exact:true}).click();await wait(()=>fs.existsSync(catalogFile));await settled();const saved=catalog().styles[0],catalogBefore=fs.readFileSync(catalogFile,'utf8');assert.equal(saved.properties.filter,'blur(1px)');assert.equal(saved.properties['backdrop-filter'],'blur(2px)');assert.ok(saved.properties['box-shadow'].includes('8px'));
   await page.getByText('Import / export styles',{exact:true}).click();const effectDownload=page.waitForEvent('download');await page.getByRole('button',{name:'Export effect styles',exact:true}).click();const exported=await effectDownload;assert.equal(exported.suggestedFilename(),'retouch-effect-styles.json');assert.deepEqual(JSON.parse(fs.readFileSync(await exported.path(),'utf8')),catalog());await page.getByLabel('Effect style library file',{exact:true}).setInputFiles({name:'effects.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(catalog()))});await page.getByText('Import complete. Existing styles were preserved.',{exact:true}).waitFor();assert.equal(fs.readFileSync(catalogFile,'utf8'),catalogBefore);

   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('min-[768px]:');await settled();await open();await page.getByLabel('Saved effect style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Apply effect style',exact:true}).click();await wait(()=>read().includes('data-rt-effect-styles'));await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).filter==='blur(1px)'));const linked=read(),other=path.join(root,'effects-unvisited.html');fs.writeFileSync(other,linked);
   await page.evaluate(operation=>window.RetouchEffectStyleRequest(operation),{type:'update',revision:require('../../src/effect-styles.cjs').read(root).revision,id:saved.id,name:saved.name,properties:{...saved.properties,filter:'blur(2px)'}});await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).filter==='blur(2px)'));assert.ok(fs.readFileSync(other,'utf8').includes('blur(2px)'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();assert.equal(fs.readFileSync(other,'utf8'),linked);assert.equal(fs.readFileSync(catalogFile,'utf8'),catalogBefore);

   const blur=page.getByLabel('Layer blur (px)',{exact:true});await blur.fill('3');await blur.press('Tab');await wait(()=>read()!==linked);await settled();const local=read();await open();await page.getByText('1 local override in this screen scope.',{exact:true}).waitFor();await page.getByRole('button',{name:'Update style from this layer',exact:true}).click();await wait(()=>catalog().styles[0].properties.filter==='blur(3px)');await settled();assert.ok(fs.readFileSync(other,'utf8').includes('blur(3px)'));await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).filter==='blur(3px)'));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===local);await settled();assert.equal(fs.readFileSync(other,'utf8'),linked);assert.equal(fs.readFileSync(catalogFile,'utf8'),catalogBefore);fs.unlinkSync(other);await open();await page.getByRole('button',{name:'Reset effect style overrides',exact:true}).click();await wait(()=>read()!==local);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).filter==='blur(1px)'));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===local);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).filter==='none'));await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await open();await page.getByRole('button',{name:'Detach effect style',exact:true}).click();await wait(()=>!read().includes('data-rt-effect-styles'));await settled();assert.equal(await app.locator('p').first().evaluate(el=>getComputedStyle(el).filter),'blur(1px)');await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByLabel('Saved effect style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Apply effect style',exact:true}).click();await wait(()=>read()!==before);await settled();const batch=read();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).filter==='blur(1px)')));await open();await page.getByText('2 of 2 layers linked in this screen scope.',{exact:true}).waitFor();
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();await page.getByLabel('Layer blur (px)',{exact:true}).fill('4');await page.getByLabel('Layer blur (px)',{exact:true}).press('Tab');await wait(()=>read()!==batch);await settled();const batchOverride=read();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByText('1 local override across the selection.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset selected effect style overrides',exact:true}).click();await wait(()=>read()!==batchOverride);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).filter==='blur(1px)')));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchOverride);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByRole('button',{name:'Detach selected effect styles',exact:true}).click();await wait(()=>!read().includes('data-rt-effect-styles'));await settled();assert.equal(await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).filter==='blur(1px)')),true);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   assert.deepEqual(errors,[]);console.log('EFFECT STYLES PASS',engine);return;
  }
  if(process.env.RT_E2E_COLOR_STYLES){
   const paletteFile=path.join(root,'.retouch/color-styles.json'),palette=()=>JSON.parse(fs.readFileSync(paletteFile,'utf8')),before=read(),open=async()=>{const summary=page.getByText('Saved color styles',{exact:true}),parents=summary.locator('xpath=ancestor::details');for(let i=0;i<await parents.count();i++){const group=parents.nth(i);if(!await group.evaluate(el=>el.open))await group.locator(':scope > summary').click();}await page.getByLabel('Color style name',{exact:true}).waitFor();};
   await open();await page.getByRole('button',{name:'Use selected layer color',exact:true}).click();assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'#000000ff');assert.equal(fs.existsSync(paletteFile),false);
   await page.getByLabel('Color target',{exact:true}).selectOption('background-color');await page.getByRole('button',{name:'Use selected layer color',exact:true}).click();assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'#00000000');assert.equal(read(),before);await page.getByLabel('Color target',{exact:true}).selectOption('color');
   if(process.env.RT_E2E_P3_COLORS){
    const originalDraft=await page.getByLabel('Color value with alpha',{exact:true}).inputValue();await page.getByLabel('Color space',{exact:true}).selectOption('display-p3');assert.ok((await page.getByLabel('Color value with alpha',{exact:true}).inputValue()).startsWith('color(display-p3 '));await page.getByLabel('Color space',{exact:true}).selectOption('srgb');assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),originalDraft);
    await page.getByLabel('Color value with alpha',{exact:true}).fill('color(display-p3 1 0 0 / 0.5)');await page.getByLabel('Color space',{exact:true}).selectOption('srgb');assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'color(display-p3 1 0 0 / 0.5)');await page.getByRole('button',{name:'Keep Display P3',exact:true}).click();await page.getByLabel('Color space',{exact:true}).selectOption('srgb');await page.getByRole('button',{name:'Convert to sRGB with clipping',exact:true}).click();assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'color(srgb 1 0 0 / 0.5)');assert.equal(fs.existsSync(paletteFile),false);assert.equal(read(),before);
    await page.getByLabel('Color style name',{exact:true}).fill('Wide gamut');await page.getByLabel('Color value with alpha',{exact:true}).fill('color(display-p3 1 0.2 0.1 / 0.75)');assert.equal(await page.getByLabel('Color RGB',{exact:true}).isDisabled(),true);const green=page.getByLabel('Display P3 green (0–1)',{exact:true});await green.fill('0.3');await green.press('Tab');assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'color(display-p3 1 0.3 0.1 / 0.75)');await green.fill('0.2');await green.press('Tab');await page.getByLabel('Color opacity (%)',{exact:true}).fill('50');assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'color(display-p3 1 0.2 0.1 / 0.5)');await page.getByRole('button',{name:'Create color style',exact:true}).click();await wait(()=>fs.existsSync(paletteFile));await settled();assert.equal(palette().styles[0].properties.color,'color(display-p3 1 0.2 0.1 / 0.5)');await page.getByLabel('Color target',{exact:true}).selectOption('background-color');await page.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor==='color(display-p3 1 0.2 0.1 / 0.5)'));await open();await page.getByRole('button',{name:'Use selected layer color',exact:true}).click();assert.equal(await page.getByLabel('Color value with alpha',{exact:true}).inputValue(),'color(display-p3 1 0.2 0.1 / 0.5)');
    const p3Linked=read(),p3Catalog=fs.readFileSync(paletteFile,'utf8'),p3Other=path.join(root,kind==='html'?'p3-unvisited.html':kind==='react'?'P3Unvisited.jsx':'p3-unvisited.liquid');fs.writeFileSync(p3Other,p3Linked);
    const localP3='color(display-p3 0.1 1 0.2 / 0.4)',nextP3='color(display-p3 0.2 0.3 1 / 0.6)',localField=page.getByLabel(kind==='html'?'Background color (CSS)':'Background color with alpha',{exact:true});await localField.fill(localP3);await localField.press('Tab');await wait(()=>read()!==p3Linked);await settled();const p3Local=read();await wait(async()=>await app.locator('h1').evaluate((el,value)=>getComputedStyle(el).backgroundColor===value,localP3));await open();await page.getByText('Linked color: Wide gamut · Local override.',{exact:true}).waitFor();
    await page.getByLabel('Color value with alpha',{exact:true}).fill(nextP3);await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>palette().styles[0].properties.color===nextP3);await settled();const p3Updated=read();assert.ok(fs.readFileSync(p3Other,'utf8').includes(nextP3));await wait(async()=>await app.locator('h1').evaluate((el,value)=>getComputedStyle(el).backgroundColor===value,localP3));await open();await page.getByRole('button',{name:'Reset linked color',exact:true}).click();await wait(()=>read()!==p3Updated);await settled();await wait(async()=>await app.locator('h1').evaluate((el,value)=>getComputedStyle(el).backgroundColor===value,nextP3));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===p3Updated);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===p3Local);await settled();assert.equal(fs.readFileSync(p3Other,'utf8'),p3Linked);assert.equal(fs.readFileSync(paletteFile,'utf8'),p3Catalog);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===p3Linked);await settled();fs.unlinkSync(p3Other);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>!fs.existsSync(paletteFile));await settled();await open();await page.getByLabel('Color target',{exact:true}).selectOption('color');
   }
   await page.getByLabel('Color style name',{exact:true}).fill('Brand');await page.getByLabel('Color value with alpha',{exact:true}).fill('#3698');await page.getByRole('button',{name:'Create color style',exact:true}).click();await wait(()=>fs.existsSync(paletteFile));await settled();const saved=palette().styles[0],created=fs.readFileSync(paletteFile,'utf8');assert.equal(saved.properties.color,'#33669988');await page.getByRole('img',{name:'Color preview #33669988',exact:true}).waitFor();
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>!fs.existsSync(paletteFile));await settled();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>fs.existsSync(paletteFile));await settled();assert.equal(fs.readFileSync(paletteFile,'utf8'),created);await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);
   if(process.env.RT_E2E_SVG_COLORS){
    await page.getByRole('treeitem',{name:'rect',exact:true}).click();await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);
    for(const property of ['fill','stroke']){const prior=read();await page.getByLabel('Color target',{exact:true}).selectOption(property);await page.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==prior);await settled();await wait(async()=>await app.locator('main > svg > rect').evaluate((el,p)=>getComputedStyle(el).getPropertyValue(p).startsWith('rgba(51, 102, 153,'),property));}
    if(kind!=='html'){
     const svgLinked=read();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('md:');await settled();const fill=page.getByLabel('SVG fill',{exact:true});await fill.fill('#00ff0080');await fill.press('Tab');await wait(()=>read()!==svgLinked);await settled();const svgLocal=read();await wait(async()=>await app.locator('main > svg > rect').evaluate(el=>getComputedStyle(el).fill.startsWith('rgba(0, 255, 0,')));
     await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('main > svg > rect').evaluate(el=>getComputedStyle(el).fill.startsWith('rgba(51, 102, 153,')));
     await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByRole('button',{name:'Reset svg fill',exact:true}).click();await wait(()=>read()!==svgLocal);await settled();await wait(async()=>await app.locator('main > svg > rect').evaluate(el=>getComputedStyle(el).fill.startsWith('rgba(51, 102, 153,')));
     await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===svgLocal);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===svgLinked);await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();
    }
    assert.equal(await app.locator('main > svg > rect').getAttribute('width'),'80');assert.equal(await app.locator('main > svg > rect').getAttribute('height'),'60');
    await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);
   }
   if(kind==='html'){
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('min-[768px]:');await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);await page.getByLabel('Color target',{exact:true}).selectOption('background-color');await page.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read().includes('data-rt-color-styles'));await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,')));const linkedSource=read(),other=path.join(root,'unvisited.html');fs.writeFileSync(other,linkedSource);
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await settled();await page.getByLabel('Style screen scope').selectOption('min-[1440px]:');await settled();await open();
    await page.getByText('Inherited color: Brand · 768px and larger',{exact:true}).waitFor();assert.equal(await page.getByRole('button',{name:'Detach linked color',exact:true}).count(),0);assert.equal(read(),linkedSource);
    await page.getByLabel('Color target',{exact:true}).selectOption('color');assert.equal(await page.getByRole('button',{name:'Apply inherited color at this scope',exact:true}).count(),0);await page.getByLabel('Color target',{exact:true}).selectOption('background-color');
    await page.getByRole('button',{name:'Apply inherited color at this scope',exact:true}).click();await wait(()=>read()!==linkedSource);await settled();const promoted=await app.locator('h1').evaluate(el=>JSON.parse(el.getAttribute('data-rt-color-styles')));assert.equal(promoted[768]['background-color'].id,saved.id);assert.equal(promoted[1440]['background-color'].id,saved.id);await open();await page.getByText('Linked color: Brand.',{exact:true}).waitFor();assert.equal(await page.getByRole('button',{name:'Apply inherited color at this scope',exact:true}).count(),0);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();await open();await page.getByText('Inherited color: Brand · 768px and larger',{exact:true}).waitFor();
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('min-[768px]:');await settled();

    const background=page.getByLabel('Background color (CSS)',{exact:true});await background.fill('#00ff00');await background.press('Tab');await wait(()=>read()!==linkedSource);await settled();const overridden=read();await open();await page.getByText('Linked color: Brand · Local override.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset linked color',exact:true}).click();await wait(()=>read()!==overridden);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,')));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===overridden);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();
    await open();await page.getByLabel('Color value with alpha',{exact:true}).fill('#ff000080');await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>palette().styles[0].properties.color==='#ff000080');await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(255, 0, 0,')));assert.ok(fs.readFileSync(other,'utf8').includes('#ff000080'));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();assert.equal(fs.readFileSync(other,'utf8'),linkedSource);assert.equal(fs.readFileSync(paletteFile,'utf8'),created);fs.unlinkSync(other);
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor==='rgba(0, 0, 0, 0)'));
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await open();await page.getByRole('button',{name:'Detach linked color',exact:true}).click();await wait(()=>!read().includes('data-rt-color-styles'));await settled();assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,')),true);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);
   }
   if(kind==='html'){
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);await page.getByLabel('Color target',{exact:true}).selectOption('background-color');
    await page.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();const batchLinked=read();await open();await page.getByText('2 of 2 layers linked in this screen scope.',{exact:true}).waitFor();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,'))));
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();const input=page.getByLabel('Background color (CSS)',{exact:true});await input.fill('#00ff00');await input.press('Tab');await wait(()=>read()!==batchLinked);await settled();const batchOverride=read();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByText('1 local override.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset selected color overrides',exact:true}).click();await wait(()=>read()!==batchOverride);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,'))));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchOverride);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchLinked);await settled();
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByRole('button',{name:'Detach selected colors',exact:true}).click();await wait(()=>!read().includes('data-rt-color-styles'));await settled();assert.equal(await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,'))),true);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchLinked);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);
   }
   if(kind==='react'||kind==='liquid'){
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('md:');await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);await page.getByLabel('Color target',{exact:true}).selectOption('background-color');await page.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read().includes('data-rt-color-styles'));await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,')));const linked=read(),other=path.join(root,kind==='liquid'?'unvisited.liquid':'Unvisited.jsx');fs.writeFileSync(other,linked);
    const paint=page.getByLabel('Background color with alpha',{exact:true});await paint.fill('#00ff0080');await paint.press('Tab');await wait(()=>read()!==linked);await settled();const local=read();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(0, 255, 0,')));
    await page.getByRole('button',{name:'Clear local background color',exact:true}).click();await wait(()=>read()!==local);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor==='rgba(0, 0, 0, 0)'));await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===local);await settled();
await open();await page.getByText('Linked color: Brand · Local override.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset linked color',exact:true}).click();await wait(()=>read()!==local);await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,')));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===local);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();

    await open();await page.getByLabel('Color value with alpha',{exact:true}).fill('#ff000080');await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>palette().styles[0].properties.color==='#ff000080');await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(255, 0, 0,')));assert.ok(fs.readFileSync(other,'utf8').includes('#ff000080'));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();assert.equal(fs.readFileSync(other,'utf8'),linked);fs.unlinkSync(other);
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor==='rgba(0, 0, 0, 0)'));
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await open();await page.getByRole('button',{name:'Detach linked color',exact:true}).click();await wait(()=>!read().includes('data-rt-color-styles'));await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,')));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linked);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
    if(kind==='react'){
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Apply color style',exact:true}).click();await wait(()=>read()!==before);await settled();const batch=read();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,'))));await open();
    const sharedPaint=page.getByLabel('Shared Background color with alpha',{exact:true});await sharedPaint.fill('#00ff0080');await sharedPaint.press('Tab');await wait(()=>read()!==batch);await settled();const sharedOverride=read();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(0, 255, 0,'))));await open();await page.getByText('2 local overrides.',{exact:true}).waitFor();
    await page.getByRole('button',{name:'Clear selected background color',exact:true}).click();await wait(()=>read()!==sharedOverride);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor==='rgba(0, 0, 0, 0)')));await open();await page.getByText('2 local overrides.',{exact:true}).waitFor();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===sharedOverride);await settled();await open();
await page.getByRole('button',{name:'Reset selected color overrides',exact:true}).click();await wait(()=>read()!==sharedOverride);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).backgroundColor.startsWith('rgba(51, 102, 153,'))));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===sharedOverride);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await open();
await page.getByText('2 of 2 layers linked in this screen scope.',{exact:true}).waitFor();await page.getByRole('button',{name:'Detach selected colors',exact:true}).click();await wait(()=>!read().includes('data-rt-color-styles'));await settled();
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batch);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();}
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();await open();await page.getByLabel('Saved color style',{exact:true}).selectOption(saved.id);
   }
   await page.getByLabel('Color style name',{exact:true}).fill('Accent');await page.getByLabel('Color opacity (%)',{exact:true}).fill('50');assert.equal(await page.getByLabel('Color opacity (%)',{exact:true}).inputValue(),'50');await page.getByRole('button',{name:'Update color style',exact:true}).click();await wait(()=>palette().styles[0].name==='Accent');await settled();assert.equal(palette().styles[0].properties.color,'color(srgb 0.2 0.4 0.6 / 0.5)');const updated=fs.readFileSync(paletteFile,'utf8');if(process.env.RT_E2E_COLOR_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_COLOR_SCREENSHOT});
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>fs.readFileSync(paletteFile,'utf8')===created);await settled();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>fs.readFileSync(paletteFile,'utf8')===updated);await settled();await open();
   await page.getByText('Import / export colors',{exact:true}).click();const downloadEvent=page.waitForEvent('download');await page.getByRole('button',{name:'Export color styles',exact:true}).click();const download=await downloadEvent;assert.equal(download.suggestedFilename(),'retouch-color-styles.json');assert.deepEqual(JSON.parse(fs.readFileSync(await download.path(),'utf8')),palette());await settled();
   const external=palette();external.styles[0].name='External';fs.writeFileSync(paletteFile,JSON.stringify(external));await page.getByLabel('Color style name',{exact:true}).fill('Stale');await page.getByRole('button',{name:'Update color style',exact:true}).click();await page.getByRole('status').filter({hasText:'Color styles changed.'}).waitFor();assert.equal(palette().styles[0].name,'External');await page.getByRole('button',{name:'Reload color styles',exact:true}).click();await wait(async()=>await page.getByLabel('Color style name',{exact:true}).inputValue()==='External');
   await page.getByRole('button',{name:'Delete color style',exact:true}).click();await page.getByRole('button',{name:'Cancel color deletion',exact:true}).click();assert.equal(palette().styles.length,1);await page.getByRole('button',{name:'Delete color style',exact:true}).click();await page.getByRole('button',{name:'Confirm delete color External',exact:true}).click();await wait(()=>palette().styles.length===0);await settled();const empty=fs.readFileSync(paletteFile,'utf8');
   await page.getByLabel('Color style library file',{exact:true}).setInputFiles({name:'colors.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(external))});await wait(()=>palette().styles.length===1);await settled();assert.deepEqual(palette(),external);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>fs.readFileSync(paletteFile,'utf8')===empty);await settled();assert.equal(read(),before);assert.deepEqual(errors,[]);console.log('COLOR PALETTE PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_HTML_SHARED_TYPE||process.env.RT_E2E_SHARED_RELATIVE){
   assert.ok(['html','react'].includes(kind));const before=read(),measure=()=>app.locator('p').evaluateAll(nodes=>nodes.map(el=>{const css=getComputedStyle(el);return {size:parseFloat(css.fontSize),leading:css.lineHeight,tracking:parseFloat(css.letterSpacing)||0,family:css.fontFamily};}));
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();const initial=await measure();assert.deepEqual(initial.map(v=>v.size),[20,32]);
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption(kind==='react'?'md:':'min-[768px]:');await settled();
   const picker=page.getByLabel('Shared Page font',{exact:true});assert.equal(await picker.locator('option:checked').innerText(),'Mixed');await picker.selectOption('monospace');await wait(()=>read()!==before);await settled();await wait(async()=>(await measure()).every(v=>v.family==='monospace'));assert.deepEqual((await measure()).map(v=>v.size),[20,32]);
   if(kind==='react'){
    const linked=read();await page.getByRole('button',{name:'Reset shared font family',exact:true}).click();await settled();await wait(async()=>JSON.stringify(await measure())===JSON.stringify(initial));
    assert.equal(await picker.locator('option:checked').innerText(),'Mixed');
    await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();assert.equal(read(),linked);await wait(async()=>(await measure()).every(v=>v.family==='monospace'));
   }
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await measure(),initial);
   if(process.env.RT_E2E_SHARED_TYPE_SCREENSHOT){await page.locator('#panel').evaluate(panel=>{panel.scrollTop+=panel.querySelector('[data-shared-section=typography]').getBoundingClientRect().top-panel.getBoundingClientRect().top-50;});await page.locator('#panel').screenshot({path:process.env.RT_E2E_SHARED_TYPE_SCREENSHOT});}
   const leading=page.getByLabel('Shared Line height (%)',{exact:true});await leading.fill('200');await leading.press('Tab');await wait(()=>read()!==before);await settled();await wait(async()=>JSON.stringify((await measure()).map(v=>v.leading))===JSON.stringify(['40px','64px']));const withLeading=read();
   const tracking=page.getByLabel('Shared Letter spacing (%)',{exact:true});await tracking.fill('10');await tracking.press('Tab');await wait(()=>read()!==withLeading);await settled();await wait(async()=>JSON.stringify((await measure()).map(v=>v.tracking))===JSON.stringify([2,3.2]));
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>JSON.stringify(await measure())===JSON.stringify(initial));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===withLeading);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByRole('button',{name:'Automatic shared line height',exact:true}).click();await wait(()=>read()!==before);await settled();await wait(async()=>(await measure()).every(v=>v.leading==='normal'));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();assert.deepEqual(await measure(),initial);assert.deepEqual(errors,[]);console.log('SHARED RELATIVE TYPOGRAPHY PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_SINGLE_COUPLED){
   await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();const initialSource=read(),node=app.locator('p').first(),measure=()=>node.evaluate(el=>({size:getComputedStyle(el).fontSize,leading:getComputedStyle(el).lineHeight})),initial=await measure();
   assert.equal(initial.leading,'36px');
   const edit=async(label,value)=>{const input=page.getByLabel(label,{exact:true});await input.fill(value);await input.press('Tab');await wait(()=>read()!==initialSource);await settled();};
   const undo=async()=>{await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===initialSource);await settled();assert.deepEqual(await measure(),initial);};
   await edit('Font size (px)','40');await wait(async()=>(await measure()).size==='40px');assert.equal((await measure()).leading,initial.leading);await undo();
   await edit('Line height (px)','50');await wait(async()=>(await measure()).leading==='50px');assert.equal((await measure()).size,initial.size);await undo();
   await page.getByLabel('Font size',{exact:true}).selectOption('text-4xl');await wait(()=>read()!==initialSource);await settled();await wait(async()=>(await measure()).size==='36px');assert.equal((await measure()).leading,initial.leading);await undo();
   await page.getByRole('button',{name:'Reset line height',exact:true}).click();await wait(()=>read()!==initialSource);await settled();assert.equal((await measure()).size,initial.size);assert.ok(!read().includes(process.env.RT_E2E_FLUID_TYPE?'text-[length:clamp(16px,2vw,20px)]/9':'text-lg/9'));assert.equal((await measure()).leading,process.env.RT_E2E_FLUID_TYPE?'30px':'28px');await undo();
   assert.deepEqual(errors,[]);console.log('SINGLE COUPLED TYPOGRAPHY PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_CANONICAL_TEXT){
   assert.notEqual(kind,'html');
   const checks=[['font-size','Font size (px)','40','40px'],['font-weight','Font weight (1–1000)','500','500'],['letter-spacing','Letter spacing (px)','1','1px'],['text-align','Text alignment','left','left'],['font-style','Font slant','normal','normal'],['text-decoration-line','Text decoration','none','none'],['text-transform','Text case','none','none']];
   for(const [property,label,value,expected]of checks){
    const before=read(),control=page.getByLabel(label,{exact:true});if(await control.evaluate(el=>el.tagName==='SELECT'))await control.selectOption(value);else{await control.fill(value);await control.press('Tab');}
    await wait(()=>read()!==before);await settled();await wait(async()=>await app.locator('h1').evaluate((el,property)=>getComputedStyle(el).getPropertyValue(property),property)===expected);
    assert.ok(!read().includes('!['+property+':'),property+' canonical class should be removed');assert.ok(read().includes('md:opacity-90'));assert.ok(read().includes('[&:hover]:opacity-50'));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   }
   await page.getByRole('button',{name:'Reset text overrides',exact:true}).click();await wait(()=>read()!==original);await settled();for(const property of Object.keys(canonicalProperties))assert.ok(!read().includes('!['+property+':'));
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();assert.deepEqual(errors,[]);console.log('CANONICAL TYPOGRAPHY EDIT PASS',kind,engine);return;
  }
  if(process.env.RT_E2E_TEXT_STYLES){
   const catalogFile=path.join(root,'.retouch/text-styles.json'),catalog=()=>JSON.parse(fs.readFileSync(catalogFile,'utf8'));
   const openLibrary=async()=>{const summary=page.getByText('Saved text styles',{exact:true});if(!await summary.evaluate(el=>el.parentElement.open))await summary.click();await page.getByLabel('Text style name',{exact:true}).waitFor();};
   await openLibrary();await page.getByLabel('Text style name',{exact:true}).fill('Heading');await page.getByRole('button',{name:'Save current typography',exact:true}).click();
   await wait(()=>fs.existsSync(catalogFile)&&catalog().styles.length===1);await page.getByRole('button',{name:'Rename text style',exact:true}).waitFor();
   const saved=catalog().styles[0];assert.equal(saved.properties['font-size'],'32px');assert.equal(saved.properties['font-weight'],'700');assert.equal(saved.properties['font-family'],await family());assert.equal(read(),original);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>!fs.existsSync(catalogFile));await settled();
   await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>fs.existsSync(catalogFile)&&catalog().styles[0].id===saved.id);await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
   if(kind==='react'||kind==='liquid'){
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();const smallSize=await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize);
    await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Apply text style',exact:true}).click();await wait(()=>read().includes('data-rt-text-styles'));await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');
    const beforeReactInheritance=read();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('md:');await settled();await openLibrary();await page.getByText('Inherited style: Heading · All sizes',{exact:true}).waitFor();assert.equal(await page.getByRole('button',{name:'Detach text style',exact:true}).count(),0);assert.equal(read(),beforeReactInheritance);
    await page.getByRole('button',{name:'Apply inherited style at this scope',exact:true}).click();await wait(()=>read()!==beforeReactInheritance);await settled();assert.equal(await app.locator('p').first().evaluate(el=>JSON.parse(el.getAttribute('data-rt-text-styles'))['md:'].id),saved.id);await openLibrary();await page.getByText('Linked style: Heading',{exact:true}).waitFor();
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeReactInheritance);await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();
    const beforeReactOverride=read();await page.getByLabel('Font size (px)',{exact:true}).fill('40');await page.getByLabel('Font size (px)',{exact:true}).press('Tab');await wait(()=>read()!==beforeReactOverride);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='40px');const reactOverrideSource=read();
    await openLibrary();await page.getByText('1 local override in this screen scope.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset text style overrides',exact:true}).click();await wait(()=>read()!==reactOverrideSource);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');await openLibrary();await page.getByText('No local overrides in this screen scope.',{exact:true}).waitFor();
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===reactOverrideSource);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeReactOverride);await settled();
    const linkedSource=read();assert.equal(await app.locator('p').first().evaluate(el=>JSON.parse(el.getAttribute('data-rt-text-styles'))[''].id),saved.id);
    const reactLibraryBefore=fs.readFileSync(catalogFile,'utf8'),reactOther=path.join(root,kind==='liquid'?'sections/unvisited.liquid':'components/Unvisited.jsx');fs.mkdirSync(path.dirname(reactOther),{recursive:true});fs.writeFileSync(reactOther,linkedSource);
    await page.getByLabel('Font size (px)',{exact:true}).fill('48');await page.getByLabel('Font size (px)',{exact:true}).press('Tab');await wait(()=>read()!==linkedSource);await settled();const reactBeforeUpdate=read();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);await page.getByRole('button',{name:'Update style from this layer',exact:true}).click();await wait(()=>catalog().styles[0].properties['font-size']==='48px');await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='48px');assert.ok(fs.readFileSync(reactOther,'utf8').includes('![font-size:48px]'));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===reactBeforeUpdate);await settled();assert.equal(fs.readFileSync(reactOther,'utf8'),linkedSource);assert.equal(fs.readFileSync(catalogFile,'utf8'),reactLibraryBefore);fs.unlinkSync(reactOther);await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');
    await openLibrary();await page.getByText('Linked style: Heading',{exact:true}).waitFor();await page.getByRole('button',{name:'Detach text style',exact:true}).click();await wait(()=>!read().includes('data-rt-text-styles'));await settled();assert.equal(await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize),'32px');
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('md:');await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
    await page.getByRole('button',{name:'Apply text style',exact:true}).click();await wait(()=>read().includes('data-rt-text-styles'));await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');assert.equal(await app.locator('p').first().evaluate(el=>JSON.parse(el.getAttribute('data-rt-text-styles'))['md:'].id),saved.id);
    await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)===smallSize);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
   }
   if(kind==='html'){
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
    await page.getByRole('button',{name:'Apply text style',exact:true}).click();await wait(()=>read().includes('data-rt-text-styles'));await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');
    const beforeInherited=read();await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('min-[768px]:');await settled();await openLibrary();
    await page.getByText('Inherited style: Heading · All sizes',{exact:true}).waitFor();assert.equal(await page.getByRole('button',{name:'Detach text style',exact:true}).count(),0);assert.equal(read(),beforeInherited);
    await page.getByRole('button',{name:'Apply inherited style at this scope',exact:true}).click();await wait(()=>read()!==beforeInherited);await settled();
    const scopedLinks=await app.locator('p').first().evaluate(el=>JSON.parse(el.getAttribute('data-rt-text-styles')));assert.equal(scopedLinks[0].id,saved.id);assert.equal(scopedLinks[768].id,saved.id);
    await openLibrary();assert.equal(await page.getByText('Inherited style: Heading · All sizes',{exact:true}).count(),0);await page.getByText('Linked style: Heading',{exact:true}).waitFor();
    const beforeLargerScope=read();await page.getByLabel('Screen size',{exact:true}).focus();await page.getByLabel('Screen size',{exact:true}).selectOption('1440x900');await settled();await page.getByLabel('Style screen scope').selectOption('min-[1440px]:');await settled();await openLibrary();await page.getByText('Inherited style: Heading · 768px and larger',{exact:true}).waitFor();assert.equal(read(),beforeLargerScope);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeInherited);await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();
    const beforeOverride=read();await page.getByLabel('Font size (CSS)',{exact:true}).fill('40px');await page.getByLabel('Font size (CSS)',{exact:true}).press('Tab');await wait(()=>read()!==beforeOverride);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='40px');const overriddenSource=read();
    await openLibrary();await page.getByText('1 local override in this screen scope.',{exact:true}).waitFor();await page.getByRole('button',{name:'Reset text style overrides',exact:true}).click();await wait(()=>read()!==overriddenSource);await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');await openLibrary();await page.getByText('No local overrides in this screen scope.',{exact:true}).waitFor();assert.equal(await page.getByRole('button',{name:'Reset text style overrides',exact:true}).isDisabled(),true);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===overriddenSource);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeOverride);await settled();
    const linkedSource=read(),libraryBeforeUpdate=fs.readFileSync(catalogFile,'utf8'),otherPage=path.join(root,'unvisited.html');fs.writeFileSync(otherPage,linkedSource);
    await app.locator('p').first().evaluate(el=>{el.style.setProperty('font-size','48px','important');});await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
    await page.getByRole('button',{name:'Update style from this layer',exact:true}).click();await wait(()=>catalog().styles[0].properties['font-size']==='48px');await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='48px');assert.ok(fs.readFileSync(otherPage,'utf8').includes('font-size:48px'));assert.notEqual(read(),linkedSource);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();assert.equal(fs.readFileSync(catalogFile,'utf8'),libraryBeforeUpdate);assert.equal(fs.readFileSync(otherPage,'utf8'),linkedSource);fs.unlinkSync(otherPage);await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');
    await openLibrary();await page.getByRole('button',{name:'Detach text style',exact:true}).click();await wait(()=>!read().includes('data-rt-text-styles'));await settled();assert.equal(await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize),'32px');
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===linkedSource);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();
    const baseSize=await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize);
    await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption('min-[768px]:');await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
    await page.getByRole('button',{name:'Apply text style',exact:true}).click();await wait(()=>read().includes('data-rt-text-styles'));await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)==='32px');
    await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('p').first().evaluate(el=>getComputedStyle(el).fontSize)===baseSize);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();await page.getByLabel('Style screen scope').selectOption('');await settled();
    await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
   }
   if(kind==='html'||kind==='react'){
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
    assert.equal(await page.getByRole('button',{name:'Save current typography',exact:true}).count(),0);
    if(kind==='react'){
     const picker=page.getByLabel('Shared Page font',{exact:true});assert.equal(await picker.inputValue(),'');assert.equal(await picker.locator('option:checked').innerText(),'Mixed');
     if(process.env.RT_E2E_COUPLED_TYPE){
      const before=read(),measure=()=>app.locator('p').evaluateAll(nodes=>nodes.map(el=>({size:getComputedStyle(el).fontSize,leading:getComputedStyle(el).lineHeight}))),initial=await measure();
      const size=page.getByLabel('Shared Font size (px)',{exact:true});await size.fill('40');await size.press('Tab');await wait(()=>read()!==before);await settled();await wait(async()=>(await measure()).every(value=>value.size==='40px'));assert.deepEqual((await measure()).map(v=>v.leading),initial.map(v=>v.leading));
      await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
      const leading=page.getByLabel('Shared Line height (px)',{exact:true});await leading.fill('50');await leading.press('Tab');await wait(()=>read()!==before);await settled();await wait(async()=>(await measure()).every(value=>value.leading==='50px'));assert.deepEqual((await measure()).map(v=>v.size),initial.map(v=>v.size));
      await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
     }
     const families=await app.locator('p').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).fontFamily));await picker.selectOption('monospace');await wait(()=>read()!==original);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).fontFamily==='monospace')));assert.ok(read().includes('other-font'));assert.ok(read().includes('named-font'));
     await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();assert.deepEqual(await app.locator('p').evaluateAll(nodes=>nodes.map(el=>getComputedStyle(el).fontFamily)),families);await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
    }

    await page.getByRole('button',{name:'Apply text style',exact:true}).click();await wait(()=>read()!==original);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).fontSize==='32px')));
    assert.deepEqual(await app.locator('p').evaluateAll(nodes=>nodes.map(el=>JSON.parse(el.getAttribute('data-rt-text-styles')))),[{[kind==='html'?'0':'']:{id:saved.id,properties:saved.properties}},{[kind==='html'?'0':'']:{id:saved.id,properties:saved.properties}}]);
    if(kind==='react'){
     const beforeShared=read(),checks=[['Font size (px)','40','fontSize','40px'],['Font weight (1–1000)','550','fontWeight','550'],['Line height (px)','44','lineHeight','44px'],['Letter spacing (px)','-1','letterSpacing','-1px'],['Text alignment','center','textAlign','center'],['Font slant','italic','fontStyle','italic'],['Text case','uppercase','textTransform','uppercase']];
     for(const [label,value,property,expected]of checks){const control=page.getByLabel('Shared '+label,{exact:true});if(await control.evaluate(el=>el.tagName==='SELECT'))await control.selectOption(value);else{await control.fill(value);await control.press('Tab');}await settled();await wait(async()=>await app.locator('p').evaluateAll((nodes,{property,expected})=>nodes.every(el=>getComputedStyle(el)[property]===expected),{property,expected}));}
     await openLibrary();await page.getByText('14 local overrides across the selection.',{exact:true}).waitFor();
     for(let i=0;i<checks.length;i++){await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();}await wait(()=>read()===beforeShared);
    }
    if(kind==='html'){
     const beforeShared=read(),leading=page.getByLabel('Shared Line height (%)',{exact:true});await leading.fill('200');await leading.press('Tab');await wait(()=>read()!==beforeShared);await settled();await openLibrary();await page.getByText('2 of 2 layers linked in this screen scope.',{exact:true}).waitFor();await page.getByText('2 local overrides across the selection.',{exact:true}).waitFor();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).lineHeight==='64px')));
     await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeShared);await settled();
    }
    const batchLinked=read();await openLibrary();await page.getByText('2 of 2 layers linked in this screen scope.',{exact:true}).waitFor();
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await settled();const fontControl=page.getByLabel(kind==='html'?'Font size (CSS)':'Font size (px)',{exact:true});await fontControl.fill(kind==='html'?'40px':'40');await fontControl.press('Tab');await wait(()=>read()!==batchLinked);await settled();const batchOverride=read();
    await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await openLibrary();await page.getByText('1 local override across the selection.',{exact:true}).waitFor();if(kind==='react'){assert.equal(await page.getByLabel('Shared Font size (px)',{exact:true}).inputValue(),'');assert.equal(await page.getByLabel('Shared Font size (px)',{exact:true}).getAttribute('placeholder'),'Mixed');}await page.getByRole('button',{name:'Reset selected text style overrides',exact:true}).click();await wait(()=>read()!==batchOverride);await settled();await wait(async()=>await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).fontSize==='32px')));
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchOverride);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchLinked);await settled();
    await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Shift']});await settled();await openLibrary();await page.getByRole('button',{name:'Detach selected text styles',exact:true}).click();await wait(()=>!read().includes('data-rt-text-styles'));await settled();assert.equal(await app.locator('p').evaluateAll(nodes=>nodes.every(el=>getComputedStyle(el).fontSize==='32px')),true);
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===batchLinked);await settled();
    await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
   }
   await page.getByRole('button',{name:'Save current typography',exact:true}).click();await wait(async()=>await page.getByRole('status').filter({hasText:'Text style names must be unique.'}).count()===1);assert.equal(catalog().styles.length,1);
   await page.getByLabel('Text style name',{exact:true}).fill('Display');await page.getByRole('button',{name:'Rename text style',exact:true}).click();await wait(()=>catalog().styles[0].name==='Display');assert.equal(catalog().styles[0].id,saved.id);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>catalog().styles[0].name==='Heading');await settled();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>catalog().styles[0].name==='Display');await settled();
   await page.reload();await app.locator('h1').waitFor();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();await openLibrary();await page.getByLabel('Saved text style',{exact:true}).selectOption(saved.id);
   assert.equal(await page.getByLabel('Text style name',{exact:true}).inputValue(),'Display');
   const external=catalog();external.styles[0].name='External';fs.writeFileSync(catalogFile,JSON.stringify(external));const externalBytes=fs.readFileSync(catalogFile,'utf8');
   await page.getByLabel('Text style name',{exact:true}).fill('Stale');await page.getByRole('button',{name:'Rename text style',exact:true}).click();await wait(async()=>await page.getByRole('status').filter({hasText:'Text styles changed.'}).count()===1);assert.equal(fs.readFileSync(catalogFile,'utf8'),externalBytes);
   await page.getByRole('button',{name:'Reload text styles',exact:true}).click();await wait(async()=>await page.getByLabel('Text style name',{exact:true}).inputValue()==='External');
   await page.getByRole('button',{name:'Delete text style',exact:true}).click();await page.getByRole('button',{name:'Cancel deletion',exact:true}).click();assert.equal(catalog().styles.length,1);
   if(process.env.RT_E2E_TEXT_STYLES_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_TEXT_STYLES_SCREENSHOT});
   await page.getByRole('button',{name:'Delete text style',exact:true}).click();await page.getByRole('button',{name:'Confirm delete External',exact:true}).click();await wait(()=>catalog().styles.length===0);await settled();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>catalog().styles[0]?.name==='External');await settled();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>catalog().styles.length===0);await settled();assert.equal(read(),original);
   await openLibrary();const emptyBytes=fs.readFileSync(catalogFile,'utf8'),portable={version:1,styles:[saved]},upload=async value=>page.getByLabel('Text style library file',{exact:true}).setInputFiles({name:'styles.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(value))});
   await page.getByText('Import / export styles',{exact:true}).click();await upload(portable);await wait(()=>catalog().styles.length===1);await settled();assert.deepEqual(catalog(),portable);const importedBytes=fs.readFileSync(catalogFile,'utf8');
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>fs.readFileSync(catalogFile,'utf8')===emptyBytes);await settled();await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>fs.readFileSync(catalogFile,'utf8')===importedBytes);await settled();await openLibrary();
   await page.getByText('Import / export styles',{exact:true}).click();const downloadEvent=page.waitForEvent('download');await page.getByRole('button',{name:'Export text styles',exact:true}).click();const download=await downloadEvent;assert.equal(download.suggestedFilename(),'retouch-text-styles.json');assert.deepEqual(JSON.parse(fs.readFileSync(await download.path(),'utf8')),portable);await settled();
   await upload({...portable,styles:[{...saved,properties:{'font-size':'99px'}}]});await wait(async()=>await page.getByRole('status').filter({hasText:'A different version of'}).count()===1);assert.equal(fs.readFileSync(catalogFile,'utf8'),importedBytes);
   await upload(portable);await wait(async()=>await page.getByRole('status').filter({hasText:'Import complete.'}).count()===1);assert.equal(fs.readFileSync(catalogFile,'utf8'),importedBytes);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>fs.readFileSync(catalogFile,'utf8')===emptyBytes);await settled();assert.equal(read(),original);assert.deepEqual(errors,[]);console.log('TEXT STYLE LIBRARY PASS',kind,engine);return;
  }
  const options=await page.getByLabel('Page font',{exact:true}).locator('option').evaluateAll(els=>els.map(el=>el.value)),custom=options.find(v=>v.includes('Studio_Test'));assert.ok(custom);assert.ok(options.some(v=>v.includes('Page Face')));assert.equal(read(),original);
  await page.getByText('Browse page fonts',{exact:true}).click();const search=page.getByRole('searchbox',{name:'Search page fonts'});await search.fill('NoSuchFamilyZZZ');await wait(async()=>await page.locator('.font-browser [role=status]').getAttribute('data-scanning')==='false');assert.equal(await page.getByRole('group',{name:'Matching fonts'}).getByRole('button').count(),0);assert.ok((await page.getByRole('status').allTextContents()).some(t=>t.includes('No matching fonts')));assert.equal(read(),original);assert.equal(await page.getByLabel('Page font',{exact:true}).inputValue(),await family());await search.press('Escape');assert.equal(await page.locator('.font-browser').getAttribute('open'),null);assert.equal(await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).getAttribute('aria-selected'),'true');
  const choose=async(value,browse=false)=>{snapshots.push(read());if(browse){await page.getByText('Browse page fonts',{exact:true}).click();await search.fill('sTuDiO_test');assert.equal(read(),snapshots.at(-1));const matching=page.getByRole('group',{name:'Matching fonts'}).getByRole('button');assert.equal(await matching.count(),1);if(process.env.RT_E2E_FONT_SEARCH_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_FONT_SEARCH_SCREENSHOT});await search.press('Tab');assert.equal(await matching.evaluate(el=>el===el.ownerDocument.activeElement),true);await page.keyboard.press('Enter');}else await page.getByLabel('Page font',{exact:true}).selectOption(value);await wait(()=>read()!==snapshots.at(-1));await settled();const normalize=s=>s.replace(/[\"']/g,'').replace(/\s*,\s*/g,',');try{await wait(async()=>normalize(await family())===normalize(value));}catch(error){console.log('FONT DIAGNOSTIC',JSON.stringify({source:read(),family:await family(),classes:await app.locator('h1').getAttribute('class')}));throw error;}if(kind==='liquid'&&process.env.RT_E2E_LIQUID_DYNAMIC){const alternate=await browser.newPage({viewport:await app.locator('html').evaluate(el=>({width:el.ownerDocument.defaultView.innerWidth,height:el.ownerDocument.defaultView.innerHeight}))});try{await alternate.goto(url+'/?alternate=1');assert.equal(normalize(await alternate.locator('h1').evaluate(el=>getComputedStyle(el).fontFamily)),normalize(value));assert.equal(await alternate.locator('h1').evaluate(el=>getComputedStyle(el).fontWeight),'700');}finally{await alternate.close();}}};
  await choose('monospace');assert.equal(await family(),'monospace');assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).fontWeight),'700');assert.match(read(),/type-editorial/);if(kind!=='html'){await app.locator('h1').hover();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).opacity)==='0.5');await page.getByRole('button',{name:'Undo',exact:true}).hover();}
  await choose(options.find(v=>v.includes('Page Face')));assert.match(await family(),/Page Face/);
  await choose(options.find(v=>v.includes('Page Face')&&v.includes(',')));assert.match(await family(),/Page Face/);
  await choose(custom,true);assert.match(await family(),/Studio_Test/);assert.doesNotMatch(await family(),/Studio Test/);
  await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await choose('serif');assert.equal(await family(),'serif');await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();assert.match(await family(),/Studio_Test/);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
  if(kind!=='html')await wait(async()=>{try{return await page.frameLocator('iframe[title="Typography preview"]').locator('body div').evaluate(el=>getComputedStyle(el).fontFamily)==='serif';}catch(error){if(/Frame was detached/.test(error.message))return false;throw error;}});
  snapshots.push(read());await page.getByRole('button',{name:'Reset font family',exact:true}).click();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>/Studio_Test/.test(await family()));if(process.env.RT_E2E_FONT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_FONT_SCREENSHOT});
  while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}assert.equal(read(),original);await wait(async()=>(variableFont?/Variable Test/:/Georgia/).test(await family()));if(process.env.RT_E2E_CUSTOM_WEIGHT){
   const weight=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).fontWeight),input=page.getByLabel(kind==='html'?'Font weight (CSS)':'Font weight (1–1000)',{exact:true});
   await page.getByLabel('Style screen scope').selectOption('');await settled();
   const writeWeight=async value=>{snapshots.push(read());await input.fill(value);await input.press('Tab');await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>await weight()===value);if(kind!=='html')assert.equal(await input.inputValue(),value,'inspector weight must reflect the rendered revision');};
   await writeWeight('537.5');let baseInk;
   const ink=()=>app.locator('h1').evaluate(async el=>{const d=el.ownerDocument;await d.fonts.ready;const face=[...d.fonts].find(f=>f.family.replace(/["']/g,'')==='Variable Test');if(face?.status!=='loaded')throw Error('Variable font is not loaded');const css=d.defaultView.getComputedStyle(el),canvas=d.createElement('canvas');canvas.width=400;canvas.height=80;const ctx=canvas.getContext('2d');ctx.font=css.fontWeight+' 32px '+css.fontFamily;ctx.fillText(el.textContent,5,50);const data=ctx.getImageData(0,0,400,80).data;let sum=0;for(let i=3;i<data.length;i+=4)sum+=data[i];return sum;});
   if(process.env.RT_E2E_VARIABLE_FONT)baseInk=await ink();assert.match(await family(),(variableFont?/Variable Test/:/Georgia/));if(kind!=='html'){assert.equal(await page.getByLabel('Font weight',{exact:true}).inputValue(),'');assert.equal(await page.getByLabel('Font weight',{exact:true}).locator('option:checked').textContent(),'Inherited / custom');for(const invalid of ['0','1001']){const before=read();await input.fill(invalid);await input.press('Tab');assert.equal(await input.evaluate(el=>el.checkValidity()),false);assert.equal(read(),before);}await input.fill('537.5');}
   const beforeScope=read();await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();assert.equal(read(),beforeScope,'changing scope after restoring an unchanged field must not write');await writeWeight('725.5');if(process.env.RT_E2E_VARIABLE_FONT){const heavyInk=await ink();assert.ok(heavyInk>baseInk*1.05,JSON.stringify({baseInk,heavyInk}));console.log('VARIABLE GLYPH INK',JSON.stringify({baseInk,heavyInk}));}
   if(process.env.RT_E2E_FONT_WEIGHT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_FONT_WEIGHT_SCREENSHOT});
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await weight()==='537.5');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>await weight()==='725.5');
   if(kind!=='html'){snapshots.push(read());await page.getByLabel('Font weight',{exact:true}).selectOption('font-medium');await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>await weight()==='500');}
   snapshots.push(read());await page.getByRole('button',{name:'Reset font weight',exact:true}).click();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>await weight()==='537.5');assert.match(await family(),(variableFont?/Variable Test/:/Georgia/));
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await settled();await wait(()=>read()===expected);await settled();}await wait(async()=>await weight()==='700');assert.equal(read(),original);
   console.log(engine+' '+kind+': PASS custom fractional weights, scope isolation, reset, family retention and exact undo'+(kind==='html'?'':', range validation and preset replacement'));
  }
  if(process.env.RT_E2E_OPTICAL_SIZING){
    await page.getByLabel('Style screen scope').selectOption('');await settled();
    const extent=()=>app.locator('h1').evaluate(async el=>{await el.ownerDocument.fonts.ready;const range=el.ownerDocument.createRange();range.selectNodeContents(el);return range.getBoundingClientRect().width;}),autoWidth=await extent();
    const optical=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).fontOpticalSizing);
    const change=async(action,expected)=>{snapshots.push(read());await action();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>await optical()===expected);if(kind!=='html')await wait(async()=>{try{return await page.frameLocator('iframe[title="Typography preview"]').locator('body div').evaluate(el=>getComputedStyle(el).fontOpticalSizing)===expected;}catch(error){if(/Frame was detached|Execution context was destroyed/.test(error.message))return false;throw error;}});};
    await change(()=>page.getByLabel('Optical sizing',{exact:true}).selectOption('none'),'none');if(process.env.RT_E2E_OPTICAL_FONT){const offWidth=await extent();assert.ok(Math.abs(autoWidth-offWidth)>.1,JSON.stringify({autoWidth,offWidth}));console.log(engine+' '+kind+': optical glyph widths '+JSON.stringify({autoWidth,offWidth}));}
    await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await change(()=>page.getByLabel('Optical sizing',{exact:true}).selectOption('auto'),'auto');
    await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await optical()==='none');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>await optical()==='auto');
    await change(()=>page.getByRole('button',{name:'Reset optical sizing',exact:true}).click(),'none');
    const axisSection=page.locator('details').filter({has:page.locator('summary').getByText('Variable font axes',{exact:true})});if(await axisSection.getAttribute('open')===null)await page.getByText('Variable font axes',{exact:true}).click();
    await change(()=>page.getByLabel('Add font axis',{exact:true}).selectOption('opsz'),'none');await wait(async()=>await page.getByText('The explicit Optical size axis overrides automatic sizing. Remove that axis to let the font adapt to text size.',{exact:true}).count()===1);
    if(process.env.RT_E2E_OPTICAL_FONT){
      await page.getByRole('button',{name:'Inspect declared font axes',exact:true}).click();await wait(async()=>/Optical Size \(opsz\): 8 to 144 · default 14/.test(await page.getByRole('group',{name:'Optical size axis controls',exact:true}).textContent()));
      const input=page.getByLabel('Optical size axis',{exact:true});assert.equal(await input.getAttribute('min'),'8');assert.equal(await input.getAttribute('max'),'144');
      if(process.env.RT_E2E_FONT_PRESETS){
        const preset=page.getByLabel('Font style preset',{exact:true});assert.equal(await preset.locator('option').count(),21);
        await change(()=>preset.selectOption({label:'Bold Italic'}),'none');await wait(async()=>await preset.locator('option:checked').textContent()==='Bold Italic');
        const rendered=await app.locator('h1').evaluate(el=>getComputedStyle(el).fontVariationSettings);assert.match(rendered,/"wght" 700/);assert.match(rendered,/"slnt" -10/);assert.match(rendered,/"opsz" 14/);assert.equal(rendered.split(',').length,13);
        await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await app.locator('h1').evaluate(el=>getComputedStyle(el).fontVariationSettings)==='normal');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>await preset.locator('option:checked').textContent()==='Bold Italic');
        const hiddenAxis=page.getByLabel('XOPQ axis',{exact:true}),advanced=page.locator('details').filter({has:page.locator('summary').getByText('Advanced font axes',{exact:true})}).last();await wait(async()=>!(await hiddenAxis.isVisible())&&await page.getByLabel('Weight axis',{exact:true}).isVisible());
        const beforeAdvanced=read();await page.getByText('Advanced font axes',{exact:true}).click();assert.equal(read(),beforeAdvanced);assert.equal(await hiddenAxis.isVisible(),true);
        await change(async()=>{await hiddenAxis.fill('97');await hiddenAxis.press('Tab');},'none');await wait(async()=>/"XOPQ" 97/.test(await app.locator('h1').evaluate(el=>getComputedStyle(el).fontVariationSettings)));assert.notEqual(await advanced.getAttribute('open'),null);
        const beforeAdvancedEdit=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeAdvancedEdit);await settled();await wait(async()=>await preset.locator('option:checked').textContent()==='Bold Italic');assert.notEqual(await advanced.getAttribute('open'),null);await page.getByText('Advanced font axes',{exact:true}).click();assert.equal(await hiddenAxis.isVisible(),false);assert.equal(read(),beforeAdvanced);
        if(process.env.RT_E2E_FONT_PRESETS_SCREENSHOT){await wait(async()=>{try{await preset.scrollIntoViewIfNeeded();return true;}catch(error){if(/not attached/.test(error.message))return false;throw error;}});await page.screenshot({path:process.env.RT_E2E_FONT_PRESETS_SCREENSHOT});}
        console.log(engine+' '+kind+': PASS named Bold Italic preset, all 13 coordinates, advanced axis editing/undo and responsive isolation');
      }
      let reinspections=0;const metadataRequest=request=>{if(request.url().endsWith('/rt/__api/font-axes'))reinspections++;};page.on('request',metadataRequest);
      await change(async()=>{await input.fill('8');await input.press('Tab');},'none');await wait(async()=>await input.getAttribute('max')==='144');assert.equal(await page.getByRole('slider',{name:'Adjust Optical Size axis',exact:true}).count(),1);const small=await extent();await change(async()=>{await input.fill('144');await input.press('Tab');},'none');const large=await extent();assert.ok(Math.abs(small-large)>.1,JSON.stringify({small,large}));
      await change(()=>page.getByLabel('Optical sizing',{exact:true}).selectOption('auto'),'auto');assert.ok(Math.abs((await extent())-large)<.05);await wait(async()=>await input.getAttribute('max')==='144');assert.equal(reinspections,0);page.off('request',metadataRequest);console.log(engine+' '+kind+': PASS real opsz metadata, cached ranges after source reload, visible axis changes and explicit-axis precedence '+JSON.stringify({small,large}));
    }
    while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}assert.equal(read(),original);await wait(async()=>await optical()==='auto');
    if(await axisSection.getAttribute('open')!==null)await page.getByText('Variable font axes',{exact:true}).click();
    console.log(engine+' '+kind+': PASS optical sizing, responsive isolation, scoped reset, explicit-axis explanation, type preview and exact undo');
  }
  if(process.env.RT_E2E_FONT_AXES){
   if(process.env.RT_E2E_VARIABLE_FONT){
    const beforeMetadata=read(),font=fs.readFileSync(path.join(fixture,'node_modules/next/dist/next-devtools/server/font/geist-latin.woff2')).toString('base64');
    const metadata=await page.evaluate(async encoded=>{const response=await fetch('/rt/__api/font-axes',{method:'POST',headers:{'x-retouch-token':window.__RT_TOKEN,'content-type':'application/octet-stream'},body:Uint8Array.from(atob(encoded),char=>char.charCodeAt(0))});return {status:response.status,...await response.json()};},font);
    assert.equal(metadata.status,200);assert.equal(metadata.ok,true);assert.deepEqual(metadata.axes,[{tag:'wght',name:'Weight',min:100,default:400,max:900,hidden:false}]);assert.equal(read(),beforeMetadata);
    console.log(engine+' '+kind+': PASS authenticated real WOFF2 metadata API without source changes');
   }
   await page.getByLabel('Style screen scope').selectOption('');await settled();await page.getByText('Variable font axes',{exact:true}).click();
   const axes=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).fontVariationSettings);
   const write=async(action,expected)=>{snapshots.push(read());await action();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>expected.test(await axes()));if(kind!=='html')await wait(async()=>{try{return expected.test(await page.frameLocator('iframe[title="Typography preview"]').locator('body div').evaluate(el=>getComputedStyle(el).fontVariationSettings));}catch(error){if(/Frame was detached|Execution context was destroyed/.test(error.message))return false;throw error;}});};
   await write(()=>page.getByLabel('Add font axis',{exact:true}).selectOption('wght'),/wght/);
   await write(async()=>{const input=page.getByLabel('Weight axis',{exact:true});await input.fill('200');await input.press('Tab');},/200/);
   const extent=()=>app.locator('h1').evaluate(async el=>{await el.ownerDocument.fonts.ready;const range=el.ownerDocument.createRange();range.selectNodeContents(el);return range.getBoundingClientRect().width;});const lightWidth=await extent();
   await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();
   await write(async()=>{const input=page.getByLabel('Weight axis',{exact:true});await input.fill('850');await input.press('Tab');},/850/);const heavyWidth=await extent();
   if(process.env.RT_E2E_VARIABLE_FONT)assert.ok(Math.abs(heavyWidth-lightWidth)>.01,JSON.stringify({lightWidth,heavyWidth}));
   if(process.env.RT_E2E_FONT_AXES_SCREENSHOT){await page.getByLabel('Weight axis',{exact:true}).scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_FONT_AXES_SCREENSHOT});}
   if(process.env.RT_E2E_FONT_DISCOVERY){
    const beforeInspect=read();let requests=0;const listener=request=>{if(request.url().endsWith('/rt/__api/font-axes'))requests++;};page.on('request',listener);
    await page.getByRole('button',{name:'Inspect declared font axes',exact:true}).click();await wait(async()=>/Weight \(wght\): 100 to 900 · default 400/.test(await page.getByRole('group',{name:'Weight axis controls',exact:true}).textContent()));assert.equal(requests,1);page.off('request',listener);assert.equal(read(),beforeInspect);
    const weightInput=page.getByLabel('Weight axis',{exact:true});assert.equal(await weightInput.getAttribute('min'),'100');assert.equal(await weightInput.getAttribute('max'),'900');
    if(process.env.RT_E2E_FONT_DISCOVERY_SCREENSHOT){await page.getByRole('button',{name:'Use Weight default',exact:true}).scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_FONT_DISCOVERY_SCREENSHOT});}
    await weightInput.fill('950');await weightInput.press('Tab');assert.equal(await weightInput.evaluate(node=>node.checkValidity()),false);assert.equal(read(),beforeInspect);await weightInput.fill('850');await weightInput.press('Tab');await settled();
    await write(()=>page.getByRole('group',{name:'Weight axis controls',exact:true}).getByRole('button',{name:'Use Weight default',exact:true}).click(),/400/);const beforeDefault=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeDefault);await settled();await wait(async()=>/850/.test(await axes()));assert.equal(await page.getByLabel('Weight axis',{exact:true}).getAttribute('max'),'900');
    const beforeFailure=read(),failureRoute='**/rt/__api/font-axes';await page.route(failureRoute,route=>route.fulfill({status:422,contentType:'application/json',body:JSON.stringify({ok:false,reason:'Font metadata could not be read.'})}));
    await page.getByRole('button',{name:'Inspect declared font axes',exact:true}).click();await wait(async()=>/Font metadata could not be read/.test(await page.getByLabel('Declared font axes',{exact:true}).textContent()));assert.equal(read(),beforeFailure);assert.equal(await page.getByLabel('Weight axis',{exact:true}).getAttribute('max'),'10000');assert.equal(await page.getByRole('button',{name:'Use Weight default',exact:true}).count(),0);
    await page.unroute(failureRoute);await page.getByRole('button',{name:'Inspect declared font axes',exact:true}).click();await wait(async()=>await page.getByLabel('Weight axis',{exact:true}).getAttribute('max')==='900');assert.equal(read(),beforeFailure);
    const slider=page.getByRole('group',{name:'Weight axis controls',exact:true}).getByRole('slider',{name:'Adjust Weight axis',exact:true});const sliderBox=async()=>{let box;await wait(async()=>{try{await slider.scrollIntoViewIfNeeded();box=await slider.boundingBox();return !!box;}catch(error){if(/not attached/.test(error.message))return false;throw error;}});return box;};await sliderBox();
    const beforeSlide=read(),inlineBefore=await app.locator('h1').getAttribute('style');let rect=await slider.boundingBox();assert.ok(rect);
    await page.mouse.move(rect.x+rect.width*.8,rect.y+rect.height/2);await page.mouse.down();await page.mouse.move(rect.x+rect.width*.3,rect.y+rect.height/2,{steps:8});assert.equal(read(),beforeSlide);assert.ok(Math.abs(Number((await axes()).match(/"wght"\s+([\d.]+)/)[1])-Number(await slider.inputValue()))<.01);assert.ok(Math.abs((await extent())-heavyWidth)>.01);assert.ok(Math.abs(Number(await weightInput.inputValue())-Number(await slider.inputValue()))<.01);if(process.env.RT_E2E_FONT_LIVE_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_FONT_LIVE_SCREENSHOT});await page.keyboard.press('Escape');await page.mouse.up();await settled();assert.equal(read(),beforeSlide);assert.equal(await slider.inputValue(),'850');assert.equal(await app.locator('h1').getAttribute('style'),inlineBefore);assert.match(await axes(),/850/);
    if(process.env.RT_E2E_FONT_PREVIEW_CLEANUP){
      rect=await sliderBox();await page.mouse.move(rect.x+rect.width*.8,rect.y+rect.height/2);await page.mouse.down();await page.mouse.move(rect.x+rect.width*.3,rect.y+rect.height/2,{steps:8});
      await app.locator('h1').evaluate(el=>el.style.setProperty('color','rgb(1, 2, 3)'));await page.keyboard.press('Escape');await page.mouse.up();await settled();assert.match(await axes(),/850/);assert.equal(await app.locator('h1').evaluate(el=>el.style.color),'rgb(1, 2, 3)');assert.equal(read(),beforeSlide);
      await app.locator('h1').evaluate((el,original)=>original===null?el.removeAttribute('style'):el.setAttribute('style',original),inlineBefore);await settled();
      rect=await sliderBox();await page.mouse.move(rect.x+rect.width*.8,rect.y+rect.height/2);await page.mouse.down();await page.mouse.move(rect.x+rect.width*.3,rect.y+rect.height/2,{steps:8});
      await slider.evaluate(el=>el.remove());await wait(async()=>await app.locator('h1').getAttribute('style')===inlineBefore);await page.mouse.up();await settled();await wait(async()=>/850/.test(await axes()));assert.equal(read(),beforeSlide);
      await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click();await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await settled();
      console.log(engine+' '+kind+': PASS live-preview cleanup on control removal and preservation of concurrent page styles');
    }
    rect=await sliderBox();
    await page.mouse.move(rect.x+rect.width*.8,rect.y+rect.height/2);await page.mouse.down();await page.mouse.move(rect.x+rect.width*.4,rect.y+rect.height/2,{steps:8});const slid=Number(await slider.inputValue());assert.ok(slid>100&&slid<800);assert.equal(read(),beforeSlide);assert.ok(Math.abs(Number((await axes()).match(/"wght"\s+([\d.]+)/)[1])-slid)<.01);
    await write(()=>page.mouse.up(),/wght/);const rendered=Number((await axes()).match(/"wght"\s+([\d.]+)/)[1]);assert.ok(Math.abs(rendered-slid)<.01);assert.equal(await app.locator('h1').getAttribute('style'),inlineBefore);
    const beforeGesture=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeGesture);await settled();await wait(async()=>/850/.test(await axes()));
    await write(()=>slider.press('Home'),/100/);
    if(process.env.RT_E2E_AXIS_KEYBOARD){
      await wait(()=>slider.evaluate(el=>el===el.ownerDocument.activeElement));
      let releaseFocus,focusHeld=false;const focusGate=new Promise(resolve=>{releaseFocus=resolve;}),holdFocus=async route=>{focusHeld=true;await focusGate;await route.continue();};await page.route('**/rt/__api/op',holdFocus);
      try{await write(async()=>{await page.keyboard.press('End');await wait(()=>focusHeld);await page.locator('#panel').evaluate(el=>{el.scrollTop=el.scrollHeight;});releaseFocus();},/900/);await wait(()=>slider.evaluate(el=>el===el.ownerDocument.activeElement));assert.equal(await slider.evaluate(el=>{const r=el.getBoundingClientRect(),p=el.ownerDocument.getElementById('panel').getBoundingClientRect();return r.top>=p.top&&r.bottom<=p.bottom;}),true);}finally{releaseFocus();await page.unroute('**/rt/__api/op',holdFocus);}
      const beforeEnd=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeEnd);await settled();await wait(async()=>/100/.test(await axes()));
      console.log(engine+' '+kind+': PASS slider keyboard focus survives source save and inspector reconstruction');
    }
    const beforeKey=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===beforeKey);await settled();await wait(async()=>/850/.test(await axes()));
    if(process.env.RT_E2E_FONT_DISCOVERY_SCREENSHOT){await slider.scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_FONT_DISCOVERY_SCREENSHOT});}
    console.log(engine+' '+kind+': PASS declared font discovery, metadata request, range validation, default application, cached metadata, failure recovery, live glyph preview, pointer commit/cancel, keyboard slider and exact undo');
   }
   await write(()=>page.getByLabel('Add font axis',{exact:true}).selectOption('wdth'),/wdth/);assert.match(await axes(),/850/);
   await write(()=>page.getByRole('button',{name:'Remove Width axis',exact:true}).click(),/850/);assert.doesNotMatch(await axes(),/wdth/);
   const beforeCustom=read();await page.getByLabel('Add font axis',{exact:true}).selectOption('custom');assert.equal(read(),beforeCustom);
   const tag=page.getByRole('textbox',{name:'Custom axis tag',exact:true});await tag.fill('BAD');await page.getByRole('button',{name:'Add custom axis',exact:true}).click();assert.equal(await tag.evaluate(node=>node.checkValidity()),false);assert.equal(read(),beforeCustom);
   await tag.fill('wght');await page.getByRole('button',{name:'Add custom axis',exact:true}).click();assert.equal(await tag.evaluate(node=>node.validationMessage),'This axis is already listed.');assert.equal(read(),beforeCustom);
   await tag.fill('GRAD');await page.getByLabel('Initial axis value',{exact:true}).fill('-12.5');await write(()=>page.getByLabel('Initial axis value',{exact:true}).press('Enter'),/GRAD/);assert.match(await axes(),/-12.5/);assert.match(await axes(),/850/);
   await write(()=>page.getByRole('button',{name:'Remove GRAD axis',exact:true}).click(),/850/);assert.doesNotMatch(await axes(),/GRAD/);
   const beforeCancel=read();await page.getByLabel('Add font axis',{exact:true}).selectOption('custom');await tag.fill('GRAD');await tag.press('Escape');assert.equal(read(),beforeCancel);assert.equal(await page.getByLabel('Add font axis',{exact:true}).inputValue(),'');
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>/200/.test(await axes()));await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>/850/.test(await axes()));
   await write(()=>page.getByRole('button',{name:'Reset font axes',exact:true}).click(),/200/);
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}assert.equal(read(),original);await wait(async()=>await axes()==='normal');
   console.log(engine+' '+kind+': PASS variable axes, independent removal, breakpoint isolation, reset and exact undo; glyph extents '+JSON.stringify({lightWidth,heavyWidth}));
  }
  if(process.env.RT_E2E_LINE_HEIGHT){
   const height=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).lineHeight),initialHeight=await height(),initialFamily=await family(),initialWeight=await app.locator('h1').evaluate(el=>getComputedStyle(el).fontWeight),input=page.getByLabel(kind==='html'?'Line height (CSS)':'Line height (px)',{exact:true});
   const write=async(action,expected)=>{snapshots.push(read());await action();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>await height()===expected);assert.equal(await family(),initialFamily);assert.equal(await app.locator('h1').evaluate(el=>getComputedStyle(el).fontWeight),initialWeight);};
   const pixels=async value=>{await input.fill(value+(kind==='html'?'px':''));await input.press('Tab');};
   await page.getByLabel('Style screen scope').selectOption('');await settled();await write(()=>pixels('80'),'80px');
   await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await write(()=>page.getByRole('button',{name:'Automatic line height',exact:true}).click(),'normal');
   if(kind!=='html'){assert.equal(await input.inputValue(),'');assert.equal(await input.getAttribute('placeholder'),'Automatic');await write(()=>page.getByRole('button',{name:'Reset text overrides',exact:true}).click(),'80px');const automatic=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===automatic);await settled();await wait(async()=>await height()==='normal');}
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>await height()==='80px');await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>await height()==='normal');
   await write(()=>pixels('45'),'45px');await write(()=>page.getByRole('button',{name:'Reset line height',exact:true}).click(),'80px');
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}await wait(async()=>await height()===initialHeight);assert.equal(read(),original);
   console.log(engine+' '+kind+': PASS automatic/custom line height, responsive isolation, family/weight retention, reset and exact undo');
  }
  if(process.env.RT_E2E_RELATIVE_SPACING){
   const spacing=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).letterSpacing),initial=await spacing();
   const edit=async(label,value,expected)=>{snapshots.push(read());const input=page.getByLabel(label,{exact:true});await input.fill(value);await input.press('Tab');await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>Math.abs(parseFloat(await spacing())-expected)<.02);};
   await page.getByLabel('Style screen scope').selectOption('');await settled();await edit('Letter spacing (%)','10',3.2);
   assert.match(read(),/0\.1em/);await edit(kind==='html'?'Font size (CSS)':'Font size (px)',kind==='html'?'40px':'40',4);
   await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await edit('Letter spacing (%)','-5',-2);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>Math.abs(parseFloat(await spacing())-4)<.02);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>Math.abs(parseFloat(await spacing())+2)<.02);
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}await wait(async()=>await spacing()===initial);assert.equal(read(),original);
   console.log(engine+' '+kind+': PASS percentage letter spacing scales with font size, negative tablet override, phone isolation and exact undo');
  }
  if(process.env.RT_E2E_NUMERIC){
   const numeric=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).fontVariantNumeric),initial=await numeric();
   const same=(actual,expected)=>actual.split(' ').sort().join(' ')===expected.split(' ').sort().join(' ');
   const change=async(label,value,expected)=>{snapshots.push(read());await page.getByLabel(label,{exact:true}).selectOption(value);await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>same(await numeric(),expected));assert.equal(await page.locator('.numeric-typography').getAttribute('open'),'');};
   await page.getByLabel('Style screen scope').selectOption('');await settled();
   if(process.env.RT_E2E_VARIABLE_FONT){snapshots.push(read());const weight=page.getByLabel(kind==='html'?'Font weight (CSS)':'Font weight (1–1000)',{exact:true});await weight.fill('400');await weight.press('Tab');await wait(()=>read()!==snapshots.at(-1));await settled();await wait(()=>app.locator('h1').evaluate(el=>getComputedStyle(el).fontWeight==='400'));}
   await page.getByText('Number formatting',{exact:true}).click();
   const digitWidths=()=>app.locator('h1').evaluate(async el=>{const d=el.ownerDocument;await d.fonts.ready;const css=d.defaultView.getComputedStyle(el),probe=d.createElement('span');probe.style.cssText='position:absolute;visibility:hidden;white-space:pre;';for(const property of ['font-family','font-size','font-weight','font-variant-numeric'])probe.style.setProperty(property,css.getPropertyValue(property));d.body.append(probe);try{return ['111111','888888'].map(text=>{probe.textContent=text;return probe.getBoundingClientRect().width;});}finally{probe.remove();}});
   await change('Number width','tabular-nums','tabular-nums');
   if(process.env.RT_E2E_VARIABLE_FONT){const widths=await digitWidths();assert.ok(Math.abs(widths[0]-widths[1])<.1,JSON.stringify(widths));console.log('TABULAR DIGIT WIDTHS',JSON.stringify(widths));}

   if(process.env.RT_E2E_VARIABLE_FONT){await change('Number width','proportional-nums','proportional-nums');const widths=await digitWidths();assert.ok(Math.abs(widths[0]-widths[1])>.5,JSON.stringify(widths));console.log('PROPORTIONAL DIGIT WIDTHS',JSON.stringify(widths));await change('Number width','tabular-nums','tabular-nums');}
   await change('Number style','oldstyle-nums','tabular-nums oldstyle-nums');
   await change('Fractions','diagonal-fractions','tabular-nums oldstyle-nums diagonal-fractions');
   await change('Ordinals','ordinal','tabular-nums oldstyle-nums diagonal-fractions ordinal');
   await change('Zero style','slashed-zero','tabular-nums oldstyle-nums diagonal-fractions ordinal slashed-zero');
   if(process.env.RT_E2E_NUMERIC_SCREENSHOT){await page.getByLabel('Zero style',{exact:true}).scrollIntoViewIfNeeded();await page.screenshot({path:process.env.RT_E2E_NUMERIC_SCREENSHOT});}
   const baseNumeric=await numeric();
   await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();
   await change('Number width','proportional-nums','proportional-nums oldstyle-nums diagonal-fractions ordinal slashed-zero');

   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>same(await numeric(),baseNumeric));
   await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();
   snapshots.push(read());await page.getByRole('button',{name:'Reset number formatting',exact:true}).click();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>same(await numeric(),baseNumeric));
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}await wait(async()=>same(await numeric(),initial));assert.equal(read(),original);
   console.log(engine+' '+kind+': PASS numeric feature composition, open controls survive edits, responsive isolation, reset and exact undo');
  }
  if(process.env.RT_E2E_PANEL_TAB){
   await page.getByLabel('Style screen scope').selectOption('');await settled();
   const before=read(),input=page.getByLabel('Line height (%)',{exact:true}),next=page.getByRole('button',{name:'Use relative line height',exact:true});
   await input.fill('181');await input.press('Tab');await wait(()=>read()!==before);await settled();
   await wait(()=>next.evaluate(el=>el===el.ownerDocument.activeElement));
   await page.keyboard.press('Shift+Tab');assert.equal(await input.evaluate(el=>el===el.ownerDocument.activeElement),true);
   await input.press('Tab');assert.equal(await next.evaluate(el=>el===el.ownerDocument.activeElement),true);
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   assert.equal(await page.getByRole('button',{name:'Undo',exact:true}).evaluate(el=>el.ownerDocument.activeElement?.getAttribute('aria-label'))==='Line height (%)',false);
   let release,intercepted=false;const gate=new Promise(resolve=>{release=resolve;});
   const hold=async route=>{intercepted=true;await gate;await route.continue();};
   await page.route('**/rt/__api/op',hold);
   try{
    await input.fill('182');await input.press('Tab');await wait(()=>intercepted);
    await page.getByRole('searchbox',{name:'Find a layer',exact:true}).click();release();
    await wait(()=>read()!==before);await settled();
    assert.equal(await page.getByRole('searchbox',{name:'Find a layer',exact:true}).evaluate(el=>el===el.ownerDocument.activeElement),true,'a deliberate click during save must retain focus');
   }finally{release();await page.unroute('**/rt/__api/op',hold);}
   await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===before);await settled();
   console.log(engine+' '+kind+': PASS Tab retains next control after save/rebuild, Shift+Tab returns, unchanged Tab does not write, explicit click cancels queued focus, exact undo');
  }
  if(process.env.RT_E2E_CONVERT_SPACING){
   await page.getByLabel('Style screen scope').selectOption('');await settled();
   const write=async action=>{snapshots.push(read());await action();await wait(()=>read()!==snapshots.at(-1));await settled();};
   const edit=async(label,value)=>write(async()=>{const input=page.getByLabel(label,{exact:true});await input.fill(value);await input.press('Tab');});
   const metrics=()=>app.locator('h1').evaluate(el=>{const s=getComputedStyle(el);return [parseFloat(s.lineHeight),parseFloat(s.letterSpacing)];});
   await edit(kind==='html'?'Line height (CSS)':'Line height (px)',kind==='html'?'64px':'64');
   await edit(kind==='html'?'Line height (CSS)':'Line height (px)',kind==='html'?'48px':'48');
   await edit(kind==='html'?'Letter spacing (CSS)':'Letter spacing (px)',kind==='html'?'3.2px':'3.2');
   assert.equal(await page.getByLabel('Line height (%)',{exact:true}).inputValue(),'150');
   assert.equal(await page.getByLabel('Letter spacing (%)',{exact:true}).inputValue(),'10');
   await write(()=>page.getByRole('button',{name:'Use relative line height',exact:true}).click());
   await write(()=>page.getByRole('button',{name:'Use relative letter spacing',exact:true}).click());
   assert.deepEqual(await metrics(),[48,3.2]);
   if(process.env.RT_E2E_CONVERT_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_CONVERT_SCREENSHOT});
   await edit(kind==='html'?'Font size (CSS)':'Font size (px)',kind==='html'?'40px':'40');
   await wait(async()=>JSON.stringify(await metrics())==='[60,4]');
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}assert.equal(read(),original);
   console.log(engine+' '+kind+': PASS unchanged-value conversion preserves appearance, scales both spacing properties with font size, and exact undo');
  }
  if(process.env.RT_E2E_RELATIVE_LINE_HEIGHT){
   const height=()=>app.locator('h1').evaluate(el=>getComputedStyle(el).lineHeight),initial=await height();
   const edit=async(label,value,expected)=>{snapshots.push(read());const input=page.getByLabel(label,{exact:true});await input.fill(value);await input.press('Tab');await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>Math.abs(parseFloat(await height())-expected)<.02);};
   await page.getByLabel('Style screen scope').selectOption('');await settled();await edit('Line height (%)','175',56);
   await edit(kind==='html'?'Font size (CSS)':'Font size (px)',kind==='html'?'40px':'40',70);
   assert.equal(Number(await page.getByLabel('Line height (%)',{exact:true}).inputValue()),175);
   await page.getByLabel('Style screen scope').selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await edit('Line height (%)','200',80);
   await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await settled();await wait(async()=>Math.abs(parseFloat(await height())-70)<.02);await page.getByLabel('Screen size',{exact:true}).selectOption('768x1024');await settled();await wait(async()=>Math.abs(parseFloat(await height())-80)<.02);
   snapshots.push(read());await page.getByRole('button',{name:'Reset line height',exact:true}).click();await wait(()=>read()!==snapshots.at(-1));await settled();await wait(async()=>Math.abs(parseFloat(await height())-70)<.02);
   while(snapshots.length){const expected=snapshots.pop();await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}await wait(async()=>await height()===initial);assert.equal(read(),original);
   console.log(engine+' '+kind+': PASS percentage line height scales with font size, tablet override, phone isolation, reset and exact undo');
  }
  assert.deepEqual(errors,[]);console.log(engine+' '+kind+': PASS searchable page font catalog, keyboard apply/cancel, no-match preservation, declared and used families, underscore preservation, weight/named-style retention, responsive isolation, reset, preview and exact source undo');
 }catch(error){if(process.env.RT_E2E_FAILURE_TRACE&&browser)try{console.error('UI FAILURE',await browser.contexts()[0].pages()[0].evaluate(()=>({selection:sel&&{id:sel.info.id,hash:sel.info.hash,kind:sel.info.kind},busy:[panelTasks,undoBusy,sourceRequests],undo:{disabled:document.getElementById('undoBtn').disabled,rect:document.getElementById('undoBtn').getBoundingClientRect().toJSON()},dialogs:[...document.querySelectorAll('dialog[open]')].map(el=>el.textContent),footer:[...doc().querySelectorAll('footer')].map(el=>el.outerHTML),ui:document.body.innerText.slice(-2500)})));console.error('SOURCE FAILURE',read());if(process.env.RT_E2E_FAILURE_SCREENSHOT)await browser.contexts()[0].pages()[0].screenshot({path:process.env.RT_E2E_FAILURE_SCREENSHOT});}catch{}if(logs)console.error(logs);throw error;}finally{if(browser)await browser.close();if(child){child.kill('SIGTERM');await stopped;}if(server){server.retouchIndex.close();server.closeAllConnections();await new Promise(r=>server.close(r));}fs.rmSync(root,{recursive:true,force:true});}
})().catch(error=>{console.error(error);process.exitCode=1;});
