'use strict';
const assert=require('node:assert/strict');
exports.run=async({page,app,read,wait,settled,kind})=>{
 const original=read();if(kind!=='html'){await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Meta']});await settled();await app.locator('h1').click({button:'right'});const menu=page.getByRole('menu',{name:'Canvas actions',exact:true});assert.equal(await menu.locator('[data-action-id="layer-frameSelection"]').isDisabled(),true);await page.keyboard.press('Escape');await page.getByRole('treeitem',{name:'p · Named text',exact:true}).click({modifiers:['Meta']});await settled();}await page.getByRole('treeitem',{name:'h1 · Headline',exact:true}).click();await page.getByRole('treeitem',{name:'p · Other text',exact:true}).click({modifiers:['Shift']});await settled();assert.equal(await page.getByRole('treeitem',{selected:true}).count(),2);
 await app.locator('h1').click({button:'right'});await page.getByRole('menu',{name:'Canvas actions',exact:true}).locator('[data-action-id="layer-frameSelection"]').click();await wait(()=>read()!==original);await settled();const framed=read();await wait(async()=>await app.locator('[data-rt-frame] > h1').count()===1);assert.equal(await app.locator('[data-rt-frame] > p').count(),1);assert.equal(await page.getByRole('treeitem',{selected:true}).count(),1);
 await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===original);await settled();await wait(async()=>await app.locator('[data-rt-frame]').count()===0);assert.equal(await page.getByRole('treeitem',{selected:true}).count(),2);
 await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===framed);await settled();await wait(async()=>await app.locator('[data-rt-frame] > h1').count()===1);
 await page.getByRole('treeitem',{name:'div · Frame',exact:true}).click({button:'right'});await page.getByRole('menu',{name:'Canvas actions',exact:true}).locator('[data-action-id="layer-removeFrame"]').click();await wait(()=>read()===original);await settled();await wait(async()=>await app.locator('[data-rt-frame]').count()===0);assert.equal(await page.getByRole('treeitem',{selected:true}).count(),2);
 await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===framed);await settled();await wait(async()=>await app.locator('[data-rt-frame] > h1').count()===1);
 await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===original);await settled();await wait(async()=>await app.locator('[data-rt-frame]').count()===0);assert.equal(await page.getByRole('treeitem',{selected:true}).count(),2);
 console.log(kind+': PASS frame selection and release through context menus, rendered parents, selection restoration and exact undo/redo');
};
