'use strict';
const assert=require('node:assert/strict');
exports.run=async({page,app,read,wait,settled,kind})=>{
 const contexts=await page.evaluate(()=>{const svg=tag=>document.createElementNS('http://www.w3.org/2000/svg',tag),group=svg('g');group.append(svg('text'));return [[svg('rect'),svg('circle')],[svg('text'),svg('tspan')],[group,svg('path')],[document.createElement('div')]].map(elements=>{const parent=document.createElement('section');RetouchReactSelection.sharedGroups(parent,elements);return [parent.dataset.sharedSvg||'',parent.dataset.sharedSvgText||''];});});assert.deepEqual(contexts,[['true','false'],['true','true'],['true','true'],['','']]);
 const select=async name=>{await page.getByRole('treeitem',{name,exact:true}).click();await settled();};
 const edit=async(input,value)=>{const before=read();await input.fill(value);await input.press('Enter');await wait(()=>read()!==before);await settled();};
 await select('rect');await edit(page.getByLabel('SVG fill',{exact:true}),'color(srgb 1 0 0 / 0.5)');await edit(page.getByLabel('SVG stroke',{exact:true}),'color(srgb 0 0 1 / 0.5)');
 await select('circle');await edit(page.getByLabel('SVG fill',{exact:true}),'color(display-p3 0.2 0.4 0.6 / 0.25)');await edit(page.getByLabel('SVG stroke',{exact:true}),'color(display-p3 0.1 0.8 0.2 / 0.25)');
 await select('rect');await page.getByRole('treeitem',{name:'circle',exact:true}).click({modifiers:['Shift']});await settled();

 const states=[read()],record=async()=>{await wait(()=>read()!==states.at(-1));await settled();states.push(read());},paints=property=>app.locator('main > svg > *').evaluateAll((els,p)=>els.map(el=>parent.RetouchPaintPicker.parsePaint(getComputedStyle(el).getPropertyValue(p))),property);
 const css=page.getByLabel('Shared SVG CSS properties',{exact:true});assert.equal(await css.evaluate(el=>el.open),false);assert.equal(await page.locator('#panelBody > [data-shared-section=typography]').count(),0);for(const key of ['fill','stroke'])assert.equal(await page.locator('#panelBody > [data-shared-section='+key+'] input[data-paint-property='+key+']').count(),1);const source=read();await css.locator(':scope > summary').click();assert.equal(await css.evaluate(el=>el.open),true);await css.locator(':scope > summary').click();assert.equal(read(),source);
 const untouched={fill:(await paints('fill'))[2],stroke:(await paints('stroke'))[2]};
 for(const property of ['fill','stroke']){
  const alpha=page.getByLabel('Shared SVG '+property+' opacity (%)',{exact:true});assert.equal(await alpha.isEnabled(),true);assert.equal(await alpha.getAttribute('placeholder'),'Mixed');
  await alpha.fill('35');await alpha.press('Enter');await record();const colors=await paints(property);assert.ok(colors.slice(0,2).every(p=>Math.abs(p.alpha-.35)<1e-6));assert.equal(colors[1].space,'display-p3');assert.deepEqual(colors[1].channels,property==='fill'?[.2,.4,.6]:[.1,.8,.2]);assert.deepEqual(colors[0].channels,property==='fill'?[1,0,0]:[0,0,1]);
  const input=page.getByLabel('Shared SVG '+property+(kind==='html'?'':' with alpha'),{exact:true});await input.fill('ABCDEF');await input.press('Enter');await record();const changed=await paints(property);assert.ok(changed.slice(0,2).every(p=>Math.abs(p.alpha-.35)<1e-6));assert.ok(changed.slice(0,2).every(p=>p.channels.map(n=>Math.round(n*255)).join(',')==='171,205,239'));assert.deepEqual(changed[2],untouched[property]);
  const unchanged=read();await input.focus();assert.equal(await input.inputValue(),'ABCDEF');await input.press('Tab');await settled();assert.equal(read(),unchanged);
 }
 if(process.env.RT_E2E_SHARED_SVG_ICONS){
  const caps=page.getByRole('toolbar',{name:'Shared Stroke caps',exact:true}),joins=page.getByRole('toolbar',{name:'Shared Stroke joins',exact:true}),values=()=>app.locator('main > svg > *').evaluateAll(els=>els.map(el=>({caps:getComputedStyle(el).strokeLinecap,joins:getComputedStyle(el).strokeLinejoin})));const original=(await values())[2];
  assert.equal(await caps.locator('[aria-pressed=true]').count(),0);assert.equal(await joins.locator('[aria-pressed=true]').count(),0);
  await caps.getByRole('button',{name:'Shared Square caps',exact:true}).click();await record();await wait(async()=>(await values()).slice(0,2).every(value=>value.caps==='square'));assert.equal(await page.getByRole('button',{name:'Shared Square caps',exact:true}).evaluate(el=>el===document.activeElement),true);
  const miter=joins.getByRole('button',{name:'Shared Miter join',exact:true});await miter.focus();await miter.press('ArrowRight');assert.equal(await joins.getByRole('button',{name:'Shared Bevel join',exact:true}).evaluate(el=>el===document.activeElement),true);await joins.getByRole('button',{name:'Shared Bevel join',exact:true}).press(' ');await record();await wait(async()=>(await values()).slice(0,2).every(value=>value.joins==='bevel'));assert.deepEqual((await values())[2],original);
  const before=read();await caps.getByRole('button',{name:'Shared Square caps',exact:true}).click();await settled();assert.equal(read(),before);
 }
 if(process.env.RT_E2E_SHARED_SVG_DASH){
  const values=()=>app.locator('main > svg > rect,main > svg > circle').evaluateAll(els=>els.map(el=>getComputedStyle(el).strokeDasharray));const style=page.getByLabel('Shared Stroke style',{exact:true}),dash=page.getByLabel('Shared Dash length',{exact:true}),gap=page.getByLabel('Shared Dash gap',{exact:true});assert.equal(await style.inputValue(),'dashed');assert.equal(await dash.getAttribute('placeholder'),'Mixed');assert.equal(await gap.getAttribute('placeholder'),'Mixed');
  await dash.fill('3');await dash.press('Enter');await record();assert.deepEqual(await values(),['3px, 2px','3px, 6px']);await gap.fill('9');await gap.press('Enter');await record();assert.deepEqual(await values(),['3px, 9px','3px, 9px']);
  const unchanged=read();await dash.fill('-2');await dash.press('Enter');assert.equal(await dash.evaluate(el=>el.checkValidity()),false);assert.equal(read(),unchanged);await dash.press('Escape');assert.equal(parseFloat(await dash.inputValue()),3);
  await style.selectOption('solid');await record();assert.deepEqual(await values(),['none','none']);await style.selectOption('dashed');await record();assert.deepEqual(await values(),['4px, 4px','4px, 4px']);
 }
 if(process.env.RT_E2E_SHARED_PAINT_RANGE){
  const screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true}),alpha=page.getByLabel('Shared SVG fill opacity (%)',{exact:true}),dash=page.getByLabel('Shared Dash length',{exact:true});
  await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await screen.focus();await screen.selectOption('390x844');await settled();await wait(async()=>await alpha.isDisabled()&&await dash.isDisabled());const before=read();
  await alpha.evaluate(el=>{el.value='17';el.onchange();});await dash.evaluate(el=>{el.value='19';el.onchange(new Event('change'));});await settled();assert.equal(read(),before);
  const raw=page.getByLabel('Shared SVG fill'+(kind==='html'?'':' with alpha'),{exact:true});await raw.fill('123456');await raw.press('Enter');assert.equal(read(),before);assert.equal(await raw.evaluate(el=>el.checkValidity()),false);await raw.press('Escape');const phonePaint=await paints('fill');await raw.fill('#12345680');await raw.press('Enter');await record();assert.deepEqual(await paints('fill'),phonePaint);const scoped=read();
  await screen.focus();await screen.selectOption('768x1024');await settled();await wait(async()=>await alpha.isEnabled()&&await dash.isEnabled());assert.equal(read(),scoped);await wait(async()=>(await paints('fill')).slice(0,2).every(value=>value.channels.map(n=>Math.round(n*255)).join(',')==='18,52,86'));
 }
 if(process.env.RT_E2E_SHARED_SVG_STROKES){
  const strokeState=()=>app.locator('main > svg > *').evaluateAll(els=>els.map(el=>{const css=getComputedStyle(el);return Object.fromEntries(['stroke-width','stroke-linecap','stroke-linejoin','stroke-dasharray','stroke-dashoffset','stroke-miterlimit','vector-effect'].map(key=>[key,css.getPropertyValue(key)]));})),initial=await strokeState();
  for(const [label,value,property,expected]of [['SVG stroke width','7','stroke-width','7px'],['SVG line ends','round','stroke-linecap','round'],['SVG line joins','bevel','stroke-linejoin','bevel'],['SVG dash pattern','4 2','stroke-dasharray','4px, 2px'],['SVG dash offset','2','stroke-dashoffset','2px'],['SVG miter limit','8','stroke-miterlimit','8'],['SVG stroke scaling','non-scaling-stroke','vector-effect','non-scaling-stroke']]){
   if(label==='SVG dash pattern')await page.getByLabel('Shared Stroke style',{exact:true}).selectOption('custom');
   const input=page.getByLabel('Shared '+label,{exact:true});await wait(async()=>input.evaluate(el=>!!el.closest('#panelBody > [data-shared-section=stroke]')));
   if(property==='stroke-linecap'||property==='stroke-linejoin')await page.getByRole('button',{name:property==='stroke-linecap'?'Shared Round caps':'Shared Bevel join',exact:true}).click();else if(await input.evaluate(el=>el.tagName==='SELECT'))await input.selectOption(value);else{await input.fill(value);await input.press('Enter');}await record();await wait(async()=>{const values=await strokeState();return values.slice(0,2).every(item=>item[property]===expected);});assert.deepEqual((await strokeState())[2],initial[2]);
  }
  const screen=page.getByLabel('Screen size',{exact:true}),scope=page.getByLabel('Style screen scope',{exact:true});await screen.focus();await screen.selectOption('768x1024');await settled();await scope.selectOption(kind==='html'?'min-[768px]:':'md:');await settled();await page.getByLabel('Shared SVG stroke width',{exact:true}).fill('11');await page.getByLabel('Shared SVG stroke width',{exact:true}).press('Enter');await record();await wait(async()=>(await strokeState()).slice(0,2).every(item=>item['stroke-width']==='11px'));await screen.focus();await screen.selectOption('390x844');await settled();await wait(async()=>(await strokeState()).slice(0,2).every(item=>item['stroke-width']==='7px'));await screen.focus();await screen.selectOption('768x1024');await settled();
  const beforeInvalid=read(),width=page.getByLabel('Shared SVG stroke width',{exact:true});await width.fill('-1');await width.press('Enter');assert.equal(await width.evaluate(el=>el.checkValidity()),false);assert.equal(read(),beforeInvalid);await width.press('Escape');assert.equal(parseFloat(await width.inputValue()),11);
 }
 if(process.env.RT_E2E_SHARED_SVG_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_SHARED_SVG_SCREENSHOT});
 for(const expected of states.slice(0,-1).reverse()){await page.getByRole('button',{name:'Undo',exact:true}).click();await wait(()=>read()===expected);await settled();}
 for(const expected of states.slice(1)){await page.getByRole('button',{name:'Redo',exact:true}).click();await wait(()=>read()===expected);await settled();}
 console.log(kind+': PASS compact shared SVG fill/stroke, mixed alpha, P3 channels, hex, untouched sibling and exact undo/redo');
};
