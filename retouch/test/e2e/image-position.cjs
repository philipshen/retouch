'use strict';
const fs=require('node:fs'),os=require('node:os'),path=require('node:path'),assert=require('node:assert/strict'),{once}=require('node:events');
const fixture=process.env.RT_INSPECTOR_FIXTURE,engine=process.env.RT_E2E_BROWSER||'chromium';if(!fixture)throw Error('Set RT_INSPECTOR_FIXTURE');
(async()=>{
 const root=fs.mkdtempSync(path.join(os.tmpdir(),'retouch-image-position-')),file=path.join(root,'index.html'),source='<html><body><input value="initial"><img alt="Color image" src="/image.svg" style="display:block;width:200px;height:200px;object-fit:cover;object-position:50% 50%"></body></html>';fs.writeFileSync(file,source);fs.writeFileSync(path.join(root,'image.svg'),'<svg xmlns="http://www.w3.org/2000/svg" width="400" height="200"><path fill="red" d="M0 0h200v200H0z"/><path fill="blue" d="M200 0h200v200H200z"/></svg>');
 const server=require('../../src/html-site.cjs').start({root,port:0,quiet:true});if(!server.listening)await once(server,'listening');let browser;
 try{
  browser=await require(path.join(fixture,'node_modules/playwright'))[engine].launch();const page=await browser.newPage({viewport:{width:1400,height:1000}}),errors=[];page.on('pageerror',error=>errors.push(error.message));await page.goto('http://localhost:'+server.address().port+'/rt');const app=page.frameLocator('#app'),img=app.getByAltText('Color image',{exact:true});await img.waitFor();await img.evaluate(el=>el.decode());await app.locator('input').evaluate(el=>{el.value='retained';window.imageIdentity={};window.imageOriginalIdentity=window.imageIdentity;});await page.getByLabel('Screen size',{exact:true}).selectOption('390x844');await page.getByRole('treeitem',{name:'img · Color image',exact:true}).click();
  const idle=()=>page.waitForFunction(()=>document.getElementById('panelBody').getAttribute('aria-busy')!=='true'),position=()=>img.evaluate(el=>getComputedStyle(el).objectPosition);await idle();
  for(const zoom of [50,100,200]){
   await page.getByLabel('Canvas zoom (%)',{exact:true}).fill(String(zoom));await page.getByLabel('Canvas zoom (%)',{exact:true}).press('Tab');await page.getByRole('button',{name:'Reposition image',exact:true}).click();const handle=page.getByRole('button',{name:'Move image within frame',exact:true}),rect=await handle.boundingBox();await page.mouse.move(rect.x+rect.width/2,rect.y+rect.height/2);await page.mouse.down();await page.mouse.move(rect.x+rect.width/2+50*zoom/100,rect.y+rect.height/2,{steps:5});assert.equal(await position(),'25% 50%');assert.equal(await page.getByLabel('Image horizontal position (%)',{exact:true}).inputValue(),'25');assert.equal(fs.readFileSync(file,'utf8'),source);if(zoom===100&&process.env.RT_E2E_IMAGE_POSITION_SCREENSHOT)await page.screenshot({path:process.env.RT_E2E_IMAGE_POSITION_SCREENSHOT});await page.keyboard.press('Escape');await page.mouse.up();assert.equal(await position(),'50% 50%');assert.equal(fs.readFileSync(file,'utf8'),source);
   await page.getByRole('button',{name:'Reposition image',exact:true}).click();const b=await handle.boundingBox();await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();await page.mouse.move(b.x+b.width/2+50*zoom/100,b.y+b.height/2,{steps:5});await page.mouse.up();await idle();assert.equal(await position(),'25% 50%');assert.notEqual(fs.readFileSync(file,'utf8'),source);await page.getByRole('button',{name:'Undo',exact:true}).click();await idle();assert.equal(fs.readFileSync(file,'utf8'),source);assert.equal(await position(),'50% 50%');
  }
  await page.getByRole('button',{name:'Reposition image',exact:true}).click();await page.getByRole('button',{name:'Move image within frame',exact:true}).waitFor();await page.keyboard.press('Shift+ArrowRight');await page.keyboard.press('Shift+ArrowRight');assert.equal(await position(),'40% 50%');await page.keyboard.press('Enter');await idle();await page.getByRole('button',{name:'Undo',exact:true}).click();await idle();assert.equal(fs.readFileSync(file,'utf8'),source);assert.deepEqual(await app.locator('input').evaluate(el=>[el.value,window.imageIdentity===window.imageOriginalIdentity]),['retained',true]);assert.deepEqual(errors,[]);console.log(engine+': PASS image drag at 50/100/200%, cancel, grouped keyboard edit, exact undo and preview state');
 }finally{if(browser)await browser.close();server.retouchIndex.close();server.closeAllConnections();await new Promise(resolve=>server.close(resolve));fs.rmSync(root,{recursive:true,force:true});}
})().catch(error=>{console.error(error);process.exitCode=1;});
